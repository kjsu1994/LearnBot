# LearnBot Ultimate Goal Execution Plan

This file is the working roadmap for reaching LearnBot's long-term product goal. Each future Goal run should read this file together with `agent.md` and the `LearnBot Service Final Architecture Plan` section in `README.md` before changing code.

## Product Goal

LearnBot should become a web-first local RAG agent service for small internal teams.

- Users normally interact through the LearnBot website for chat, review, approvals, evidence, and results.
- The central LearnBot server owns retrieval, ranking, model calls, planning, validation, approval, conversation history, diagnostics, and orchestration.
- User-owned file changes, tests, builds, git operations, and rollback restoration should be executed by each user's Local Agent on that user's PC.
- Server-local apply/test/rollback is allowed only as a prototype, shared sandbox, migration bridge, or admin/debug capability. It must not become the default product path.
- The final developer experience should include a simple PowerShell-friendly `learnbot` CLI, similar in spirit to running `codex`.
- Proven open-source components should be used where they improve quality or reduce risk, but LearnBot's product direction must not be bent to fit a library.

Current pointer override after Run 607: Continue the Codex-style closed-loop core by graduating the disabled `claimable-dry-run-preview` backend boundary from a hidden endpoint into the Code workspace review surface, still without enabling real model execution, queue release, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, a claimable dry-run enqueue, a pushed Local Agent request, a second approved mutation, or final answer publication. It added `validatedDryRunIntentTransitionPreviewClient`, which resolves a request id from the validated dry-run intent handoff/eligibility/transition route and performs only `GET /api/code-agent/local-patch-request/dry-run-intent/{requestId}/claimable-dry-run-preview` through the existing `run/request` path. `useCodeRagController` now stores `codeAgentValidatedDryRunIntentTransitionPreview`, clears it with stale eligibility/patch/request/observation state, and exposes `previewCodeAgentValidatedDryRunIntentTransition`. `App.jsx` and `CodeWorkspace` pass the state/action through to the Code Agent panel. The Code workspace now shows `Preview claimable dry-run transition` only for `learnbot.code-agent.validated-dry-run-intent-review-handoff.v1` handoffs and renders `learnbot.server.validated-revised-patch-dry-run-transition-preview.v1` with source identity, prerequisites, the nested transition gate, the original eligibility status, the would-be `learnbot.server.validated-revised-patch-claimable-dry-run-request-preview.v1`, target files, blockers, and explicit disabled controls: `requestPersisted=false`, `requestCreationEnabled=false`, `queueEnabled=false`, `pushEnabled=false`, `claimEnabled=false`, `claimable=false`, `dryRunOnly=true`, `mutationAllowed=false`, and `approvalBypassAllowed=false`. Focused frontend tests pin the transition client route extraction, summary renderer, and Code workspace SSR button/panel display beside the existing eligibility review. Verified with `node frontend\src\features\code\validatedDryRunIntentTransitionPreviewClient.test.mjs`, `node frontend\src\components\code\validatedDryRunIntentTransitionPreviewSummary.test.mjs`, `node frontend\src\features\code\validatedDryRunIntentEligibilityClient.test.mjs`, `node frontend\src\components\code\validatedDryRunIntentEligibilitySummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, and `npm run build` from `frontend`; Vite succeeded with the existing large chunk warning. Attempted focused backend Maven revalidation for `CodeAgentLocalPatchRequestServiceTest,CodeAgentControllerTest`, but the escalation was rejected by the environment usage limit, so no backend rerun happened in this run. Remaining risk: the transition preview is still read-only UI/review; no claimable non-mutating dry-run is actually created, no Local Agent consumes the persisted intent, no real local-model output path drives the loop end to end, no second mutation/test/rollback orchestration runs, and no real partial reindex or final publication/acknowledgement save runs. Next work should add the actual guarded release contract for a non-mutating claimable dry-run request, still behind explicit disabled/feature-gated request creation, queue, push, claim, mutation, approval-bypass, and partial-reindex boundaries until the Local Agent consumption and approval surface are proven.

Current pointer override after Run 605: Continue the Codex-style closed-loop core by moving the persisted validated dry-run intent eligibility gate from route text into an actual guarded frontend read action and Code workspace review display, still without enabling real model execution, queue release, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, a claimable dry-run enqueue, a pushed Local Agent request, a second approved mutation, or final answer publication. It added `validatedDryRunIntentEligibilityClient`, which extracts the request id from the `GET /api/code-agent/local-patch-request/dry-run-intent/{requestId}/eligibility` handoff route and performs only that GET through the existing `run/request` client path. `useCodeRagController` now stores `codeAgentValidatedDryRunIntentEligibility`, clears stale eligibility when patch/request/observation state changes, and exposes `inspectCodeAgentValidatedDryRunIntentEligibility`. `CodeWorkspace` now shows an `Inspect dry-run eligibility` button only for `learnbot.code-agent.validated-dry-run-intent-review-handoff.v1` handoffs, and renders the fetched `learnbot.server.validated-revised-patch-dry-run-eligibility.v1` response with source identity, prerequisite state, target files, check lines, blockers, and the nested `futureDryRunReleaseGate` while preserving disabled `requestCreationEnabled=false`, `queueEnabled=false`, `pushEnabled=false`, `claimEnabled=false`, `claimable=false`, `mutationAllowed=false`, and `approvalBypassAllowed=false` controls. `App.jsx` also now passes the existing runner review/final-result/M8/approved-flow props through to `CodeWorkspace` instead of leaving those review surfaces on defaults. Focused frontend tests pin the new client, summary renderer, and Code workspace SSR route display; focused backend tests revalidated the persisted intent eligibility and next-action contracts. Verified with `node frontend\src\features\code\validatedDryRunIntentEligibilityClient.test.mjs`, `node frontend\src\components\code\validatedDryRunIntentEligibilitySummary.test.mjs`, `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, `npm run build` from `frontend`, and approved Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentLocalPatchRequestServiceTest,CodeAgentControllerTest" test`, which ran 42 tests with 0 failures. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: the eligibility display is still read-only review; there is no claimable non-mutating dry-run release, no Local Agent consumption of the persisted intent, no real local-model output path driving the loop end to end, no second mutation/test/rollback orchestration, and no real partial reindex or final publication/acknowledgement save. Next work should add the next disabled backend transition/readiness boundary from an eligible persisted dry-run intent toward a future claimable non-mutating dry-run request, or add the first Local Agent consumption preview for that persisted intent, keeping queue/push/claim/mutation/approval-bypass disabled until the release contract is explicitly implemented.

Current pointer override after Run 604: Continue the Codex-style closed-loop core by making the persisted validated dry-run intent eligibility gate visible from the loop next-action handoff and Code workspace runner summary, still without enabling real model execution, queue release, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, a claimable dry-run enqueue, a pushed Local Agent request, a second approved mutation, or final answer publication. It updated `CodeAgentLoopPreviewService.nextAction` so the latest `LOCAL_AGENT_APPROVAL_REQUEST_CREATED` event with `validatedDryRunIntent=true` now returns a `learnbot.code-agent.validated-dry-run-intent-review-handoff.v1` handoff summary. That summary carries `VALIDATED_DRY_RUN_INTENT_REVIEW`, source event/request identity, approval state, review surface, persisted/dry-run flags, the explicit `GET /api/code-agent/local-patch-request/dry-run-intent/{requestId}/eligibility` route, and disabled `requestCreationEnabled=false`, `queueEnabled=false`, `pushEnabled=false`, `claimEnabled=false`, `claimable=false`, `mutationAllowed=false`, and `approvalBypassAllowed=false` controls. The frontend runner handoff helper now renders this as a dedicated `dry-run review` badge, includes the eligibility route in route text, includes validated dry-run intent/readiness flags, and includes queue/claimable/approval-bypass disabled state in the disabled-controls line. Focused tests now pin both backend next-action handoff data and frontend runner handoff rendering. Verified with `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, approved Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest" test` which ran 15 tests with 0 failures, and `npm run build` from `frontend`, which succeeded with the existing large chunk warning only. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: the Code workspace still displays the eligibility route as read-only handoff text rather than fetching and rendering the live eligibility response; there is still no claimable non-mutating dry-run release, no Local Agent consumption, no real local-model output path driving the loop end to end, no second mutation/test/rollback orchestration, and no real partial reindex or final publication/acknowledgement save. Next work should add a guarded frontend/client read action that fetches the persisted dry-run intent eligibility response from the handoff route and displays ready/blocked checks in the Code workspace review surface while keeping request creation, queue, push, claim, mutation, approval bypass, and RAG freshness updates disabled.

Current pointer override after Run 603: Continue the Codex-style closed-loop core by surfacing the new persisted validated dry-run intent eligibility gate in the Code workspace UI/timeline or by wiring it into loop next-action handoff data, still without enabling real model execution, queue release, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, a claimable dry-run enqueue, a pushed Local Agent request, a second approved mutation, or final answer publication. It added a disabled backend eligibility/readiness gate for persisted validated dry-run intents. `GET /api/code-agent/local-patch-request/dry-run-intent/{requestId}/eligibility` now resolves the current user and delegates to `CodeAgentLocalPatchRequestService.inspectValidatedDryRunIntentEligibility`. The service reads the persisted request through `LocalAgentToolGatewayService.findForUser`, verifies it is a `USER_LOCAL_AGENT` `patch.apply` request with `approvalState=REQUIRED`, `validatedDryRunIntent=true`, `dryRunIntentPersisted=true`, `dryRunOnly=true`, `mutationAllowed=false`, non-empty target files, and a non-empty diff, then returns `learnbot.server.validated-revised-patch-dry-run-eligibility.v1`. Even when all prerequisites pass, the gate reports `READY_DRY_RUN_RELEASE_DISABLED` and a nested `learnbot.server.validated-revised-patch-dry-run-release-gate.v1` with `requestCreationEnabled=false`, `queueEnabled=false`, `pushEnabled=false`, `claimEnabled=false`, `claimable=false`, `dryRunOnly=true`, `mutationAllowed=false`, and `approvalBypassAllowed=false`. Unsafe persisted rows return `BLOCKED_DRY_RUN_RELEASE_DISABLED` with blocking check keys while still keeping all execution controls disabled. Focused backend tests now prove the ready-but-disabled gate, unsafe blocked gate, and controller delegation for the current user. Verified with approved Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLocalPatchRequestServiceTest,CodeAgentControllerTest" test`, which ran 27 tests with 0 failures. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: this eligibility gate is backend-only and not yet rendered in the Code workspace, not connected to next-action handoff data, not used to create or release a claimable non-mutating dry-run, not consumed by the Local Agent, not driven end-to-end from a real local-model output, not followed by second mutation/test/rollback orchestration, and not followed by partial reindex or final publication/acknowledgement save. Next work should expose this eligibility response in the Code workspace review surface or loop next-action summary, preserving the disabled request creation/push/claim/mutation flags in the UI.

Current pointer override after Run 602: Continue the Codex-style closed-loop core by adding a disabled eligibility/review gate for the persisted validated dry-run intent before any future claimable non-mutating dry-run, still without enabling real model execution, queue release, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, a claimable dry-run enqueue, a pushed Local Agent request, a second approved mutation, or final answer publication. It surfaced the Run 601 persisted validated dry-run intent in the frontend Code workspace loop timeline review surface by extending `agentLoopTimelineSummary`. Timeline event lines now render `validated dry-run intent`, `dry-run intent persisted`, `review surface`, `request persisted`, `queue`, `push`, `claimable`, `dry-run only`, `mutation allowed`, and `dry-run intent review required` when those fields are present on `LOCAL_AGENT_APPROVAL_REQUEST_CREATED` details. The existing `CodeWorkspace.jsx` timeline history renderer already displays these event lines, so the UI now exposes the persisted intent as read-only loop review data without adding a new button, endpoint call, claim path, push path, release path, or mutation control. `agentLoopTimelineSummary.test.mjs` now pins a `VALIDATED_DRY_RUN_INTENT_REVIEW` approval-created event showing `CODE_WORKSPACE_LOOP_REVIEW`, `request persisted true`, `queue false`, `push false`, `claimable false`, `dry-run only true`, and `mutation allowed false`, plus the review reason. Verified with `node frontend\src\components\code\agentLoopTimelineSummary.test.mjs` and `npm run build` from `frontend`. The Vite build succeeded with the existing large chunk warning only. Remaining risk: this is UI visibility only; there is still no disabled backend eligibility gate deciding when a persisted validated dry-run intent could become a future claimable non-mutating dry-run, no Local Agent consumes it, no real local-model output path drives it end-to-end, no second mutation/test/rollback orchestration runs, and no partial reindex or final publication/acknowledgement save runs. Next work should add a backend disabled eligibility gate/read model for persisted validated dry-run intents that reports prerequisites for a future claimable non-mutating dry-run while keeping request creation, push, claim, mutation, approval bypass, and RAG freshness updates disabled.

Current pointer override after Run 601: Continue the Codex-style closed-loop core by surfacing the persisted validated dry-run intent in the frontend Code workspace/loop review UI or by adding a disabled release/review gate for it, still without enabling real model execution, queue release, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, a claimable dry-run enqueue, a pushed Local Agent request, a second approved mutation, or final answer publication. It connected the Run 600 persisted validated dry-run intent to the existing loop timeline/next-action read model without adding a new execution path. `CodeAgentLoopTimelineRepository.appendApprovalRequestCreated` now detects `validatedDryRunIntent=true` on the persisted `APPROVAL_REQUIRED` `patch.apply` request and records a review-specific audit event shape inside the existing `LOCAL_AGENT_APPROVAL_REQUEST_CREATED` event: `decisionKey=VALIDATED_DRY_RUN_INTENT_REVIEW`, `reviewSurface=CODE_WORKSPACE_LOOP_REVIEW`, persisted/request/dry-run flags, `queueEnabled=false`, `pushEnabled=false`, `claimable=false`, `dryRunOnly=true`, `mutationAllowed=false`, `dryRunIntentReviewRequired=true`, and the usual release/request-creation/push/claim/mutation/final-result/publication/acknowledgement disabled flags. `CodeAgentLoopPreviewService.nextAction` already consumes the latest approval-created event, so the new details now surface as a `WAIT_FOR_APPROVAL` next action whose reason tells the user to review the persisted validated dry-run intent before any future claimable non-mutating dry-run. Focused repository/service/backend tests now prove the timeline JSON contains the dry-run-intent review fields and that next-action preserves those details while keeping request creation, push, claim, and mutation disabled. Verified with approved Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopTimelineRepositoryTest,CodeAgentLoopPreviewServiceTest,CodeAgentLocalPatchRequestServiceTest,CodeAgentControllerTest" test`, which ran 52 tests with 0 failures. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: this is backend/timeline visibility only; the Code workspace frontend does not yet render a dedicated dry-run-intent review panel, no disabled eligibility gate decides when this persisted intent could become a claimable non-mutating dry-run, no Local Agent consumes it, no real local-model output path drives it end-to-end, no second mutation/test/rollback orchestration runs, and no partial reindex or final publication/acknowledgement save runs. Next work should render `VALIDATED_DRY_RUN_INTENT_REVIEW`/`validatedDryRunIntent` in the Code workspace loop review surface with explicit non-claimable/dry-run/mutation-disabled state, then add a disabled eligibility gate for future claimable non-mutating dry-run release.

Current pointer override after Run 600: Continue the Codex-style closed-loop core by connecting the persisted validated dry-run intent to a visible web review surface or loop timeline event, still without enabling real model execution, queue release, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, a claimable dry-run enqueue, a pushed Local Agent request, a second approved mutation, or a final answer publication. It added `POST /api/code-agent/local-patch-request/dry-run-intent` using the same `CodeAgentValidatedPatchDryRunPreviewRequest` payload as the preview endpoint. `CodeAgentLocalPatchRequestService.persistValidatedDryRunIntent` now reuses `previewValidatedDryRunRequest`, refuses blocked handoffs without persistence, and when the preview is ready persists an `APPROVAL_REQUIRED` `patch.apply` Local Agent request through the existing approval-request path with `validatedDryRunIntent=true`, `dryRunOnly=true`, `mutationAllowed=false`, `approvalPersistenceRequired=true`, `approvalPersisted=true`, `requestPersisted=true`, `queueEnabled=false`, `pushEnabled=false`, and `claimable=false`. The response uses `learnbot.server.validated-revised-patch-dry-run-intent.v1`, returns a persisted request summary, preserves the original preview, and keeps dry-run review distinct from any claimable queue/release path. Focused backend tests now prove ready handoffs create exactly one approval-required non-claimable dry-run intent, unsafe handoffs do not call `createApprovalRequest`, and the controller resolves repository-space access before delegating both preview and persisted-intent calls. Verified with approved Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLocalPatchRequestServiceTest,CodeAgentControllerTest" test`, which ran 24 tests with 0 failures. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: the persisted row is still not rendered by the web closed-loop UI, not correlated into loop timeline review events, not released to a claimable Local Agent dry-run, not connected to a real local-model output path, not followed by second mutation/test/rollback orchestration, and not followed by partial reindex or final publication/acknowledgement save. Next work should expose this persisted dry-run intent in the Code workspace/loop review surface or append a timeline event for it, then add a disabled release/review gate that can decide when a persisted dry-run intent is eligible for a future claimable non-mutating dry-run.

Current pointer override after Run 599: Continue the Codex-style closed-loop core by graduating the new backend dry-run preview contract toward durable server orchestration, still without enabling real model execution, queueing, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, real dry-run enqueue from model output, a persisted backend approval object from the revised diff, or a fully model-derived second retry mutation. It added `CodeAgentValidatedPatchDryRunPreviewRequest` and `POST /api/code-agent/local-patch-request/dry-run-preview`, which accepts `learnbot.local-agent.validated-revised-patch-dry-run-handoff.v1`, resolves repository-space access like the existing local patch request path, and delegates to a new non-persisting backend preview service. `CodeAgentLocalPatchRequestService.previewValidatedDryRunRequest` now shapes the validated handoff into a would-be `patch.apply` server queue row with `dryRunOnly=true`, `mutationAllowed=false`, `approvalRequiredBeforeMutation=true`, expected indexed file hashes, source repository/workspace identity, `requestPersisted=false`, `queueEnabled=false`, `pushEnabled=false`, and `claimable=false`. It surfaces ready vs blocked status, validation warnings, blockers for unsafe/not-ready handoffs, and approval prerequisites for the web UI without calling `createApprovalRequest` or inserting a durable tool row. Focused backend tests now prove the ready preview contract, unsafe handoff blockers, non-persistence, disabled queue/claim flags, and controller repository-space delegation. Verified with approved Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLocalPatchRequestServiceTest,CodeAgentControllerTest" test`, which ran 21 tests with 0 failures. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: this is still a preview/read-model contract only; no durable `patch.apply` dry-run row is created from the validated revised diff, no UI surface renders this endpoint yet, no backend approval object is persisted from that preview, no Local Agent claim/release path consumes it, no second approved mutation runs after approval, and no real partial reindex or final publication/acknowledgement save runs. Next work should either wire this preview into the web closed-loop UI/review surface or add the next disabled durable-intent step that can persist the preview as a non-claimable dry-run request while keeping approval, queue release, mutation, and RAG freshness disabled.

Current pointer override after Run 598: Continue the Codex-style closed-loop core by moving the model-output-derived dry-run handoff from Local Agent report fixtures toward a backend/server queue preview contract, still without enabling real model execution, queueing, mutation, approval bypass, or partial reindex. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, real dry-run enqueue from model output, or a fully model-derived second retry mutation. It extended `learnbot.local-agent.patch-test-second-attempt.v1` so the second-attempt report can consume `learnbot.local-agent.validated-revised-patch-dry-run-handoff.v1` as an optional dry-run source. The report now records `dryRunInputSource`, a nested `dryRunHandoff` summary with schema/status/readiness, consumed state, handoff target files, dry-run response target files, target-match status, and disabled queue/request/claimable flags. The direct `patch-test-second-attempt-contract` now proves a ready validated handoff is consumed and target-matched against the `patch.apply` dry-run response before reaching `APPROVAL_REQUIRED`. The durable queue second-attempt fixture now creates a model-output-derived validation and dry-run handoff from the revised diff, passes that handoff into `patchTestSecondAttempt`, and proves the queue-backed dry-run response consumes/matches it rather than only relying on fixture-authored proposal metadata. `ExtractDryRunTargetFiles` now handles JSON/object-list file arrays returned through the HTTP queue path. `local-agent/README.md` documents the handoff consumption in the second-attempt self-test. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test patch-test-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test validated-revised-patch-dry-run-handoff-contract`, `dotnet run --project local-agent --no-build -- self-test local-model-revised-patch-output-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, and a generated approved-flow report assertion showing `validatedRevisedPatchDryRunHandoff`, `dryRunHandoff`, and `dryRunInputSource`. Remaining risk: this is still Local Agent report/fixture wiring; no local model is invoked, no validated diff is persisted by the backend as a durable `patch.apply` dry-run request, no backend approval object is persisted from a real dry-run, no second approved mutation is executed after approval, and no real partial reindex or final publication/acknowledgement save runs. Next work should add a backend/server queue preview contract for a validated revised patch dry-run request: accept the validated handoff input, expose the would-be `patch.apply` request row with `dryRunOnly=true`/`mutationAllowed=false`, keep claimability disabled, and surface blockers/approval prerequisites for the web UI.

Current pointer override after Run 597: Continue the Codex-style closed-loop core by connecting the disabled dry-run handoff into the existing second-attempt dry-run report path or durable queue fixture, still without enabling real queueing, mutation, approval bypass, or local-model execution. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, real dry-run enqueue from model output, or a fully model-derived second retry mutation. It added `learnbot.local-agent.validated-revised-patch-dry-run-handoff.v1` after `localModelRevisedPatchOutput` in the generated `codex-closed-loop-report.v1`. The handoff consumes accepted model-output validation `nextToolInput`, exposes the shaped `patch.apply` dry-run input, target files, diff preview/size, and source status, but keeps `dryRunQueueEnabled=false`, `dryRunRequestPersisted=false`, `dryRunClaimable=false`, `mutationAllowed=false`, `patchApplyMutationAllowed=false`, and `approvalRequiredBeforeMutation=true`. When model output is absent or invalid, the handoff reports `BLOCKED_OUTPUT_NOT_READY` and does not expose patch input. `local-agent/README.md` documents the new `validated-revised-patch-dry-run-handoff-contract` self-test. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test validated-revised-patch-dry-run-handoff-contract`, `dotnet run --project local-agent --no-build -- self-test local-model-revised-patch-output-contract`, `dotnet run --project local-agent --no-build -- self-test patch-test-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`, and a generated approved-flow report assertion showing `validatedRevisedPatchDryRunHandoff` plus `learnbot.local-agent.validated-revised-patch-dry-run-handoff.v1`. Remaining risk: this is still a disabled handoff/read-model contract; no local model is invoked, no validated diff is actually persisted as a durable `patch.apply` dry-run request, no backend approval object is persisted from a real dry-run, no second approved mutation is executed after approval, and no real partial reindex or final publication/acknowledgement save runs. Next work should either make `patchTestSecondAttempt` consume this handoff's `patchApplyInput` when a dry-run response exists, or extend the durable queue second-attempt fixture with a model-output-derived dry-run input instead of a fixture-authored dry-run diff.

Current pointer override after Run 596: Continue the Codex-style closed-loop core by wiring validated local-model revised diff output into the existing `patch.apply` dry-run handoff, while still keeping the actual model call, dry-run queueing, mutation, approval persistence, and partial reindex disabled. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, real dry-run enqueue from model output, or a fully model-derived second retry mutation. It added `learnbot.local-agent.local-model-revised-patch-output-validation.v1` after `localModelRevisedPatchRequest` in the generated `codex-closed-loop-report.v1`. The validation contract accepts only a non-empty unified diff within `maxDiffChars` that touches planned target files, extracts `+++ b/...` diff targets, compares them with declared output target files and planned target files, and blocks empty, oversized, missing-target, out-of-target, or declared-target-not-in-diff output. Accepted output becomes `READY_FOR_DRY_RUN` with `patchApplyDryRunOnly=true`, `patchApplyMutationAllowed=false`, `mutationAllowed=false`, `approvalRequiredBeforeMutation=true`, and a disabled next-tool input for `patch.apply`; absent output reports `WAITING_FOR_MODEL_OUTPUT`. `local-agent/README.md` documents the new `local-model-revised-patch-output-contract` self-test. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test local-model-revised-patch-output-contract`, `dotnet run --project local-agent --no-build -- self-test local-model-revised-patch-request-contract`, `dotnet run --project local-agent --no-build -- self-test patch-test-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`, and a generated approved-flow report assertion showing `localModelRevisedPatchOutput` plus `learnbot.local-agent.local-model-revised-patch-output-validation.v1`. Remaining risk: this is still a validation/report contract; no local model is invoked, no validated diff is actually queued as a durable `patch.apply` dry-run request, no backend approval object is persisted from that dry-run, no second approved mutation is executed after approval, and no real partial reindex or final publication/acknowledgement save runs. Next work should connect accepted `localModelRevisedPatchOutput.nextToolInput` to the existing second-attempt dry-run boundary or durable queue fixture as a disabled/preview handoff first, then graduate it into a real queued dry-run only after server-side approval review surfaces are ready.

Current pointer override after Run 595: Continue the Codex-style closed-loop core by moving from the disabled local-model request boundary to a concrete revised-diff proposal output contract, still stopping before `patch.apply` dry-run and approval. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM execution, real partial reindex, real partial-reindex job enqueue, or a fully model-derived second retry mutation. It added `learnbot.local-agent.local-model-revised-patch-request.v1` after `revisedPatchProposalPlan` in the generated `codex-closed-loop-report.v1`. The request shapes failed `command.runAllowed` evidence and read target-file snippets into a capped local-model input/output contract with `maxTargetFiles=8`, `maxFailureEvidenceChars=600`, `maxFileEvidenceChars=2000`, `maxInputChars=12000`, `maxDiffChars=24000`, explicit target files, read evidence, failure evidence, unified-diff-only output constraints, and expected output schema `learnbot.local-agent.local-model-revised-patch-output.v1`. It keeps `modelCallEnabled=false`, `localLlmPlanningEnabled=false`, `mutationAllowed=false`, `patchApplyQueued=false`, `dryRunRequiredBeforeApproval=true`, and `approvalRequiredBeforeMutation=true`; missing read evidence blocks the model request. The multi-file read report now carries a bounded `contentSnippet` so the model request can be built from actual file observations rather than only path/byte metadata. `local-agent/README.md` documents the new `local-model-revised-patch-request-contract` self-test. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test local-model-revised-patch-request-contract`, `dotnet run --project local-agent --no-build -- self-test revised-patch-proposal-plan-contract`, `dotnet run --project local-agent --no-build -- self-test multi-file-read-report-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test patch-test-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`, and a generated approved-flow report assertion showing `localModelRevisedPatchRequest`, `learnbot.local-agent.local-model-revised-patch-request.v1`, and `maxDiffChars`. Remaining risk: this is still a disabled model request/report boundary; no local model is invoked, no revised unified diff output is parsed or validated, no dry-run is enqueued from model output, no backend approval object is persisted from that output, no second approved mutation is executed after approval, and no real partial reindex or final publication/acknowledgement save runs. Next work should add the matching local-model revised patch output validation contract: accept only capped unified diff touching the planned target files, reject empty/oversized/out-of-target diffs, keep mutation disabled, and hand the validated output to the existing `patchTestSecondAttempt` dry-run boundary.

Current pointer override after Run 594: Continue the Codex-style closed-loop core by turning the new failed-log proposal plan into either a real local-model proposal generator boundary or a server/queue API contract that can persist and request approval for the generated revised patch. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM generation, real partial reindex, real partial-reindex job enqueue, or a fully model-derived second retry mutation. It added `learnbot.local-agent.revised-patch-proposal-plan.v1` in `LocalAgentApprovedExecutionFlowContract`: a failed `command.runAllowed` plus known target files now produces a bounded proposal plan with command id/status/exit evidence, capped stdout/stderr/error evidence, target files/count/readiness, disabled local-LLM generation, `mutationAllowed=false`, `dryRunOnly=true`, `dryRunRequiredBeforeApproval=true`, and `approvalRequiredBeforeMutation=true`. The generated `codex-closed-loop-report.v1` now includes `revisedPatchProposalPlan` between `patchTestRetryDecision` and `patchTestSecondAttempt`; `patchTestSecondAttempt` and the durable queue second-attempt self-test now consume proposal entries derived from this plan rather than directly relying on fixture-authored proposal metadata. `local-agent/README.md` documents the new self-test. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-proposal-plan-contract`, `dotnet run --project local-agent --no-build -- self-test patch-test-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, and a generated approved-flow report assertion showing `revisedPatchProposalPlan` plus `learnbot.local-agent.revised-patch-proposal-plan.v1`. Remaining risk: the plan is still a disabled contract/report slice; no local model creates a real revised diff from stdout/stderr, no backend approval object is persisted from the proposal, no second approved mutation is executed after user approval, and no real partial reindex or final publication/acknowledgement save runs. Next work should connect this proposal-plan output to a concrete proposal-generation boundary: either an explicit local LLM planning request with strict file/diff caps and no mutation, or a backend queue/report endpoint that stores the proposal plan and drives the approval UI before enqueueing the revised dry-run.

Current pointer override after Run 593: Continue the Codex-style closed-loop core by pivoting from the now-modeled disabled partial-reindex reporting chain to the larger missing Codex-like loop gap: deriving a revised patch proposal from failed `command.runAllowed` stdout/stderr instead of relying on fixture-authored retry patches. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM planning, real partial reindex, real partial-reindex job enqueue, or a fully model-derived second retry mutation. It added a disabled partial-reindex enqueue/readiness boundary under the existing `learnbot.local-agent.partial-reindex-plan.v1`. The new `learnbot.local-agent.partial-reindex-enqueue-boundary.v1` carries repository id, release/source/session/user/agent/workspace identity, target files/count, repository/target readiness, marker-required state, and explicit disabled flags for enqueue, job creation, partial reindex, RAG freshness update, and mutation. `approvedExecutionFlowFinalResultHandoff` now exposes boundary status, ready state, and repository id; timeline/preview summaries preserve the same status/ready fields; and the Code workspace final-result handoff UI renders the enqueue boundary status, readiness, and repository id while all execution controls stay disabled. Verified with `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, approved backend Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest" test` which ran 65 tests with 0 failures, `npm run build` from `frontend`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: partial reindex is still a disabled readiness boundary only; no reindex job is created or executed, no final publication or acknowledgement save exists, the second retry patch is still not autonomously produced from failure logs, and the Local Agent still trusts claimable queued work rather than independently querying an approval service. Next work should stop expanding disabled RAG reporting unless needed for a concrete UI/API gate, and instead add a backend/Local Agent contract that turns a failed allowlisted command result into a bounded revised-patch proposal plan with stdout/stderr evidence, target files, no mutation, and explicit approval requirement.

Current pointer override after Run 592: Continue the Codex-style closed-loop core by deciding whether to keep strengthening the disabled partial-reindex/reporting chain or move back to the next Local Agent closed-loop execution gap: autonomous retry patch generation from failed logs. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM planning, real partial reindex, or a fully model-derived second retry mutation. It added a disabled partial-reindex plan object to the approved-flow final-result/RAG freshness handoff. `LocalAgentRagFreshnessMarkerBuilder` now emits `learnbot.local-agent.partial-reindex-plan.v1` under `partialReindexPlan`, carrying source/release/session/user/agent/workspace identity, approval request id from post-retry verification, target files, target file count, intended freshness action, and explicit disabled flags for partial reindex, RAG freshness update, publication, acknowledgement save, and mutation. `approvedExecutionFlowFinalResultHandoff` now exposes that plan plus plan status, target files, and freshness action. Timeline/preview summaries preserve `partialReindexPlanStatus`, and the Code workspace final-result handoff UI renders the plan status, action, and target files while keeping final result, publication, RAG freshness update, partial reindex, acknowledgement, follow-up mutation, and mutation controls disabled. Verified with `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, approved backend Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest" test` which ran 65 tests with 0 failures, `npm run build` from `frontend`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: partial reindex is still a disabled plan/read-model only; no reindex job is enqueued or executed, no final publication or acknowledgement save exists, the second retry patch is still not autonomously produced from failure logs, and the Local Agent still trusts claimable queued work rather than independently querying an approval service. Next work should either add a real-but-disabled partial-reindex enqueue/readiness boundary tied to repository id and target files, or pivot to the larger missing Codex-like loop gap: deriving the revised patch proposal from failed `command.runAllowed` stdout/stderr instead of using fixture-authored retry patches.

Current pointer override after Run 591: Continue the Codex-style closed-loop core by adding a disabled partial-reindex plan/marker object that names target files and the intended freshness action, without executing reindex or enabling publication/acknowledgement/follow-up mutation. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM planning, real partial reindex, or a fully model-derived second retry mutation. It surfaced the Run 590 post-retry verification/freshness handoff in the Code workspace final-result UI. `agentLoopRunnerHandoffSummary` now renders `approval linked`, `post-retry verification passed`, and `partial reindex marker required` on the approved execution-flow completed line, and renders final-result handoff details for post-retry pass state, approval/release linkage, and partial reindex marker requirement before the stale RAG marker. Route-level Code workspace coverage now proves those fields appear in rendered markup for the completed-flow final-result handoff while final result, publication, RAG freshness update, partial reindex, acknowledgement, follow-up mutation, and mutation controls remain false. Verified with `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, `npm run build` from `frontend`, approved backend Maven `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest" test` which ran 65 tests with 0 failures, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Remaining risk: this is still UI/read-model visibility only; there is no dedicated partial-reindex plan object yet, no real partial reindex execution, no final publication, no acknowledgement save, no autonomous retry patch generation from failure logs, and the Local Agent still trusts claimable queued work rather than independently querying an approval service. Next work should add a narrow disabled partial-reindex plan/marker object to the backend final-result/RAG freshness handoff, including target files, source/release/approval identity, intended action, and disabled execution flags, then render it in the same Code workspace final-result handoff UI.

Current pointer override after Run 590: Continue the Codex-style closed-loop core by moving from audit-only post-retry/freshness handoff into the next narrow user-visible reporting surface, while keeping publication, acknowledgement save, real partial reindex, and follow-up mutation disabled. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM planning, real partial reindex, or a fully model-derived second retry mutation. It wired the Run 589 `postRetryVerification` contract into the approved-flow final-result/RAG freshness path. `appendLoopApprovedExecutionFlowCompletedEvent` now refuses completed-flow handoff unless the approved execution rows are also `approvalRequestLinked=true`, passes the approved-flow inspection into `approvedExecutionFlowFinalResultHandoff`, and the final-result handoff now exposes `postRetryVerification`, pass/link booleans, and `postRetryVerificationPartialReindexMarkerRequired`. `LocalAgentRagFreshnessMarkerBuilder` now accepts optional post-retry verification context and carries observed/passed/approval-linked/release-linked/partial-reindex-marker-required fields alongside the existing stale-index marker. `CodeAgentLoopTimelineRepository.appendApprovedExecutionFlowCompleted` and `CodeAgentLoopPreviewService` now preserve those fields so the runner preview/final-result boundary can report whether the approved retry verification actually passed before warning about stale RAG freshness. Added a gateway regression that completes a durable approved execution flow and proves the final-result handoff contains the linked passing post-retry verification and partial reindex marker requirement, plus preview coverage that preserves the same fields. Verified with approved backend Maven: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest" test` ran 65 tests with 0 failures, and `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,LocalAgentPatchMutationInputBuilderTest,CodeAgentLocalPatchRequestServiceTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopPreviewServiceTest" test` ran 88 tests with 0 failures. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Local Agent regression checks also passed: `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`. Remaining risk: this is still a final-result/read-model handoff, not a real partial reindex or final publication path; the UI does not yet render the new post-retry verification/freshness fields; acknowledgement save remains disabled; the second retry patch is still not autonomously produced from failure logs by a local model; and the Local Agent still trusts claimable queued work rather than independently querying an approval service. Next work should surface these post-retry verification and partial-reindex/stale-index fields in the Code workspace final-result handoff UI, then add a narrow disabled partial-reindex plan/marker object that names target files and index freshness action without executing reindex.

Current pointer override after Run 589: Continue the Codex-style closed-loop core by connecting the approved retry flow to final-report/RAG freshness surfaces, using the newly explicit post-retry verification contract as the handoff point. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM planning, real partial reindex, or a fully model-derived second retry mutation. It extended `LocalAgentApprovedExecutionFlowContract` so approved-flow summaries now expose `approvalRequestLinked`, carry each step's `approvalRequestId`, and include `learnbot.local-agent.post-retry-verification.v1`. The post-retry verification slice observes the terminal `command.runAllowed` result, requires `PASSED`, and only marks `partialReindexMarkerRequired=true` when the verification command is linked to the same nonblank approval id and same source/release attempt as the mutation sequence. Service and live repository contract fixtures now persist/assert that approval id through the approved execution rows, so a passing verification command is no longer treated as sufficient unless it belongs to the approved retry chain. Verified with approved backend Maven: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest" test` ran 50 tests with 0 failures, and `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,LocalAgentPatchMutationInputBuilderTest,CodeAgentLocalPatchRequestServiceTest,CodeAgentLoopToolSelectionServiceTest" test` ran 73 tests with 0 failures. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Local Agent regression checks also passed: `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`. Remaining risk: this is still a read-model/contract slice; the final-result handoff does not yet consume `postRetryVerification` directly, real partial reindex has not executed, final report publication and acknowledgement save remain disabled, UI rendering for this verification state is missing, the second retry patch is still not autonomously produced from failure logs by a local model, and the Local Agent still trusts claimable queued work rather than independently querying an approval service. Next work should wire `postRetryVerification` into the approved-flow final-result handoff/RAG freshness marker so final reporting explicitly states whether the approved retry's verification passed and whether target files need partial reindex or stale-index disclosure.

Current pointer override after Run 588: Continue the Codex-style closed-loop core by carrying the persisted approval id through the guarded release-created execution sequence. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM planning, second approved retry mutation execution, post-retry test execution, or real RAG freshness update. It updated `LocalAgentToolGatewayService.createApprovedExecutionSequenceRowsIfEnabled` so the guarded release-created follow-up rows are created only when the mutation input and released patch row both contain the same nonblank persisted `approvalRequestId`. The same id is now copied into every release-created `command.runAllowed`, `git.status`, and `rollback.restore` follow-up row under `learnbot.local-agent.approved-execution-sequence.v1`. A new regression proves mismatched released-row approval ids throw before any follow-up queue rows are created. Verified with approved backend Maven: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,LocalAgentPatchMutationInputBuilderTest,CodeAgentLocalPatchRequestServiceTest,CodeAgentLoopToolSelectionServiceTest" test` ran 73 tests with 0 failures. Local Agent regression checks also passed: `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`. Remaining risk: this protects the release-created sequence boundary, but it is still feature-flagged and not a real second retry mutation flow from failed test logs; UI rendering for approval persistence is still missing; the Local Agent still trusts claimable queued work rather than querying an approval service; no post-retry `command.runAllowed`, final report publication, acknowledgement save, or partial RAG freshness update has executed. Next work should add the post-retry verification slice: after an approved id-matched mutation sequence, require a follow-up allowlisted test command result to be linked to the same approval/release id, then produce a partial RAG freshness marker/report instead of only generic approved-flow rows.

Current pointer override after Run 587: Continue the Codex-style closed-loop core by making the persisted approval-id contract visible to API/UI readiness surfaces before broadening second-mutation execution. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM planning, second approved mutation execution, post-retry test execution, or real RAG freshness update. It added `approvalPersistence` summaries to `CodeAgentLoopToolSelectionService` patch approval responses and `LocalAgentToolGatewayService` patch release readiness/gate maps. The summary exposes schema, `approvalRequestId`, id-present status, persistence-required/persisted flags, and a UI-ready release blocking reason, so callers do not need to parse raw request input to explain why an approved patch is still non-claimable. Release readiness now also includes `approvalRequestPersisted` as an explicit precondition, so legacy approved-held rows without `approvalRequestId` remain blocked even when dry-run, repository verification, snapshot, rollback, and capabilities are otherwise ready. Verified with approved backend Maven: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,LocalAgentToolGatewayServiceTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentPatchMutationInputBuilderTest" test` ran 72 tests with 0 failures. Local Agent regression checks also passed: `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`. Remaining risk: approval persistence is still represented inside the existing tool execution row rather than a dedicated approval review table; UI rendering for the new `approvalPersistence` summary is not implemented; release still depends on feature flags and fresh linked dry-run evidence; the Local Agent still trusts claimable queued work rather than querying an approval service; no second approved retry mutation, post-retry test, final report publication, acknowledgement save, or partial reindex has executed. Next work should extend the release/queue path so the second approved mutation sequence can be created only from the matching persisted `approvalRequestId`, then add post-retry `command.runAllowed` and partial RAG freshness marker/report contracts.

Current pointer override after Run 586: Continue the Codex-style closed-loop core by moving the revised patch approval-id contract from Local Agent report fixtures into the backend server approval/release boundary. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, publication, acknowledgement save, autonomous local-LLM planning, second approved mutation execution, post-retry test execution, or real RAG freshness update. It updated `CodeAgentLocalPatchRequestService.prepare` so validated `patch.apply` approval rows now persist a deterministic `approvalRequestId` plus `approvalPersistenceRequired=true` and `approvalPersisted=true` in the durable `local_agent_tool_executions.input` JSON. It updated `LocalAgentToolGatewayService.inspectPatchExecutionReadiness` with an `approvalRequestIdPresent` check, so an approved-held patch without a persisted approval id cannot pass release readiness. It updated `LocalAgentPatchMutationInputBuilder` so release mutation input refuses missing approval ids and carries the same `approvalRequestId` into the claimable mutation input. Tests now assert the validated approval request input, tool-selection response, release mutation input, gateway readiness fixtures, and live Postgres fixture carry this persisted approval id contract. Verified with approved backend Maven: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLocalPatchRequestServiceTest,LocalAgentPatchMutationInputBuilderTest,CodeAgentLoopToolSelectionServiceTest,LocalAgentToolGatewayServiceTest" test` ran 71 tests with 0 failures. Restricted Maven still fails before dependency resolution because sandbox networking blocks Maven Central. Local Agent regression checks also passed: `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`. `git diff --check` reported only LF-to-CRLF warnings. Remaining risk: the persisted approval id is stored inside the existing tool execution row, not a dedicated approval table or UI review object; release still depends on the existing feature flag and fresh linked dry-run evidence; Local Agent still executes a claimable queued request rather than independently querying an approval service; no second approved retry mutation, post-retry test, final report publication, acknowledgement save, or partial reindex has been executed. Next work should add a narrow backend/API inspection or UI-ready response that surfaces this persisted `approvalRequestId` and release-blocking reason clearly, then extend the release/queue path so the second approved mutation sequence can be created only from that approved id and followed by post-retry test and partial reindex markers.

Current pointer override after Run 585: Continue the Codex-style closed-loop core by adding a server-facing approval-id/persistence gate contract for revised patch mutation. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, real database-backed approval persistence, autonomous local-LLM planning, second approved mutation execution, post-retry test execution, or real RAG freshness update. It extended `learnbot.local-agent.revised-patch-approval-request.v1` in `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs` so a `READY` approval request now carries a deterministic `approvalRequestId`, marks `approvalPersistenceRequired=true`, and adds `approvalRequestIdPresent`/`approvalRequestIdMatchesPersistedRequest` to the next queued mutation preconditions. It added `learnbot.local-agent.revised-patch-approval-persistence.v1` and `learnbot.local-agent.revised-patch-mutation-gate.v1` report builders plus `self-test revised-patch-approval-gate-contract`. The gate report permits a revised mutation candidate only when the approval request is persisted and approved, the candidate includes the matching `approvalRequestId`, `mutationAllowed=true`, `dryRunOnly=false`, and a snapshot manifest id is present. Missing or mismatched approval ids are `BLOCKED`. The queue-backed second-attempt self-test now also builds the persistence report and verifies both an `ALLOWED` matching-id gate and a `BLOCKED` missing-id gate without executing the second mutation. `local-agent/README.md` documents the new self-test. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-gate-contract`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-request-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`. Remaining risk: approval persistence is still an in-memory/report contract, not a backend database record or web UI approval object; the Local Agent mutation tool itself still trusts the already-approved queued tool request and does not independently query server approval ids; the revised patch is still fixture-authored; there is no second approved mutation execution, post-retry test run, or real partial reindex. Next work should move this approval-id contract into the backend/server queue surface with a persisted approval record or preview endpoint, then have the queued second mutation include and validate that persisted approval id before Local Agent execution.

Current pointer override after Run 584: Continue the Codex-style closed-loop core by adding an approval-request report contract for a revised patch dry-run result. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, approval persistence, second approved mutation execution, post-retry test execution, or real RAG freshness update. It added `learnbot.local-agent.revised-patch-approval-request.v1` inside `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs`, built from the existing `patchTestSecondAttempt` report and an optional revised `patch.apply` dry-run response. The report becomes `READY` only when the second-attempt status is `APPROVAL_REQUIRED`, the dry-run passed, the dry-run response is the expected mutation-disabled `REJECTED`/`UNSAFE_TOOL` result, and no actual mutation was executed. It records approval requirement, `approvalStateRequired=APPROVED`, target files, diff evidence, dry-run evidence, snapshot/rollback observations, `mutationAllowedBeforeApproval=false`, stale-index disclosure, partial reindex requirement after approval, and next queued mutation preconditions (`approvalState=APPROVED`, `mutationAllowed=true`, `dryRunOnly=false`, target files matching dry-run, snapshot manifest present). The generated `learnbot.local-agent.codex-closed-loop-report.v1` now includes `revisedPatchApprovalRequest` after `patchTestSecondAttempt`. The queue-backed second-attempt self-test now also builds and verifies this approval request from the queued failed command and revised dry-run responses. Added `self-test revised-patch-approval-request-contract` and documented it in `local-agent/README.md`. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test revised-patch-approval-request-contract`, `dotnet run --project local-agent --no-build -- self-test patch-test-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, and a generated `--report` assertion showing `revisedPatchApprovalRequest`, `learnbot.local-agent.revised-patch-approval-request.v1`, and `NOT_READY` for the no-retry successful path. Remaining risk: this is still an in-memory report/contract; approval is not persisted server-side, not shown in the web UI, not tied to an approval id, and does not yet enqueue the second mutation only after a saved approval. The revised patch is still fixture-authored rather than derived by a local model from stdout/stderr, and there is still no post-approval re-test or real partial reindex. Next work should add a persisted approval-request/approval-id contract on the server-facing queue/report boundary, then gate the second mutation request on that approval id.

Current pointer override after Run 583: Continue the Codex-style closed-loop core by making the durable queue-backed flow carry a failed command and second-attempt dry-run path while still stopping before mutation. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, second approved mutation execution, post-retry test execution, or real RAG freshness update. It added `self-test approved-server-queue-second-attempt-contract` in `local-agent/Tools/Flow/ApprovedServerQueueFlowContract.cs`. The fixture queues an approved first `patch.apply` mutation, an allowlisted `command.runAllowed` using `dotnet.test` that fails in the temporary workspace, post-patch `git.status`, a revised `patch.apply` dry-run with `dryRunOnly=true` and `mutationAllowed=false`, and finally `rollback.restore`. It then builds the existing `patchTestRetryDecision` and `patchTestSecondAttempt` reports from the queued responses and verifies that the retry decision is `RETRY_RECOMMENDED`, the revised dry-run response is `REJECTED`/`UNSAFE_TOOL` with `dryRun=true`, `preflightPassed=true`, `mutationApplied=false`, and the second-attempt status is `APPROVAL_REQUIRED` with `actualMutationExecuted=false`. Rollback restores the original file. `local-agent/Program.cs` now exposes the new self-test command, and `local-agent/README.md` documents the durable queue second-attempt boundary. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test patch-test-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, and `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`. Remaining risk: the revised patch is still fixture-authored rather than derived by a local model from stdout/stderr, the queue does not yet persist a real user approval request for the revised patch, does not execute the second mutation after approval, does not re-run tests after that approval, and does not trigger real partial reindex. Next work should add an approval-request report/contract for the revised dry-run result, including target file, diff evidence, dry-run snapshot/rollback manifest, stale-index disclosure, and the explicit condition that the next queued mutation may run only after approval.

Current pointer override after Run 582: Continue the Codex-style closed-loop core by connecting the retry-decision report into a bounded second-attempt proposal contract that stops before mutation. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, real retry execution, or real RAG freshness update. It added `learnbot.local-agent.patch-test-second-attempt.v1` inside `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs`, built from the existing `patchTestRetryDecision` result and an optional `patch.apply` dry-run response. The report records whether retry is needed, revised patch proposal metadata, dry-run execution/pass state, dry-run status/failure/error, preflight/snapshot observations, `analysisOnly=true`, `mutationAllowed=false`, `actualMutationExecuted=false`, and the next step. If retry is not recommended, status is `NOT_NEEDED`; if retry is recommended but no dry-run has happened, status is `DRY_RUN_REQUIRED`; if the revised patch dry-run fails, status is `REPLAN_REQUIRED`; if dry-run passes with no mutation, status is `APPROVAL_REQUIRED` and `nextStep=request_user_approval_for_revised_patch`. The generated `learnbot.local-agent.codex-closed-loop-report.v1` now includes `patchTestSecondAttempt` after `patchTestRetryDecision`, so the local loop shape is now `workspace.tree -> workspace.search -> candidateSelection -> multiple file.read/multiFileRead -> git.status -> patch.apply -> command.runAllowed -> git.status -> patchTestRetryDecision -> patchTestSecondAttempt -> rollback/report/RAG freshness marker`. Added `self-test patch-test-second-attempt-contract` and documented it in `local-agent/README.md`. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test patch-test-second-attempt-contract`, `dotnet run --project local-agent --no-build -- self-test patch-test-retry-decision-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, and a generated `--report` assertion showing `patchTestSecondAttempt`, `learnbot.local-agent.patch-test-second-attempt.v1`, and `NOT_NEEDED`. Remaining risk: this is still a contract/report layer; the system does not yet derive a revised patch from stdout/stderr with a real local model, enqueue the second dry-run through the durable server queue, persist a user approval request, execute the second approved mutation, re-run tests after approval, or trigger real partial reindex. Next work should make the queue-backed flow carry the second-attempt dry-run request/response path for a failed command fixture, still stopping before mutation until explicit approval.

Current pointer override after Run 581: Continue the Codex-style closed-loop core by adding an audit-only patch/test failure-analysis and retry-decision report after the allowlisted command step. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, real retry execution, or real RAG freshness update. It added `learnbot.local-agent.patch-test-retry-decision.v1` inside `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs`, built from the existing `command.runAllowed` and post-patch `git.status` responses. The report records command status, failure code, exit code, timeout/truncation, stdout/stderr snippets, post-patch git cleanliness, diagnostic signals, `analysisOnly=true`, `mutationAllowed=false`, and whether a retry is recommended. Successful commands produce `NO_RETRY_NEEDED`; failed allowlisted test/timeouts produce `RETRY_RECOMMENDED` with `nextStep=replan_patch_from_failure_logs`; unsafe, unapproved, workspace, or path-boundary failures produce `BLOCKED` instead of retrying. The generated `learnbot.local-agent.codex-closed-loop-report.v1` now includes `patchTestRetryDecision` after `patchTestLoop`, so the local loop shape is now `workspace.tree -> workspace.search -> candidateSelection -> multiple file.read/multiFileRead -> git.status -> patch.apply -> command.runAllowed -> git.status -> patchTestRetryDecision -> rollback/report/RAG freshness marker`. Added `self-test patch-test-retry-decision-contract` and documented it in `local-agent/README.md`. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test patch-test-retry-decision-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, `dotnet run --project local-agent --no-build -- self-test multi-file-read-report-contract`, and a generated `--report` assertion showing `patchTestRetryDecision`, `learnbot.local-agent.patch-test-retry-decision.v1`, and `NO_RETRY_NEEDED`. Remaining risk: retry is still a report-only decision; the Local Agent does not yet execute an actual second planning/read/patch/test cycle, derive fixes from stdout/stderr with a local model, request a new approval for the revised patch, or trigger real partial reindex. Next work should connect this retry-decision report into a bounded second-attempt contract that models `failed command output -> revised patch proposal -> dry-run -> approval-required stop`, keeping mutation disabled until approval.

Current pointer override after Run 580: Continue the Codex-style closed-loop core by making the approved direct and queue-backed read-only flow actually execute multiple `file.read` requests after candidate discovery instead of only modeling multi-file reads in the report. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, retry after failed tests, or real RAG freshness update. It updated `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs` so the temporary closed-loop fixture now contains both `src/App.cs` and `src/Feature.cs`, `workspace.search` discovers both `Name` matches, the flow reads both files through approved `file.read` requests, and `BuildCodexClosedLoopReport`/`WriteApprovedExecutionFlowReport` accept a list of read responses. The generated `readOnlyToolLoop` now records multiple `file.read` step evidences, and `multiFileRead` reports `selectedFileCount=2`, `readFileCount=2`, and `allSelectedFilesRead=true` when all selected files are read. It updated `local-agent/Tools/Flow/ApprovedServerQueueFlowContract.cs` so the durable polling path queues and completes both `file.read` requests before pre-patch `git.status`, patch, allowed command, post-patch status, and rollback. `local-agent/README.md` documents the approved closed-loop sequence as multiple `file.read` observations rather than a single read. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test multi-file-read-report-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, and a generated `--report` assertion showing `"readFileCount": 2`, `src/Feature.cs`, and `"allSelectedFilesRead": true`. Remaining risk: candidate selection is still deterministic and driven by a literal search query in the fixture; it does not yet derive search terms from arbitrary user goal text, call a local model, generate a patch from read observations, analyze failed test output, decide whether to retry, or perform real partial reindex. Next work should add a failed `command.runAllowed` output analysis and retry-decision report to close the patch/test loop before broader mutation.

Current pointer override after Run 579: Continue the Codex-style closed-loop core by connecting selected candidate files into an audit-only multi-file read report. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, or real RAG freshness update. It added `learnbot.local-agent.multi-file-read-report.v1` inside `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs`, built from the existing `candidateSelection` report plus one or more `file.read` responses. The report compares selected files with actual read files, records read status/request id/bytes/returned bytes/truncation, exposes missing selected files, failed read count, `allSelectedFilesRead`, and keeps `readOnly=true`/`mutationAllowed=false` while reusing the existing `file.read` tool instead of opening a new execution surface. Added `self-test multi-file-read-report-contract` to pin full success, partial missing-file behavior, truncation visibility, and disabled mutation. The generated `learnbot.local-agent.codex-closed-loop-report.v1` now includes `multiFileRead` after `candidateSelection`, so the local read-only loop shape is now `workspace.tree -> workspace.search -> candidateSelection -> multiFileRead/file.read -> git.status -> patch/test/status/rollback/report`. `local-agent/README.md` documents the new self-test and updated closed-loop sequence. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test multi-file-read-report-contract`, `dotnet run --project local-agent --no-build -- self-test read-only-candidate-selection-contract`, `dotnet run --project local-agent --no-build -- self-test workspace-search-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, and a generated `--report` assertion showing `multiFileRead`, `multi-file-read-report`, and `allSelectedFilesRead`. Remaining risk: the report can model multiple selected/read files, but the current direct/queue approved-flow fixture still reads one file; it does not yet enqueue multiple `file.read` requests from selected files, derive search terms from arbitrary user goal text, call a local model, retry after failed tests, or perform real partial reindex. Next work should extend the queue/direct read-only flow to actually issue multiple `file.read` requests from selected files, then add failed `command.runAllowed` output analysis and retry-decision reporting before broader mutation.

Current pointer override after Run 578: Continue the Codex-style closed-loop core by adding the audit-only candidate-selection contract that turns local read-only observations into concrete next-file choices. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, or real RAG freshness update. It added `learnbot.local-agent.read-only-candidate-selection.v1` inside `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs`, built from `workspace.tree` and `workspace.search` responses. The report selects ranked files for the next `file.read` step, prefers search matches, falls back explicitly to tree candidates when search evidence is unavailable, records search/tree evidence counts, keeps `readOnly=true`, `mutationAllowed=false`, and leaves model ranking disabled. Added `self-test read-only-candidate-selection-contract` to pin search-preferred selection, tree fallback selection, selected-file source labels, next-tool routing, and disabled mutation/model-ranking flags. The generated `learnbot.local-agent.codex-closed-loop-report.v1` now includes `candidateSelection` between `readOnlyToolLoop` and `patchTestLoop`, so the local loop shape is now `workspace.tree -> workspace.search -> candidateSelection -> file.read -> git.status -> patch/test/status/rollback/report`. `local-agent/README.md` documents the new self-test and updated closed-loop sequence. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test read-only-candidate-selection-contract`, `dotnet run --project local-agent --no-build -- self-test workspace-search-contract`, `dotnet run --project local-agent --no-build -- self-test workspace-tree-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, and a generated `--report` assertion showing `candidateSelection`, `read-only-candidate-selection`, and `selectedFiles`. Remaining risk: candidate selection is still deterministic and audit-only; it does not yet derive search terms from arbitrary user goal text, call a local model, read multiple selected files automatically, retry after failed tests, or perform real partial reindex. Next work should connect selected files into a multi-file read contract/report, then add failed `command.runAllowed` output analysis and retry-decision reporting before broader mutation.

Current pointer override after Run 577: Continue the Codex-style closed-loop core from project enumeration into bounded read-only candidate search. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, or real RAG freshness update. It added bounded read-only `workspace.search` support to `local-agent/Program.cs`: the tool is advertised in heartbeat capabilities, available through the durable tool handler, exposed as `learnbot file search --workspace-id <workspace-id> --query <text>`, constrained to approved workspace roots, rejects path escape, skips generated directories and binary/oversized files, uses case-insensitive text matching, and caps searched files, bytes per file, and returned matches. Added `self-test workspace-search-contract` to pin candidate search, excluded generated directories, binary skip, match truncation, missing-query failure, and path-escape refusal. `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs` and `local-agent/Tools/Flow/ApprovedServerQueueFlowContract.cs` now start the read-only loop with `workspace.tree`, then `workspace.search`, then `file.read` and pre-patch `git.status`, so both direct and queue-backed closed-loop contracts model project enumeration plus candidate search before mutation. The generated `learnbot.local-agent.codex-closed-loop-report.v1` now records `workspace.search` as `candidateSearch` inside `readOnlyToolLoop`. `local-agent/README.md` documents the new tool and updated closed-loop sequence. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test workspace-search-contract`, `dotnet run --project local-agent --no-build -- self-test workspace-tree-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, and a generated `--report` assertion showing `workspace.search`, `candidateSearch`, and `readOnlyToolLoop`. Remaining risk: this still uses a literal query supplied by the server/CLI; it does not yet rank candidate files from arbitrary user goal text, call a local model, choose and read multiple files automatically, retry after failed tests, or perform real partial reindex. Next work should add a read-only candidate-selection/report contract that turns `workspace.tree` plus `workspace.search` observations into selected files for `file.read`, then connect failed `command.runAllowed` output into a retry-decision report before broader mutation.

Current pointer override after Run 576: Continue the Codex-style closed-loop pivot by adding the first real Local Agent project exploration tool instead of adding more setup scripts. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a production service supervisor, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, autonomous local-LLM planning, or real RAG freshness update. It added bounded read-only `workspace.tree` support to `local-agent/Program.cs`: the tool is advertised in heartbeat capabilities, available through the durable tool handler, exposed as `learnbot file tree --workspace-id <workspace-id>`, constrained to approved workspace roots, capped by max entries/depth, rejects path escape, and skips large/generated directories such as `.git`, `node_modules`, `bin`, `obj`, `target`, and `dist`. Added `self-test workspace-tree-contract` to pin path containment, excluded-directory behavior, truncation, and successful listing of nested source files. `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs` and `local-agent/Tools/Flow/ApprovedServerQueueFlowContract.cs` now start the read-only loop with `workspace.tree` before `file.read` and pre-patch `git.status`, and the generated closed-loop report records `workspace.tree` as project exploration inside `readOnlyToolLoop`. `local-agent/README.md` documents the new tool and the updated closed-loop sequence. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test workspace-tree-contract`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, and a generated `--report` run showing `workspace.tree` in both top-level `steps` and `closedLoop.readOnlyToolLoop`. Remaining risk: this still enumerates files only; it does not search file contents, rank candidate files from a user goal, call a local model, read multiple selected files automatically, retry after failed tests, or perform real partial reindex. Next work should add a bounded read-only content search or candidate-selection contract under the approved workspace, then connect failed `command.runAllowed` output into a retry-decision report before enabling broader mutation.

Current pointer override after Run 575: Pivot from M8 script/setup productization back to the Codex-style closed-loop core requested by the user. This run did not add new PowerShell scripts, did not create a backend device-code endpoint, did not execute login, did not issue a real pairing token, did not start a production service supervisor, did not enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or real RAG freshness update. It strengthened the Local Agent approved execution flow contract in `local-agent/Tools/Flow/ApprovedExecutionFlowContract.cs` so the self-test now models a fuller local Codex loop: read-only `file.read` and pre-patch `git.status`, approved `patch.apply`, allowlisted `command.runAllowed`, post-patch `git.status`, approved `rollback.restore`, and a generated `learnbot.local-agent.codex-closed-loop-report.v1` section with `readOnlyToolLoop`, `patchTestLoop`, `rollbackLoop`, `finalReport`, and `ragFreshnessMarker`. The marker explicitly reports `STALE_UNTIL_PARTIAL_REINDEX`, `partialReindexRequired=true`, `partialReindexEnabled=false`, target files, and final-report stale-index disclosure requirements. The queue-backed Local Agent approved server flow in `local-agent/Tools/Flow/ApprovedServerQueueFlowContract.cs` now also queues and completes the same pre-mutation read-only `file.read` and `git.status` observations before the existing patch/test/status/rollback sequence, proving the durable polling path can carry the same loop shape. `local-agent/README.md` now describes `approved-execution-flow-contract` as a narrow Codex-style closed loop rather than only a patch/test/rollback slice. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test approved-execution-flow-contract`, `dotnet run --project local-agent --no-build -- self-test approved-server-queue-flow-contract`, a generated `--report` run showing `readOnlyToolLoop`, `patchTestLoop`, `finalReport`, and `ragFreshnessMarker`, `dotnet run --project local-agent --no-build -- self-test patch-apply-mutation-contract`, `dotnet run --project local-agent --no-build -- self-test rollback-restore-contract`, `dotnet run --project local-agent --no-build -- self-test allowed-test-runner-contract`, `dotnet run --project local-agent --no-build -- self-test patch-dry-run-contract`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, `dotnet run --project local-agent --no-build -- self-test pair-atomic-config-contract`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentPairFromWebTokenResult.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentPairFromWebTokenPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentBrowserPairingPlan.test.ps1`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupRunPlan.test.ps1`. Remaining risk: this is still a contract/self-test loop rather than an autonomous planner loop from arbitrary user goal text; there is no Local LLM planner yet that chooses files, iterates on failing tests, edits multiple files, or triggers real partial reindex; final report generation and RAG freshness update remain audit-only markers. Next work should continue closed-loop completeness, not script growth: add a real read-only project exploration loop surface that can enumerate/select files under an approved workspace and feed observations into patch planning, then connect patch/test failure logs back into a retry decision contract before enabling any broader mutation.

Current pointer override after Run 574: Continue M8 productization from the structured `pair-from-web-token` result contract by hardening the underlying Local Agent `learnbot pair` config-write boundary. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a background process, install/start/stop/delete a Windows Service, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It changed `local-agent/Program.cs` so `learnbot pair` now builds a candidate config, sends the initial heartbeat with that candidate, and saves config only after the heartbeat succeeds. If the server is unreachable or rejects the token during the initial heartbeat, a new config file is not created and an existing config remains preserved. Added `self-test pair-atomic-config-contract` to verify both no-config-created-on-failed-pair and existing-config-preserved-on-failed-repair cases. `local-agent/README.md` now documents the atomic pair config-write behavior. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentPairFromWebTokenResult.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentPairFromWebTokenPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentBrowserPairingPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupRunPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServiceCommandPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, `dotnet run --project local-agent --no-build -- self-test pair-atomic-config-contract`, and a temp-config unreachable-server wrapper run that returned structured `FAILED` JSON while `config-exists=False`. Remaining risk: successful browser-token pairing still needs a real reachable server and valid web-issued token; there is still no true browser/device-code callback, no web UI pairing-token copy/paste affordance review, no service supervisor, no log rotation, no signed installer, no auto-update, and real `setup` still uses direct username/password login. Next M8 step should add a structured browser/device-code pairing request/claim preview contract on the backend/web side, or continue CLI productization with structured start/stop/log result contracts for day-to-day Local Agent lifecycle UX.

Current pointer override after Run 573: Continue M8 productization from the guarded `pair-from-web-token` execution action into a structured result contract. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a background process, install/start/stop/delete a Windows Service, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `scripts/local-agent/setup/LocalAgentPairFromWebTokenResult.ps1` with schema `learnbot.local-agent.pair-from-web-token-result.v1`, so `scripts/local-agent.ps1 -Action pair-from-web-token` now returns structured `SUCCEEDED`, `FAILED`, or `BLOCKED` JSON instead of raw helper output. The result contract preserves readiness blockers without executing local commands, records per-step status for `pairLocalAgentWithPastedToken`, `registerWorkspace`, and `showStatus`, stops after the first failed step, redacts the pasted pairing token from displayed commands and captured output, and keeps `cliPasswordCollected=false`/`tokenSecretPrinted=false`. The existing plan now exposes the safe parsed `agentId` value for result execution while keeping the token hidden. Added `scripts/local-agent/setup/LocalAgentPairFromWebTokenResult.test.ps1` to pin success, blocked, failed-step, stop-after-failure, no-command-on-blocked, and token-redaction behavior. `local-agent/README.md` now documents that `pair-from-web-token` returns the result schema. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentPairFromWebTokenResult.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentPairFromWebTokenPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentBrowserPairingPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupRunPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServiceCommandPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action pair-from-web-token -Server http://localhost:8083/ -WorkspacePath .\missing-workspace-for-pair-test -PairingAgentId not-a-guid -Transport auto`, which returned structured `BLOCKED` JSON, and a temp-config unreachable-server run against `http://127.0.0.1:9/`, which returned structured `FAILED` JSON at `pairLocalAgentWithPastedToken` without printing `secret-token`. Remaining risk: the successful path still needs a real reachable server and valid web-issued token because `learnbot pair` sends a heartbeat and writes config before heartbeat failure is known; there is still no true browser/device-code callback, no web UI pairing-token copy/paste affordance review, no service supervisor, no log rotation, no signed installer, and real `setup` still uses direct username/password login. Next M8 step should add a structured browser/device-code pairing request/claim contract on the backend/web side, or harden the Local Agent `pair` command so heartbeat failure can be reported without leaving a partially paired temp config unless explicitly accepted.

Current pointer override after Run 572: Continue M8 productization from the pasted web-token setup boundary into a guarded execution action. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, start a background process, install/start/stop/delete a Windows Service, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `scripts/local-agent.ps1 -Action pair-from-web-token`, which builds the existing `learnbot.local-agent.pair-from-web-token-plan.v1`, asserts readiness before side effects, then invokes `learnbot pair`, `learnbot workspace add`, and `learnbot agent status` only after workspace/GUID/token inputs pass the same contract. The action accepts `-PairingAgentId`/`-PairingToken` or `LEARNBOT_PAIRING_AGENT_ID`/`LEARNBOT_PAIRING_TOKEN`, avoids CLI password collection, and does not print the pasted token itself. `local-agent/README.md` now documents both the plan and execution forms, including that the underlying `learnbot pair` command still sends its initial heartbeat to the configured server. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action pair-from-web-token -Server http://localhost:8083/ -WorkspacePath .\missing-workspace-for-pair-test -PairingAgentId not-a-guid -Transport auto`, which failed before execution with `workspacePath, agentId, pairingToken` blockers; `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentPairFromWebTokenPlan.test.ps1`; `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentBrowserPairingPlan.test.ps1`; `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupRunPlan.test.ps1`; `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`; `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServiceCommandPlan.test.ps1`; `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`; `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`; `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`; `dotnet build local-agent\LearnBot.LocalAgent.csproj`; `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`; and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action pair-from-web-token-plan -Server http://localhost:8083/ -WorkspacePath . -PairingAgentId 11111111-1111-1111-1111-111111111111 -PairingToken secret-token -Transport auto`, whose output kept the token value hidden. Remaining risk: successful `pair-from-web-token` execution still requires a reachable server and a real web-issued token because `learnbot pair` sends a heartbeat; there is no true browser/device-code callback, no web UI token copy/paste affordance review in this run, no log rotation, no service supervisor, and real `setup` still uses direct username/password login. Next M8 step should either add a safer browser/device-code pairing endpoint and web UI flow, or add an execution result contract around `pair-from-web-token` so failed heartbeat/config-write/workspace-registration states are reported as structured JSON instead of raw command failure.

Current pointer override after Run 571: Continue M8 productization from the browser-owned Local Agent pairing preview contract into a pasted web-token setup boundary. This run did not create a backend device-code endpoint, execute login, issue a real pairing token, pair the Local Agent, write local config, register a workspace, start a background process, install/start/stop/delete a Windows Service, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `scripts/local-agent/setup/LocalAgentPairFromWebTokenPlan.ps1` with schema `learnbot.local-agent.pair-from-web-token-plan.v1`, a preview-only contract that accepts `AgentId`, `PairingToken`, `WorkspacePath`, server, transport, optional agent exe, and optional config path inputs, validates GUID/token/workspace readiness, and never prints the pasted token secret. `scripts/local-agent.ps1` now exposes `-Action pair-from-web-token-plan` with `-PairingAgentId`/`-PairingToken` parameters and matching `LEARNBOT_PAIRING_AGENT_ID`/`LEARNBOT_PAIRING_TOKEN` env fallback. The plan shows disabled `learnbot pair`, `workspace add`, and `status` steps while keeping `networkCallsEnabled=false`, `localCommandsEnabled=false`, `localConfigWriteEnabled=false`, `workspaceRegistrationEnabled=false`, `cliPasswordCollection=false`, and `serverLocalMutationEnabled=false`. Added `scripts/local-agent/setup/LocalAgentPairFromWebTokenPlan.test.ps1` to pin ready/blocked states, agent-id validation, token-presence validation, no CLI password collection, no config/workspace writes, assertion failure for blocked plans, and no token-secret output. `local-agent/README.md` now documents `pair-from-web-token-plan`. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentPairFromWebTokenPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentBrowserPairingPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupRunPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServiceCommandPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action pair-from-web-token-plan -Server http://localhost:8083/ -WorkspacePath . -PairingAgentId 11111111-1111-1111-1111-111111111111 -PairingToken secret-token -Transport auto`. Remaining risk: this is still preview-only; there is no real `pair-from-web-token` execution path, no true browser/device-code callback, no server-issued one-time device flow, and real `setup` still uses direct username/password login after its readiness guard. Next M8 step should add a guarded execution action that invokes `learnbot pair` and `workspace add` only after this readiness contract passes and still avoids password collection, or start the backend/web device-code pairing endpoint that removes manual token copy/paste.

Current pointer override after Run 570: Continue M8 productization from the browser-owned Local Agent pairing preview contract. This run did not create a new backend device-code endpoint, execute login, issue a real pairing token, pair the Local Agent, write local config, register a real workspace, start a background process, install/start/stop/delete a Windows Service, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `scripts/local-agent/setup/LocalAgentBrowserPairingPlan.ps1` with schema `learnbot.local-agent.browser-pairing-plan.v1`, a preview-only setup path where the browser owns login and pairing-token creation, the CLI does not accept or collect the password, token secrets are not printed, and the follow-up `learnbot pair`, `workspace add`, and `status` commands are shown as disabled plan steps. `scripts/local-agent.ps1` now exposes `-Action browser-pairing-plan`, deriving the normalized server/workspace/transport from the existing setup-plan contract while bypassing login id requirements with a browser-owned login placeholder. Added `scripts/local-agent/setup/LocalAgentBrowserPairingPlan.test.ps1` to pin ready and blocked browser-pairing states, no CLI password collection, no token secret output, and preview-only network/local command behavior. `local-agent/README.md` now documents browser-pairing-plan. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentBrowserPairingPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupRunPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServiceCommandPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action browser-pairing-plan -Server http://localhost:8083/ -WorkspacePath . -Transport auto`. Remaining risk: this is still preview-only and depends on the current web UI/API user manually creating a pairing token; there is no true device-code/browser callback flow, no CLI prompt for pasted agent id/token, and real `setup` still uses direct username/password login after its readiness guard. Next M8 step should add a guarded `browser-pair`/`pair-from-web-token` CLI contract that accepts pasted agent id/token and workspace path without password collection, then later connect it to a real web/device-code pairing UX.

Current pointer override after Run 569: Continue M8 productization from the setup-run readiness guard now wired into the real setup helper. This run did not perform a real setup, execute login, issue a real pairing token, pair the Local Agent, write local config, register a real workspace, start a background process, install/start/stop/delete a Windows Service, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `Assert-LearnBotLocalAgentSetupRunReady` to `scripts/local-agent/setup/LocalAgentSetupRunPlan.ps1`, and `scripts/local-agent.ps1 -Action setup` now builds the existing setup-plan/setup-run-plan and asserts readiness after login id collection but before prompting for the password or making any network calls. This keeps missing `loginId`/`workspacePath` failures on the same setup-run contract used by `-Action setup-run-plan` instead of reaching the direct login helper path. `scripts/local-agent/setup/LocalAgentSetupRunPlan.test.ps1` now pins both ready assertion success and blocked assertion failure without network calls or local config writes. `local-agent/README.md` documents that the current setup helper checks the setup-run readiness boundary before password prompt/server calls. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupRunPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServiceCommandPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action setup-run-plan -Server http://localhost:8083/ -WorkspacePath . -LoginId user@example.com -Transport auto`. Remaining risk: real setup still uses direct username/password login after readiness passes, and still lacks browser-based pairing/device-code UX, signed installer, updater, real Windows Service supervisor, log rotation, and CLI chat/fix/review. Next M8 step should add a browser-token or pairing-code guided setup contract that avoids collecting the password in the CLI, then migrate real setup toward that safer flow.

Current pointer override after Run 568: Continue M8 productization from the guided setup run preview boundary. This run did not execute login, issue a real pairing token, pair the Local Agent, write local config, register a real workspace, start a background process, install/start/stop/delete a Windows Service, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `scripts/local-agent/setup/LocalAgentSetupRunPlan.ps1` with schema `learnbot.local-agent.setup-run-plan.v1`, a preview-only guided setup execution boundary that consumes the existing setup-plan contract, reports ready/blocked state and missing inputs, keeps every setup step `executionEnabled=false`, keeps `networkCallsEnabled=false`, `localCommandsEnabled=false`, `localConfigWriteEnabled=false`, `workspaceRegistrationEnabled=false`, and keeps pairing token secrets hidden. `scripts/local-agent.ps1` now exposes `-Action setup-run-plan`, so users can inspect the actual guided setup execution boundary before the current real `setup` helper performs web login/pairing/workspace registration. Added `scripts/local-agent/setup/LocalAgentSetupRunPlan.test.ps1` to pin ready and blocked setup-run-plan states without network calls or local config writes. `local-agent/README.md` now documents setup-run-plan alongside setup-plan, lifecycle-status, service-plan, and service-command-plan. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupRunPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServiceCommandPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action setup-run-plan -Server http://localhost:8083/ -WorkspacePath . -LoginId user@example.com -Transport auto`. Remaining risk: guided setup execution remains preview-only; the real setup helper still uses direct username/password login and immediate local pair/workspace commands, and there is still no browser-based pairing flow, signed installer, updater, real Windows Service supervisor, log rotation, or CLI chat/fix/review. Next M8 step should convert the existing `setup` helper to pass through the setup-run-plan boundary before execution, or add a browser-token/pairing-code guided setup contract that avoids collecting the password in the CLI.

Current pointer override after Run 567: Continue M8 productization from the guarded Windows Service command preview boundary. This run did not install, start, stop, delete, or enable a real Windows Service; did not execute login, issue a real pairing token, register a real workspace, start a background process, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `scripts/local-agent/lifecycle/LocalAgentServiceCommandPlan.ps1` with schema `learnbot.local-agent.service-command-plan.v1`, a preview-only command boundary for service `install`, `start`, `stop`, and `uninstall` actions. The boundary consumes the existing service-plan contract, returns the planned command, keeps `executionEnabled=false`, always reports `serviceCommandExecutionDisabled`, adds prerequisite blockers such as `installedExecutable`, `pairedConfig`, `approvedWorkspace`, or `serviceNotInstalled` as appropriate, and keeps token secrets hidden with `executesServiceCommand=false`. `scripts/local-agent.ps1` now exposes `-Action service-command-plan -ServiceAction install|start|stop|uninstall`. Added `scripts/local-agent/lifecycle/LocalAgentServiceCommandPlan.test.ps1` to pin ready-but-disabled install behavior and service-missing start refusal without touching real services. `local-agent/README.md` now documents service-command-plan. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServiceCommandPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action service-command-plan -ServiceAction install -Transport auto`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action service-command-plan -ServiceAction start -Transport auto`. Remaining risk: service operations are still preview-only and there is no real Windows Service supervisor, recovery policy, least-privilege account decision, log rotation, signed installer, updater, or guided browser pairing. Next M8 step should connect `setup-plan` into a safer guided setup execution path, or explicitly add an approval/escalation gate before any real privileged service command is implemented.

Current pointer override after Run 566: Continue M8 productization from the new Windows Service preview plan contract. This run did not install, start, stop, delete, or enable a real Windows Service; did not execute login, issue a real pairing token, register a real workspace, start a background process, enable signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `scripts/local-agent/lifecycle/LocalAgentServicePlan.ps1` with schema `learnbot.local-agent.service-plan.v1`, a preview-only Windows Service readiness plan that checks the installed executable, paired config, approved workspace count, existing service state, service name, transport, interval, planned install/start/stop/uninstall commands, and safety flags (`requiresAdminApproval=true`, `executesServiceCommand=false`, `typedToolsOnly=true`, `arbitraryShellExecution=false`, `serverLocalMutationEnabled=false`, `tokenSecretPrinted=false`). `scripts/local-agent.ps1` now exposes `-Action service-plan`, deriving the config path from the existing `learnbot agent status` contract and the executable path from the installed/default Local Agent resolution. Added `scripts/local-agent/lifecycle/LocalAgentServicePlan.test.ps1` to pin ready and blocked service-plan states without touching real services. `local-agent/README.md` now documents `service-plan` alongside `lifecycle-status`. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentServicePlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action service-plan -Transport auto -IntervalSeconds 20`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action lifecycle-status`. Remaining risk: service-plan is still preview-only and does not implement a real service supervisor, recovery policy, least-privilege account decision, log rotation, signed installer, updater, or real guided browser pairing. Next M8 step should either connect `setup-plan` into a safer guided setup execution path or add the first guarded service install/uninstall dry-run/approval boundary before enabling any real privileged command.

Current pointer override after Run 565: Continue M8 productization from the new Local Agent lifecycle-status contract. This run did not execute login, issue a real pairing token, register a real workspace, start a background process, enable Windows Service registration, signed installer, auto-update, server-side mutation, publication, acknowledgement save, or RAG freshness update. It added `scripts/local-agent/lifecycle/LocalAgentLifecycleStatus.ps1` with schema `learnbot.local-agent.lifecycle-status.v1`, a read-only internal-pilot lifecycle view that reports config existence, paired/configured state without token secrets, workspace counts, run-state file status, process liveness, stale running state detection, log existence/size, optional agent executable path, Windows Service installed/running/start-type status, recommended lifecycle commands, and explicit safety flags (`typedToolsOnly=true`, `arbitraryShellExecution=false`, `serverLocalMutationEnabled=false`, `serviceInstallEnabled=false`, `hardenedRestartPolicy=false`). `scripts/local-agent.ps1` now exposes `-Action lifecycle-status`, deriving config/state/log paths from the existing `learnbot agent status` contract before rendering the lifecycle contract. Added `scripts/local-agent/lifecycle/LocalAgentLifecycleStatus.test.ps1` to pin configured/stale-state/service-disabled/token-hidden behavior without starting processes or touching real services. `local-agent/README.md` now documents the lifecycle-status command. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\lifecycle\LocalAgentLifecycleStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action lifecycle-status`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action status`. Remaining risk: lifecycle-status is still read-only and script-helper based; real guided setup still uses the direct username/password helper flow, background start/stop lacks restart policy and PID ownership hardening beyond the current guard, Windows Service registration is not implemented, logs UX is still basic tailing, and CLI chat/fix/review commands are absent. Next M8 step should either connect `setup-plan` into a safer guided `learnbot setup` path or add the next lifecycle slice for service-install preview/start-stop contracts, while preserving typed tools and keeping server-side mutation controls unchanged.

Current pointer override after Run 564: Continue M8 productization from the guided Local Agent setup-plan contract. This run did not execute login, issue a real pairing token, register a real workspace, start a background process, enable Windows Service mode, or change server-side mutation controls. It added `scripts/local-agent/setup/LocalAgentSetupPlan.ps1` with schema `learnbot.local-agent.setup-plan.v1`, a read-only guided setup plan that normalizes the server URL, checks workspace existence, reports whether login id/password inputs are present, lists missing inputs, describes the five intended setup steps (`login`, `issuePairingToken`, `pairLocalAgent`, `registerWorkspace`, `showStatus`), and keeps safety flags explicit (`typedToolsOnly=true`, `arbitraryShellExecution=false`, `serverLocalMutationEnabled=false`, `tokenSecretPrinted=false`, `windowsService=false`, `signedInstaller=false`). `scripts/local-agent.ps1` now exposes `-Action setup-plan` so users can preview the pairing/workspace setup flow before running the existing real `setup` action. Added `scripts/local-agent/setup/LocalAgentSetupPlan.test.ps1` to pin ready and blocked setup-plan contracts without network calls or user PATH changes. `local-agent/README.md` now documents the setup-plan command. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\setup\LocalAgentSetupPlan.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action setup-plan -Server http://localhost:8083/ -WorkspacePath . -LoginId user@example.com -Transport auto`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent.ps1 -Action status`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, and `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent-install.ps1 -Action status -InstallDir "C:\Users\honeybadger\Desktop\LearnBot\.tmp\learnbot-install"`. Remaining risk: setup-plan is still preview-only; real setup still uses direct username/password helper flow and lacks browser-based pairing UX, Windows Service registration, hardened background lifecycle, richer log diagnostics, signed MSI/EXE distribution, auto-update, proxy/internal-network handling, and CLI chat/fix/review commands. Next M8 step should either turn this setup plan into a safer guided `learnbot setup` CLI path or add service-lifecycle readiness/status contracts for background operation.

Current pointer override after Run 563: Continue M8 productization from the new Local Agent installer status contract. This run did not change server-side mutation, publication, acknowledgement save, RAG freshness update, Windows Service lifecycle, signed installer, auto-update, or agent orchestration controls. It added `scripts/local-agent/install/LocalAgentInstallStatus.ps1` with `learnbot.local-agent.install-status.v1`, including install mode, install directory, executable path, installed state, user PATH visibility, optional installed-executable `learnbot status` smoke output, recommended `status`/`doctor`/`start` commands, and explicit internal-pilot limitations (`windowsService=false`, `signedInstaller=false`, `autoUpdate=false`, `backgroundProcessManager=false`). `scripts/local-agent-install.ps1` now reuses that helper for `install`, `status`, and `uninstall`; install/status responses can include the installed executable's status JSON, and the post-install guidance now points at top-level `learnbot status`. Added `scripts/local-agent/install/LocalAgentInstallStatus.test.ps1` to pin the contract without modifying the real user PATH. `local-agent/README.md` now documents `.\scripts\local-agent-install.ps1 -Action status` and the install-status schema. Verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent\install\LocalAgentInstallStatus.test.ps1`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent-install.ps1 -Action status -InstallDir "C:\Users\honeybadger\Desktop\LearnBot\.tmp\learnbot-install"`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent-install.ps1 -Action install -InstallDir "C:\Users\honeybadger\Desktop\LearnBot\.tmp\learnbot-install-contract" -Clean`, and `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`. Remaining risk: M8 still lacks guided pairing/setup, Windows Service registration, hardened background start/stop, richer log diagnostics, signed MSI/EXE distribution, auto-update, proxy/internal-network handling, and CLI chat/fix/review commands. Next M8 step should add a small guided setup contract around pairing/workspace registration or a service-lifecycle readiness contract, while preserving typed tools and keeping server-side mutation controls unchanged.

Current pointer override after Run 562: M8 proper has started with the first PowerShell-friendly `learnbot` CLI productization slice. This run kept server-side mutation, publication, acknowledgement save, RAG freshness update, installer service lifecycle, and automatic orchestration unchanged. The Local Agent executable already published as `learnbot`; this run added top-level `learnbot status` as a short alias for Local Agent status and kept `learnbot doctor` as the top-level diagnostic command. Status/doctor JSON assembly moved out of the oversized `Program.cs` into `local-agent/Cli/CliReadinessReports.cs`, which reports command name, version, configured/paired state, server URL, agent id, transport, workspace counts, config/log/state paths, executable path, running state, and doctor checks for pairing, workspace registration, local state writability, token-secret hiding, typed-tool boundary, and side-effect approval boundary. The doctor report deliberately does not expose the pairing token and does not enable any local side-effect controls. A new `self-test cli-status-doctor-contract` creates a temporary agent config/workspace, verifies top-level CLI readiness reports show paired/workspace/transport state, verifies the token secret is absent from serialized status/doctor JSON, and verifies safe tool/approval boundary checks are present. `local-agent/README.md` now documents `dotnet run --project local-agent -- status`, `doctor`, and installed `learnbot status` / `learnbot doctor` usage. Verified with `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `dotnet run --project local-agent --no-build -- self-test cli-status-doctor-contract`, `dotnet run --project local-agent --no-build -- status`, `dotnet run --project local-agent --no-build -- doctor`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\local-agent-install.ps1 -Action install -InstallDir "C:\Users\honeybadger\Desktop\LearnBot\.tmp\learnbot-install" -Clean`, `& "C:\Users\honeybadger\Desktop\LearnBot\.tmp\learnbot-install\learnbot.exe" status`, and `& "C:\Users\honeybadger\Desktop\LearnBot\.tmp\learnbot-install\learnbot.exe" self-test cli-status-doctor-contract`. The first parallel `dotnet build`/`dotnet run` attempt hit a transient Debug DLL file lock from another .NET host process; the sequential rerun passed. Remaining risk: M8 still lacks a real PowerShell installer flow that pairs via browser/web token UX, Windows Service registration, start/stop lifecycle hardening, logs UX beyond tailing, signed MSI/EXE distribution, auto-update, proxy support, and CLI chat/fix/review commands. Next M8 step should add a small installer/status contract around `scripts/local-agent-install.ps1` or a `learnbot setup`/`learnbot workspace` guided flow, while preserving typed tools and keeping server-side mutation controls unchanged.

Current pointer override after Run 561: Continue from the new M8 entry readiness gate. This run did not open any new execution, publication, acknowledgement, RAG update, CLI packaging, installer, or mutation capability. It added a read-only M7-closure/M8-entry readiness slice: `CodeAgentLoopM8EntryReadinessResponse`, `CodeAgentLoopRunnerService.previewM8EntryReadiness`, and `POST /api/code-agent/loop/runner/m8-entry-readiness`. The readiness gate reuses the completed-flow final-result publication preview and returns `M7_CLOSURE_READY` / `M8_ENTRY_READY` only when the approved execution-flow completed handoff, nested final-result handoff, and disabled final-result publication preview are all present. Even when ready, it explicitly keeps `m8WorkEnabled=false`, `cliPackagingEnabled=false`, `installerEnabled=false`, `publicationEnabled=false`, `finalAnswerDeliveryEnabled=false`, `acknowledgementSaveEnabled=false`, `ragFreshnessUpdateEnabled=false`, and `mutationEnabled=false`. The Code workspace now exposes a `Check M8 entry` action next to the final-result handoff preview, stores the M8 readiness response, and renders `agent loop runner M8 entry readiness` with M7 closure, M8 entry, M8 work, publication, acknowledgement-save, and mutation flags. Verified with `node frontend\src\features\code\runner\agentLoopRunnerM8EntryReadinessClient.test.mjs`, `node frontend\src\features\code\runner\agentLoopRunnerFinalResultPublicationPreviewClient.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest" test`, and `npm run build` from `frontend`. The first sandboxed Maven attempt failed only because Maven Central access was blocked; the approved rerun passed. Remaining risk: M8 is now explicitly enterable from a read-only readiness perspective, but real M8 work has not started. There is still no PowerShell `learnbot` CLI packaging flow, installer/service lifecycle, signed distribution, final-answer publication/delivery, acknowledgement save, RAG freshness update/partial reindex, or automatic multi-step orchestration. Next work should start M8 proper with the smallest productization slice, preferably a PowerShell-friendly local CLI/status/doctor packaging boundary that uses the existing `.tools`/Local Agent layout without changing server-side mutation controls.

Current pointer override after Run 560: Continue M7 agent-loop completion after adding the audit-only final-result publication preview handoff. This run added `POST /api/code-agent/loop/runner/final-result-publication-preview`, backed by `CodeAgentLoopRunnerService.previewFinalResultPublication`, which reuses the completed-flow runner preview and exposes `READY_FINAL_RESULT_PUBLICATION_DISABLED` only when the approved execution-flow completed handoff and nested final-result handoff schemas are present. The response keeps final result publication, final-answer generation/delivery, acknowledgement save, RAG freshness update, partial reindex, follow-up mutation, and mutation disabled. The Code workspace now has a `Preview final result handoff` action for `STOP_AND_REPORT`/`READY_FINAL_RESULT_DISABLED` states, renders the publication preview decision and disabled publication/acknowledgement-save/mutation flags, and still shows the nested final-result handoff summary. This run also fixed the frontend selected read-only guard to accept the backend's new `git.diff` prepared candidate as well as `git.status`. Verified with `node frontend\src\features\code\runner\agentLoopRunnerFinalResultPublicationPreviewClient.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, `node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` from `frontend`, `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest" test`, and `npm run build` from `frontend`. The first sandboxed Maven attempt failed only because Maven Central access was blocked; the approved rerun passed. Remaining risk: M7 still has manual continuation only, no automatic multi-step orchestration, no model-safe `file.read` contract, no actual final-answer publication/delivery, no acknowledgement save, no RAG freshness update/partial reindex, and normal unflagged release is still incomplete. Next step should close M7 by either adding the smallest automatic/manual transition needed around final reporting or explicitly documenting M8 entry criteria; do not spend more time expanding read-only tools unless `file.read` has a clear bounded target contract.

Current pointer override after Run 559: Continue M7 agent-loop completion after the first narrow read-only safe-tool expansion beyond `git.status`. This run reviewed the Local Agent tool surface and confirmed `git.diff` is already implemented by the Local Agent, advertised in heartbeat capabilities, accepted by the server read-only queue path, and bounded by Local Agent `maxBytes` handling. `CodeAgentLoopRunnerService` now keeps the first `QUEUE_READ_ONLY_OBSERVATION` candidate as `git.status`, but when the loop timeline already has a succeeded non-mutating `git.status` observation and fewer succeeded `git.diff` observations, the next prepared read-only candidate becomes `git.diff` with `maxBytes=6000`; if timeline data is absent it falls back to the previous `git.status` behavior. Runner guardrails now expose allowed candidate tools `git.status` and `git.diff`, and enqueue still accepts only non-side-effectful, approval-not-required, mutation-disabled read-only candidates. `CodeAgentLoopToolSelectionService` now validates the model response against the actual prepared read-only candidate instead of hard-coding `git.status`, allows only `git.status`/`git.diff`, keeps mismatched model output on deterministic fallback, counts both read-only tools toward the continuation iteration budget, and leaves request creation/enqueue/push/claim/final-result/publication/acknowledgement/mutation disabled in preview/continuation surfaces. Endpoint smoke coverage now proves the sequence `git.status` preview/enqueue/claim/complete -> continuation prepares and model-selects `git.diff` without mutation. Verified this run with `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest" test`, `dotnet build local-agent\LearnBot.LocalAgent.csproj`, `node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` from `frontend`, and `npm run build` from `frontend`. The first sandboxed Maven attempt failed only because Maven Central access was blocked by sandbox networking; the approved rerun passed. Remaining risk: the M7 loop is still manually continued and only spans `git.status`/bounded `git.diff`; there is no automatic multi-step orchestration, no model-selected `file.read`, no real final report/final-answer publication, no acknowledgement save, no actual RAG freshness update/partial reindex, and normal unflagged release remains incomplete. Next M7 step should stop expanding read-only tools unless `file.read` has a clear model-safe path target contract; otherwise move to final-answer/report publication handoff completion so M7 can close and M8 can begin.

Current pointer override after Run 558: Continue M7 agent-loop completion after route/API proof for the read-only continuation iteration-limit state. No new execution capability was added in this run. The selected-read-only route smoke now includes a limit-reached continuation fixture with `ITERATION_LIMIT_REACHED`, `iterationCount=6`, `maxIterations=6`, `remainingIterations=0`, and `iterationLimitReached=true`; the rendered Code workspace must show the limit-reached budget line, omit the `agent loop runner observation continuation next model preview` line, and render the `Continue read-only step` button disabled even if a stale prepared runner preview is present. The endpoint smoke now also calls `/api/code-agent/loop/runner/continue-after-observation` a second time with a 2/2 timeline budget and verifies `ITERATION_LIMIT_REACHED`, zero remaining iterations, request creation/enqueue/push/claim/mutation all false, and no additional model-selection call beyond the three proven preview/enqueue/normal-continuation calls. Verified this run with `node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` from `frontend`, `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentControllerTest" test`, `node frontend\src\features\code\runner\agentLoopRunnerObservationContinuationClient.test.mjs`, and `npm run build` from `frontend`. The sandboxed Maven attempt again failed only because Maven Central access was blocked by sandbox networking; the approved rerun passed. Remaining risk: M7 loop execution is still manual and limited to read-only `git.status`; there is no broader typed safe-tool set, no automatic multi-step orchestration, no real final report/final-answer publication, no acknowledgement save, no actual RAG freshness update/partial reindex, and normal unflagged release remains incomplete. Next M7 step should review whether a safe read-only tool beyond `git.status` already exists end-to-end in the Local Agent and, only if it does, add a narrow model-selection/queueing slice for that tool; otherwise move toward final-answer/report publication handoff completion rather than spending more time on `git.status` loop hardening.

Current pointer override after Run 557: Continue M7 agent-loop completion after the first read-only continuation iteration-budget/read-model surface. `CodeAgentLoopObservationContinuationResponse` now carries `iterationCount`, `maxIterations`, `remainingIterations`, and `iterationLimitReached`. `CodeAgentLoopToolSelectionService.continueAfterReadOnlyObservation` reads the existing loop timeline through `CodeAgentLoopPreviewService.recentTimelines`, counts succeeded non-mutating `git.status` `LOCAL_AGENT_OBSERVATION_RESULT` events for the loop, uses the timeline `maxSteps` as the manual continuation cap, and refuses to create a next runner/model preview with `ITERATION_LIMIT_REACHED` once the count reaches the cap. Missing or pending observations still return no next preview. Successful observations below the cap still return `NEXT_MODEL_TOOL_PREVIEW_READY` with all request creation/enqueue/push/claim/final-result/publication/acknowledgement/mutation flags false. The Code workspace continuation panel now renders `agent loop runner observation continuation budget: iteration ... / max ... / remaining ... / limit reached ...`, and stale model preview state is cleared when continuation returns no next preview. The selected-read-only route smoke pins the visible budget line and the endpoint smoke pins the JSON budget fields at 1/6/5/false in the proven `select-tool-preview -> enqueue-selected-read-only -> claim/complete -> continue-after-observation` path. A new service regression proves a 2/2 budget returns `ITERATION_LIMIT_REACHED`, does not call the runner preview/model selector, and keeps enqueue/mutation disabled. Verified this run with `node frontend\src\features\code\runner\agentLoopRunnerObservationContinuationClient.test.mjs`, `node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` from `frontend`, `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentControllerTest" test`, and `npm run build` from `frontend`. The sandboxed Maven attempt again failed only because Maven Central access was blocked by sandbox networking; the approved rerun passed. Remaining risk: the loop is still manual and limited to read-only `git.status`; there is no automatic multi-step orchestration, no broader typed safe-tool set, no real final report/final-answer publication, no acknowledgement save, no actual RAG freshness update/partial reindex, and normal unflagged release is incomplete. Next M7 step should either add a compact route/API proof for the iteration-limit reached UI state, or begin a narrow safe-tool expansion beyond `git.status` only if the tool schema, Local Agent implementation, and approval boundaries are already present.

Current pointer override after Run 556: Continue M7 agent-loop completion after the first bounded/manual continuation UI action. The observation-continuation server slice from Run 555 remains preview-only; this run added the user-visible `Continue read-only step` action inside the Code workspace continuation panel. The action is enabled only when the existing guarded selected-read-only enqueue predicate is satisfied by the continuation-updated runner/model preview, and it reuses the proven `/api/code-agent/loop/runner/enqueue-selected-read-only` path rather than adding a new execution path. The route-level selected-read-only smoke now proves the full visible sequence: prepared runner preview enables `Preview model tool`, the first `/select-tool-preview` stays preview-only, the first `/enqueue-selected-read-only` queues read-only `git.status`, observation refresh calls `/api/local-agents/tools/{requestId}`, timeline refresh runs, `/continue-after-observation` returns `NEXT_MODEL_TOOL_PREVIEW_READY`, the continuation block renders all request creation/enqueue/push/claim/final-result/publication/acknowledgement/mutation controls as disabled, and the explicit `Continue read-only step` button is enabled for the next manual read-only queue. No backend execution capability, mutation control, final-result publication, acknowledgement save, or automatic loop execution was added. Verified this run with `node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` from `frontend`, `node frontend\src\features\code\runner\agentLoopRunnerObservationContinuationClient.test.mjs`, `node frontend\src\features\code\runner\agentLoopRunnerSelectedReadOnlyClient.test.mjs`, `npm run build` from `frontend`, and `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentControllerTest" test`. The sandboxed Maven attempt again failed only because Maven Central access was blocked by sandbox networking; the approved rerun passed. Remaining risk: the loop is still manual and limited to read-only `git.status`; the service still lacks bounded automatic multi-step orchestration, a broader typed safe-tool set, real final report/final-answer publication, acknowledgement save, actual RAG freshness update/partial reindex, and normal unflagged release. Next M7 step should add a small loop-iteration budget/read-model surface so the UI and backend can show how many manual read-only continuation steps have occurred and refuse further continuation after a configured small limit, before moving toward any automatic loop execution.

Current pointer override after Run 555: Continue M7 agent-loop completion after the first narrow server-side observation-continuation slice. This run added `CodeAgentLoopObservationContinuationRequest` and `CodeAgentLoopObservationContinuationResponse` under `backend/src/main/java/com/learnbot/dto/loop`, plus `POST /api/code-agent/loop/runner/continue-after-observation`. `CodeAgentLoopToolSelectionService.continueAfterReadOnlyObservation` now looks up the queued Local Agent observation for the current user, refuses to advance when it is missing or not `SUCCEEDED`, and when the read-only `git.status` observation has succeeded it returns the next runner preview and next model tool-selection preview without creating, enqueueing, pushing, claiming, publishing, acknowledging, or enabling mutation. The selected-read-only endpoint smoke now proves the sequence `select-tool-preview -> enqueue-selected-read-only -> Local Agent claim/complete -> continue-after-observation`, with continuation exposing `NEXT_MODEL_TOOL_PREVIEW_READY` and all request creation/enqueue/push/claim/mutation flags still false. The Code workspace now has `agentLoopRunnerObservationContinuationClient.js`, stores `codeAgentLoopRunnerObservationContinuation`, automatically calls the continuation endpoint after refreshing a queued observation, updates runner/model preview state from the continuation response, and renders an `agent loop runner observation continuation` block with disabled controls and the next model preview. Route-level coverage proves observation refresh still calls `/api/local-agents/tools/{requestId}` and timeline refresh, then calls `/api/code-agent/loop/runner/continue-after-observation`, renders the continuation decision, and keeps request creation, enqueue, push, claim, final-result, publication, acknowledgement, and mutation disabled. Verified this run with `node frontend\src\features\code\runner\agentLoopRunnerObservationContinuationClient.test.mjs`, `node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` from `frontend`, `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentControllerTest" test`, `node frontend\src\features\code\runner\agentLoopRunnerToolSelectionPreviewClient.test.mjs`, `node frontend\src\features\code\runner\agentLoopRunnerQueuedObservationClient.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, and `npm run build` from `frontend`. The first sandboxed Maven attempt again failed only because Maven Central access was blocked by sandbox networking; the approved rerun passed. Remaining risk: continuation is still preview-only and limited to the single allowed read-only `git.status` follow-up; there is still no bounded automatic multi-step server loop, no broader typed read-only tool set, no real final report/final-answer publication, no acknowledgement save, no actual RAG freshness update/partial reindex, and normal unflagged release remains incomplete. Next M7 step should add a bounded/manual "continue selected read-only step" action that consumes the continuation's model preview and queues the next read-only observation only after the user explicitly triggers it, while preserving the same disabled mutation/final-result/publication/acknowledgement guardrails.

Current pointer override after Run 554: Continue M7 agent-loop completion after the first narrow model-driven follow-up selection UI/API slice. The backend already had `/api/code-agent/loop/runner/select-tool-preview`; this run wired it into the Code workspace through `frontend/src/features/code/runner/agentLoopRunnerToolSelectionPreviewClient.js`, `useCodeRagController.js`, and `CodeWorkspace.jsx`. The Code workspace now exposes a preview-only `Preview model tool` action when the runner has a prepared read-only `git.status` candidate, stores the model tool-selection preview separately from enqueue results, and renders a `agent loop runner model tool preview` block showing model attempted/accepted, selected tool, read-only/approval/mutation state, request creation, enqueue, push, claim, final-result, publication, acknowledgement, and mutation flags. Route-level coverage in `agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` now proves `/select-tool-preview` is called before enqueue, does not call enqueue, renders the preview-only model decision/control flags, and still leaves the existing `/enqueue-selected-read-only` path as the only request-creation/enqueue step. Backend endpoint smoke coverage now calls `/select-tool-preview` before `/enqueue-selected-read-only` in the selected read-only runner flow and verifies preview keeps request creation, enqueue, push, claim, and mutation disabled with no repository create or tool pusher call; enqueue remains the only path that creates and pushes the read-only Local Agent request. Verified this run with `node frontend\src\features\code\runner\agentLoopRunnerToolSelectionPreviewClient.test.mjs`, `node frontend\src\features\code\runner\agentLoopRunnerSelectedReadOnlyClient.test.mjs`, `node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` from `frontend`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentControllerTest" test`, and `npm run build` from `frontend`. The first sandboxed Maven attempt failed only because dependency resolution to Maven Central was blocked by sandbox networking; the approved rerun passed. Remaining risk: the model-selected follow-up loop is still limited to previewing/queuing the allowed read-only `git.status` candidate; there is still no automatic multi-step LLM loop over arbitrary safe tools, no real final report/final-answer publication, no acknowledgement save, no actual RAG freshness update/partial reindex, and normal unflagged release remains incomplete. Next M7 step should add a narrow server-side loop continuation after a completed read-only observation, so accepted `git.status` observations can trigger the next model decision preview without enabling mutation or final-result publication.

Current pointer override after Run 553: Continue M7 agent-loop completion after the guarded release path, durable accepted-observation intake, final report summary, explicit RAG freshness marker, final-answer publication handoff, acknowledgement-save handoff, Code workspace UI exposure, repeatable approved server-queue Local Agent smoke wrapper, live Spring-server bridge smoke, true live Spring durable approved-row Local Agent polling smoke, the first flag-guarded server-created approved execution sequence slice, the first successful end-to-end flagged live verification of that server-created sequence path, a narrow Code workspace release action wired to the proven guarded `/release-for-execution` endpoint, a route-level release UI flow smoke, a dedicated release UI/live-flow wrapper, the first post-release approved-flow completion handoff, live-stack proof that this completion handoff is emitted after the approved Local Agent sequence, a true logged-in runner-preview API proof over the completed live sequence, the first audit-only final-result/report publication handoff attached to that completion event, and route-level UI coverage for the final-result delivery/acknowledgement disabled handoff over the completed-flow summary. `agentLoopRunnerHandoffSummary.js` now renders a separate `agent loop runner final-result handoff` line when the completed approved-flow summary contains `learnbot.code-agent.approved-execution-flow-final-result-handoff.v1`; the line exposes final report summary, RAG marker, publication handoff, acknowledgement handoff, stale disclosure modeling, delivery, final-answer generation, publication, acknowledgement save, RAG freshness update, partial reindex, follow-up mutation, and mutation flags. `CodeWorkspace.jsx` renders that line in the existing runner handoff panel, and `agentLoopRunnerPreviewRouteFlow.test.mjs` now includes a completed-flow runner-preview fixture so the route-level Code workspace smoke proves the final-result delivery/acknowledgement handoff is visible while all controls remain disabled. Verified this run with `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` from `frontend`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\quality\local-agent-flow\assert-live-server-release-ui-flow-contract.ps1`, and `npm run build` from `frontend`. This run intentionally did not rerun the live Docker release flow because no backend execution path or live smoke script behavior changed; Run 552 already proved the same backend runner-preview payload live, and this run only tightened route-level UI rendering for that payload. Remaining risk: final-result/report publication is still audit-only; real final report generation, publication, final-answer delivery, acknowledgement save, actual RAG freshness update/partial reindex, model-driven follow-up selection beyond the proven sequence, and normal unflagged release remain incomplete. Next M7 step should move from display coverage toward a narrow model-driven follow-up selection slice, or add a disabled API handoff for final-answer delivery/acknowledgement if the next user-visible completion boundary still needs a server-side contract.

Current pointer: continue Milestone 5 by adding a real browser-level read-only UI smoke for the Code workspace readiness panel when the in-app browser is available, or by reviewing the next narrow backend/frontend gap in the disabled Local Agent mutation readiness chain. The backend fresh-observation enqueue regression now verifies that queued release-attempt observation requests use new ids, the held source patch remains `APPROVED_HELD` with approval `APPROVED`, and the patch dry-run observation keeps `dryRunOnly=true`, `mutationAllowed=false`, `sourceRequestId`, `releaseAttemptId`, and `freshObservationOnly=true`. The MockMvc fresh-observations regression now routes `POST /api/local-agents/tools/{requestId}/fresh-observations` and verifies it returns only the two observation request ids, never the held source patch id, with wire-name tool names, `freshObservationOnly=true`, dry-run patch metadata, and no mutation-allowed repository observation payload. The backend result-completion readiness regression now also pins source session/user/agent/workspace ids, source tool-runner delivery-receipt context, source post-execution observation context, acknowledgement-save disabled flags, and the explicit refusal message on the disabled `mutationResultCompletionBoundary`, so future completed-result envelopes remain tied to the correct Local Agent context while completed-result transition, persistence, observation capture, result intake, acknowledgement save, and mutation stay disabled. The backend result-intake-persistence regression now pins source session/user/agent/workspace ids, source observation-acceptance gate schema/status, final-answer/final-response/delivery/receipt/acknowledgement disabled flags, and refusal messaging on the disabled `mutationResultIntakePersistenceGate`, so accepted-observation persistence, rollback fallback, RAG freshness update, aggregation, publication, final-answer generation, delivery, receipt, acknowledgement save, and mutation remain closed. The backend rollback-fallback and RAG freshness readiness regressions now pin source session/user/agent/workspace ids, upstream gate schema/status, acknowledgement-save disabled flags, and refusal messaging on the disabled `mutationRollbackFallbackGate` and `mutationRagFreshnessGate`, so rollback fallback execution, RAG freshness update, aggregation, publication, final-answer generation, acknowledgement save, and mutation remain closed. The backend result-aggregation and publication readiness regressions now pin source session/user/agent/workspace ids, upstream gate schema/status, acknowledgement-save disabled flags, and refusal messaging on the disabled `mutationResultAggregationGate` and `mutationPublicationGate`, so aggregation, publication, final-answer generation, acknowledgement save, and mutation remain closed. The backend final-answer generation and completion readiness regressions now pin source session/user/agent/workspace ids, upstream gate schema/status, acknowledgement-save disabled flags, and refusal messaging on the disabled `mutationFinalAnswerGenerationGate` and `mutationFinalAnswerCompletionGate`, so final-answer generation, completion, delivery, acknowledgement save, and mutation remain closed. The backend final-answer persistence and conversation-save readiness regressions now pin source session/user/agent/workspace ids, upstream gate schema/status, acknowledgement-save disabled flags, and refusal messaging on the disabled `mutationFinalAnswerPersistenceGate` and `mutationFinalAnswerConversationSaveGate`, so final-answer persistence, conversation-turn save, user-visible completion, delivery, acknowledgement save, and mutation remain closed. The backend final-answer user-visible completion and final-response handoff readiness regressions now pin source session/user/agent/workspace ids, upstream gate schema/status, acknowledgement-save disabled flags, and refusal messaging on the disabled `mutationFinalAnswerUserVisibleCompletionGate` and `mutationFinalResponseHandoffGate`, so user-visible completion, final-response handoff, delivery handoff, final-answer delivery, acknowledgement save, and mutation remain closed. The backend final-answer delivery and delivery-receipt readiness regressions now pin source session/user/agent/workspace ids, upstream gate schema/status, acknowledgement-save disabled flags, and refusal messaging on the disabled `mutationFinalAnswerDeliveryGate` and `mutationFinalAnswerDeliveryReceiptGate`, so final-answer delivery, delivery handoff, delivery receipt recording, acknowledgement save, and mutation remain closed. The backend mutation completion and handoff summary regressions now pin source session/user/agent/workspace ids, source completion-summary context, source delivery-receipt context, aggregate acknowledgement-save disabled flags, and refusal messaging on `mutationCompletionSummary` and `mutationHandoffSummary`, so completion handoff, final response, delivery receipt, acknowledgement save, and mutation remain closed. The backend execution-readiness boundary regression now pins source session/user/agent/workspace ids, source handoff-summary context, source delivery-receipt context, source execution-gate context, source write-helper safety context, disabled runtime/tool-runner/write-helper/result-intake/acknowledgement-save flags, and refusal messaging on `mutationExecutionReadinessBoundary`, so runtime execution, result intake, final-response handoff, delivery receipt, acknowledgement save, and mutation remain closed. The backend tool-runner boundary regression now pins source session/user/agent/workspace ids, source execution-readiness context, source delivery-receipt context, source execution-gate context, expected/running/completed request counts, disabled running/result-intake/tool-runner/acknowledgement-save flags, and refusal messaging on `mutationToolRunnerBoundary`, so runner invocation, running transition, result completion, result intake, acknowledgement save, and mutation remain closed. The route-level Code workspace readiness panel has a Vite SSR smoke that renders `CodeWorkspace.jsx` with a disabled release-attempt fixture and verifies result-intake, mutation dispatch-envelope, mutation dispatch-preflight, mutation dispatch-decision, mutation request-blueprint, mutation request-creation, mutation request-push, mutation request-claim, mutation execution-gate, mutation write-helper safety, mutation post-execution observation, mutation observation acceptance, fresh observation request/evidence/completeness/enqueue audit surfaces, disabled release-fresh-observation queue control, release-attempt final readiness, final mutation report contract, final mutation report finalization, final-answer publication boundary, release enablement checklist, mutation execution-readiness, mutation tool-runner, result-completion, result-intake-persistence, rollback-fallback, RAG freshness, result-aggregation, publication, final-answer generation, final-answer completion, final-answer persistence, final-answer conversation-save, final-answer user-visible completion, final-response handoff, final-answer delivery, delivery-receipt, mutation completion summary, and mutation handoff summary text appears in actual panel markup. The disabled release-fresh-observation queue assertion is scoped to the same rendered button element, so a different disabled control cannot satisfy the check accidentally. The smoke fixture now also renders the completion-summary final-response handoff item and the handoff-summary finalResponse stage so acknowledgement-save disabled text is pinned in the actual route markup, not only helper-level tests. The smoke and route prop harness scan the disabled latest-attempt fixture and full route props recursively through `mutationDisabledFlagGuard.js`; its `assertNoForbiddenTrueFlags` helper fails with exact paths if release, request creation, push, claim, running/execution, tool runner, write helper, apply, test, rollback restore, RAG freshness, result aggregation, publication, final-answer, delivery, receipt, acknowledgement save, or mutation control flags become `true`, and the helper now combines an explicit denylist with Local Agent control prefix/suffix detection so newly added mutation-related `*Enabled`, `*Allowed`, `claimableAfter*`, or other claimable-style flags are harder to miss. The route render props are assembled by `codeWorkspaceReadinessSmokeHarness.mjs`, with a lightweight regression covering the held request, disabled patch execution gate, release-attempt model shape, fresh-observation request/evidence/completeness/enqueue audit fields, and generated-props disabled flag guard. `mutationDisabledFlagGuard`, `mutationResultIntakeBoundary`, `mutationDispatchEnvelopeContract`, `mutationDispatchPreflightBoundary`, `mutationDispatchDecisionModel`, `mutationRequestBlueprint`, `mutationRequestCreationGate`, `mutationRequestPushGate`, `mutationRequestClaimGate`, `mutationExecutionGate`, `mutationWriteHelperSafetyGate`, `mutationPostExecutionObservationGate`, `mutationObservationAcceptanceGate`, `mutationExecutionReadinessBoundary`, `mutationToolRunnerBoundary`, `mutationResultCompletionBoundary`, `mutationResultIntakePersistenceGate`, `mutationRollbackFallbackGate`, `mutationRagFreshnessGate`, `mutationResultAggregationGate`, `mutationPublicationGate`, `mutationFinalAnswerGenerationGate`, `mutationFinalAnswerCompletionGate`, `mutationFinalAnswerPersistenceGate`, `mutationFinalAnswerConversationSaveGate`, `mutationFinalAnswerUserVisibleCompletionGate`, `mutationFinalResponseHandoffGate`, `mutationFinalAnswerDeliveryGate`, `mutationFinalAnswerDeliveryReceiptGate`, `mutationCompletionSummary`, and `mutationHandoffSummary` are covered by lightweight frontend helper/render regressions while backend side-effect paths remain closed. Do not enable release, claim, request creation, request blueprint, push, running transition, execution, tool runner, write helper, apply, test, rollback restore, completed-result transition, result persistence, observation capture, observation acceptance, result intake, rollback fallback, RAG freshness update, result aggregation, publication, final-answer generation, final-answer completion, final-answer persistence, conversation save, user-visible completion, final response handoff, delivery handoff, final-answer delivery, delivery receipt, acknowledgement save, dispatch, dispatch decision, dispatch preflight, or mutation yet. Next narrow review should use the real browser-level read-only UI smoke when available, or continue with the next backend/frontend gap in the disabled Local Agent mutation readiness chain.

Current pointer addendum after Run 364: Milestone 5/6 now has Local Agent execution lease/timeout, the first real `rollback.restore` Local Agent slice, a narrow real `patch.apply` Local Agent mutation slice, a first approved `command.runAllowed` allowlisted test/build runner slice, backend preparation for the future release-to-claim transition to produce a Local Agent compatible mutation payload, a backend release prerequisite that source-only fallback evidence cannot make a held patch claimable, and a backend response-completion classifier/read model for audit-only mutation result intake candidates. Actual patch mutation is still only inside the Local Agent runtime for already-approved requests, one file at a time, with `mutationAllowed=true`, `dryRunOnly=false`, a managed rollback snapshot manifest, approved workspace containment, managed snapshot layout validation, and immediate target-hash/hunk recheck through the guarded temp-file rewrite helper. Completed release-attempt mutation-sequence responses from `patch.apply`, `command.runAllowed`, post-write `git.status`, and `rollback.restore` now preserve both a disabled `mutationResultIntakeCandidate` and an `acceptedMutationObservation` envelope inside the completed tool response, and accepted observations are also stored in a dedicated durable read-model table keyed by source request and release attempt. Readiness reads that durable intake model first, falls back to legacy response JSON for older rows, summarizes durable observations by tool/status/count, and feeds those audit-only counts into the existing final mutation report contract, RAG freshness gate, mutation result aggregation plan, final mutation report draft, finalization boundary, final-answer publication boundary, final-answer generation gate, final-answer completion gate, final-answer persistence gate, final-answer conversation-save gate, user-visible completion gate, final-response handoff gate, final-answer delivery gate, and delivery-receipt gate. The final-answer publication boundary requires the disabled final mutation report draft and carries its schema/status/section keys into the Code workspace display; the final-answer generation gate carries that publication-boundary context, draft sections, accepted-observation counts, and missing-result/stale-index risk visibility before refusing generation; the downstream completion, persistence, conversation-save, user-visible completion, final-response handoff, final-answer delivery, and delivery-receipt gates now also carry the upstream publication/draft/observation/risk context before refusing completion, persistence, conversation save, user-visible completion, final-response handoff, delivery, delivery handoff, delivery receipt, and acknowledgement save. The existing delivery-receipt gate exposes an explicit acknowledgement-save policy/check, and mutation completion/handoff summaries carry that disabled acknowledgement-save context through an `acknowledgementSaveRefusal` item and an `acknowledgementSave` handoff stage. Aggregation execution, final report generation, publication, final-answer generation, final-answer completion, final-answer persistence, conversation save, user-visible completion, final-response handoff, final-answer delivery, delivery receipt, acknowledgement save, RAG freshness updates, rollback fallback execution, and follow-up mutation remain disabled. The next implementation step should continue wiring the existing release/claim/result-intake chain without adding new gates: review the next backend/frontend gap before enabling real aggregation/final reporting, likely carrying final-answer/publication context into completion/handoff summaries or adding a browser-level read-only UI smoke when the in-app browser is available.

Current pointer addendum after Run 365: The existing mutation completion summary, handoff summary, execution-readiness boundary, tool-runner boundary, and result-completion boundary now carry the upstream delivery-receipt publication/draft/accepted-observation/risk context forward as read-only source context. The Code workspace renders those source-context lines in the existing readiness panel, and backend/frontend regressions verify the context fields remain present without adding any new gate or enabling release, request creation, push, claim, running transition, execution, tool runner, write helper, apply, test, rollback restore, completed-result transition, result persistence, observation capture, result intake, publication, final answer, delivery receipt, acknowledgement save, or mutation. Next narrow review should continue the existing disabled release/claim/result-intake chain toward the next missing read-only orchestration link, or add the real browser-level read-only UI smoke when the in-app browser backend is available.

Current pointer addendum after Run 404: The existing mutation tool-runner and mutation result-completion disabled surfaces now render publication-gate status/schema/session/user/agent/workspace identity together with publication boundary, accepted-observation summary/risk/latest, and rollback-summary source context. Tool-runner uses the identity copied from execution-readiness, result-completion uses identity copied from tool-runner, and the Code workspace renders the complete identity before publication observation and rollback-summary context. No new gate or execution control was added; runner invocation, running transition, completed-result transition, result persistence, observation capture, result intake, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from result-completion into result-intake persistence/rollback/RAG freshness context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 405: The existing mutation result-intake persistence, rollback fallback, and RAG freshness disabled displays now keep their own session/user/agent/workspace id identity together in the visible ids line while preserving the existing publication-gate status/schema/session/user/agent/workspace source context, accepted-observation readiness/latest, accepted-observation summary/risk, and rollback-summary context. No new gate or execution control was added; accepted-observation persistence, rollback fallback execution, RAG freshness update, aggregation execution, publication, final-answer generation, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from RAG freshness into result-aggregation/publication visible id/source-context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 406: The existing mutation result-aggregation and publication disabled displays now keep their own session/user/agent/workspace id identity together in the visible ids line while preserving the existing publication-gate status/schema/session/user/agent/workspace source context, accepted-observation summary/risk/latest, and rollback-summary context. No new gate or execution control was added; aggregation execution, publication, final-answer generation, final-answer completion, final-answer delivery, final-response handoff, delivery receipt, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from publication into final-answer generation/completion visible id/source-context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 407: The existing mutation final-answer generation and final-answer completion disabled displays now keep their own session/user/agent/workspace id identity together in the visible ids line while preserving the existing publication-gate status/schema/session/user/agent/workspace source context, publication-boundary context, accepted-observation summary/risk/latest, and rollback-summary context. No new gate or execution control was added; final-answer generation, final-answer completion, final-answer persistence, conversation save, user-visible completion, final-response handoff, final-answer delivery, delivery receipt, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from final-answer completion into final-answer persistence/conversation-save visible id/source-context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 408: The existing mutation final-answer persistence and final-answer conversation-save disabled displays now keep their own session/user/agent/workspace id identity together in the visible ids line while preserving the existing publication-gate status/schema/session/user/agent/workspace source context, publication-boundary context, accepted-observation summary/risk/latest, and rollback-summary context. No new gate or execution control was added; final-answer persistence, conversation save, user-visible completion, final-response handoff, final-answer delivery, delivery receipt, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from conversation-save into user-visible completion/final-response handoff visible id/source-context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 409: The existing mutation final-answer user-visible completion and final-response handoff disabled displays now keep their own session/user/agent/workspace id identity together in the visible ids line while preserving the existing publication-gate status/schema/session/user/agent/workspace source context, publication-boundary context, accepted-observation summary/risk/latest, and rollback-summary context. No new gate or execution control was added; user-visible completion, final-response handoff, delivery handoff, final-answer delivery, delivery receipt, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from final-response handoff into final-answer delivery/delivery-receipt visible id/source-context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 410: The existing mutation final-answer delivery and delivery-receipt disabled displays now keep their own session/user/agent/workspace id identity together in the visible ids line while preserving the existing publication-gate status/schema/session/user/agent/workspace source context, publication-boundary context, accepted-observation summary/risk/latest, and rollback-summary context. No new gate or execution control was added; final-answer delivery, delivery handoff, delivery receipt recording, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from delivery-receipt into mutation completion/handoff/execution summary visible id/source-context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 411: The existing mutation completion summary, mutation handoff summary, and mutation execution-readiness disabled displays now render their own session/user/agent/workspace id identity before the existing publication-gate source context. No new gate or execution control was added; completion handoff, runtime execution, request creation, push, claim, tool runner, result intake, acknowledgement save, and mutation remain disabled. The display-only readiness chain is now saturated enough that the next recommended step should pivot to Milestone 6: verify a narrow real Local Agent execution-flow slice around an already-approved request using the existing lease/timeout, `patch.apply`, `rollback.restore`, and allowlisted `command.runAllowed` test runner paths, without opening broad mutation or adding new gates.

Current pointer addendum after Run 412: Milestone 6 work has pivoted from display-only readiness polish to a narrow real Local Agent execution-flow verification slice. The Local Agent heartbeat now advertises the side-effect tool capabilities it already implements: `patch.apply`, `command.runAllowed`, and `rollback.restore`. A new `self-test approved-execution-flow-contract` exercises one approved temporary-workspace flow through managed snapshot creation, approved single-file `patch.apply`, allowlisted `command.runAllowed`, `git.status` observation, and approved `rollback.restore`, while checking session/user/agent/workspace identity remains consistent. No broad release, request creation, push, claim, server orchestration, result intake, acknowledgement save, or new gate was enabled. Next recommended step: connect this verified Local Agent-side flow to a narrow server-side execution-flow contract that models the same ordered request/result sequence for an already-approved request without opening broad mutation.

Current pointer addendum after Run 413: The verified Local Agent-side approved execution flow is now mirrored by a narrow backend server-side contract. `LocalAgentApprovedExecutionFlowContract` summarizes an already-completed ordered response sequence for `patch.apply`, `command.runAllowed`, post-write `git.status`, and `rollback.restore`, checks shared session/user/agent/workspace identity, checks source/release-attempt linkage, keeps per-step verification and accepted-observation status visible, and explicitly keeps request creation, push, claim, result intake, acknowledgement save, and follow-up mutation disabled. No broad mutation, release gate, request creation, push, claim, result intake, acknowledgement save, or new gate was enabled. Next recommended step: connect this contract to repository-backed lease/response persistence in a narrow test so an approved sequence can be proven across claimed rows and completed result envelopes without opening broad mutation.

Current pointer addendum after Run 414: The approved execution-flow contract is now connected to repository-backed lease/response persistence in the opt-in live PostgreSQL repository regression. The new path creates four already-approved `local_agent_tool_executions` rows for `patch.apply`, `command.runAllowed`, post-write `git.status`, and `rollback.restore`, claims and completes them in order through repository `claimNext`/`complete`, rebuilds completed response envelopes from persisted rows, and proves the same contract remains ordered, identity-consistent, release-attempt linked, terminal, and disabled for request creation, push, broad claim enabling, result intake, acknowledgement save, follow-up mutation, and orchestration. No broad mutation, server orchestration, request creation, push, result intake persistence, acknowledgement save, or new gate was enabled. Next recommended step: move one layer up from repository-backed proof to a narrow service-level execution-flow read model or dry orchestration candidate that reuses persisted approved rows without making production request creation/push/claim generally available.

Current pointer addendum after Run 415: The repository-backed approved execution-flow proof is now available one layer higher as a read-only service model. `LocalAgentToolGatewayService.inspectApprovedExecutionFlow` accepts caller-owned persisted execution ids, reads them through `repository.find`, rebuilds completed response envelopes, delegates to `LocalAgentApprovedExecutionFlowContract`, and returns repository-backed/read-model-only summary flags while keeping request creation, push, claim, completion writes, result intake persistence, acknowledgement save, orchestration, and follow-up mutation disabled. No broad mutation, production request creation, server push, production claim release, result intake persistence, acknowledgement save, or new gate was enabled. Next recommended step: add a narrow API/controller or UI-facing read-only inspection surface for this service model, or first add negative service tests for missing/mismatched user rows and incomplete/nonterminal rows if the API boundary should remain deferred.

Current pointer addendum after Run 416: The service-level approved execution-flow read model now has explicit fallback/negative coverage before any API/controller exposure. `inspectApprovedExecutionFlow` rejects missing rows, rows not owned by the caller, rows not targeting `USER_LOCAL_AGENT`, rows that are not approved, and rows that are not terminal with `finishedAt` present; the regression tests confirm missing, mismatched-user, and nonterminal rows fail without repository create/claim/complete or WebSocket push. No broad mutation, production request creation, server push, production claim release, result intake persistence, acknowledgement save, orchestration, or new gate was enabled. Next recommended step: expose this hardened read-only service model through a narrow API/controller inspection endpoint, keeping it read-only and request-id based.

Current pointer addendum after Run 417: The hardened approved execution-flow read model is now exposed through a narrow read-only API endpoint. `POST /api/local-agents/tools/approved-execution-flow/inspection` accepts an ordered `requestIds` list, uses the current authenticated user, delegates to `LocalAgentToolGatewayService.inspectApprovedExecutionFlow`, and returns the repository-backed/read-model-only summary while the controller test verifies it does not call claim, complete, or fresh-observation enqueue paths. No broad mutation, production request creation, server push, production claim release, result intake persistence, acknowledgement save, orchestration, Local Agent transport execution, or new gate was enabled. Next recommended step: add a frontend/API client read-only inspection surface for this endpoint, or add controller-level validation/negative tests for empty/oversized request id lists before wiring UI.

Current pointer addendum after Run 418: The approved execution-flow read-only endpoint is now connected to the Code workspace client/UI surface. `useCodeRagController` stores `codeAgentApprovedExecutionFlowInspection`, calls `POST /api/local-agents/tools/approved-execution-flow/inspection` with ordered request ids supplied by readiness/release-attempt models, and clears stale summaries when related Local Agent observations change. `CodeWorkspace` renders the repository-backed/read-model-only summary with ordered/identity/release-linkage/terminal state, disabled request creation/push/claim/result-intake/acknowledgement/follow-up-mutation/orchestration flags, request ids, and per-step verification. No broad mutation, production request creation, server push, production claim release, result intake persistence, acknowledgement save, orchestration, Local Agent transport execution, or new gate was enabled. Next recommended step: add controller-level validation tests for empty/oversized request id lists and a hook-level API client test, then move toward deriving the ordered request ids from durable completed rows instead of relying on readiness-provided ids.

Current pointer addendum after Run 419: The approved execution-flow inspection API/client now has guardrail coverage. Controller validation rejects empty and oversized ordered `requestIds` before `inspectApprovedExecutionFlow` can run, and the frontend API client helper filters blank ids, posts the bounded ordered ids to `POST /api/local-agents/tools/approved-execution-flow/inspection`, stores the summary, and no-ops when no ids are available. No broad mutation, production request creation, server push, production claim release, result intake persistence, acknowledgement save, orchestration, Local Agent transport execution, or new gate was enabled. Next recommended step: derive ordered approved-flow request ids from durable completed rows or repository read models instead of relying on readiness-provided ids.

Current pointer addendum after Run 420: The approved execution-flow inspection surface now has a durable completed-row input path. `LocalAgentToolExecutionRepository.findCompletedApprovedExecutionFlowRowsForReleaseAttempt` selects the latest completed approved `patch.apply`, `command.runAllowed`, `git.status`, and `rollback.restore` rows for a release attempt in contract order, `LocalAgentToolGatewayService.inspectApprovedExecutionFlowForReleaseAttempt` summarizes them with `requestIdSource=durableCompletedRows`, and `POST /api/local-agents/tools/approved-execution-flow/inspection/by-release-attempt` exposes the read-only path. The Code workspace inspection client now prefers `releaseAttemptId` and uses caller-provided `requestIds` only as fallback. No broad mutation, production request creation, server push, production claim release, result intake persistence, acknowledgement save, orchestration, Local Agent transport execution, or new gate was enabled. Next recommended step: tighten incomplete/missing durable-flow row fallback messaging or add repository-level SQL coverage for multiple completed rows per tool before enabling any broader orchestration.

Current pointer addendum after Run 366: The existing result-intake-persistence, rollback-fallback, RAG freshness, and result-aggregation disabled surfaces now carry accepted-observation/risk source context forward as read-only data. Intake persistence exposes accepted-observation readiness/latest status context; rollback fallback carries that intake context; RAG freshness carries rollback latest-observation context plus missing-result/stale-index risk visibility; result aggregation carries the RAG freshness observation summary, latest-observation, and risk context. The Code workspace renders those source-context lines inside the existing readiness panel. No new gate or execution control was added; release, request creation, push, claim, running transition, execution, tool runner, write helper, apply, test, rollback restore, completed-result transition, result persistence, observation capture, result intake persistence, rollback fallback, RAG freshness update, aggregation, publication, final answer, delivery receipt, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same existing disabled chain toward publication/final-answer readiness context consistency or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 367: The existing publication and final-answer generation disabled surfaces now continue the result-aggregation accepted-observation/risk source context forward as read-only data. Publication carries aggregation observation summary, latest-observation, and missing-result/stale-index risk context; final-answer generation carries the same publication source context alongside its existing final-answer publication-boundary context. The Code workspace renders both source-context lines inside the existing readiness panel. No new gate or execution control was added; release, request creation, push, claim, running transition, execution, tool runner, write helper, apply, test, rollback restore, result persistence, observation capture, result intake persistence, rollback fallback, RAG freshness update, aggregation, publication, final-answer generation/completion/delivery, delivery receipt, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from final-answer generation/completion or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 368: The existing final-answer completion and final-answer persistence disabled surfaces now continue the publication source accepted-observation/risk context forward as read-only data. Completion carries the final-answer generation gate's publication-source observation summary, latest-observation, and missing-result/stale-index risk context alongside the existing publication-boundary generation context; persistence carries that completion source context forward in its existing source-context line. The Code workspace renders the completion source-context line and the expanded persistence source-context line inside the existing readiness panel. No new gate or execution control was added; final-answer completion, persistence, conversation save, user-visible completion, final-response handoff, delivery, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from persistence/conversation-save or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 369: The existing final-answer conversation-save and user-visible completion disabled surfaces now continue the publication-source accepted-observation/risk context forward as read-only data. Conversation-save carries the persistence gate's publication-source observation summary, latest-observation, and missing-result/stale-index risk context; user-visible completion carries that conversation-save source context forward in its existing source-context line. The Code workspace renders the expanded conversation-save and user-visible completion source-context lines inside the existing readiness panel. No new gate or execution control was added; conversation save, user-visible completion, final-response handoff, delivery, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream toward final-response handoff/delivery or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 370: The existing final-response handoff, final-answer delivery, and delivery-receipt disabled surfaces now continue the publication-source accepted-observation/risk context forward as read-only data. Final-response handoff carries the user-visible completion gate's publication-source observation summary, latest-observation, tool, verification, and missing-result/stale-index risk context; final-answer delivery carries that handoff source context; delivery-receipt carries that delivery source context. The Code workspace renders the expanded source-context lines inside the existing readiness panel. No new gate or execution control was added; final-response handoff, delivery handoff, final-answer delivery, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback, and mutation remain disabled. Next narrow review should continue the same existing disabled chain into completion/handoff/execution summaries or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 371: The existing mutation completion summary, mutation handoff summary, and mutation execution-readiness boundary now continue the delivery-receipt publication-source accepted-observation/risk context forward as read-only data. Completion carries the delivery-receipt gate's publication-source observation summary, latest-observation, tool, verification, and missing-result/stale-index risk context; handoff carries that completion source context; execution-readiness carries that handoff source context. The Code workspace renders the expanded source-context lines inside the existing readiness panel. No new gate or execution control was added; release, request creation, push, claim, runtime execution, tool runner, result intake, final-response handoff, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback, and mutation remain disabled. Next narrow review should continue the same existing disabled chain into tool-runner/result-completion or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 372: The existing mutation tool-runner and mutation result-completion disabled surfaces now continue the delivery-receipt publication-source accepted-observation/risk context forward as read-only data. Tool-runner carries the execution-readiness boundary's publication-source observation summary, latest-observation, tool, verification, and missing-result/stale-index risk context; result-completion carries that tool-runner source context forward. The Code workspace renders the expanded source-context lines inside the existing readiness panel, and backend assertions compare the copied values against the upstream boundary maps. No new gate or execution control was added; tool runner, completed-result transition, result persistence, observation capture, result intake, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback, and mutation remain disabled. Next narrow review should continue the same existing disabled chain into result-intake persistence/rollback/RAG freshness context consistency or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 373: The existing result-intake persistence, rollback fallback, RAG freshness, and result-aggregation disabled surfaces now carry durable accepted-observation summary/risk context forward as read-only data. Result-intake persistence consumes the accepted-observation summary directly, avoiding a circular dependency on result-completion; rollback fallback carries that intake summary context; RAG freshness carries that rollback summary context alongside its own accepted-observation summary; result aggregation carries the RAG freshness rollback-summary context. The Code workspace renders the expanded source-context lines in the existing readiness panel, and backend assertions compare the copied values against the upstream maps. No new gate or execution control was added; intake persistence, rollback fallback execution, RAG freshness update, aggregation, publication, final answer, acknowledgement save, and mutation remain disabled. Next narrow review should continue the existing disabled chain into publication/final-answer context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 374: The existing publication and final-answer generation disabled surfaces now carry the aggregation rollback-summary accepted-observation/risk context forward as read-only data. Publication copies the result-aggregation rollback summary context, final-answer generation copies it from publication, and the Code workspace renders the expanded source-context lines in the existing readiness panel. No new gate or execution control was added; publication, final-answer generation/completion/delivery, acknowledgement save, RAG freshness update, rollback fallback execution, aggregation execution, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from final-answer generation into completion/persistence context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 375: The existing final-answer completion and final-answer persistence disabled surfaces now carry the publication rollback-summary accepted-observation/risk context forward as read-only data. Completion copies the final-answer generation gate's publication rollback summary context, persistence copies it from completion, and the Code workspace renders both expanded source-context lines in the existing readiness panel. No new gate or execution control was added; final-answer completion, persistence, conversation save, user-visible completion, final-response handoff, delivery, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from persistence into conversation-save/user-visible completion context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 376: The existing final-answer conversation-save and user-visible completion disabled surfaces now carry the publication rollback-summary accepted-observation/risk context forward as read-only data. Conversation-save copies the final-answer persistence gate's publication rollback summary context, user-visible completion copies it from conversation-save, and the Code workspace renders both expanded source-context lines in the existing readiness panel. No new gate or execution control was added; conversation save, user-visible completion, final-response handoff, delivery, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from user-visible completion into final-response handoff/delivery context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 377: The existing final-response handoff and final-answer delivery disabled surfaces now carry the publication rollback-summary accepted-observation/risk context forward as read-only data. Final-response handoff copies the user-visible completion gate's publication rollback summary context, final-answer delivery copies it from final-response handoff, and the Code workspace renders both expanded source-context lines in the existing readiness panel. No new gate or execution control was added; final-response handoff, delivery handoff, final-answer delivery, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from delivery into delivery-receipt context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 378: The existing final-answer delivery-receipt disabled surface now carries the publication rollback-summary accepted-observation/risk context forward as read-only data. Delivery receipt copies the final-answer delivery gate's publication rollback summary context, and the Code workspace renders the expanded delivery-receipt source-context line in the existing readiness panel. No new gate or execution control was added; delivery receipt recording, acknowledgement save, final-answer delivery, delivery handoff, final-response handoff, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from delivery receipt into mutation completion/handoff/execution summary context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 379: The existing mutation completion summary, mutation handoff summary, and mutation execution-readiness boundary now carry the delivery-receipt publication rollback-summary accepted-observation/risk context forward as read-only data. Completion copies the delivery-receipt rollback summary context, handoff copies it from completion, execution-readiness copies it from handoff, and the Code workspace renders the expanded source-context lines in the existing readiness panel. No new gate or execution control was added; completion handoff, runtime execution, request creation, push, claim, tool runner, result intake, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from execution-readiness into tool-runner/result-completion context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 380: The existing mutation tool-runner and mutation result-completion disabled surfaces now carry the delivery-receipt publication rollback-summary accepted-observation/risk context forward as read-only data. Tool-runner copies the execution-readiness rollback summary context, result-completion copies it from tool-runner, and the Code workspace renders the expanded source-context lines in the existing readiness panel. No new gate or execution control was added; runner invocation, running transition, completed-result transition, result persistence, observation capture, result intake, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same existing disabled chain downstream from result-completion into result-intake persistence/rollback/RAG freshness context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 381: The existing result-intake persistence, rollback fallback, and RAG freshness disabled surfaces now carry a durable accepted-observation rollback-summary alias forward as read-only source context. A direct result-completion dependency was not added because result-completion is assembled after completion/handoff/execution-readiness/tool-runner and would create a circular dependency back into intake assembly. No new gate or execution control was added; intake persistence, rollback fallback execution, RAG freshness update, aggregation, publication, final-answer work, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from RAG freshness into result aggregation/publication context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 382: The existing result-aggregation and publication disabled surfaces now continue the RAG freshness rollback-summary alias forward as read-only source context. Result aggregation now copies the explicit RAG rollback-summary alias instead of the older generic rollback summary source fields, and publication continues that aggregation source context forward. No new gate or execution control was added; aggregation, publication, final-answer work, acknowledgement save, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from publication into final-answer generation/completion context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 383: The existing final-answer generation and completion disabled surfaces now preserve publication-gate status/schema together with the publication rollback-summary source context. Completion copies the generation gate's source publication gate identity before refusing completion and delivery, and the Code workspace renders that identity alongside the publication observation/risk/rollback-summary context. No new gate or execution control was added; final-answer generation, completion, delivery, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from completion into final-answer persistence/conversation-save context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 384: The existing final-answer persistence and conversation-save disabled surfaces now preserve publication-gate status/schema/session/user/agent/workspace identity together with the publication rollback-summary source context. Persistence copies the completion gate's publication-gate identity, conversation-save copies it from persistence, and the Code workspace renders that identity before the publication boundary, observation/risk, latest-observation, and rollback-summary context. No new gate or execution control was added; persistence, conversation save, user-visible completion, final-response handoff, delivery, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from conversation-save into user-visible completion/final-response handoff context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 385: The existing final-answer user-visible completion and final-response handoff disabled surfaces now preserve publication-gate status/schema/session/user/agent/workspace identity together with the publication rollback-summary source context. User-visible completion copies the conversation-save gate's publication-gate identity, final-response handoff copies it from user-visible completion, and the Code workspace renders that identity before publication boundary, observation/risk, latest-observation, and rollback-summary context. No new gate or execution control was added; user-visible completion, final-response handoff, delivery handoff, delivery, delivery receipt, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from final-response handoff into final-answer delivery/delivery-receipt context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 386: The existing final-answer delivery and delivery-receipt disabled surfaces now preserve publication-gate status/schema/session/user/agent/workspace identity together with the publication rollback-summary source context. Delivery copies the final-response handoff gate's publication-gate identity, delivery-receipt copies it from delivery, and the Code workspace renders that identity before publication boundary, observation/risk, latest-observation, and rollback-summary context. No new gate or execution control was added; final-answer delivery, delivery handoff, delivery receipt recording, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from delivery-receipt into mutation completion/handoff/execution summary context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 387: The existing mutation completion summary, handoff summary, and execution-readiness boundary now preserve publication-gate status/schema/session/user/agent/workspace identity together with publication rollback-summary source context. Completion copies delivery-receipt publication-gate identity, handoff copies it from completion, execution-readiness copies it from handoff, and the Code workspace renders it before publication/draft/observation/risk/latest/rollback context. No new gate or execution control was added; completion handoff, runtime execution, request creation, push, claim, tool runner, result intake, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from execution-readiness into tool-runner/result-completion context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 388: The existing mutation tool-runner and result-completion disabled surfaces now preserve publication-gate status/schema/session/user/agent/workspace identity together with publication rollback-summary source context. Tool-runner copies execution-readiness delivery-receipt publication-gate identity, result-completion copies it from tool-runner, and the Code workspace renders it before publication/draft/observation/risk/latest/rollback context. No new gate or execution control was added; runner invocation, running transition, completed-result transition, result persistence, observation capture, result intake, acknowledgement save, publication, aggregation, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from result-completion into result-intake persistence/rollback/RAG freshness context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 389: The existing result-intake persistence, rollback fallback, and RAG freshness disabled surfaces now preserve publication-gate status/schema/session/user/agent/workspace identity together with accepted-observation summary/readiness and rollback-summary source context. Intake copies the publication-gate identity from the accepted-observation summary, rollback copies it from intake, RAG freshness copies it from rollback, and the Code workspace renders it before accepted-observation/latest/risk/rollback context. No new gate or execution control was added; intake persistence, accepted-observation persistence, rollback fallback execution, RAG freshness update, aggregation, publication, final-answer work, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from RAG freshness into result aggregation/publication context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 390: The existing result-aggregation and publication disabled surfaces now preserve publication-gate status/schema/session/user/agent/workspace identity together with RAG freshness accepted-observation/risk/latest and rollback-summary source context. Result aggregation copies the publication-gate identity from RAG freshness, publication copies it from aggregation, and the Code workspace renders it before accepted-observation/latest/risk/rollback context. No new gate or execution control was added; aggregation execution, publication, final-answer generation, acknowledgement save, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from publication into final-answer generation/completion context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 391: The existing final-answer generation and completion disabled surfaces now render publication-gate status/schema/session/agent/workspace identity together with publication accepted-observation/risk/latest and rollback-summary source context. Generation uses its existing source publication-gate identity fields, completion uses the identity copied from generation, and the Code workspace renders the identity before publication observation/risk/latest/rollback context. No new gate or execution control was added; final-answer generation, completion, delivery, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from final-answer completion into final-answer persistence/conversation-save context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 392: The existing final-answer persistence and conversation-save disabled surfaces now render publication-gate status/schema/session/agent/workspace identity together with publication boundary, accepted-observation/risk/latest, and rollback-summary source context. Persistence uses the identity copied from completion, conversation-save uses the identity copied from persistence, and the Code workspace renders it before publication boundary and observation context. No new gate or execution control was added; final-answer persistence, conversation-turn save, user-visible completion, final-response handoff, delivery, delivery receipt, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from conversation-save into user-visible completion/final-response handoff context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 393: The existing final-answer user-visible completion and final-response handoff disabled surfaces now render publication-gate status/schema/session/agent/workspace identity together with publication boundary, accepted-observation/risk/latest, and rollback-summary source context. User-visible completion uses the identity copied from conversation-save, final-response handoff uses the identity copied from user-visible completion, and the Code workspace renders it before publication boundary and observation context. No new gate or execution control was added; user-visible completion, final-response handoff, delivery handoff, final-answer delivery, delivery receipt, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from final-response handoff into final-answer delivery/delivery-receipt context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 394: The existing final-answer delivery and delivery-receipt disabled surfaces now render publication-gate status/schema/session/agent/workspace identity together with publication boundary, accepted-observation/risk/latest, and rollback-summary source context. Delivery uses the identity copied from final-response handoff, delivery-receipt uses the identity copied from delivery, and the Code workspace renders it before publication boundary and observation context. No new gate or execution control was added; final-answer delivery, delivery handoff, delivery receipt recording, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from delivery-receipt into mutation completion/handoff/execution summary context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 395: The existing mutation completion summary, mutation handoff summary, and mutation execution-readiness boundary now render publication-gate status/schema/session/agent/workspace identity together with publication boundary, accepted-observation/risk/latest, and rollback-summary source context. Completion uses the identity copied from delivery-receipt, handoff uses identity copied from completion, execution-readiness uses identity copied from handoff, and the Code workspace renders it before publication boundary and observation context. No new gate or execution control was added; completion handoff, runtime execution, request creation, push, claim, tool runner, result intake, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from execution-readiness into tool-runner/result-completion context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 396: The existing mutation tool-runner and mutation result-completion disabled surfaces now render publication-gate status/schema/session/agent/workspace identity together with publication boundary, accepted-observation/risk/latest, and rollback-summary source context. Tool-runner uses the identity copied from execution-readiness, result-completion uses identity copied from tool-runner, and the Code workspace renders it before publication boundary and observation context. No new gate or execution control was added; runner invocation, running transition, completed-result transition, result persistence, observation capture, result intake, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from result-completion into result-intake persistence/rollback/RAG freshness context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 397: The existing mutation result-intake persistence, rollback fallback, and RAG freshness disabled surfaces now render publication-gate status/schema/session/user/agent/workspace identity together with accepted-observation readiness/latest, accepted-observation summary/risk, and rollback-summary source context. Result-intake uses the identity copied from accepted-observation summary, rollback uses identity copied from intake, RAG freshness uses identity copied from rollback, and the Code workspace renders the complete identity before accepted-observation and rollback-summary context. No new gate or execution control was added; accepted-observation persistence, rollback fallback execution, RAG freshness update, aggregation execution, publication, final-answer generation, acknowledgement save, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from RAG freshness into result-aggregation/publication context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 398: The existing mutation result-aggregation and publication disabled surfaces now render publication-gate status/schema/session/user/agent/workspace identity together with accepted-observation summary/risk/latest and rollback-summary source context. Result aggregation uses the identity copied from RAG freshness, publication uses identity copied from aggregation, and the Code workspace renders the complete identity before accepted-observation and rollback-summary context. No new gate or execution control was added; aggregation execution, publication, final-answer generation, acknowledgement save, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from publication into final-answer generation/completion context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 399: The existing mutation final-answer generation and final-answer completion disabled surfaces now render publication-gate status/schema/session/user/agent/workspace identity together with publication boundary, accepted-observation summary/risk/latest, and rollback-summary source context. Generation uses the identity copied from publication, completion uses identity copied from generation, and the Code workspace renders the complete identity before publication observation and rollback-summary context. No new gate or execution control was added; final-answer generation, final-answer completion, delivery, final-response handoff, delivery receipt, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from final-answer completion into final-answer persistence/conversation-save context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 400: The existing mutation final-answer persistence and final-answer conversation-save disabled surfaces now render publication-gate status/schema/session/user/agent/workspace identity together with publication boundary, accepted-observation summary/risk/latest, and rollback-summary source context. Persistence uses the identity copied from completion, conversation-save uses identity copied from persistence, and the Code workspace renders the complete identity before publication observation and rollback-summary context. No new gate or execution control was added; final-answer persistence, conversation-turn save, user-visible completion, final-response handoff, delivery, delivery receipt, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from conversation-save into user-visible completion/final-response handoff context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 401: The existing mutation final-answer user-visible completion and final-response handoff disabled surfaces now render publication-gate status/schema/session/user/agent/workspace identity together with publication boundary, accepted-observation summary/risk/latest, and rollback-summary source context. User-visible completion uses the identity copied from conversation-save, final-response handoff uses identity copied from user-visible completion, and the Code workspace renders the complete identity before publication observation and rollback-summary context. No new gate or execution control was added; user-visible completion, final-response handoff, delivery handoff, final-answer delivery, delivery receipt, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from final-response handoff into final-answer delivery/delivery-receipt context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 402: The existing mutation final-answer delivery and delivery-receipt disabled surfaces now render publication-gate status/schema/session/user/agent/workspace identity together with publication boundary, accepted-observation summary/risk/latest, and rollback-summary source context. Delivery uses the identity copied from final-response handoff, delivery-receipt uses identity copied from delivery, and the Code workspace renders the complete identity before publication observation and rollback-summary context. No new gate or execution control was added; final-answer delivery, delivery handoff, delivery receipt recording, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from delivery-receipt into mutation completion/handoff/execution summary context consistency, or use the real browser-level read-only UI smoke when available.

Current pointer addendum after Run 403: The existing mutation completion summary, mutation handoff summary, and mutation execution-readiness boundary now render publication-gate status/schema/session/user/agent/workspace identity together with publication boundary, accepted-observation summary/risk/latest, and rollback-summary source context. Completion uses the identity copied from delivery-receipt, handoff uses identity copied from completion, execution-readiness uses identity copied from handoff, and the Code workspace renders the complete identity before publication observation and rollback-summary context. No new gate or execution control was added; completion handoff, runtime execution, request creation, push, claim, tool runner, result intake, acknowledgement save, publication, aggregation execution, RAG freshness update, rollback fallback execution, and mutation remain disabled. Next narrow review should continue the same disabled chain downstream from execution-readiness into tool-runner/result-completion context consistency, or use the real browser-level read-only UI smoke when available.

## Per-Run Rules

At the start of every work session:

- Read `agent.md`, `README.md`, and this `todo.md`.
- Review the relevant current code before making changes.
- Preserve answer quality, grounding, citation quality, safety, rollbackability, and fallbacks.
- Do not optimize speed by weakening answer quality or evidence quality.
- Keep changes scoped to the current milestone.
- Prefer purpose-specific subfolders for newly added code files; avoid adding more unrelated files to already crowded directories unless the file is a narrow adapter to existing code.
- Do not remove or rewrite unrelated user changes.
- Update this file at the end of the session with progress, verification, remaining risk, and the next recommended step.

Recommended Goal prompt:

```text
Read agent.md, README.md, and todo.md first.
Work toward LearnBot's ultimate goal using todo.md as the roadmap.
Start from the next incomplete milestone.
Review the relevant current code before changing anything.
Do not attempt to complete the entire ultimate product in one run.
Complete only the safest coherent slice for this run.
Preserve answer quality, fallbacks, rollbackability, and regression safety.
After implementation, run the relevant tests/builds.
Then update todo.md with completed work, verification, remaining risk, and the next recommended step.
```

Do not interpret this roadmap as permission to build everything at once. Each run should finish a small, verifiable slice. If a milestone is too large, split it into an explicit sub-step, implement that sub-step, verify it, and record the remaining work.

Latest Milestone 5 pointer update: backend readiness, release-attempt summaries, frontend route-level display, the display-summary helper, the release-attempt model summary helper, the patch-execution-gate summary helper, the patch-release-readiness summary helper, the repository-verification summary helper, the workspace-verification summary helper, the snapshot-readiness summary helper, the rollback-readiness summary helper, the generic readiness-check summary helper, the dry-run result summary helper, the dry-run patch-files summary helper, the dry-run snapshot-observation summary helper, the dry-run rollback-observation summary helper, the fresh-observation request-plan/enqueue-boundary/evidence-summary helpers, and the shared Code workspace SSR harness are now aligned with the Local Agent `patch-dry-run-contract` and disabled release-attempt audit model. `snapshotReadiness` and snapshot manifest validation require non-mutating dry-run evidence with `mutationApplied=false`; invalid `snapshotCreated=true` plus `mutationApplied=true` evidence is pinned in backend readiness, release-attempt final readiness/checklist/display summary, route markup, route prop harness regression, and display-summary helper regression so it cannot be mistaken for release evidence. The route-level smoke and helper tests now render release blocked when linked evidence is complete but patch preconditions fail, render the fresh-observation request plan and enqueue boundary as audit-only with request creation, push, enqueue, claim, and mutation false, render source-only fallback evidence as blocking/non-claimable, pin that pre-release revalidation requires fresh dry-run and repository verification before any future release, keep the internal patch execution gate display text helper-tested as blocked/non-claimable, keep the pre-apply release readiness checklist helper-tested as non-mutating and release-disabled, keep recorded repository verification linkage/check rows helper-tested as read-only evidence, keep effective workspace verification helper-tested as blocking/non-blocking read-only evidence, keep snapshot readiness helper-tested as non-mutating dry-run rollback safety evidence, keep rollback manifest readiness helper-tested as display-only blocking evidence, keep generic readiness check filtering helper-tested so dedicated snapshot/rollback checks are not double-rendered, and keep dry-run result, patch file, snapshot observation, and rollback observation text helper-tested as display-only evidence. The next narrow review should use the real browser-level read-only UI smoke when available, or continue with the next backend/frontend gap in the disabled Local Agent mutation readiness chain.

Before implementing a milestone, make its design concrete from the current code. If the milestone needs details that are not yet specified here, first inspect the code and then choose the smallest design that satisfies the Product Goal. Do not invent broad abstractions unless they are needed for the current milestone or clearly protect the Local Agent/server boundary.

Use this progress format after each run:

```text
## Progress Log

### YYYY-MM-DD - Run N

- Completed:
- Verified:
- Remaining risk:
- Next recommended step:
```

Keep `todo.md` compact. It should remain a roadmap plus the latest working state, not an unlimited history file.

- Keep only the latest 3-5 run entries in `todo.md`.
- Move older detailed logs to `docs/progress/YYYY-MM.md` when needed.
- Keep old progress summarized in one compact bullet if it is still useful.
- Do not paste full test logs, stack traces, diffs, or long reasoning transcripts into `todo.md`.

## Milestone 1 - Stabilize Current Patch Agent Boundary

Intent: make the current server-local patch implementation safe as a transitional capability while preventing it from becoming the default product path.

Key work:

- Review existing Code Agent/Patch Agent APIs, frontend controls, migration state, and tests.
- Keep plan, patch proposal, diff validation, approval/session concepts, and UI diff review.
- Hide, feature-flag, or clearly label server-local apply/test/rollback as prototype/admin/debug only.
- Ensure normal users are guided toward Local Agent execution for user-owned repository changes once that path exists.
- Preserve existing RAG/code search behavior.

Acceptance criteria:

- The UI and backend no longer imply that central-server file mutation is the intended final path.
- Existing code RAG question answering and patch proposal still work.
- Server-local apply/test/rollback is gated or clearly non-default.

## Milestone 2 - Define Local Agent Protocol

Intent: create a decision-complete contract between the central server and user Local Agents.

Key work:

- Introduce execution target concepts: `SERVER_LOCAL` and `USER_LOCAL_AGENT`.
- Define tool request/response envelopes with session id, user id, agent id, workspace id, tool name, input, output, status, timestamps, warnings, and error fields.
- Define initial tools:
  - `agent.status`
  - `agent.doctor`
  - `workspace.list`
  - `workspace.add`
  - `file.read`
  - `patch.apply`
  - `git.status`
  - `git.diff`
  - `command.runAllowed`
  - `rollback.restore`
- Require approval metadata for side-effectful tools.
- Ban arbitrary shell execution. Only typed tools and allowlisted commands are allowed.
- Define failure states for disconnected agent, unapproved workspace, path escape, timeout, context mismatch, test failure, and rollback refusal.

Acceptance criteria:

- Backend and Local Agent can be implemented independently from the protocol.
- Tool execution location is explicit.
- Approval and audit state are represented in the model.

## Milestone 3 - Build Central Agent Gateway

Intent: let the central server coordinate Local Agents without directly reaching user PCs.

Key work:

- Add outbound Local Agent connection support, preferably WebSocket from agent to server.
- Track connected agents per user, capabilities, version, heartbeat, and approved workspaces.
- Route tool requests to the correct connected Local Agent.
- Record tool requests, responses, approval state, and errors in session history.
- Show Local Agent connection/workspace status in the web UI.
- If no Local Agent is connected, block side-effectful actions and show clear guidance.

Acceptance criteria:

- The server can tell whether a user's Local Agent is connected.
- The web UI can show connected/disconnected and workspace-ready/not-ready states.
- Side-effectful requests do not silently fall back to server-local execution.

## Milestone 4 - Build Local Agent MVP and CLI

Intent: give each developer a lightweight local executable that manages workspace-safe tool execution.

Recommended implementation direction:

- Prefer Go or .NET for a lightweight, deployment-friendly Windows executable.
- Initial distribution: PowerShell installer downloads the executable, creates config, pairs with LearnBot, registers workspace roots, and starts the agent.
- Practical internal use: Windows Service or equivalent background process with logs, restart behavior, config, and update support.
- Mature distribution: signed MSI/EXE installer with uninstall, auto-update, proxy/internal-network support, and optional GUI settings.

Minimum CLI commands:

- `learnbot pair`
- `learnbot agent start`
- `learnbot agent stop`
- `learnbot agent status`
- `learnbot workspace add .`
- `learnbot workspace list`
- `learnbot doctor`
- `learnbot open`

Safety requirements:

- Access only user-approved workspace roots.
- Reject path traversal and workspace escape.
- Snapshot before file writes.
- Apply patches only after approval.
- Run only allowlisted test/build commands.
- Return structured observations to the server.

Acceptance criteria:

- A user can run `learnbot` commands from PowerShell to pair, start the agent, register a workspace, and confirm connection from the web UI.
- The Local Agent can execute safe read-only tools and reject unsafe requests.

## Milestone 5 - Move File Mutation to Local Agent

Intent: make user-owned file changes happen on the user's PC, not in the central server's Docker volume.

Target flow:

```text
User request in web UI
-> central RAG retrieval and planning
-> diff proposal and validation
-> user approval
-> tool request to USER_LOCAL_AGENT
-> Local Agent patch apply with snapshot
-> Local Agent allowlisted test/build
-> observations returned to server
-> final answer with changed files, test result, evidence, and residual risk
```

Key work:

- Reuse existing patch proposal and validation where possible.
- Replace default apply/test/rollback calls with Local Agent tool requests.
- Keep server-local apply/test/rollback only as non-default prototype/admin/debug path.
- Add RAG freshness handling after local file changes: partial reindex, freshness marker, or explicit stale-index warning.

Acceptance criteria:

- Approved patches modify only the user's registered local workspace.
- Server-local clone or Docker volume is not used for normal user file changes.
- Context mismatch, failed tests, disconnected agent, and rollback refusal are visible in the UI.

## Milestone 6 - Implement Agent Loop

Intent: move from one-shot patch generation to a Codex-like loop of planning, tool use, observation, and completion judgment.

Key work:

- Add a bounded loop: plan, select tool, request approval if needed, execute, observe, decide next step.
- Set maximum step count, timeout, cancellation, and user approval checkpoints.
- Preserve citations and code evidence in final answers.
- If evidence is weak or the model is uncertain, ask for clarification instead of making risky changes.
- Persist session timeline with model decisions, tool calls, observations, approvals, and final result.

Acceptance criteria:

- A request such as "fix this bug" can progress through analysis, patch proposal, apply, test, and final report.
- The system can stop safely on ambiguity, missing agent, failed tool, or approval denial.

## Milestone 7 - Quality and Regression Harness

Intent: make answer quality and agent safety measurable before and after changes.

Key work:

- Build regression cases for document RAG, code RAG, streaming, patch proposal, Local Agent tool use, and rollback.
- Track citation correctness, evidence relevance, hallucination risk, follow-up quality, patch validity, rollbackability, first-token latency, and total latency.
- Keep fallback behavior explicit for model failure, embedding failure, streaming failure, crawler failure, graph enrichment failure, and Local Agent disconnection.
- Ensure speed improvements come from reducing duplicate work, prompt waste, and unnecessary output, not from removing grounding.

Acceptance criteria:

- Important RAG and agent workflows can be compared across runs.
- Regressions in answer quality or safety are visible before release.

## Milestone 8 - Improve Web Crawling and Indexing Quality

Intent: make URL ingestion reliable, transparent, and useful for RAG answers.

Key work:

- Review crawler, extractor, indexing diagnostics, status transitions, and frontend visibility.
- Improve page extraction quality and stored metadata so users can understand what was indexed.
- Show why pages were skipped: allowlist, robots.txt, weak extraction, duplicate, depth/page limit, fetch failure, or unsupported content.
- Preserve per-page citation URLs.
- Improve recursive crawl controls: same-host/path policy, max depth, max pages, canonical URL, duplicate detection, and graceful partial success.
- Consider proven open-source extraction/parsing libraries behind a clear internal interface.

Acceptance criteria:

- Users can see which URLs were indexed, skipped, or partially processed and why.
- Searchable content remains available even when enrichment fails.
- Crawled evidence shown in answers is cleaner and more trustworthy.

## Verification Commands

Use the local Maven installation under `.tools`.

From the repository root:

```powershell
.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml test
```

From `backend`:

```powershell
..\.tools\apache-maven-3.9.9\bin\mvn.cmd test
```

Frontend:

```powershell
cd frontend
npm run build
```

Local runtime smoke checks should include:

- `.\scripts\up.ps1 -Build`
- Web UI loads at `http://localhost:8083`
- Backend routes do not return unexpected 404 for enabled features
- Local Agent disconnected state is visible and safe
- Local Agent connected state can execute only approved tools

## Progress Log

Older detailed entries are archived in `docs/progress/2026-06.md`.

### 2026-07-02 - Run 532

- Completed: Re-ran the focused backend verification for the guarded approved-patch release path using the Maven install under `.tools`. This clears the previous Run 531 verification blocker and confirms the new default-off release flag, explicit `/release-for-execution` controller delegation, and flag-on service behavior compile and pass their focused tests.
- Verified: Restricted Maven still fails because sandbox networking cannot resolve Maven Central (`Permission denied: getsockopt`). Approved focused Maven verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 56 tests with 0 failures.
- Remaining risk: This proves the release boundary only at mocked repository/controller-test level. The full Codex-like product loop still needs a narrow flag-on local-stack proof that a released approved patch is actually claimable by the Local Agent, completed as `patch.apply`, persisted as a mutation observation, fed into final reporting/RAG freshness, and surfaced as a final answer/acknowledgement. The release flag must remain default-off until that path is proven.
- Next recommended step: Inspect and extend the smallest existing Local Agent claim/complete or live-stack smoke so it covers approved patch release -> claim -> completion after the new flag-on release path, then add final report/RAG freshness/acknowledgement one slice at a time.

### 2026-07-02 - Run 533

- Completed: Added `LocalAgentToolGatewayServiceTest.releasedHeldPatchCanBeClaimedCompletedAndRecordedAsMutationObservation`, the smallest M7/M6 service-level proof that the new flag-on guarded release path feeds the existing Local Agent lifecycle. The test releases an approved-held `patch.apply` with fresh release-attempt-linked evidence, verifies the same request becomes claimable through `claimNext`, completes it as a successful `patch.apply`, and confirms the completed response is enriched with `mutationResultIntakeCandidate` plus `acceptedMutationObservation`, saved to the mutation observation intake repository, and recorded on the loop timeline through `appendObservationResult` and `appendNextDecision`.
- Verified: Restricted Maven still fails because sandbox networking cannot resolve Maven Central (`Permission denied: getsockopt`). Approved focused Maven verification passed after fixing the test to assert the enriched response: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 57 tests with 0 failures.
- Remaining risk: This is still a mocked service-level proof, not a live Spring Boot/PostgreSQL/Local Agent smoke. It proves the release, claim, complete, mutation-observation, and loop-timeline contracts line up, but does not yet prove durable rows transition correctly in a live local stack or that result aggregation, final reporting, RAG freshness update, final answer delivery, and acknowledgement save are enabled.
- Next recommended step: Move the same release -> claim -> complete proof to the durable/live path, preferably by extending the existing live Postgres Local Agent flow or quality harness seed so the released approved patch row is claimed and completed from persisted storage. After that, wire result aggregation/final report, RAG freshness, final answer, and acknowledgement in separate narrow slices.

### 2026-07-02 - Run 534

- Completed: Moved the release -> claim -> complete proof one layer down into durable PostgreSQL rows. Added `LocalAgentToolExecutionRepositoryLivePostgresTest.durableLinkedEvidenceCanReleaseHeldPatchThenClaimCompleteAndPersistMutationObservation`, which uses real `LocalAgentToolExecutionRepository`, `LocalAgentPatchReleaseAttemptRepository`, and `LocalAgentMutationObservationIntakeRepository` rows with the release flag enabled. The test creates an approved-held source patch, creates a release attempt, completes release-attempt-linked fresh observations, calls `releaseHeldPatchForExecution`, claims the same persisted source row, completes it as a real mutation, and verifies the completed row plus durable intake observation are linked to the source request/release attempt and remain audit-only for aggregation/publication/acknowledgement/RAG freshness.
- Verified: Approved focused compile/default test run passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 66 tests with 0 failures and 9 skipped opt-in live tests. `Test-NetConnection localhost -Port 5432` succeeded. Approved opt-in live PostgreSQL verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dlearnbot.live-postgres-tests=true" "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest#durableLinkedEvidenceCanReleaseHeldPatchThenClaimCompleteAndPersistMutationObservation" test` ran 1 test with 0 failures.
- Remaining risk: This proves durable row state transitions and durable accepted-observation intake, but it still does not run the actual .NET Local Agent binary against a real workspace in this path and does not yet enable result aggregation, final mutation report publication, RAG freshness update, final answer delivery, or acknowledgement save.
- Next recommended step: Stop widening release/claim. Use the durable accepted-observation data now proven by Run 534 to implement the smallest result aggregation/final mutation report slice, then follow with RAG freshness, final answer, and acknowledgement gates in separate focused changes.

### 2026-07-02 - Run 535

- Completed: Added the first downstream final-report data slice after durable mutation observation intake. `LocalAgentFinalMutationReportSummaryBuilder` now lives under the purpose-specific `service/localagent` package and builds a read-only `finalMutationReportSummary` from accepted mutation observation summary/readiness data. The summary carries release/source/session/user/agent/workspace identity, accepted observation counts, the latest accepted observation, and concrete report sections for changed files, verification, rollback, RAG freshness warning, and residual risk while keeping final report generation, publication, final answer, acknowledgement save, RAG freshness update, and mutation disabled.
- Completed integration: `LocalAgentToolGatewayService` now includes `finalMutationReportSummary` in the release-attempt readiness model after accepted-observation readiness and before the existing disabled final mutation report draft. The Run 534 live PostgreSQL regression now also asserts that a durable `ACCEPTED` `patch.apply` observation produces `READY_SUMMARY_AUDIT_ONLY`, changed-files/verification/rollback/RAG freshness/residual-risk sections, stale-index risk visibility, and disabled downstream controls.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 66 tests with 0 failures and 9 skipped opt-in live tests. Approved opt-in live PostgreSQL verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dlearnbot.live-postgres-tests=true" "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest#durableLinkedEvidenceCanReleaseHeldPatchThenClaimCompleteAndPersistMutationObservation" test` ran 1 test with 0 failures.
- Remaining risk: This is still an audit/readiness report summary, not generated final user output. It does not update the RAG index, publish a final answer, persist a conversation turn, or save user acknowledgement. The actual .NET Local Agent binary is not exercised in this release-to-report-summary test path.
- Next recommended step: Convert the `finalMutationReportSummary` RAG freshness warning into an explicit RAG freshness marker/readiness artifact tied to the changed local files or stale-index policy. After that, wire final answer publication and acknowledgement as separate narrow slices.

### 2026-07-02 - Run 536

- Completed: Added the explicit RAG freshness marker/readiness artifact requested by the Run 535 pointer. `LocalAgentRagFreshnessMarkerBuilder` now lives under the purpose-specific `service/localagent` package and builds a read-only `ragFreshnessMarker` from the source patch input and `finalMutationReportSummary`. It carries source/release/session/user/agent/workspace identity, target files, stale-index policy, freshness action, stale-index risk visibility, and `finalAnswerMustDiscloseStaleIndex=true` while keeping partial reindex, RAG freshness update, final answer, publication, acknowledgement save, and mutation disabled.
- Completed integration: `LocalAgentToolGatewayService` now passes source patch input into the release-attempt model and includes `ragFreshnessMarker` beside `finalMutationReportSummary`. The opt-in live PostgreSQL regression now verifies that release -> claim -> `patch.apply` complete -> durable accepted observation -> report summary also produces a marker with `STALE_INDEX_WARNING_REQUIRED`, `targetFiles=["src/App.java"]`, `staleIndexPolicy=REQUIRE_EXPECTED_HASH_OR_CONTEXT_MATCH`, `freshnessAction=REQUIRE_PARTIAL_REINDEX_OR_STALE_WARNING`, and disabled downstream controls.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 66 tests with 0 failures and 9 skipped opt-in live tests. Approved opt-in live PostgreSQL verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dlearnbot.live-postgres-tests=true" "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest#durableLinkedEvidenceCanReleaseHeldPatchThenClaimCompleteAndPersistMutationObservation" test` ran 1 test with 0 failures.
- Remaining risk: This still does not perform partial reindexing or update RAG freshness. It only creates a machine-readable stale-index marker that final reporting/final answer must consume. The final answer publication/handoff path and acknowledgement save remain disabled.
- Next recommended step: Build the smallest final-answer publication/handoff model that consumes `finalMutationReportSummary` and `ragFreshnessMarker`, refuses publication unless stale-index disclosure is present, and then add acknowledgement save as a separate narrow slice.

### 2026-07-02 - Run 537

- Completed: Added the downstream final-answer publication handoff/readiness artifact requested by the Run 536 pointer. `LocalAgentFinalAnswerPublicationHandoffBuilder` now lives under the purpose-specific `service/localagent` package and builds a read-only `finalAnswerPublicationHandoff` from `finalMutationReportSummary` plus `ragFreshnessMarker`. It carries source/release/session/user/agent/workspace identity, summary/marker source statuses, target files, final-answer section keys, stale-index policy/action, explicit stale-index disclosure text, and a refusal reason while keeping publication, final answer generation/delivery, acknowledgement save, partial reindex, RAG freshness update, and mutation disabled.
- Completed integration: `LocalAgentToolGatewayService` now includes `finalAnswerPublicationHandoff` beside `finalMutationReportSummary` and `ragFreshnessMarker` in the release-attempt model. The opt-in live PostgreSQL regression now verifies that release -> claim -> `patch.apply` complete -> durable accepted observation -> report summary -> RAG freshness marker also produces a `READY_HANDOFF_AUDIT_ONLY_PUBLICATION_DISABLED` handoff with `targetFiles=["src/App.java"]`, final-answer section keys, stale-index disclosure text, and all downstream controls disabled.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 66 tests with 0 failures and 9 skipped opt-in live tests. Approved opt-in live PostgreSQL verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dlearnbot.live-postgres-tests=true" "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest#durableLinkedEvidenceCanReleaseHeldPatchThenClaimCompleteAndPersistMutationObservation" test` ran 1 test with 0 failures.
- Remaining risk: This still does not publish a final user answer, save an acknowledgement, update RAG freshness, or run the .NET Local Agent binary in this regression path. It only proves the final-answer handoff model refuses publication unless stale-index disclosure is present and keeps all side-effect controls disabled.
- Next recommended step: Add the smallest acknowledgement-save handoff/receipt model that consumes `finalAnswerPublicationHandoff`, still disabled by default, then decide whether the next slice should be UI exposure or an end-to-end local-agent runtime smoke.

### 2026-07-02 - Run 538

- Completed: Added the acknowledgement-save handoff/readiness artifact requested by the Run 537 pointer. `LocalAgentAcknowledgementSaveHandoffBuilder` now lives under the purpose-specific `service/localagent` package and builds a read-only `acknowledgementSaveHandoff` from `finalAnswerPublicationHandoff`. It carries source/release/session/user/agent/workspace identity, source final-answer handoff schema/status, target files, final-answer section keys, stale-index disclosure text, acknowledgement receipt requirement/modeling, and explicit disabled controls for acknowledgement save, conversation save, delivery, publication, RAG freshness update, partial reindex, and mutation.
- Completed integration: `LocalAgentToolGatewayService` now includes `acknowledgementSaveHandoff` beside `finalAnswerPublicationHandoff` in the release-attempt model. The opt-in live PostgreSQL regression now verifies that release -> claim -> `patch.apply` complete -> durable accepted observation -> report summary -> RAG freshness marker -> final-answer handoff also produces a `READY_ACKNOWLEDGEMENT_AUDIT_ONLY_SAVE_DISABLED` acknowledgement handoff with `targetFiles=["src/App.java"]`, final-answer section keys, stale-index disclosure text, receipt modeling, and all downstream controls disabled.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 66 tests with 0 failures and 9 skipped opt-in live tests. Approved opt-in live PostgreSQL verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dlearnbot.live-postgres-tests=true" "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest#durableLinkedEvidenceCanReleaseHeldPatchThenClaimCompleteAndPersistMutationObservation" test` ran 1 test with 0 failures.
- Remaining risk: This still does not save a real acknowledgement, publish a final user answer, save a conversation turn, update RAG freshness, or run the .NET Local Agent binary in this regression path. It proves only the release-attempt read model can carry acknowledgement-save prerequisites without opening side effects.
- Next recommended step: Either expose `finalAnswerPublicationHandoff` and `acknowledgementSaveHandoff` in the Code workspace UI with disabled-flag guard coverage, or run a narrow end-to-end Local Agent runtime smoke against a temporary workspace using the existing approved release/claim/complete path before enabling any user-visible final publication.

### 2026-07-02 - Run 539

- Completed: Exposed the Run 537/538 final-answer and acknowledgement handoff read models in the Code workspace readiness panel. `CodeWorkspace.jsx` now renders `finalAnswerPublicationHandoff` and `acknowledgementSaveHandoff` with source statuses, target files, final-answer section keys, stale-index disclosure text, acknowledgement receipt requirement/modeling, blocking keys, and explicit disabled publication/final-answer/delivery/conversation/acknowledgement/RAG/partial-reindex/mutation controls.
- Completed coverage: The Code workspace SSR smoke fixture now includes both handoff models and verifies their rendered text plus the existing recursive `assertNoForbiddenTrueFlags` guard over the latest attempt and full props. This keeps newly added `publicationEnabled`, `finalAnswerGenerationEnabled`, `finalAnswerDeliveryEnabled`, `conversationSaveEnabled`, `acknowledgementSaveEnabled`, `ragFreshnessUpdateEnabled`, `partialReindexEnabled`, and `mutationAllowed` values pinned false in the UI route fixture.
- Verified: `node src\components\code\codeWorkspaceReadinessPanelSmoke.test.mjs` passed from `frontend/`. `npm run build` passed with the existing Vite large-chunk warnings only.
- Remaining risk: This is UI exposure and guard coverage only. It still does not run a live server plus Local Agent release/claim/complete smoke, save acknowledgement receipts, publish final answers, save conversation turns, or update RAG freshness.
- Next recommended step: Run or add the narrow end-to-end server plus Local Agent runtime smoke for the approved release/claim/complete path against a temporary workspace, reusing the existing `approved-execution-flow-contract` report assertion where possible and keeping automatic final publication disabled.

### 2026-07-02 - Run 540

- Completed: Added a repeatable approved server-queue Local Agent smoke wrapper. `scripts/quality/local-agent-flow/run-approved-server-queue-flow-smoke.ps1` runs `dotnet run --project local-agent -- self-test approved-server-queue-flow-contract --report ...`, then immediately validates the generated report with `assert-approved-execution-flow-report.mjs`. This proves the Local Agent can poll an HTTP queue server, process approved `patch.apply`, `command.runAllowed`, `git.status`, and `rollback.restore` requests against a temporary workspace, return responses, and restore the file through rollback.
- Completed guardrails: Strengthened `assert-approved-execution-flow-report.mjs` so approved-flow reports must contain exactly four step entries and four unique request ids, in addition to the existing mutation, command allowlist, git dirty-state, rollback, identity, temporary-workspace, and no-server-local-mutation checks. Added `assert-approved-server-queue-flow-smoke-contract.ps1` and wired it into `scripts/quality/run-regression-harness.ps1` so the wrapper remains covered by a lightweight parse/contract check.
- Verified: `powershell.exe -ExecutionPolicy Bypass -File .\scripts\quality\local-agent-flow\run-approved-server-queue-flow-smoke.ps1` passed and produced `.tmp\quality\local-agent-approved-flow\approved-server-queue-flow-smoke-report.json`; `patch.apply`, `command.runAllowed`, `git.status`, and `rollback.restore` all returned `SUCCEEDED`. `node scripts\quality\local-agent-flow\assert-approved-execution-flow-report.test.mjs`, `powershell.exe -ExecutionPolicy Bypass -File .\scripts\quality\local-agent-flow\assert-approved-server-queue-flow-smoke-contract.ps1`, direct harness parse check, and `dotnet build local-agent\LearnBot.LocalAgent.csproj` all passed.
- Remaining risk: This is still an in-process HTTP queue server smoke, not the real Spring Boot server plus PostgreSQL plus paired Local Agent stack. It does not exercise login/pairing, durable release-for-execution over the real API, final publication, acknowledgement save, or RAG freshness update.
- Next recommended step: Add or run a live Spring server plus Local Agent polling smoke that uses the real backend endpoints and durable rows for the approved release/claim/complete path in a temporary workspace, then keep final publication and acknowledgement save disabled until that live path is stable.

### 2026-07-02 - Run 531

- Completed: Returned to the M7/M6 Codex-like Local Agent loop after M8 closeout. Added a guarded release switch for the approved patch path: `LearnBotProperties.LocalAgent.patchExecutionReleaseEnabled` defaults to false and is bound from `LEARNBOT_LOCAL_AGENT_PATCH_EXECUTION_RELEASE_ENABLED` through `learnbot.local-agent.patch-execution-release-enabled`. `LocalAgentToolGatewayService.releaseHeldPatchForExecution` now reads that property instead of a hardcoded false. Existing tests can still use the old constructor, while Spring uses the new `@Autowired` constructor with `LearnBotProperties`. Added `POST /api/local-agents/tools/{requestId}/release-for-execution` as a separate explicit endpoint; the existing `/release` endpoint remains the refusal-boundary/readiness surface.
- Completed tests/code review: Added `LocalAgentToolGatewayServiceTest.releaseHeldPatchForExecutionCanReleaseWhenFlagAndFreshLinkedEvidenceAreReady`, which uses flag-on properties and fresh release-attempt-linked repository verification plus patch dry-run evidence to prove an approved-held `patch.apply` can become `APPROVED` with mutation input carrying `mutationAllowed=true`, `dryRunOnly=false`, `sourceRequestId`, and `releaseAttemptId`, without creating disabled attempts or pushing requests. Added `LocalAgentControllerTest.releasePatchExecutionForClaimDelegatesToFlagGuardedReleasePath` to pin the new endpoint delegation.
- Verification: Restricted Maven failed because sandbox networking cannot resolve Maven Central (`Permission denied: getsockopt`). The required approved Maven rerun for `LocalAgentToolGatewayServiceTest,LocalAgentControllerTest` was blocked by the Codex usage limit, so Java compile/test verification is not complete in this run. Static inspection confirmed the new property, endpoint, constructor injection, and focused tests are present.
- Remaining risk: Because Maven did not run, compile or test failures may still exist. The release flag is intentionally default-off, but when enabled it can make a held patch claimable if all existing freshness/readiness checks pass; this must be proven with focused backend tests before adding any UI button, default enablement, automatic release, final reporting, RAG freshness update, or acknowledgement flow.
- Next recommended step: Run the focused Maven tests as soon as execution is available. If they pass, add a narrow flag-on integration/smoke that exercises approved patch release through the Local Agent claim/complete path in a temporary workspace, then wire post-mutation result aggregation/RAG freshness/final answer gates one slice at a time.

### 2026-07-02 - Run 530

- Completed: Closed the M8 checklist without broadening the milestone further. Added `frontend/src/components/documents/documentWorkspaceRetryContextSmoke.test.mjs`, a focused Document workspace SSR smoke that renders a partial document job with failed `DOCUMENT_GRAPH_REBUILD` and `DOCUMENT_LLM_ENRICHMENT` diagnostics and verifies the failed retry context plus retry actions are visible in route markup. Wired the smoke into `scripts/quality/run-regression-harness.ps1` with `document-rag`, `indexing-diagnostics`, `evidence-fallback`, and `workspace-readiness` coverage. Updated `docs/quality/milestone-8-indexing-quality.md` with a closeout audit mapping each remaining M8 live check to current evidence.
- Verified: `node src\components\documents\documentWorkspaceRetryContextSmoke.test.mjs` passed from `frontend/`. Existing adjacent checks still pass: `node src\components\documents\documentWorkspaceCrawlAuditSmoke.test.mjs`, `node scripts\quality\indexing\seed-document-post-processing-retry.test.mjs`, and `node scripts\quality\indexing\evaluate-indexing-diagnostics-fixtures.test.mjs`. Current evidence remains: default harness `.tmp\quality\regression-harness-20260702-105131.json` passed 44/44 with 8/8 signals, live indexing `.tmp\quality\live-indexing-run526-current\indexing-diagnostics-live-20260702-015449.json` passed 3/3, live indexing comparison had 0 regressions, live RAG answer score `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-score-current-rescored-live.json` passed 3/3, and strict live RAG comparison had 0 regressions/warnings.
- Remaining risk: The newly added retry-context smoke was targeted-verified but the full default regression harness was not rerun after adding it, so the latest aggregate count still says 44/44 rather than a refreshed count including the new smoke. This is acceptable for leaving M8 because the change is harness/test-only and adjacent focused checks passed, but a future release-candidate run should refresh the aggregate harness once.
- Next recommended step: Resume M7/M6 agent-loop completion toward Codex-like use: model/runner next-step selection beyond read-only observation, approved side-effect release/claim sequencing, result aggregation/final report, RAG freshness after mutation, final answer publication, and acknowledgement. Do not keep expanding M8 unless new indexing/RAG regressions appear.

### 2026-07-02 - Run 529

- Completed: Tightened the live RAG answer scorer so M8 answer comparison can distinguish grounded retrieval from brittle answer wording. `scripts/quality/rag-quality/evaluate-rag-quality-fixtures.mjs` now passes the observed evidence text into follow-up scoring and reports `matchedAnswerTerms`, `matchedEvidenceTerms`, `answerTermCoverage`, and `evidenceTermCoverage`. Expected follow-up terms can now be satisfied by answer/effective-question text or by the cited evidence, while forbidden follow-up terms are still checked only against answer/effective-question text. This keeps hallucinated or off-topic answers visible while preventing translated/mojibake wording from falsely failing a grounded RAG answer whose evidence still contains the required follow-up focus.
- Verified: Added a regression in `scripts/quality/rag-quality/evaluate-rag-quality-fixtures.test.mjs` for an evidence-backed follow-up where the answer paraphrases the expected terms but the cited evidence contains them. `node scripts\quality\rag-quality\evaluate-rag-quality-fixtures.test.mjs`, `node scripts\quality\rag-quality\compare-rag-quality-reports.test.mjs`, and `node scripts\quality\rag-quality\create-live-baseline-from-capture.test.mjs` passed. Re-scoring the previous live rerun wrote `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-score-current-rerun-rescored.json` and passed 3/3 cases; strict comparison passed in `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-score-comparison-current-rerun-rescored-strict.json`.
- Verified live: A fresh current-stack live capture against `http://localhost:8083` wrote `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-fixtures-current-rescored-live.json` and `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-score-current-rescored-live.json`. It passed 3/3 cases with citation recall 1, citation precision 1, evidence coverage 1, follow-up quality 1, latency budget 3/3, and hallucination-risk flags 0. Strict baseline comparison passed in `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-score-comparison-current-rescored-live-strict.json`. The follow-up case reports `answerTermCoverage=0.5` and `evidenceTermCoverage=1`, which makes the remaining answer wording weakness visible without failing grounded retrieval.
- Remaining risk: The scorer now separates answer wording from evidence grounding, but M8 still needs a short closeout audit against the checklist before being called complete. The full default regression harness was not rerun after this narrow scorer-only change; related RAG quality tests and current-stack live comparison were run instead.
- Next recommended step: Perform the M8 closeout audit across default harness evidence, live indexing evidence, live RAG answer evidence, and any remaining UI retry-context expectation.

### 2026-07-02 - Run 528

- Completed: Refreshed the M8 live RAG answer comparison against the current local stack at `http://localhost:8083` using the expanded live baseline fixture instead of relying on older answer-comparison evidence. The first current-stack capture wrote `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-fixtures-current.json` and `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-score-current.json`; it passed 3/3 scored cases with citation recall 1, citation precision 1, evidence coverage 1, follow-up quality 1, and no hallucination-risk flags. A latency-relaxed comparison passed, but the default strict comparison `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-score-comparison-current-strict.json` failed because `live-document-example-domain-answer` and `live-document-example-domain-follow-up` exceeded the baseline by more than 20%.
- Verified: Reran the same current-stack live capture to check whether the strict latency issue was stable. The rerun wrote `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-fixtures-current-rerun.json` and `.tmp\quality\live-rag-answer-comparison-run527\rag-live-expanded-score-current-rerun.json`; latency improved and all cases stayed inside their absolute 180s budget, citation recall stayed 1, citation precision stayed 1, evidence coverage stayed 1, and hallucination-risk flags stayed 0. However, the rerun passed only 2/3 cases because `live-document-example-domain-follow-up` failed the expected follow-up wording terms (`avoid`, `operations`), reducing `followUpQuality` to 0.667. Baseline comparison rejected the rerun for that follow-up regression.
- Remaining risk: M8 live indexing/crawler/retry evidence is current and passing, but live RAG answer comparison is not yet stable enough to close M8. The evidence path is grounded, but either the follow-up answer behavior is genuinely inconsistent or the current live scorer is too brittle for translated/mojibake answer wording because it scores follow-up terms only against answer/effective-question text, not retrieved evidence. The latency comparison also needs a clear policy for local LLM variance before it can be a release gate.
- Next recommended step: Review `scripts/quality/rag-quality/evaluate-rag-quality-fixtures.mjs` and the live follow-up fixture. Fix the product behavior if the answer is actually losing the "avoid operations" focus; otherwise adjust the scorer or fixture to measure grounded follow-up quality without masking real answer regressions. Then rerun the live RAG comparison and perform the M8 closeout audit.

### 2026-07-02 - Run 527

- Completed: Ran the current local stack M8 live indexing audit instead of adding more offline-only checks. With `LEARNBOT_QUALITY_LOGIN_ID=jinsu.kim` and `LEARNBOT_QUALITY_PASSWORD=admin1234`, `node scripts\quality\indexing\run-live-indexing-audit.mjs --server http://localhost:8083 --discover --output-dir .tmp\quality\live-indexing-run526-current` discovered ready live seeds, generated the capture template, captured 3/3 live audit cases, and evaluated the current stack report. Discovery selected document job `f5bd923d-11da-44d7-9d85-f394e569380f` (`SUCCEEDED` with retry diagnostic context), code repository `9a083c8a-7a70-4ba1-99ed-d73ffdc30fbe`, failed code job `2f035401-0a47-47fc-bf08-ca6673b1a028`, and crawler document `ac1d2fb1-ece5-4832-afe1-24896df4d889` for `https://example.com/`.
- Verified: Live audit summary `.tmp\quality\live-indexing-run526-current\indexing-live-audit-summary-20260702-015449.json` passed. Live diagnostics `.tmp\quality\live-indexing-run526-current\indexing-diagnostics-live-20260702-015449.json` passed 3/3 cases with searchable fallback, retry readiness, active-index preservation, crawler policy, crawler insight, and citation-source checks passing for all three cases. Regression comparison against `.tmp\quality\live-indexing-after-document-retry-seed\indexing-diagnostics-live-20260701-132631.json` passed with 0 regressions and 0 warnings in `.tmp\quality\live-indexing-run526-current\indexing-diagnostics-live-comparison-20260702-015449.json`.
- Remaining risk: This run refreshes current-stack indexing/crawler/retry evidence, but it does not refresh the live RAG answer comparison from the same current stack. Existing answer-comparison evidence under `.tmp\quality\live-rag-answer-comparison` passed previously, but M8 closeout should either rerun/promote that evidence or explicitly record why the existing comparison is accepted. A focused UI/live retry-context smoke may still be useful if M8 requires browser-visible retry proof rather than API diagnostics proof.
- Next recommended step: Refresh the live RAG answer comparison evidence against `http://localhost:8083`, then perform a concise M8 closeout audit against the remaining checklist.

### 2026-07-02 - Run 526

- Completed: Wired the M8 Document workspace crawler-audit SSR smoke into the default regression harness. `scripts/quality/run-regression-harness.ps1` now runs `frontend-documentWorkspaceCrawlAuditSmoke.test` and counts it toward `document-rag`, `crawler-extraction`, `indexing-diagnostics`, and `evidence-fallback` coverage, so skipped URL category/action visibility is part of the aggregate M7/M8 quality gate instead of remaining a standalone focused check.
- Verified: Direct `.\scripts\quality\run-regression-harness.ps1` remains blocked by local PowerShell execution policy. A sandbox bypass run produced `.tmp\quality\regression-harness-20260702-104825.json` but failed at backend Maven dependency resolution because sandbox networking blocks Maven Central (`Permission denied: getsockopt`). Approved full rerun passed in `.tmp\quality\regression-harness-20260702-105131.json`: 44/44 steps passed, 8/8 quality signals covered, `frontend-documentWorkspaceCrawlAuditSmoke.test=passed`, and `backend-focused-quality-regressions=passed` with 198 tests, 0 failures, and 8 skipped opt-in live PostgreSQL tests.
- Remaining risk: This closes the offline/default harness gap for the M8 crawler insight UI path, but it is not yet live-stack M8 release evidence. The remaining M8 checks still need real or disposable stack proof for web seed allowlist/robots audits, failed code reindex active-index preservation, document retry UI context, and RAG answer comparison before/after indexing changes.
- Next recommended step: Move on to the smallest live-stack M8 seed/audit proof instead of adding more offline-only harness breadth.

### 2026-07-02 - Run 525

- Completed: Added a focused M8 Document workspace SSR smoke for crawler audit insight visibility. `frontend/src/components/documents/documentWorkspaceCrawlAuditSmoke.test.mjs` renders `DocumentWorkspace` with a skipped crawler audit and verifies the markup contains the derived `POLICY_BLOCK` category, the user action, the skip message, and the skipped host. `docs/quality/milestone-8-indexing-quality.md` now documents this smoke alongside the scorer/comparison gate.
- Verified: `node src\components\documents\documentWorkspaceCrawlAuditSmoke.test.mjs` passed from `frontend/`. M8 indexing tooling regressions still pass: `node scripts\quality\indexing\evaluate-indexing-diagnostics-fixtures.test.mjs`, `node scripts\quality\indexing\capture-indexing-audit-fixtures.test.mjs`, and `node scripts\quality\indexing\compare-indexing-diagnostics-reports.test.mjs`. `npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: The default full regression harness was not rerun after adding the Document workspace smoke. Live-stack M8 checks still need a real web seed, code reindex failure preservation evidence, document retry UI context, and before/after RAG answer comparison before treating M8 as release-ready.
- Next recommended step: Rerun the default regression harness so M7/M8 aggregate evidence includes the new scorer/UI smoke changes, then continue M8 with a live-stack crawler/indexing seed audit or another small indexing visibility improvement.

### 2026-07-02 - Run 524

- Completed: Connected the new M8 crawler audit insight fields to the indexing diagnostics quality gate. The scorer now emits `crawlInsight` per case and `summary.crawlInsightPassedCases`, requiring every normalized skipped crawler page to include `category`, `severity`, boolean `indexingBlocked`, and `userAction`. Live capture normalization now preserves those fields from `skippedPages`, `skipped`, or failed `crawlAudits`, and the indexing diagnostics comparison tool treats `crawlInsight.passed` or `crawlInsightPassedCases` drops as regressions. The default indexing fixture now pins a skipped `POLICY_BLOCK` URL with a user action.
- Verified: `node scripts\quality\indexing\evaluate-indexing-diagnostics-fixtures.test.mjs`, `node scripts\quality\indexing\capture-indexing-audit-fixtures.test.mjs`, and `node scripts\quality\indexing\compare-indexing-diagnostics-reports.test.mjs` passed. `node scripts\quality\indexing\evaluate-indexing-diagnostics-fixtures.mjs` produced `.tmp\quality\indexing-diagnostics-20260702-014412.json` with 3/3 cases passed, `crawlInsightPassedCases=3`, and crawler `crawlInsight.categories=["POLICY_BLOCK"]`.
- Remaining risk: This proves the quality tooling catches missing crawler insight fields, but there is still no focused Document workspace render smoke proving skipped URL category/action text appears in actual UI markup. The default full regression harness was not rerun after this scorer change in this run.
- Next recommended step: Add a focused Document workspace render smoke for crawler audit category/user-action visibility, then rerun the default regression harness to keep M7/M8 quality evidence current.

### 2026-07-02 - Run 523

- Completed: Started M8 with a narrow crawler/indexing visibility improvement. Added `com.learnbot.dto.crawl.CrawlAuditInsight` under a purpose-specific DTO subfolder and wired `CrawlAuditSummary` to expose derived `category`, `severity`, `indexingBlocked`, and `userAction` fields from existing crawl `reasonCode` values. The Document workspace crawl audit cards now show the derived category instead of only a status code when present and render the user action hint. `docs/quality/milestone-8-indexing-quality.md` documents the new fields for future diagnostics normalization and report comparison work.
- Verified: `npm run build` passed with only the existing Vite large-chunk warning. Restricted Maven attempts still fail because sandbox networking blocks Maven Central. Approved Maven verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CrawlAuditInsightTest,WebCrawlerTest" test` ran 10 tests with 0 failures.
- Remaining risk: The new insight fields are visible in API/UI payloads, but the M8 indexing diagnostics scorer and live capture normalization do not yet assert them, and there is no dedicated Document workspace smoke fixture proving the rendered skipped-URL reason/action lines. The change is read-only and does not alter crawler execution, indexing state transitions, allowlist, robots policy, or Local Agent mutation boundaries.
- Next recommended step: Continue M8 by feeding `category`, `severity`, `indexingBlocked`, and `userAction` into the indexing diagnostics normalization/scorer, or add a focused Document workspace render smoke that proves skipped URL reasons and recommended actions remain visible.

### 2026-07-02 - Run 522

- Completed: Closed the remaining M7 exit-audit evidence gap by running the default regression harness and updating `docs/quality/milestone-7-exit-audit.md` from pending to verified. The audit now records the successful full-harness report `.tmp\quality\regression-harness-20260702-103454.json` and marks M8 ready to start from a completed M7 quality gate.
- Verified: Direct `.\scripts\quality\run-regression-harness.ps1` was blocked by the local PowerShell execution policy. `powershell.exe -ExecutionPolicy Bypass -File .\scripts\quality\run-regression-harness.ps1` ran inside the sandbox and produced `.tmp\quality\regression-harness-20260702-103332.json`, but backend Maven dependency resolution failed because Maven Central access was blocked by sandbox networking (`Permission denied: getsockopt`). Approved rerun passed: `.tmp\quality\regression-harness-20260702-103454.json` reports 43/43 steps passed, 0 failed steps, 8/8 quality signals covered, 0 blocked signals, and `agent-loop-runner-safety=covered`. The backend-focused harness step ran 198 tests with 0 failures and 8 opt-in live PostgreSQL tests skipped.
- Remaining risk: M7 is complete as a quality/regression gate, but the overall Codex-like Local Agent goal is not complete. Release, request creation, push, claim, apply, test, rollback, final-answer publication, acknowledgement, and mutation remain closed by default; M8 still needs actual crawling/indexing quality improvements rather than only more harness evidence.
- Next recommended step: Start M8 by reviewing crawler/extractor/indexing diagnostics and frontend visibility, then make the smallest useful improvement that helps users understand URL indexing quality or skipped-page reasons while keeping the M7 harness green.

### 2026-07-02 - Run 513

- Completed: Added M7 timeline-history display for runner recommended actions. `agentLoopTimelineSummary.js` now renders `event.details.recommendedAction` with action key, label, enabled state, endpoint, and disabled request-creation/push/claim/mutation flags when timeline events already carry that audit data. The focused helper regression pins a `LOCAL_AGENT_APPROVAL_DECISION` event showing `CHECK_ENQUEUE_REFUSAL` while mutation and claim remain false.
- Verified: Focused frontend verification passed: `node src\components\code\agentLoopTimelineSummary.test.mjs`, `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, and `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`. Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 50 tests with 0 failures. `npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: Timeline history can now display recommended-action audit data, but the backend does not yet persist that model into selected timeline events at write time. The path remains display-only and does not enable release, request creation, push, claim, running transition, tool execution, write helper, apply, test, rollback restore, result intake, RAG freshness update, publication, final-answer generation/delivery, acknowledgement save, or broad mutation.
- Next recommended step: Continue M7 by either persisting recommended-action details into selected loop timeline events where next-action intent is recorded, or extracting a shared frontend formatter for runner/next-action/timeline recommended-action text to reduce duplication before broader UI wiring.

### 2026-07-02 - Run 512

- Completed: Extended the M7 runner recommended-action read model into the raw next-action surface. `CodeAgentLoopNextActionResponse` now includes a backward-compatible `recommendedAction` map, and `CodeAgentLoopPreviewService.nextAction` fills it with safe action guidance for previewing the runner step, checking enqueue refusal, reviewing release refusal, stopping/reporting, or asking the user. The model keeps `requestCreationEnabled=false`, `pushEnabled=false`, `claimEnabled=false`, and `mutationEnabled=false`. The frontend runner handoff summary now also reads `response.nextAction.recommendedAction`, so endpoint-shaped reload responses with nested next-action data can show the same safe recommendation.
- Verified: Focused frontend verification passed: `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, and `node src\features\code\agentLoopRunnerPreviewClient.test.mjs`. Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 50 tests with 0 failures. `npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: The recommendation is now present on runner preview and raw next-action responses, but it is not yet persisted into timeline event details or rendered by the timeline history helper. It still does not enable release, request creation, push, claim, running transition, tool execution, write helper, apply, test, rollback restore, result intake, RAG freshness update, publication, final-answer generation/delivery, acknowledgement save, or broad mutation.
- Next recommended step: Continue M7 by either persisting/displaying the recommended-action model in timeline history or extracting a compact shared frontend next-action summary helper, while keeping all side-effect and mutation controls closed.

### 2026-07-02 - Run 511

- Completed: Added the M7 runner recommended-action read model. `CodeAgentLoopRunnerPreviewResponse` now includes a `recommendedAction` map with schema `learnbot.code-agent.runner-recommended-action.v1`, action key, label, endpoint, enabled state, reason, and disabled request-creation/push/claim/mutation flags. `CodeAgentLoopRunnerService` computes safe next UI actions for read-only queueing, release-refusal review, enqueue-refusal checking, missing Local Agent workspace selection, stop/report, and ask-user states without enabling any new Local Agent mutation path. The Code workspace and `useCodeRagController` now prefer `recommendedAction.actionKey` for the existing runner buttons while keeping legacy fallback checks, and `agentLoopRunnerHandoffSummary` renders the recommendation as visible read-only runner guidance.
- Verified: Focused frontend verification passed: `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, `node src\features\code\agentLoopRunnerPreviewClient.test.mjs`, and `node src\features\code\agentLoopRunnerReleaseReviewClient.test.mjs`. Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 37 tests with 0 failures. `npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: The recommendation is currently attached to runner preview/review UI flow, not yet to timeline history or the raw `GET /api/code-agent/loop/next-action` response. It still does not enable release, request creation, push, claim, running transition, tool execution, write helper, apply, test, rollback restore, result intake, RAG freshness update, publication, final-answer generation/delivery, acknowledgement save, or broad mutation.
- Next recommended step: Continue M7 by carrying the same recommended-action model into the next-action/timeline inspection surface or a small shared frontend helper, so reload/history views can show the same safe next step without duplicating `runnerDecision` interpretation.

### 2026-07-02 - Run 509

- Completed: Added the M7 frontend route/client reload regression for the endpoint-shaped generic release-refusal terminal stop response. `agentLoopRunnerPreviewRouteFlow.test.mjs` now models the backend runner preview shape with nested `nextAction.handoffSummary`, calls `previewAgentLoopRunner` as a refresh, verifies stale enqueue result state is cleared, and renders `CodeWorkspace` from the refreshed response. The route markup must still show the `release refused` handoff, source request/release attempt/source event details, refusal stop boundary, and disabled request creation/enqueue/push/claim/claimable/verification/rollback/RAG/final/publication/final-answer/delivery/acknowledgement/mutation controls while `Check enqueue refusal` and `Review release refusal` remain disabled.
- Verified: Focused frontend verification passed: `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\features\code\agentLoopRunnerPreviewClient.test.mjs`, and `node src\features\code\agentLoopRunnerReleaseReviewClient.test.mjs`. `npm run build` passed. Vite reported only the existing large-chunk warning.
- Remaining risk: The generic release-refusal terminal stop is now covered through backend endpoint reload and frontend route/client reload fixtures, but M7 still lacks a compact regression-harness index/report that groups these agent-loop safety cases with other quality regressions for easier release comparison. The path still does not make the held source patch claimable, execute mutation, run verification commands, restore rollback, update RAG freshness, publish a final report, generate/deliver a final answer, or save acknowledgement.
- Next recommended step: Continue M7 by adding a compact regression harness/index entry for the agent-loop release-refusal safety cases so the backend/frontend terminal-stop coverage is discoverable as part of the broader M7 quality and safety suite.

### 2026-07-02 - Run 508

- Completed: Added the M7 backend endpoint reload regression for generic release-refusal terminal stop state. `CodeAgentLoopRunnerEndpointSmokeTest.runnerEndpointsReloadGenericReleaseRefusalAsTerminalStopWithoutQueueing` now builds a real `CodeAgentLoopPreviewService` over a mocked timeline repository containing a generic `LOCAL_AGENT_RELEASE_BOUNDARY_REFUSED` event without the creation-disabled handoff model, then verifies `GET /api/code-agent/loop/next-action`, runner preview, enqueue-read-only, and release-review endpoints preserve the terminal `STOP_WITH_REASON` / `NO_REQUEST_PREPARED` state and the `learnbot.code-agent.release-boundary-refusal-summary.v1` handoff summary.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest#runnerEndpointsReloadGenericReleaseRefusalAsTerminalStopWithoutQueueing" test` ran 1 test with 0 failures. Approved related backend regression passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerServiceTest" test` ran 36 tests with 0 failures.
- Remaining risk: This proves the backend endpoint reload path, but the frontend route/client reload fixture still uses static props rather than endpoint-shaped reload data from a refreshed runner preview. The path still does not make the held source patch claimable, execute mutation, run verification commands, restore rollback, update RAG freshness, publish a final report, generate/deliver a final answer, or save acknowledgement.
- Next recommended step: Continue M7 by adding a frontend route/client reload fixture that starts from the endpoint-shaped terminal stop response and verifies the Code workspace still renders `release refused` with stale enqueue/release controls disabled after refresh.

### 2026-07-02 - Run 507

- Completed: Exposed the Run 506 M7 post-release-refusal stop summary in the Code workspace UI/client path. `agentLoopRunnerHandoffSummary` now treats `STOP_WITH_REASON` / `learnbot.code-agent.release-boundary-refusal-summary.v1` as a `release refused` handoff, renders the source request/release attempt/source event/sequence, and shows the refusal stop boundary with boundary status/action, blocking reasons, and disabled release/request creation/push/claim/claimable/verification/rollback/RAG/final/publication/final-answer/delivery/acknowledgement/mutation flags. The route-level runner smoke now includes the terminal stop preview and verifies the stale enqueue and release-review controls are both disabled while the stop summary remains visible.
- Verified: Focused frontend verification passed: `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, and `node src\features\code\agentLoopRunnerPreviewClient.test.mjs`. `npm run build` passed. Vite reported only the existing large-chunk warning.
- Remaining risk: The stop state is now visible in the UI/client route, but there is not yet a full browser/runtime refresh smoke proving a recorded `LOCAL_AGENT_RELEASE_BOUNDARY_REFUSED` timeline is reloaded from the backend and shown as the same terminal stop after page refresh. The path still does not make the held source patch claimable, execute mutation, run verification commands, restore rollback, update RAG freshness, publish a final report, generate/deliver a final answer, or save acknowledgement.
- Next recommended step: Continue M7 by adding a narrow endpoint/UI route regression that reloads the recorded timeline/runner preview after release refusal and proves the terminal stop survives refresh without stale enqueue/release controls.

### 2026-07-02 - Run 506

- Completed: Added the next M7 backend bridge after visible release-review refusal. `CodeAgentLoopTimelineRepository.releaseBoundaryDetails` now records the next action as reporting the refused release/mutation-disabled state. `CodeAgentLoopPreviewService.fromReleaseBoundary` now returns `STOP_WITH_REASON` plus a `learnbot.code-agent.release-boundary-refusal-summary.v1` handoff summary for generic release-boundary refusal events, while preserving the existing `READY_HANDOFF_CREATION_DISABLED` path when mutation handoff preflight/blueprint/creation-disabled preconditions are present. `CodeAgentLoopRunnerService.previewNextStep` now preserves non-queue next-action handoff summaries, so a release-refusal stop can be shown without creating, enqueueing, pushing, claiming, or executing a Local Agent request.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopTimelineRepositoryTest" test` ran 41 tests with 0 failures. Approved related M7 backend regression passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 100 tests with 0 failures.
- Remaining risk: The backend now exposes a safe stop decision after generic release refusal, but the Code workspace UI/client handoff summary does not yet explicitly render the new `learnbot.code-agent.release-boundary-refusal-summary.v1` stop surface. It still does not make the held source patch claimable, execute mutation, run verification commands, restore rollback, update RAG freshness, publish a final report, generate/deliver a final answer, or save acknowledgement.
- Next recommended step: Continue M7 by exposing the `STOP_WITH_REASON` release-refusal handoff summary in the Code workspace UI/client path and adding route-level guard assertions that all claim/mutation/final-answer controls remain disabled.

### 2026-07-02 - Run 505

- Completed: Exposed the M7 release-review/refusal bridge in the Code workspace UI/client path. Added `agentLoopRunnerReleaseReviewClient` for `POST /api/code-agent/loop/runner/release-review`, added `codeAgentLoopRunnerReleaseReviewResult` state and `reviewCodeAgentLoopRunnerReleaseGate` in `useCodeRagController`, passed the result/action through `CodeWorkspace`, and added a dedicated "Review release refusal" button that is enabled only for `RELEASE_READINESS_REFRESHED_RELEASE_GATED` / `WAIT_RELEASE_GATE_READINESS_REFRESHED`. The old "Check enqueue refusal" path no longer handles the refreshed-readiness state. `agentLoopRunnerHandoffSummary` now labels release-review results as `release review` and renders the returned release boundary refusal with status, action, disabled release/request creation/push/claim/claimable/write-helper/apply/test/rollback/RAG/mutation flags, and blocking reasons.
- Verified: Focused frontend verification passed: `node src\features\code\agentLoopRunnerReleaseReviewClient.test.mjs`, `node src\features\code\agentLoopRunnerReadOnlyEnqueueClient.test.mjs`, `node src\features\code\agentLoopRunnerPreviewClient.test.mjs`, `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, and `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`. `npm run build` passed. Vite reported only the existing large-chunk warning.
- Remaining risk: This is the UI/client bridge for a disabled release-review refusal. It still does not make the held source patch claimable, execute mutation, run verification commands, restore rollback, update RAG freshness, publish a final report, generate/deliver a final answer, save acknowledgement, or produce a model-driven stop/ask-user decision after refusal.
- Next recommended step: Continue M7 by adding the next narrow loop decision after visible release-refusal review: consume the recorded `LOCAL_AGENT_RELEASE_BOUNDARY_REFUSED`/review state and produce a safe next action such as stop-with-reason or ask-user, while preserving the no-claim/no-mutation/no-publication boundary.

### 2026-07-02 - Run 504

- Completed: Added the explicit M7 backend release-review/refusal bridge from the refreshed readiness runner state. `CodeAgentLoopReleaseReviewResponse` models the runner-visible result, `CodeAgentLoopRunnerService.reviewReleaseGate` only proceeds when preview returns `WAIT_RELEASE_GATE_READINESS_REFRESHED`, extracts the refreshed handoff `sourceRequestId`, calls the existing `LocalAgentToolGatewayService.inspectPatchReleaseBoundary`, and returns `RELEASE_REVIEW_REFUSED_GATE_DISABLED` with request creation, enqueue, push, claim, mutation, final result, publication, and acknowledgement disabled. `POST /api/code-agent/loop/runner/release-review` exposes the bridge with the existing repository-space authorization, and controller/service tests cover both the allowed refreshed-readiness path and the non-readiness no-op path.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest" test` ran 29 tests with 0 failures. Approved related M7 backend regression passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 99 tests with 0 failures.
- Remaining risk: The bridge is backend-visible but not yet wired into the Code workspace UI/client as a dedicated release-review action. It still does not make the held source patch claimable, execute mutation, run verification commands, restore rollback, update RAG freshness, publish the final report, generate or deliver a final answer, or save acknowledgement.
- Next recommended step: Continue M7 by wiring the Code workspace client/UI to call `POST /api/code-agent/loop/runner/release-review` from the `WAIT_RELEASE_GATE_READINESS_REFRESHED` state and render the returned release boundary refusal while preserving all disabled claim/mutation/final-answer controls.

### 2026-07-02 - Run 503

- Completed: Exposed the M7 release-readiness-refresh state in the Code workspace runner handoff UI/client path. `agentLoopRunnerHandoffSummary` now recognizes `RELEASE_READINESS_REFRESHED_RELEASE_GATED` / `WAIT_RELEASE_GATE_READINESS_REFRESHED`, labels the badge as readiness, renders source request, release attempt, source event/sequence, ready-to-release state, readiness message, warning/check counts, failed check keys, patch release/execution gate status, final release-attempt readiness, and the disabled controls. `CodeWorkspace` now renders the release-readiness summary line, and both `CodeWorkspace` and `useCodeRagController` allow the existing read-only "Check enqueue refusal" path for the refreshed-readiness release-gated state while preserving the no-claim/no-mutation runner boundary.
- Verified: Focused frontend verification passed: `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, and `node src\features\code\agentLoopRunnerReadOnlyEnqueueClient.test.mjs`. `npm run build` passed. Vite reported only the existing large-chunk warning.
- Remaining risk: This is the UI/client visibility bridge for the backend release-readiness refresh state. It still does not create an explicit release-review/refusal event after the refreshed readiness, make the held source patch claimable, execute mutation, run verification commands, restore rollback, update RAG freshness, publish the final report, generate/deliver a final answer, or save acknowledgement.
- Next recommended step: Continue M7 by adding an explicit backend release-review/refusal bridge from `RELEASE_READINESS_REFRESHED_RELEASE_GATED`, recording a user-visible release review/refusal result while keeping source patch claim, mutation, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, delivery, and acknowledgement disabled.

### 2026-07-02 - Run 502

- Completed: Added the M7 backend bridge that performs a read-only release-readiness refresh after fresh release-attempt evidence completes. `LocalAgentToolGatewayService.complete` now calls `inspectPatchExecutionReadiness` for the held source patch after `LOCAL_AGENT_RELEASE_FRESH_OBSERVATIONS_COMPLETE` is recorded, then appends `LOCAL_AGENT_RELEASE_READINESS_REFRESHED` through `CodeAgentLoopTimelineRepository`. The new event records source request/release attempt/session/agent/workspace ids, readiness message, warning/check counts, failed check keys, patch release/execution gate status, final release-attempt readiness, and disabled release/claim/claimable/mutation/verification/rollback/RAG/final-answer/delivery/acknowledgement flags. `CodeAgentLoopPreviewService.nextAction` reads the event as `RELEASE_READINESS_REFRESHED_RELEASE_GATED`, and `CodeAgentLoopRunnerService.previewNextStep` preserves it as `WAIT_RELEASE_GATE_READINESS_REFRESHED` with no candidate, no enqueue, no source patch claim, and no mutation.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopTimelineRepositoryTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerServiceTest,LocalAgentToolGatewayServiceTest" test` ran 83 tests with 0 failures. Approved related M7 backend regression suite passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 116 tests with 0 failures.
- Remaining risk: The backend now records the refreshed release-readiness state, but the Code workspace UI/client handoff summary does not yet render this new `RELEASE_READINESS_REFRESHED_RELEASE_GATED` / `WAIT_RELEASE_GATE_READINESS_REFRESHED` state. Release review remains audit-only; source patch claim, mutation execution, verification command execution, rollback restore, RAG freshness update, final report publication, final-answer generation, delivery, and acknowledgement are still incomplete and intentionally gated.
- Next recommended step: Continue M7 by exposing `RELEASE_READINESS_REFRESHED_RELEASE_GATED` / `WAIT_RELEASE_GATE_READINESS_REFRESHED` in the Code workspace runner handoff UI/client path, then add the next explicit release-review/refusal bridge while keeping source patch claim and mutation disabled.

### 2026-07-02 - Run 501

- Completed: Exposed the M7 fresh-evidence-complete release-gated state in the Code workspace runner handoff UI/client path. `agentLoopRunnerHandoffSummary` now recognizes `FRESH_EVIDENCE_COMPLETE_RELEASE_GATED` / `WAIT_RELEASE_GATE_FRESH_EVIDENCE_COMPLETE`, labels the badge as fresh evidence, renders source request, release attempt, source event/sequence, evidence completeness/status, required/linked/missing/source-only/blocking counts, linked keys, and queued-observation details when present. `CodeWorkspace` now renders the fresh-observation summary line, and both `CodeWorkspace` and `useCodeRagController` allow the existing read-only "Check enqueue refusal" path for `WAIT_FOR_FRESH_OBSERVATION_RESULTS` and `FRESH_EVIDENCE_COMPLETE_RELEASE_GATED` while preserving the no-claim/no-mutation runner boundary.
- Verified: Focused frontend verification passed: `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, and `node src\features\code\agentLoopRunnerReadOnlyEnqueueClient.test.mjs`. `npm run build` passed. Vite reported only the existing large-chunk warning.
- Remaining risk: This is the UI/client visibility bridge for the completed fresh-evidence backend state. It still does not refresh release readiness from that state, make the held source patch claimable, execute mutation, run verification commands, restore rollback, update RAG freshness, publish the final report, generate/deliver a final answer, or save acknowledgement.
- Next recommended step: Continue M7 by adding the backend bridge that performs a read-only release-readiness refresh from `FRESH_EVIDENCE_COMPLETE_RELEASE_GATED`, records the readiness result in the loop timeline, and keeps source patch claim, mutation, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, delivery, and acknowledgement disabled.

### 2026-07-02 - Run 500

- Completed: Added the M7 read-only bridge that distinguishes queued fresh observations from completed fresh evidence while keeping release and mutation gated. `CodeAgentLoopTimelineRepository` now records `LOCAL_AGENT_RELEASE_FRESH_OBSERVATIONS_COMPLETE` with the source request id, release attempt id, evidence completeness/status, linked/missing/source-only counts, and disabled release/claim/mutation/verification/rollback/RAG/final-answer/delivery/acknowledgement flags. `LocalAgentToolGatewayService.complete` now checks linked release-attempt repository verification and patch dry-run output after a fresh-observation response completes, then appends the completion event only when both required observations are `RELEASE_ATTEMPT_LINKED`. `CodeAgentLoopPreviewService.nextAction` reads that event as `FRESH_EVIDENCE_COMPLETE_RELEASE_GATED`, and `CodeAgentLoopRunnerService.previewNextStep` preserves it as `WAIT_RELEASE_GATE_FRESH_EVIDENCE_COMPLETE` without preparing a candidate, enqueueing more work, making the source patch claimable, or enabling mutation.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopTimelineRepositoryTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerServiceTest,LocalAgentToolGatewayServiceTest" test` ran 80 tests with 0 failures. Approved related M7 backend regression suite passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 113 tests with 0 failures. Frontend helper smoke `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs` also passed.
- Remaining risk: The backend/runner state now distinguishes fresh-evidence-complete, but the Code workspace UI does not yet render this new complete-state summary as explicitly as the prior release-gate handoff. The bridge is still read-only; release-readiness refresh, source patch claim, mutation, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, delivery, and acknowledgement remain incomplete.
- Next recommended step: Continue M7 by exposing `FRESH_EVIDENCE_COMPLETE_RELEASE_GATED` / `WAIT_RELEASE_GATE_FRESH_EVIDENCE_COMPLETE` in the Code workspace runner handoff UI/client path, or add the next backend bridge that performs a read-only release-readiness refresh from this state while keeping claim and mutation disabled.

### 2026-07-02 - Run 499

- Completed: Added the M7 backend bridge that records and reads release-gate fresh-observation enqueue results in loop runner state without opening claimable mutation work. `CodeAgentLoopTimelineRepository` now appends `LOCAL_AGENT_RELEASE_FRESH_OBSERVATIONS_ENQUEUED` with the approved-held source request id, release attempt id, queued request ids, queued tool names, queued approval states, and disabled release/claim/mutation/verification/rollback/RAG/final-answer/delivery/acknowledgement flags. `LocalAgentToolGatewayService.enqueueReleaseAttemptFreshObservations` records that event after the linked `git.status` and `patch.apply` dry-run requests are created and pushed, but still leaves the source patch held and non-claimable. `CodeAgentLoopPreviewService.nextAction` reads the new event as `WAIT_FOR_FRESH_OBSERVATION_RESULTS`, and `CodeAgentLoopRunnerService.previewNextStep` preserves it as `WAIT_RELEASE_GATE_FRESH_OBSERVATION_RESULTS` with no candidate and no auto-enqueue.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopTimelineRepositoryTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerServiceTest,LocalAgentToolGatewayServiceTest" test` ran 76 tests with 0 failures. Approved related M7 backend regression suite passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 109 tests with 0 failures. Frontend helper smoke `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs` also passed.
- Remaining risk: The loop now records and reads fresh-observation enqueue state, but it still does not aggregate the two completed fresh-observation results into a release-readiness transition. A completed single fresh observation can still produce the generic observation next-decision path until a dedicated release-attempt evidence-completeness bridge is added. Claimable release, mutation execution, verification command execution, rollback restore, RAG freshness update, final report publication, final-answer generation, delivery, and acknowledgement remain incomplete.
- Next recommended step: Continue M7 by connecting completed fresh-observation results back into release readiness/runner state: when both linked `git.status` and patch dry-run observations for the release attempt are complete, expose a read-only “fresh evidence complete but release still gated” next-action/handoff. Keep source patch claim, mutation, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, delivery, and acknowledgement disabled.

### 2026-07-02 - Run 498

- Completed: Exposed the new M7 release-gate fresh-observation handoff in the Code workspace frontend path. `agentLoopRunnerHandoffSummary` now recognizes `WAIT_FOR_RELEASE_GATE` / `WAIT_RELEASE_GATE_FRESH_OBSERVATIONS`, labels it as a release-gate handoff, renders the approved-held source request id, source event/sequence, approval/held/release-required fields, and the readiness, fresh-observations, and release-boundary routes. The disabled-control summary now also covers runner auto-enqueue, fresh-observation auto-enqueue, source patch request creation, source patch push/claim, verification command execution, rollback restore, RAG freshness update, final-answer generation, and delivery when those fields are present. `CodeWorkspace` and `useCodeRagController` now allow the existing disabled runner handoff check for release-gate states as well as creation-disabled states, while still routing through the no-claim/no-mutation runner boundary.
- Verified: Focused frontend verification passed: `node src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, and `node src\features\code\agentLoopRunnerReadOnlyEnqueueClient.test.mjs`. `npm run build` passed. Vite reported only the existing large-chunk warning.
- Remaining risk: This is a frontend/client bridge over the backend handoff, not the next backend orchestration step. The runner still does not automatically enqueue release-attempt fresh observations, store those queued observation ids in loop runner state, make the held patch claimable, execute verification commands, restore rollback, update RAG freshness, publish final reports, deliver final answers, or save acknowledgements.
- Next recommended step: Continue M7 by adding the backend bridge that records/reads fresh-observation enqueue results from the release-gate handoff into loop runner state without opening source patch claim, mutation, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, delivery, or acknowledgement.

### 2026-07-02 - Run 497

- Completed: Connected the approved-held `WAIT_FOR_RELEASE_GATE` state to the existing release-attempt fresh-observation/readiness path at the runner layer without auto-executing it. `CodeAgentLoopRunnerService.previewNextStep` now returns `WAIT_RELEASE_GATE_FRESH_OBSERVATIONS` for `WAIT_FOR_RELEASE_GATE` and carries a `learnbot.code-agent.release-gate-fresh-observation-handoff.v1` summary with the source patch request id, current approval status, `GET /api/local-agents/tools/{requestId}/readiness`, `POST /api/local-agents/tools/{requestId}/fresh-observations`, and `POST /api/local-agents/tools/{requestId}/release`. The summary explicitly keeps runner auto-enqueue, fresh-observation auto-enqueue, source patch request creation, push, claim, mutation, verification command execution, rollback restore, RAG freshness update, final result, publication, and acknowledgement disabled. `enqueueReadOnlyNextStep` preserves that handoff but does not queue a read-only runner request from release-gate state.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 73 tests with 0 failures. Approved related M7 backend regression suite passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 106 tests with 0 failures.
- Remaining risk: This is still a backend/MockMvc proof, not a live PostgreSQL/Ollama/Local Agent stack. The runner now points approved-held patch work at the correct release-attempt routes, but the Code workspace may not yet expose the new handoff cleanly, and the loop still does not automatically record fresh-observation enqueue results, make the source patch claimable, run verification commands, restore rollback, update RAG freshness, publish final reports, deliver final answers, or save acknowledgements.
- Next recommended step: Continue M7 by exposing the `WAIT_RELEASE_GATE_FRESH_OBSERVATIONS` handoff in the frontend runner/readiness UI, or by adding a backend runner bridge that consumes the existing fresh-observations enqueue result and shows the queued release-attempt observation ids in loop state while mutation remains gated.

### 2026-07-02 - Run 496

- Completed: Closed the next M7 loop-state gap after user approval. `CodeAgentLoopTimelineRepository.appendApprovalDecision` now records `APPROVAL_APPROVED_HELD` details for `APPROVED`/`APPROVED_HELD` patch requests, including the next release/fresh-observation action hint and disabled release/request-creation/push/claim/mutation/final-result/publication/acknowledgement flags. `CodeAgentLoopPreviewService.nextAction` now interprets the latest approved-held `LOCAL_AGENT_APPROVAL_DECISION` as `WAIT_FOR_RELEASE_GATE`, so the loop moves from approval waiting into release-readiness/fresh-observation handoff without opening claimable mutation work.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. The first approved focused run caught a test fixture compile issue (`Map.of` too many entries), which was fixed with `Map.ofEntries`. Approved focused backend verification then passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 69 tests with 0 failures. Approved related M7 backend regression suite passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 104 tests with 0 failures.
- Remaining risk: This is still a backend/MockMvc proof, not a live PostgreSQL/Ollama/Local Agent stack. The loop now points approved-held patch work at release readiness, but it does not yet automatically enqueue release-attempt fresh observations from `next-action`, make the held patch claimable, run verification commands, restore rollback, update RAG freshness, publish final reports, deliver final answers, or save acknowledgements.
- Next recommended step: Continue M7 by connecting the `WAIT_FOR_RELEASE_GATE` state to the existing release-attempt fresh-observation enqueue/readiness path with exact disabled-gate evidence, while keeping release, claim, mutation, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, delivery, and acknowledgement gated.

### 2026-07-02 - Run 495

- Completed: Closed a real M7 loop-state gap after `patch.apply` approval request creation. `LocalAgentToolGatewayService.createApprovalRequest` now records a durable loop timeline event through `CodeAgentLoopTimelineRepository.appendApprovalRequestCreated` whenever the request input carries repository/loop identity. `CodeAgentLoopPreviewService.nextAction` now treats the latest `LOCAL_AGENT_APPROVAL_REQUEST_CREATED` event as `WAIT_FOR_APPROVAL`, so the runner does not accidentally re-enter the previous read-only queue decision while a side-effectful approval request is awaiting the user. Endpoint smoke coverage now verifies both normal and validated patch approval request paths append the approval-request-created event before approval/release remains held.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 68 tests with 0 failures. Approved related M7 backend regression suite passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest" test` ran 103 tests with 0 failures.
- Remaining risk: This is still a backend/MockMvc proof, not a live PostgreSQL/Ollama/Local Agent stack. Approval request creation is now timeline-visible, but the approved-held patch still does not become claimable, and verification command execution, rollback restore, result intake, RAG freshness update, final report publication, final-answer generation, delivery, acknowledgement, and mutation execution remain incomplete.
- Next recommended step: Continue M7 by carrying the approved-held patch state into the release-boundary/fresh-observation bridge with exact disabled-gate evidence, or run the opt-in live PostgreSQL/local-agent proof as a checkpoint before going deeper. Do not enter M8 until the M7 approval/release bridge and at least one local-agent-backed loop path are sufficiently closed.

### 2026-07-02 - Run 494

- Completed: Verified the Run 493 M7 side-effect release-refusal timeline readback smoke. The strengthened `CodeAgentLoopRunnerEndpointSmokeTest.validatedPatchApprovalEndpointCanQueueFreshObservationsWithoutReleasingSourcePatch` now compiles and runs with its new `GET /api/code-agent/loop/timelines` assertions, proving the modeled release-boundary refusal is visible as `LOCAL_AGENT_RELEASE_BOUNDARY_REFUSED` for `patch.apply` with `mayMutate=false`, disabled release/request-creation/push/claim/mutation flags, disabled mutation request blueprint and creation gate, and zero durable/claimable mutation rows before the next-action endpoint returns `READY_HANDOFF_CREATION_DISABLED`.
- Verified: Restricted Maven still fails at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` ran 6 tests with 0 failures. Approved related M7 backend regression suite passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest" test` ran 93 tests with 0 failures.
- Remaining risk: This is still a controlled MockMvc/backend proof, not a live PostgreSQL/Ollama/Local Agent stack. Side-effectful `patch.apply` remains approval/release modeled only; no verification command execution, rollback restore, RAG freshness update, final report publication, final-answer generation, delivery, acknowledgement, or mutation execution is enabled.
- Next recommended step: Continue M7 either by adding/running the live PostgreSQL/local-agent quality proof for the selected read-only and release-refusal paths, or by moving one step deeper into the side-effectful `patch.apply` approval/release bridge with exact disabled-gate evidence. Keep verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, acknowledgement save, and mutation gated.

### 2026-07-02 - Run 493

- Completed: Started the M7 side-effectful `patch.apply` approval/release bridge after the selected read-only runner proof. Strengthened `CodeAgentLoopRunnerEndpointSmokeTest.validatedPatchApprovalEndpointCanQueueFreshObservationsWithoutReleasingSourcePatch` so the same validated patch approval -> approved-held -> fresh release-attempt observations -> release-boundary refusal flow also checks the user-facing timeline readback. After the mocked release boundary is appended, the test now calls `GET /api/code-agent/loop/timelines?repositoryId=...&limit=20` and pins the visible `LOCAL_AGENT_RELEASE_BOUNDARY_REFUSED` event with `patch.apply`, `requiresApproval=true`, `mayMutate=false`, disabled release/request-creation/push/claim/mutation flags, the disabled mutation request blueprint status `REFUSED_REQUEST_CREATION_DISABLED`, mutation request creation gate status `REFUSED_CREATION_DISABLED`, and zero durable/claimable mutation execution rows before the existing `GET /api/code-agent/loop/next-action` `READY_HANDOFF_CREATION_DISABLED` assertions.
- Verified: Restricted Maven still failed at dependency resolution because sandbox networking blocks Maven Central: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` failed with `Permission denied: getsockopt`. The approved Maven rerun could not execute because the approval review rejected it due to the current usage limit. No passing backend test result was produced in this run.
- Remaining risk: The code change is a test-only smoke strengthening and should not affect runtime behavior, but it is not compiled or executed yet. The added JSONPath assertions may still need adjustment once Maven can run. Product risk remains: release is still disabled, no claimable side-effectful mutation row is created, verification command execution, rollback restore, RAG freshness update, final report publication, final-answer generation, delivery, and acknowledgement remain incomplete.
- Next recommended step: Rerun `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` as soon as backend Maven execution is available, then rerun the related M7 suite. If it passes, continue either with the opt-in live PostgreSQL/local-agent quality proof or the next side-effectful `patch.apply` approval/release bridge slice while keeping mutation gated.

### 2026-07-02 - Run 492

- Completed: Strengthened the M7 selected read-only runner backend proof at the HTTP boundary. `CodeAgentLoopRunnerEndpointSmokeTest.enqueueSelectedReadOnlyEndpointCanQueueClaimCompleteAndRecordTimeline` still drives `POST /api/code-agent/loop/runner/enqueue-selected-read-only` through the real runner/tool-selection/gateway stack with a mocked model-selected `git.status`, captures the queued request, simulates Local Agent claim and successful completion, and verifies timeline `appendObservationResult` plus `appendNextDecision`. It now also exercises the user-facing readback path used by the Code workspace: `GET /api/local-agents/tools/{requestId}` returns the completed `SUCCEEDED` `USER_LOCAL_AGENT` `git.status` execution with `repositoryId`, `loopId`, `freshObservationOnly=true`, `mutationAllowed=false`, and repository verification `MATCH`; `GET /api/code-agent/loop/timelines?repositoryId=...&limit=5` returns `LOCAL_AGENT_OBSERVATION_RESULT` followed by `LOOP_NEXT_DECISION_RECORDED` with next-decision request creation, push, claim, and mutation disabled.
- Verified: Restricted Maven failed at dependency resolution because sandbox networking blocks Maven Central: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` failed with `Permission denied: getsockopt`. Approved focused backend verification passed with the same command: `CodeAgentLoopRunnerEndpointSmokeTest` ran 6 tests with 0 failures. Approved related M7 regression suite passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest,LocalAgentToolGatewayServiceTest" test` ran 85 tests with 0 failures.
- Remaining risk: This is stronger than the previous frontend mock and endpoint-only append proof, but it still uses mocked repositories/services rather than a live PostgreSQL stack plus real Local Agent process. The selected read-only loop path is now covered through the HTTP surfaces the UI uses, but a true durable/live quality run is still needed before treating it as production-like. Side-effectful `patch.apply`, verification command execution, rollback restore, RAG freshness update, final report publication, final-answer generation, delivery, and acknowledgement remain incomplete.
- Next recommended step: Either run/add the opt-in live PostgreSQL/local-agent quality proof for the selected read-only runner path, or proceed to the next M7 side-effectful bridge: model the `patch.apply` approval/release handoff behind disabled gates, with exact evidence and refusal behavior, while keeping verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, acknowledgement save, and mutation gated.

### 2026-07-02 - Run 491

- Completed: Added the M7 UI refresh bridge after a selected read-only runner enqueue. New `frontend/src/features/code/runner/agentLoopRunnerQueuedObservationClient.js` fetches only `GET /api/local-agents/tools/{requestId}` for the queued runner request, clears stale observation state on missing ids or failed `run`, and keeps the helper under the runner-specific frontend folder. `useCodeRagController` now stores `codeAgentLoopRunnerQueuedObservationResult`, clears it with stale runner state, exposes `refreshCodeAgentLoopRunnerQueuedObservation`, and refreshes loop timelines after a successful queued-observation fetch so completed Local Agent observations and follow-up next decisions can appear in the existing loop history panel. `CodeWorkspace.jsx` now renders a "Refresh read-only observation" action for queued runner requests and displays the refreshed execution status/tool/target/approval/mutation/fresh-observation/repository-verification summary without adding claim, mutation, publication, final-answer, or acknowledgement controls.
- Verified: `node frontend\src\features\code\runner\agentLoopRunnerQueuedObservationClient.test.mjs`, `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, and `cd frontend; node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` passed, including the route-level proof that the refresh calls `/api/local-agents/tools/{requestId}`, then refreshes loop timelines, and renders the completed `SUCCEEDED`/`USER_LOCAL_AGENT`/`git.status` observation with `mutationAllowed=false` and repository verification `MATCH`. Existing runner/readiness regressions still passed: `node frontend\src\features\code\runner\agentLoopRunnerSelectedReadOnlyClient.test.mjs`, `node frontend\src\features\code\agentLoopRunnerPreviewClient.test.mjs`, `node frontend\src\features\code\agentLoopRunnerReadOnlyEnqueueClient.test.mjs`, `cd frontend; node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, `cd frontend; node src\components\code\codeWorkspaceReadinessSmokeHarness.test.mjs`, and `cd frontend; node src\components\code\codeWorkspaceReadinessPanelSmoke.test.mjs`. `cd frontend; npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: This is still a frontend/client and SSR-route proof over mocked queued-observation and timeline refresh responses. It closes the UI gap after selected read-only enqueue, but it does not itself prove a live Local Agent claimed/completed the durable request or that PostgreSQL contains the resulting `LOCAL_AGENT_OBSERVATION_RESULT` and `LOOP_NEXT_DECISION_RECORDED` events. Side-effectful `patch.apply`, verification command execution, rollback restore, RAG freshness update, final report publication, final-answer generation, delivery, and acknowledgement remain incomplete.
- Next recommended step: Add the stronger M7 backend/live proof around preview -> model-selected read-only enqueue -> durable Local Agent queue/claim/complete -> loop timeline `LOCAL_AGENT_OBSERVATION_RESULT` and `LOOP_NEXT_DECISION_RECORDED`, preferably through the existing quality harness or a focused endpoint smoke that exercises the durable queue and timeline repository. Keep side-effectful patch/test/rollback release, RAG freshness update, final publication, final-answer generation, acknowledgement save, and mutation gated.

### 2026-07-02 - Run 490

- Completed: Wired the existing M7 model-selected read-only runner endpoint into the Code workspace client path without enabling mutation. Added `frontend/src/features/code/runner/agentLoopRunnerSelectedReadOnlyClient.js` under a runner-specific subfolder, plus a focused client regression that posts only to `POST /api/code-agent/loop/runner/enqueue-selected-read-only`, stores the response, clears stale enqueue state on missing ids or failed `run`, and pins `git.status`, `NOT_REQUIRED`, `mutationAllowed=false`, `requestCreationEnabled=true`, `enqueueEnabled=true`, `pushEnabled=true`, and disabled claim/final-result/publication/acknowledgement/mutation flags. `useCodeRagController`, `App.jsx`, and `CodeWorkspace.jsx` now expose a gated `enqueueCodeAgentLoopRunnerSelectedReadOnly` action and a "Queue read-only step" control that is enabled only when the runner preview prepared exactly a `git.status`/`NOT_REQUIRED`/`mutationAllowed=false` candidate. `agentLoopRunnerHandoffSummary.js` now also renders queued read-only runner results that do not carry a creation-disabled handoff summary.
- Verified: `node frontend\src\features\code\runner\agentLoopRunnerSelectedReadOnlyClient.test.mjs`, `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, and `cd frontend; node src\components\code\agentLoopRunnerSelectedReadOnlyRouteFlow.test.mjs` passed. Existing runner/readiness regressions still passed: `node frontend\src\features\code\agentLoopRunnerPreviewClient.test.mjs`, `node frontend\src\features\code\agentLoopRunnerReadOnlyEnqueueClient.test.mjs`, `cd frontend; node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, `cd frontend; node src\components\code\codeWorkspaceReadinessSmokeHarness.test.mjs`, and `cd frontend; node src\components\code\codeWorkspaceReadinessPanelSmoke.test.mjs`. `cd frontend; npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: This is a frontend/client plus SSR-route proof over mocked responses. It proves the web UI can call the already-covered backend endpoint and display the queued read-only result, but it is not a live stack proof that the queued request is claimed/completed by a Local Agent and reflected back into timeline observation and next-decision state. Side-effectful `patch.apply`, verification command execution, rollback restore, RAG freshness update, final report publication, final-answer generation, delivery, and acknowledgement remain incomplete.
- Next recommended step: Add the next M7 live/backend proof around preview -> model-selected read-only enqueue -> durable Local Agent queue/claim/complete -> loop timeline `LOCAL_AGENT_OBSERVATION_RESULT` and `LOOP_NEXT_DECISION_RECORDED`, or wire a narrow UI refresh path that loads the completed read-only observation after Local Agent completion. Keep side-effectful patch/test/rollback release, RAG freshness update, final publication, final-answer generation, acknowledgement save, and mutation gated.

### 2026-07-02 - Run 489

- Completed: Finished the M7 disabled enqueue-read-only client path without enabling mutation. Added `frontend/src/features/code/agentLoopRunnerReadOnlyEnqueueClient.js` and its focused regression so the client posts only to `POST /api/code-agent/loop/runner/enqueue-read-only`, stores the returned refusal, clears stale enqueue state on missing ids or failed `run`, and preserves `NOT_ENQUEUED`, `queuedRequest=null`, and disabled request creation/enqueue/push/claim/mutation flags. `useCodeRagController`, `App.jsx`, and `CodeWorkspace.jsx` now expose a gated `enqueueCodeAgentLoopRunnerReadOnly` action and render a "Check enqueue refusal" control that is disabled until a loop preview exists and the runner preview action is `READY_HANDOFF_CREATION_DISABLED`.
- Verified: `node frontend\src\features\code\agentLoopRunnerReadOnlyEnqueueClient.test.mjs`, `node frontend\src\features\code\agentLoopRunnerPreviewClient.test.mjs`, and `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs` passed. From `frontend`, `node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs`, `node src\components\code\codeWorkspaceReadinessSmokeHarness.test.mjs`, and `node src\components\code\codeWorkspaceReadinessPanelSmoke.test.mjs` passed. `cd frontend; npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: This is still a frontend/client and SSR-route proof over mocked runner responses. It proves the disabled enqueue-refusal result reaches the UI without opening side effects, but it is not a live backend/Local Agent browser click or production stack smoke. Model-selected read-only runner orchestration, side-effectful patch/test/rollback release, RAG freshness update, final report publication, final-answer generation, delivery, and acknowledgement remain incomplete.
- Next recommended step: Continue M7 with the next safe runner step: either wire a model-selected read-only runner client/server path beyond the creation-disabled handoff case, or run a stronger live runner proof that exercises preview/enqueue-read-only against the backend and durable Local Agent queue. Keep `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, acknowledgement save, and mutation gated.

### 2026-07-02 - Run 488

- Completed: Added the M7 route/client smoke for the real Code workspace runner-preview path without enabling execution. New `frontend/src/components/code/agentLoopRunnerPreviewRouteFlow.test.mjs` renders `CodeWorkspace` with a loop preview and connected Local Agent identity, verifies the preview-only "Preview runner state" control is visible and not disabled, simulates the same route callback through `agentLoopRunnerPreviewClient`, asserts the only HTTP call is `POST /api/code-agent/loop/runner/preview`, asserts `enqueue-read-only` is never called, clears stale enqueue state, and rerenders the route with a mocked `READY_HANDOFF_CREATION_DISABLED` response. The rerendered markup must show the creation-disabled handoff schema/status, `WAIT_CREATION_GATE_DISABLED`, expected request count 4, durable mutation rows 0, persisted/pushed/claimable 0, and disabled request creation/enqueue/push/claim/final-result/publication/acknowledgement/mutation flags.
- Verified: `cd frontend; node src\components\code\agentLoopRunnerPreviewRouteFlow.test.mjs` passed. Related regressions passed: `node frontend\src\features\code\agentLoopRunnerPreviewClient.test.mjs`, `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node frontend\src\components\code\codeWorkspaceReadinessSmokeHarness.test.mjs`, and `cd frontend; node src\components\code\codeWorkspaceReadinessPanelSmoke.test.mjs`. `cd frontend; npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: This is a route/client smoke using SSR plus a mocked callback, not a full browser DOM click with jsdom or Playwright. The real UI runner preview is now covered through the same route props and client helper, but the product still does not create, push, claim, execute, refresh RAG, aggregate, publish, generate final answers, or save acknowledgement for mutation work.
- Next recommended step: Add the next M7 client/server slice for a still-disabled `enqueue-read-only` result flow: keep the UI explicit that enqueue is disabled/refused for creation-disabled handoff states, store `codeAgentLoopRunnerEnqueueResult` only when a safe endpoint response is returned, and prove it still does not enable `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, acknowledgement save, or mutation. Alternatively, move to a model-selected read-only runner client path if enqueue remains intentionally backend-only for now.

### 2026-07-02 - Run 487

- Completed: Wired the M7 creation-disabled runner refusal into the real Code workspace client flow without enabling execution. Added `frontend/src/features/code/agentLoopRunnerPreviewClient.js`, which calls only `POST /api/code-agent/loop/runner/preview`, stores the preview response, clears stale enqueue results, and clears stale preview/enqueue state on missing ids or failed calls. `useCodeRagController` now keeps `codeAgentLoopRunnerPreview` and `codeAgentLoopRunnerEnqueueResult`, fetches runner preview after a successful loop preview, exposes `previewCodeAgentLoopRunner`, and clears stale runner summaries when repository/loop state changes. `App.jsx` passes the state/actions into `CodeWorkspace`, and `CodeWorkspace` adds a preview-only "Preview runner state" control that calls the runner preview endpoint without adding enqueue, create, push, claim, execution, or mutation paths.
- Verified: `node frontend\src\features\code\agentLoopRunnerPreviewClient.test.mjs` passed and proves the client posts to `/api/code-agent/loop/runner/preview`, never calls `enqueue-read-only`, preserves disabled creation/enqueue/push/claim/mutation flags, and clears stale state on missing ids or failed calls. `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs`, `node frontend\src\components\code\codeWorkspaceReadinessSmokeHarness.test.mjs`, and `cd frontend; node src\components\code\codeWorkspaceReadinessPanelSmoke.test.mjs` passed. `cd frontend; npm run build` passed with only the existing Vite large-chunk warning.
- Remaining risk: This proves the client helper, hook state, App prop pass-through, and Code workspace render path compile and have focused regressions, but it is not yet a browser-level click/API mock smoke against the actual "Preview runner state" button. The product still does not create, push, claim, execute, refresh RAG, aggregate, publish, generate final answers, or save acknowledgement for mutation work.
- Next recommended step: Add a narrow browser/API mock smoke or route-level interaction test for the actual "Preview runner state" control: seed a loop preview id and Local Agent identity, click the preview-only control, mock `READY_HANDOFF_CREATION_DISABLED`, and assert the creation-disabled handoff summary appears while no enqueue/read-only, mutation, `patch.apply`, verification command, rollback restore, RAG freshness, final publication, final-answer, or acknowledgement control is enabled. If interaction tooling is deferred, the next server/client slice should be a still-disabled enqueue-read-only result preview that preserves the same no-mutation guarantees.

### 2026-07-02 - Run 486

- Completed: Added the frontend/UI M7 rendering contract for the creation-disabled runner refusal without adding any execution control. New `frontend/src/components/code/agentLoopRunnerHandoffSummary.js` turns backend runner preview/enqueue responses into display text for status/schema, runner decision, source boundary status, expected request count, durable mutation row count, persisted/pushed/claimable zero counts, disabled creation/enqueue/push/claim/final-result/publication/acknowledgement/mutation flags, nested preview state, and message. `CodeWorkspace.jsx` now accepts `codeAgentLoopRunnerPreview` and `codeAgentLoopRunnerEnqueueResult`, renders the handoff summary as read-only UI, and does not add buttons or tool execution paths. The readiness smoke harness now includes a `NOT_ENQUEUED` creation-disabled runner response, and route-level SSR assertions prove the summary appears in actual Code workspace markup while the recursive disabled-flag guard still passes.
- Verified: `node frontend\src\components\code\agentLoopRunnerHandoffSummary.test.mjs` passed. `node frontend\src\components\code\codeWorkspaceReadinessSmokeHarness.test.mjs` passed. Running the SSR smoke from the repo root failed because Vite resolves `/src/...` from the frontend root; rerunning from `frontend` with `node src\components\code\codeWorkspaceReadinessPanelSmoke.test.mjs` passed. `npm run build` in `frontend` passed, with only the existing Vite large-chunk warning.
- Remaining risk: This proves the Code workspace can render the runner handoff response if it is supplied as props, but `useCodeRagController` does not yet call the runner preview/enqueue endpoint or store the response from the backend. The product still does not create, push, claim, execute, refresh RAG, aggregate, publish, generate final answers, or save acknowledgement for mutation work.
- Next recommended step: Add the smallest client-flow slice: `useCodeRagController` should call `POST /api/code-agent/loop/runner/preview` for the selected repository/loop/agent/workspace, store `codeAgentLoopRunnerPreview`, expose it to `CodeWorkspace`, and include a lightweight API-client/hook regression that confirms `READY_HANDOFF_CREATION_DISABLED` remains display-only with no enqueue or mutation controls enabled.

### 2026-07-02 - Run 485

- Completed: Added endpoint-level M7 smoke coverage for the creation-disabled runner refusal. `CodeAgentLoopRunnerEndpointSmokeTest.runnerEndpointsExposeCreationDisabledHandoffSummaryWithoutQueueing` now calls `POST /api/code-agent/loop/runner/preview` and `POST /api/code-agent/loop/runner/enqueue-read-only` with a mocked `READY_HANDOFF_CREATION_DISABLED` next-action. The preview response now has pinned JSON for `WAIT_CREATION_GATE_DISABLED`, disabled creation/enqueue/push/claim/mutation/final-result/publication/acknowledgement flags, and the creation-disabled handoff summary. The enqueue response is pinned to `NOT_ENQUEUED` while preserving the same summary at the top level and inside `preview`. The test verifies no repository request creation, Local Agent push, claim, approved-held patch release, or mutation-input release is called.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest" test` failed with `Permission denied: getsockopt`. Approved focused verification passed with the same command: 35 tests, 0 failures. Approved broader M7 loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 119 tests with 0 failures and 8 skipped opt-in live PostgreSQL tests.
- Remaining risk: The backend HTTP contract is now pinned, but the Code workspace/client layer still does not have a dedicated regression proving this runner handoff summary is visible to users without enabling blocked controls. The product still does not create, push, claim, execute, refresh RAG, aggregate, publish, generate final answers, or save acknowledgement for mutation work.
- Next recommended step: Add a frontend/client contract or Code workspace render smoke for the creation-disabled runner response: consume the runner preview/enqueue JSON, display expected request count, durable zero-row count, persisted/pushed/claimable zero counts, and disabled creation/push/claim/mutation flags, while verifying no UI control enables enqueue, request creation, `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, acknowledgement save, or mutation.

### 2026-07-02 - Run 484

- Completed: Wired the M7 creation-disabled handoff summary into the runner refusal path without opening execution. `CodeAgentLoopRunnerPreviewResponse` and `CodeAgentLoopRunnerEnqueueResponse` now carry `handoffSummary` while keeping compatibility constructors for existing callers. `CodeAgentLoopRunnerService.previewNextStep` returns typed runner decision `WAIT_CREATION_GATE_DISABLED` when next-action is `READY_HANDOFF_CREATION_DISABLED`, preserving expected request count, durable mutation row count, persisted/pushed/claimable zero counts, and disabled creation/push/claim/mutation flags. `enqueueReadOnlyNextStep` still returns `NOT_ENQUEUED` for that state, but now preserves the same summary through the enqueue response and never calls `enqueueReadOnly`.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused verification passed after fixing a `Map.of` test helper compile issue: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest" test` ran 29 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 118 tests with 0 failures and 8 skipped opt-in live tests.
- Remaining risk: The service-level runner refusal now preserves the handoff context, but the HTTP runner preview/enqueue endpoint smoke has not yet pinned the JSON shape for that refusal path. The product still does not create, push, claim, execute, aggregate, publish, refresh RAG, generate final answers, or save acknowledgement for mutation work.
- Next recommended step: Add endpoint-level runner preview/enqueue smoke coverage for `READY_HANDOFF_CREATION_DISABLED`: mock/seed next-action with a handoff summary, call the runner preview and enqueue endpoints, assert `WAIT_CREATION_GATE_DISABLED`/`NOT_ENQUEUED`, assert the summary is present in JSON, and verify no Local Agent enqueue/push/claim/mutation path is called.

### 2026-07-02 - Run 483

- Completed: Added a first-class M7 creation-disabled handoff summary to the read-only next-action response. `CodeAgentLoopNextActionResponse` now carries `handoffSummary` while preserving the existing constructor shape for current callers. `CodeAgentLoopPreviewService.nextAction` populates that summary only for `READY_HANDOFF_CREATION_DISABLED`, extracting the mutation request blueprint/creation/push/claim gate statuses plus expected request count, durable mutation row count, persisted/pushed/claimable zero counts, and disabled creation/push/claim/mutation/finalization flags from the release-boundary timeline details. The endpoint smoke now verifies the JSON response exposes that summary without creating, pushing, claiming, or executing mutation rows.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentControllerTest,CodeAgentLoopRunnerServiceTest" test` ran 32 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 116 tests with 0 failures and 8 skipped opt-in live tests.
- Remaining risk: The next-action endpoint now exposes precise creation-disabled context, but runner preview/enqueue still treats non-read-only actions mostly as generic wait/no-enqueue decisions. The product still does not create, push, claim, execute, aggregate, publish, refresh RAG, generate the final answer, or save acknowledgement for mutation work.
- Next recommended step: Extend `CodeAgentLoopRunnerService.previewNextStep` and `enqueueReadOnlyNextStep` so `READY_HANDOFF_CREATION_DISABLED` returns a typed wait/refusal decision that includes the `handoffSummary`, rather than only a generic no-request response. Keep it read-only and do not enable request creation, push, claim, `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, acknowledgement save, or mutation yet.

### 2026-07-02 - Run 482

- Completed: Added the M7 durable/live zero-row proof for the disabled mutation request-creation envelope. `LocalAgentToolExecutionRepository` now exposes `countMutationEnabledExecutionRowsForReleaseAttempt`, counting actual `USER_LOCAL_AGENT` mutation-sequence rows for a release attempt only when `input.mutationAllowed=true`. `LocalAgentToolGatewayService` threads that repository-backed count into `mutationRequestCreationGate.durableMutationExecutionRowCount`. The service/unit and HTTP endpoint smokes now pin that field at `0`, and the opt-in live PostgreSQL release-boundary regression verifies a real release refusal carries `expectedRequestCount=4`, `durableMutationExecutionRowCount=0`, `persistedRequestCount=0`, `pushedRequestCount=0`, and `claimableRequestCount=0` while the source patch stays `APPROVED_HELD`.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 57 tests with 0 failures and 8 skipped opt-in live tests. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 116 tests with 0 failures and 8 skipped opt-in live tests. Approved opt-in live PostgreSQL verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest" "-Dlearnbot.live-postgres-tests=true" test` ran 8 tests with 0 failures and 0 skipped.
- Remaining risk: The durable count proves no mutation-enabled execution rows are created while the gate is disabled, but the product still does not create, push, claim, execute, aggregate, publish, refresh RAG, generate the final answer, or save acknowledgement for mutation work. The runner can identify `READY_HANDOFF_CREATION_DISABLED`, but it does not yet surface the durable zero-row details as a first-class next-action/runner reason for UI or orchestration decisions.
- Next recommended step: Extend the M7 runner/next-action response for `READY_HANDOFF_CREATION_DISABLED` so it includes the creation-disabled handoff summary: expected request count, durable mutation row count, persisted/pushed/claimable zero counts, and disabled creation/push/claim flags. Keep it read-only and do not enable request creation, push, claim, `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, acknowledgement save, or mutation yet.

### 2026-07-02 - Run 481

- Completed: Added the next M7 creation-disabled execution-row envelope without opening mutation. `LocalAgentToolGatewayService` now includes an `expectedExecutionRow` for each disabled mutation request blueprint item, modeling the exact would-be Local Agent rows for `patch.apply`, `command.runAllowed`, post-write `git.status`, and `rollback.restore`. Each row carries source/release/session/user/agent/workspace identity, `USER_LOCAL_AGENT`, expected initial status (`APPROVED`, `APPROVED`, `PENDING`, `APPROVAL_REQUIRED`), input-contract flags, and explicit `created=false`, `persisted=false`, `pushed=false`, `claimable=false`, and `mutationAllowed=false`. Unit and endpoint smokes now verify the HTTP-visible release refusal exposes this row envelope while the creation gate still reports four expected rows and zero persisted/pushed/claimable rows.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolGatewayServiceTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 49 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 116 tests with 0 failures and 8 skipped opt-in live tests.
- Remaining risk: This still models the future mutation rows only as a disabled blueprint. No `local_agent_tool_executions` mutation rows are created, pushed, claimable, running, or executable; no `patch.apply`, allowlisted verification command, rollback restore, RAG freshness update, final publication, final-answer generation, or acknowledgement persistence is enabled. The proof is HTTP/unit-level, not yet a durable/live PostgreSQL zero-row proof.
- Next recommended step: Add the next M7 durable/live bridge proving the expected execution-row envelope round-trips through release-attempt/timeline evidence or a live PostgreSQL slice while the mutation execution table still has zero created/pushed/claimable rows for that release attempt. Do not enable request creation, push, claim, `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, final-answer generation, acknowledgement save, or mutation yet.

### 2026-07-02 - Run 480

- Completed: Added the M7 loop-level distinction for a release refusal whose mutation handoff is already modeled but creation remains disabled. `CodeAgentLoopPreviewService.nextAction` now inspects `LOCAL_AGENT_RELEASE_BOUNDARY_REFUSED` timeline details and returns `READY_HANDOFF_CREATION_DISABLED` when the release-attempt model contains `READY_PREFLIGHT_DISABLED`, `REFUSED_REQUEST_CREATION_DISABLED`, and `REFUSED_CREATION_DISABLED`. The fallback remains `WAIT_FOR_RELEASE_GATE` when the handoff model is absent or incomplete. The endpoint smoke now carries the release-attempt handoff details into the synthetic timeline event and proves `GET /api/code-agent/loop/next-action` exposes `READY_HANDOFF_CREATION_DISABLED` while request creation, push, claim, mutation, final result, publication, and acknowledgement remain disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 13 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 116 tests with 0 failures and 8 skipped opt-in live tests.
- Remaining risk: The central loop can now distinguish a generic release-gate wait from a preflight-ready but creation-disabled mutation handoff, but it still does not create, persist, push, claim, or execute mutation rows. The real Codex-like path still needs gated mutation request creation, Local Agent `patch.apply`, allowlisted verification command execution, rollback fallback, RAG freshness update, mutation result aggregation, final publication, final answer generation, and acknowledgement persistence.
- Next recommended step: Add the next M7 gate-disabled creation-envelope proof over durable/live evidence: when `READY_HANDOFF_CREATION_DISABLED` is present, model the exact persisted mutation execution rows that would be created (`patch.apply`, `command.runAllowed`, `git.status`, `rollback.restore`) and prove the creation gate still refuses persistence/push/claim with zero created rows. Do not enable request creation, push, claim, `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, or acknowledgement yet.

### 2026-07-02 - Run 479

- Completed: Added the next M7 release-to-mutation handoff proof at the HTTP smoke layer without opening claim or mutation. `CodeAgentLoopRunnerEndpointSmokeTest.validatedPatchApprovalEndpointCanQueueFreshObservationsWithoutReleasingSourcePatch` now also verifies the release refusal response exposes the disabled mutation handoff model: `mutationDispatchEnvelopeContract` is `READY_DISPATCH_DISABLED`, dispatch preflight is `READY_PREFLIGHT_DISABLED` with all required capabilities covered, `mutationDispatchDecisionModel` refuses dispatch, `mutationRequestBlueprint` is `REFUSED_REQUEST_CREATION_DISABLED` with four ordered Local Agent mutation request blueprints, `mutationRequestCreationGate` refuses persistence with zero persisted/pushed/claimable rows, and claim/execution gates remain refused. The smoke keeps release gate, request creation, push, claim, `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, and acknowledgement disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused endpoint verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` ran 5 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 115 tests with 0 failures and 8 skipped opt-in live tests.
- Remaining risk: The handoff envelope and request blueprint are now HTTP-visible, but no mutation row is created or claimable. The next real product gap is enabling a tightly gated creation/release path for the mutation request sequence and then proving Local Agent execution, allowlisted verification, rollback fallback, RAG freshness marking, final result aggregation, publication, final answer, and acknowledgement. This must remain behind explicit release/creation/claim gates until each boundary has durable/live coverage.
- Next recommended step: Add the next M7 gate-disabled durable/HTTP bridge that records the mutation handoff blueprint onto the loop timeline or runner response after release refusal, so the central loop can distinguish `WAIT_FOR_RELEASE_GATE` from `READY_HANDOFF_CREATION_DISABLED` and choose whether to ask the user, wait for a feature gate, or stop with a precise reason. Do not create, push, claim, or execute mutation rows yet.

### 2026-07-02 - Run 478

- Completed: Extended the M7 endpoint/runner smoke through the HTTP-visible release-boundary and next-action path. `CodeAgentLoopRunnerEndpointSmokeTest.validatedPatchApprovalEndpointCanQueueFreshObservationsWithoutReleasingSourcePatch` now drives validated patch approval, user approval to `APPROVED_HELD`, fresh release-attempt observations, Local Agent completion of repository verification and patch dry-run, readiness inspection, `POST /api/local-agents/tools/{requestId}/release`, and `GET /api/code-agent/loop/next-action`. The smoke proves the release endpoint returns `RELEASE_REFUSED_GATE_DISABLED`/`REFUSAL_ONLY`, appends `LOCAL_AGENT_RELEASE_BOUNDARY_REFUSED`, and the real `CodeAgentLoopPreviewService` maps that timeline event to `WAIT_FOR_RELEASE_GATE` with request creation, push, claim, mutation, final result, publication, and acknowledgement still disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused endpoint verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` ran 5 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 115 tests with 0 failures and 8 skipped opt-in live tests.
- Remaining risk: The full HTTP-visible safe refusal path is now covered, but mutation is still intentionally disabled. The system still does not release a claimable mutation row, execute real `patch.apply`, run allowlisted verification commands, restore rollback on failure, refresh RAG freshness, aggregate a mutation result, publish a final report, generate a final answer, or save acknowledgement.
- Next recommended step: Start the next M7 slice that introduces the release-to-claimable mutation handoff behind a still-disabled feature gate/model: produce and verify the exact mutation request blueprint/envelope that would be released from an approved-held source patch only when all readiness evidence exists, while keeping actual claim, push, `patch.apply`, verification command execution, rollback restore, RAG freshness update, final publication, and acknowledgement disabled until the gate is explicitly opened.

### 2026-07-02 - Run 477

- Completed: Added the next M7 audit/runner bridge for release-boundary refusal without opening mutation. `CodeAgentLoopTimelineRepository.appendReleaseBoundaryRefusal` now records `LOCAL_AGENT_RELEASE_BOUNDARY_REFUSED` events with the held patch request identity, boundary status/action mode, blocking reasons, release-attempt model, patch execution gate, release enablement checklist, and all execution controls closed. `LocalAgentToolGatewayService.inspectPatchReleaseBoundary` appends that event when the source request carries repository/loop context. `CodeAgentLoopPreviewService.nextAction` now reads the latest release-boundary refusal and returns `WAIT_FOR_RELEASE_GATE`, keeping request creation, push, claim, mutation, final result, publication, and acknowledgement disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopTimelineRepositoryTest,CodeAgentLoopPreviewServiceTest,LocalAgentToolGatewayServiceTest" test` ran 59 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 115 tests with 0 failures and 8 skipped opt-in live tests.
- Remaining risk: The central loop can now observe durable release refusal and choose a safe wait state, but the source patch remains intentionally non-claimable. Real mutation is still disabled: no source-patch release, claim, `patch.apply`, allowlisted verification command execution, rollback restore, RAG freshness update, mutation result aggregation, final publication, final answer, or acknowledgement has been enabled.
- Next recommended step: Add the next M7 endpoint/runner smoke that drives the full validated approval -> fresh linked observations -> release-boundary refusal -> `GET /api/code-agent/loop/next-action` path and proves the HTTP-visible runner state returns `WAIT_FOR_RELEASE_GATE` while source-patch release, claim, mutation, verification execution, rollback, RAG freshness update, final publication, and acknowledgement remain disabled.

### 2026-07-02 - Run 476

- Completed: Added the next M7 non-mutating release-boundary proof over durable linked evidence. `LocalAgentToolExecutionRepositoryLivePostgresTest.durableLinkedEvidenceReleaseBoundaryRefusesGateDisabledWithoutClaimingSource` now persists the approved-held source patch, disabled release-attempt envelope, and fresh linked observation rows in PostgreSQL, completes the repository-verification and dry-run rows, then calls `LocalAgentToolGatewayService.inspectPatchReleaseBoundary`. The live regression proves the boundary returns `RELEASE_REFUSED_GATE_DISABLED`, `REFUSAL_ONLY`, keeps every release/mutation control false (`releaseGateEnabled`, `claimEnabled`, `writeHelperEnabled`, `requestCreationEnabled`, `pushEnabled`, `claimable`, `mutationAllowed`, `applyEnabled`, `testEnabled`, `rollbackRestoreEnabled`, `ragFreshnessUpdateEnabled`), preserves the disabled release-attempt model, and leaves the source patch `APPROVED_HELD`/non-claimable with no claimable queue rows.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused default verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 57 tests with 0 failures and 8 skipped opt-in live tests. Approved opt-in live PostgreSQL verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dlearnbot.live-postgres-tests=true" "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest#durableLinkedEvidenceReleaseBoundaryRefusesGateDisabledWithoutClaimingSource" test` ran 1 live test with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 113 tests with 0 failures and 8 skipped opt-in live tests.
- Remaining risk: Durable readiness and release-boundary refusal are now proven against local PostgreSQL, but actual source-patch release remains intentionally disabled. The test still uses a constructed validated patch input rather than model-generated diff plus real indexed repository fixture. The loop still does not generate the diff from model output, execute real `patch.apply`, run allowlisted verification commands, rollback on failure, refresh RAG, aggregate mutation results, publish a final answer, or save acknowledgement.
- Next recommended step: Add the next M7 audit/runner bridge that records the durable release-boundary refusal back onto the loop timeline or runner response, so the central loop can observe “ready evidence exists but release is disabled/non-claimable” and choose a safe next state (`WAIT_FOR_RELEASE_GATE`, `ASK_USER`, or `STOP_WITH_REASON`) without enabling source-patch release, claim, mutation, verification command execution, rollback, RAG freshness update, final publication, or acknowledgement.

### 2026-07-02 - Run 475

- Completed: Moved the M7 linked fresh-evidence proof into an opt-in durable PostgreSQL slice. `LocalAgentToolExecutionRepositoryLivePostgresTest.durableFreshObservationsFeedReadinessWhileSourcePatchStaysHeld` now persists an approved-held source `patch.apply` row, a disabled non-claimable release-attempt row, and the two fresh observation rows created through the real `LocalAgentToolGatewayService`. It claims/completes the fresh `git.status` repository verification and non-mutating `patch.apply` dry-run rows, then proves readiness reads release-attempt-linked durable evidence while the source patch remains `APPROVED_HELD`, non-claimable, and blocked by `releaseGateEnabled=false`. The live run also exposed a real JDBC bug: PostgreSQL JSONB `?` operators in `LocalAgentToolExecutionRepository` were parsed as positional placeholders by `NamedParameterJdbcTemplate`; those queries now use `jsonb_exists(...)` for repository verification and accepted-observation lookups.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused default verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 56 tests with 0 failures and 7 skipped opt-in live tests. Approved opt-in live PostgreSQL verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dlearnbot.live-postgres-tests=true" "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest#durableFreshObservationsFeedReadinessWhileSourcePatchStaysHeld" test` ran 1 live test with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 112 tests with 0 failures and 7 skipped opt-in live tests.
- Remaining risk: Durable linked-evidence readiness is now proven against local PostgreSQL, but the source patch release remains intentionally disabled and no mutation is claimable. The durable slice uses server-side constructed validated patch input rather than a model-generated diff and real indexed repository fixture. The loop still does not generate the diff from model output, execute real `patch.apply`, run allowlisted verification commands, rollback on failure, refresh RAG, aggregate mutation results, publish a final answer, or save acknowledgement.
- Next recommended step: Add the next M7 non-mutating release-boundary proof over durable linked evidence: call the release boundary after the live/durable linked evidence is present and prove it returns `RELEASE_REFUSED_GATE_DISABLED`, preserves the disabled release-attempt model, and leaves the source patch `APPROVED_HELD`/non-claimable. Do not enable source-patch release, claim, mutation, verification command execution, rollback, RAG freshness update, final publication, or acknowledgement yet.

### 2026-07-02 - Run 474

- Completed: Extended the M7 fresh-observation endpoint smoke so the two queued release-attempt observations now complete through `POST /api/local-agents/tools/{requestId}/response` and feed readiness as linked evidence. The validated `APPROVED_HELD` source patch still comes from `POST /api/code-agent/loop/runner/validated-patch-approval-request`; after `/fresh-observations`, the smoke posts a matching `git.status` repository identity response and a non-mutating `patch.apply` dry-run response with snapshot manifest and rollback restore preconditions. It then proves `GET /api/local-agents/tools/{sourceRequestId}/readiness` consumes only `RELEASE_ATTEMPT_LINKED` evidence: `snapshotManifestPreview`, `rollbackRestorePreconditions`, and `workspaceRepositoryVerified` now pass, repository/snapshot linkage is marked `releaseAttemptLinked=true`, and `releaseGateEnabled=false` still keeps `readyToRelease=false`, `claimEnabled=false`, and `mutationEnabled=false`. Source patch release, source patch claim, mutation-input release, final publication, and acknowledgement remain disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 58 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 111 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: This is still an endpoint-level mocked-persistence smoke, not a live durable Spring Boot/PostgreSQL/Local Agent run. The source patch remains intentionally non-claimable because release is disabled. The loop still does not generate the diff from model output, execute real `patch.apply`, run allowlisted verification commands, rollback on failure, refresh RAG, aggregate mutation results, publish a final answer, or save acknowledgement.
- Next recommended step: Move this linked-evidence proof into a durable/live slice: persist the validated approved-held source patch and the two fresh observation rows in PostgreSQL, complete the fresh `git.status` and dry-run `patch.apply` rows, and prove readiness reads the linked durable rows while the source patch remains `APPROVED_HELD` and non-claimable. Do not enable source-patch release, claim, mutation, verification command execution, rollback, RAG freshness update, final publication, or acknowledgement yet.

### 2026-07-02 - Run 473

- Completed: Added the next M7 endpoint bridge from a validated `APPROVED_HELD` patch approval request into release-attempt fresh-observation queueing without opening mutation. `CodeAgentLoopRunnerEndpointSmokeTest.validatedPatchApprovalEndpointCanQueueFreshObservationsWithoutReleasingSourcePatch` now drives `POST /api/code-agent/loop/runner/validated-patch-approval-request`, approves the created `patch.apply` request through `POST /api/local-agents/tools/{requestId}/approval`, then calls `POST /api/local-agents/tools/{requestId}/fresh-observations` through the real `LocalAgentController` and `LocalAgentToolGatewayService`. The smoke proves only two new observation request ids are pushed (`git.status` repository verification and approved `patch.apply` dry-run), both carry `sourceRequestId`, `releaseAttemptId`, and `freshObservationOnly=true`, the patch dry-run carries `dryRunOnly=true` and `mutationAllowed=false`, and the original source patch id is not pushed or made claimable. Release, source-patch claim, mutation-input release, final publication, and acknowledgement remain disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest" test` ran 58 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,LocalAgentControllerTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 111 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: The fresh-observation endpoint bridge now exists at HTTP smoke level, but it still uses mocked persistence/release-attempt rows and does not complete those observation requests with real Local Agent outputs. Readiness still needs an endpoint-level or live durable regression proving fresh linked `git.status` repository verification and fresh linked patch dry-run completion are consumed by readiness, while `releaseGateEnabled=false` keeps the source patch non-claimable. The loop still does not generate the diff from model output, execute `patch.apply`, run allowlisted verification commands, rollback on failure, refresh RAG, aggregate results, publish a final answer, or save acknowledgement.
- Next recommended step: Add an endpoint/service smoke that completes the two fresh observation requests created from the validated `APPROVED_HELD` patch: return a matching `git.status` repository identity and a non-mutating patch dry-run with snapshot/rollback preview, then prove readiness consumes only release-attempt-linked fresh evidence and still refuses source-patch release because `releaseGateEnabled=false`. Do not enable source-patch release, claim, mutation, verification command execution, rollback, RAG freshness update, final publication, or acknowledgement yet.

### 2026-07-02 - Run 472

- Completed: Added the endpoint/integration-style M7 smoke for `POST /api/code-agent/loop/runner/validated-patch-approval-request` using the real `CodeAgentLocalPatchRequestService` over controlled mocked file-loader/repository dependencies. The smoke now drives the HTTP endpoint through model side-effect boundary selection, server patch validation payload preparation, Local Agent approval-request persistence, user approval to `APPROVED_HELD`, and readiness inspection. It proves the resulting `patch.apply` request carries the validated diff, target files, expected file metadata, snapshot policy, rollback policy, stale-index policy, repository identity, workspace identity, and `loopId`, while still keeping push, claim, release, mutation, final publication, and acknowledgement disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest" test` ran 32 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,CodeAgentLocalPatchRequestServiceTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 101 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: This proves a full validated approval payload can be created and held, but it still uses mocked indexed-file/repository inputs and mocked Local Agent status. The loop still does not generate the diff itself from model output, collect fresh Local Agent repository verification, collect patch dry-run evidence, release a claimable mutation row, execute `patch.apply`, run allowlisted verification commands, rollback on failure, refresh RAG, aggregate results, publish a final answer, or save acknowledgement.
- Next recommended step: Add the next release-attempt evidence bridge for the validated `APPROVED_HELD` request: create or link fresh Local Agent observation requests for repository verification, snapshot-manifest preview, and patch dry-run evidence, prove those observation requests can be queued/completed without making the source patch claimable, then have readiness consume only fresh matching evidence while `releaseGateEnabled=false` still blocks actual mutation. Do not enable release, claim, `patch.apply`, verification command execution, rollback, RAG freshness update, final publication, or acknowledgement yet.

### 2026-07-02 - Run 471

- Completed: Added the next M7 bridge from model-proposed `patch.apply` into a validated, non-executing patch approval payload. `CodeAgentLoopPatchApprovalPayloadRequest` and `POST /api/code-agent/loop/runner/validated-patch-approval-request` now require the loop side-effect boundary to choose `patch.apply`, then delegate to the existing `CodeAgentLocalPatchRequestService.prepare` path so diff validation, target-file normalization, expected file hashes, snapshot policy, rollback policy, repository identity, workspace identity, and `loopId` are carried into the `APPROVAL_REQUIRED` Local Agent request. If the model stays read-only or the side-effect boundary is not satisfied, no validated approval request is created. Release, push, claim, mutation, final publication, and acknowledgement remain disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 30 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 99 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: This connects the loop to the validated patch approval payload path, but the endpoint-level smoke still mocks the lower patch payload service rather than exercising indexed file loading through a real repository fixture. The loop still does not generate the diff itself from model output, collect fresh Local Agent repository verification, run patch dry-run evidence, release a claimable mutation row, execute allowlisted verification commands, rollback on failure, refresh RAG, aggregate results, publish a final report, or save acknowledgement.
- Next recommended step: Add an endpoint/integration-style smoke for `validated-patch-approval-request` using the real `CodeAgentLocalPatchRequestService` over controlled mocked file-loader/repository dependencies, then approve it to `APPROVED_HELD` and prove readiness passes the static payload checks (`diffPresent`, `targetFilesPresent`, `expectedFilesPresent`, `snapshotRequired`, policies) while still refusing release on missing fresh Local Agent evidence and `releaseGateEnabled=false`. Do not enable release, push, claim, mutation, RAG freshness update, final publication, or acknowledgement yet.

### 2026-07-02 - Run 470

- Completed: Extended the M7 endpoint smoke so a loop-created metadata-only `patch.apply` request is approved to `APPROVED_HELD` and then inspected through `GET /api/local-agents/tools/{requestId}/readiness`. The smoke now proves the same HTTP path refuses release because executable patch evidence is missing: `diffPresent`, `targetFilesPresent`, `expectedFilesPresent`, `snapshotRequired`, `snapshotManifestPreview`, `workspaceRepositoryVerified`, and `releaseGateEnabled` all remain failed, while `patchReleaseReadiness` and `patchExecutionGate` stay `BLOCKED` with `releaseGateEnabled=false`, `claimEnabled=false`, and `mutationEnabled=false`. This preserves the session/user/agent/workspace/loop identity chain while preventing release, push, claim, mutation, RAG freshness update, final publication, and acknowledgement.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused endpoint verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` ran 3 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 96 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: The loop can now persist, approve, hold, and refuse release for a side-effectful patch request, but the request still lacks a real validated patch payload. The system still does not generate or carry the model-produced unified diff into the loop approval request, run fresh repository verification/dry-run evidence, release a claimable mutation row, execute allowlisted verification commands, rollback on failure, refresh RAG, aggregate results, publish a final report, or save acknowledgement.
- Next recommended step: Add the next M7 bridge that turns a model-proposed `patch.apply` step into a validated non-executable patch approval payload: include diff/targetFiles/expected file metadata from the existing patch planning/validation path, keep it `APPROVAL_REQUIRED` then `APPROVED_HELD`, and prove readiness still refuses release until fresh Local Agent repository verification and dry-run evidence are present. Do not enable release, push, claim, mutation, RAG freshness update, final publication, or acknowledgement yet.

### 2026-07-02 - Run 469

- Completed: Added an endpoint-level M7 approval-held bridge regression for the loop-created patch approval request. `CodeAgentLoopRunnerEndpointSmokeTest.patchApprovalRequestEndpointCanBeApprovedHeldWithoutPushClaimOrMutation` now drives `POST /api/code-agent/loop/runner/patch-approval-request` through the real runner/selection/gateway stack with a model-proposed `patch.apply`, captures the persisted `APPROVAL_REQUIRED` request, then drives `POST /api/local-agents/tools/{requestId}/approval` through `LocalAgentController` using the same gateway. The test proves the request transitions only to `APPROVED` + `APPROVED_HELD`, appends the loop approval-decision timeline event with repository/loop/session/agent/workspace identity, and does not push, claim, release, or mutate.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused endpoint verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` ran 3 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 96 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: Approval now has endpoint-level evidence for the loop-created request, but the request payload is still boundary metadata rather than a validated executable patch payload. The release/readiness path still must refuse execution until validated diff, target files, expected hashes, snapshot policy, workspace/repository verification, fresh dry-run evidence, and explicit release approval are present. Real `patch.apply`, allowlisted verification command, rollback, RAG freshness update, final report publication, final answer, and acknowledgement remain incomplete.
- Next recommended step: Add a focused release-readiness regression for the loop-created `APPROVED_HELD` request proving `GET /api/local-agents/tools/{requestId}/readiness` or the release boundary refuses release because validated patch payload/evidence is missing, while preserving session/user/agent/workspace/loop identity and keeping release, push, claim, mutation, RAG freshness update, final publication, and acknowledgement disabled.

### 2026-07-02 - Run 468

- Completed: Added the first M7 persisted side-effect approval slice for the loop runner without opening execution. `CodeAgentLoopPatchApprovalRequestResponse` and `POST /api/code-agent/loop/runner/patch-approval-request` now reuse the patch approval preview and persist a real `patch.apply` Local Agent request only when the model proposes the side-effectful boundary and agent/workspace identity is present. The created row is `USER_LOCAL_AGENT`, `PATCH_APPLY`, `REQUIRED`, and `APPROVAL_REQUIRED`; release evidence, release gate, enqueue, push, claim, mutation, final result, publication, and acknowledgement all remain disabled in the response. Guardrails document that `APPROVAL_REQUIRED` is not claimable by the existing `claimNext` statuses (`PENDING`, `APPROVED`).
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest" test` ran 24 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 95 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: This persists an approval request, but it still does not collect/record user approval through the loop runner path, produce release-attempt evidence, release an approved-held patch, execute `patch.apply`, run allowlisted verification, rollback, refresh RAG, publish a final report, or save acknowledgement. The generated approval input is intentionally non-mutating boundary metadata, so future release readiness must still require a validated diff, target files, expected hashes, snapshot policy, workspace/repository verification, and fresh dry-run evidence before any claimable mutation row is released.
- Next recommended step: Add the next narrow bridge from this persisted approval request into the existing approval decision/readiness chain: approve/deny should append loop timeline evidence, approved requests should become `APPROVED_HELD` and still non-claimable, and readiness should clearly refuse release until validated patch payload and release evidence exist. Do not enable release, push, claim, mutation, RAG freshness update, final publication, or acknowledgement yet.

### 2026-07-02 - Run 467

- Completed: Added the M7 `patch.apply` approval-request preview without opening execution. `CodeAgentLoopApprovalRequestPreviewResponse` and `POST /api/code-agent/loop/runner/patch-approval-preview` now turn a side-effectful `patch.apply` boundary into preview-only approval metadata: `PATCH_APPLY`, `USER_LOCAL_AGENT`, `REQUIRED` approval, `approvalRequired=true`, `releaseRequired=true`, `releaseEvidenceAvailable=false`, `releaseGateEnabled=false`, `requestCreationEnabled=false`, `enqueueEnabled=false`, `claimEnabled=false`, and `mutationAllowed=false`. Missing agent/workspace or a model decision that stays read-only returns no approval candidate. Tests verify no `LocalAgentToolGatewayService.enqueueReadOnly` call is made and no request is created, pushed, queued, claimed, or mutated.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest" test` ran 21 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 92 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: This is still preview-only metadata. It does not persist a real approval request, collect user approval, create or link a release attempt, run fresh release evidence, release an approved-held patch, execute verification/rollback, refresh RAG, publish a final report, or save acknowledgement.
- Next recommended step: Add the next gated slice that persists a real `patch.apply` approval request only when agent/workspace identity is present, while keeping it `APPROVAL_REQUIRED`/non-claimable and proving no release, claim, queue dispatch, mutation, or final publication can occur before explicit approval and release evidence.

### 2026-07-01 - Run 466

- Completed: Added the first M7 side-effectful tool boundary preview without opening mutation. `CodeAgentLoopSideEffectBoundaryResponse` and `POST /api/code-agent/loop/runner/side-effect-boundary-preview` now expose a model-assisted boundary that can recognize a proposed `patch.apply` step, but returns only approval/release requirements and closed execution flags. The service marks `approvalRequired=true` and `releaseRequired=true` for `patch.apply`, while `releaseGateEnabled`, request creation, enqueue, push, claim, mutation, final result, publication, and acknowledgement all remain false. If the model keeps the next step read-only or fails, the boundary returns a no-side-effect/failure state with the same closed controls. Tests also verify the boundary does not call `LocalAgentToolGatewayService.enqueueReadOnly`.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest" test` ran 17 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 88 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: This is a model/approval/release boundary only. It does not yet create a real side-effectful approval request, link that request to a release attempt, run fresh release-attempt observations, release an approved-held patch, refresh RAG after mutation, publish a final report, or save acknowledgement.
- Next recommended step: Connect the boundary to a non-executing approval-request preview for `patch.apply`: produce the exact request metadata that would require user approval, prove it cannot be enqueued or claimed, and add fallback tests for missing agent/workspace/release evidence before enabling any release path.

### 2026-07-01 - Run 465

- Completed: Added endpoint-level M7 smoke coverage for the model-selected read-only enqueue path. `CodeAgentLoopRunnerEndpointSmokeTest.enqueueSelectedReadOnlyEndpointCanQueueClaimCompleteAndRecordTimeline` now drives `POST /api/code-agent/loop/runner/enqueue-selected-read-only` through `MockMvc` with a real `CodeAgentLoopRunnerService`, real `CodeAgentLoopToolSelectionService`, real `LocalAgentToolGatewayService`, and a mocked allowed Ollama JSON decision. The test verifies repository-space authorization, model-accepted `git.status` selection, read-only request enqueue/push, Local Agent claim, successful completion, and loop timeline `appendObservationResult` plus `appendNextDecision` using the original `repositoryId`, `loopId`, response, and request input. It also verifies `mutationAllowed=false` and that approved-held patch release paths stay closed.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` ran 2 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 85 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: This proves the HTTP/controller/service/gateway/timeline contract with mocked persistence and controlled model output, not a live Spring Boot/PostgreSQL/Ollama/Local Agent stack. Side-effectful `patch.apply`, verification commands, rollback, RAG freshness, final report, publication, and acknowledgement remain incomplete and should remain gated.
- Next recommended step: Begin the explicit approval/release bridge for side-effectful `patch.apply`: model only the release candidate and approval requirement first, then add a refusal/fallback regression proving no side-effectful request is queued until approval and release prerequisites are satisfied.

### 2026-07-01 - Run 464

- Completed: Connected the gated model tool-selection preview to the first safe enqueue path. Added `CodeAgentLoopSelectedToolEnqueueResponse` and `POST /api/code-agent/loop/runner/enqueue-selected-read-only`. The new service path calls the model-selection preview, revalidates the selected candidate against the read-only guardrail (`git.status`, `NOT_REQUIRED`, non-side-effectful, `mutationAllowed=false`), and then queues through `LocalAgentToolGatewayService.enqueueReadOnly`. It records whether the queued request came from a model-accepted choice (`ENQUEUED_MODEL_SELECTED_READ_ONLY_OBSERVATION`) or deterministic fallback after unsafe/unavailable model selection (`ENQUEUED_FALLBACK_READ_ONLY_OBSERVATION`). Request creation/enqueue/push are enabled only for that read-only observation; claim, mutation, final result, publication, and acknowledgement remain disabled in the runner response.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest" test` ran 14 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 84 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: The selected-tool enqueue path now reaches the durable Local Agent queue only for read-only `git.status`, but it still does not claim/complete through this new endpoint-level path in a dedicated smoke, and it does not release side-effectful `patch.apply`, verification commands, rollback, RAG freshness, final report, publication, or acknowledgement.
- Next recommended step: Add an endpoint/service smoke for `enqueue-selected-read-only` that captures the queued model-selected request, simulates Local Agent claim/complete, and asserts timeline observation plus next-decision. After that, begin the explicit approval/release bridge for side-effectful `patch.apply` without broadening arbitrary command execution.

### 2026-07-01 - Run 463

- Completed: Added the first gated model-driven next-tool selection preview for the agent loop. `CodeAgentLoopToolSelectionService` now calls the existing `OllamaClient` only after the runner has prepared a safe read-only candidate, asks the model to choose from the current allowlist, accepts only a JSON decision for `git.status` with `readOnly=true`, `requiresApproval=false`, and `mutationAllowed=false`, and falls back to the deterministic read-only candidate when the model fails or proposes an unsafe tool. Added `CodeAgentLoopToolSelectionResponse` and `POST /api/code-agent/loop/runner/select-tool-preview`, preserving repository-space authorization and keeping request creation, enqueue, push, claim, and mutation disabled in this preview surface.
- Verified: Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentControllerTest,CodeAgentLoopRunnerEndpointSmokeTest" test` ran 12 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopToolSelectionServiceTest,CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 81 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: Model selection is now present only as a safe preview over the single read-only `git.status` candidate. It does not yet create side-effectful patch/test/rollback requests, release approved mutation work, refresh RAG after edits, publish a final mutation report, or save user acknowledgement.
- Next recommended step: Use the model selection preview inside the runner decision path so a selected safe candidate can be enqueued automatically when it remains read-only, then add an explicit approval/release path for side-effectful `patch.apply` only after the read-only selected-tool loop is stable.

### 2026-07-01 - Run 462

- Completed: Added an endpoint-level runner read-only smoke in `CodeAgentLoopRunnerEndpointSmokeTest`. The test drives `POST /api/code-agent/loop/runner/enqueue-read-only` through `MockMvc`, with a real `CodeAgentLoopRunnerService` and `LocalAgentToolGatewayService` over mocked repositories. It verifies repository-space authorization, queues the read-only `git.status` Local Agent request from the HTTP path, captures the pushed queued request, simulates Local Agent claim and successful completion, and asserts the loop timeline receives both `appendObservationResult` and `appendNextDecision` with the original `repositoryId`, `loopId`, response, and runner-generated request input. Mutation remains disabled and approved-held patch release paths are not opened.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest" test` ran 1 test with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerEndpointSmokeTest,CodeAgentLoopRunnerServiceTest,CodeAgentControllerTest,LocalAgentToolGatewayServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolExecutionRepositoryLivePostgresTest" test` ran 77 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: The endpoint smoke proves the Spring MVC JSON path and backend queue/claim/complete/timeline contracts, but it still uses mocked persistence and a controlled Local Agent completion. The product is not yet Codex-like because model-driven next-tool selection, side-effectful patch/test/rollback release, final report publication, RAG freshness update after mutation, and user acknowledgement remain incomplete.
- Next recommended step: Add model-driven next-tool selection for the loop runner in a narrow gated form: from the latest timeline state, select `ASK_USER`, `STOP_WITH_REASON`, read-only observation, or approved side-effectful candidate metadata without executing mutation. Only after that should the runner release approved `patch.apply`/test/rollback work through the durable Local Agent queue.

### 2026-07-01 - Run 461

- Completed: Ran the runner read-only durable proof against the live localhost PostgreSQL path and added a focused wrapper for repeatable execution. `scripts/quality/local-agent-flow/run-runner-read-only-live-postgres.ps1` now runs the single opt-in test `LocalAgentToolExecutionRepositoryLivePostgresTest#runnerReadOnlyQueuePersistsClaimCompletionAndLoopTimelineEvents` with `-Dlearnbot.live-postgres-tests=true`. The quality harness live runner step now calls that wrapper through `powershell.exe -ExecutionPolicy Bypass -File ...` so Windows execution policy does not block the step. The live proof confirms the runner-created read-only `git.status` request can persist, be claimed, complete, and write loop observation/next-decision timeline events through real PostgreSQL rows.
- Verified: `Test-NetConnection localhost:5432` succeeded, while `docker ps` was blocked by Docker API permission. Direct `.ps1` execution was blocked by Windows execution policy, so the wrapper was verified with `powershell.exe -ExecutionPolicy Bypass -File .\scripts\quality\local-agent-flow\run-runner-read-only-live-postgres.ps1`; approved execution ran 1 live PostgreSQL test with 0 failures and 0 skipped. PowerShell parse checks passed for both the quality harness and the new wrapper. Restricted Maven still fails because sandbox networking blocks Maven Central. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 76 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: This proves the durable repository/gateway/timeline path live, but it is still not an endpoint-level smoke through authenticated `POST /api/code-agent/loop/runner/enqueue-read-only` and Local Agent HTTP/WebSocket completion. Model-driven next tool selection, side-effectful patch/test/rollback release, final report, RAG freshness update, and acknowledgement remain incomplete.
- Next recommended step: Add an endpoint-level runner read-only smoke that authenticates a user, prepares or seeds a loop decision, calls `POST /api/code-agent/loop/runner/enqueue-read-only`, lets the Local Agent or a controlled completion simulate `git.status`, and asserts the persisted timeline events. After that, proceed toward model-driven next-tool selection before widening side-effectful execution.

### 2026-07-01 - Run 460

- Completed: Wired the opt-in durable runner read-only regression into the quality harness. `scripts/quality/run-regression-harness.ps1` now accepts `-IncludeLiveRunnerReadOnlyPostgres`; when enabled it runs `LocalAgentToolExecutionRepositoryLivePostgresTest#runnerReadOnlyQueuePersistsClaimCompletionAndLoopTimelineEvents` with `-Dlearnbot.live-postgres-tests=true`. The step is named `local-agent-runner-read-only-live-postgres` and covers `local-agent-runner-loop`, `local-agent-live-postgres`, `approved-execution-flow`, and `local-agent-safety`. The harness report now records `includeLiveRunnerReadOnlyPostgres` so live durable-loop runs are auditable.
- Verified: PowerShell parse check passed for `scripts\quality\run-regression-harness.ps1`. Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,CodeAgentLoopRunnerServiceTest,LocalAgentToolGatewayServiceTest" test` ran 56 tests with 0 failures and 6 skipped opt-in live tests. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 76 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: The quality harness can now run the live durable runner read-only proof, but this run did not start or use a live PostgreSQL stack with `-IncludeLiveRunnerReadOnlyPostgres`. Endpoint-level smoke, model-driven next tool selection, side-effectful patch/test/rollback release, final report, RAG freshness update, and acknowledgement remain incomplete.
- Next recommended step: Run the harness against a live local stack with `.\scripts\quality\run-regression-harness.ps1 -IncludeLiveRunnerReadOnlyPostgres` after PostgreSQL is available. If that passes, add endpoint-level runner smoke or move the read-only live proof into the regular release checklist before widening the loop to side-effectful tools.

### 2026-07-01 - Run 459

- Completed: Added an opt-in durable-row regression for the runner read-only path in `LocalAgentToolExecutionRepositoryLivePostgresTest`. The new live Postgres test creates a real loop timeline through `CodeAgentLoopTimelineRepository`, appends an accepted next-decision event, uses `CodeAgentLoopRunnerService` plus a real `LocalAgentToolGatewayService` over real `LocalAgentToolExecutionRepository` rows, enqueues a read-only `git.status` follow-up, claims it, completes it, and asserts the persisted tool row plus the latest timeline events are `LOCAL_AGENT_OBSERVATION_RESULT` followed by `LOOP_NEXT_DECISION_RECORDED`. It keeps mutation off and verifies the runner-generated request input carries `repositoryId`, `loopId`, `freshObservationOnly=true`, and `mutationAllowed=false`.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,CodeAgentLoopRunnerServiceTest,LocalAgentToolGatewayServiceTest" test` ran 56 tests with 0 failures and 6 skipped opt-in live tests. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=LocalAgentToolExecutionRepositoryLivePostgresTest,CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 76 tests with 0 failures and 6 skipped opt-in live tests.
- Remaining risk: The durable runner test is present but not executed by default because `learnbot.live-postgres-tests=true` was not set and no live PostgreSQL stack was exercised in this run. It is stronger coverage when enabled, but the next proof still needs an actual local stack or harness invocation that runs the opt-in live test or equivalent endpoint-level smoke against PostgreSQL. Model-driven tool selection, side-effectful patch/test/rollback release, final report, RAG freshness update, and acknowledgement remain incomplete.
- Next recommended step: Run or wire a controlled quality harness target that starts/uses the local PostgreSQL-backed stack and executes the new live runner read-only test with `-Dlearnbot.live-postgres-tests=true`, then move to endpoint-level smoke if that passes. Only after this read-only durable loop is proven live should side-effectful patch/test/rollback orchestration be broadened.

### 2026-07-01 - Run 458

- Completed: Added the missing Milestone 6/7 regression that proves the new runner read-only queue path can flow through the existing Local Agent lifecycle. `CodeAgentLoopRunnerServiceTest.runnerReadOnlyEnqueueCanBeClaimedCompletedAndRecordedOnLoopTimeline` now builds a real `LocalAgentToolGatewayService` over mocked repositories, prepares a `QUEUE_READ_ONLY_OBSERVATION` loop decision, calls `enqueueReadOnlyNextStep`, claims the queued `git.status` request as the Local Agent, completes it with a successful observation, and verifies `CodeAgentLoopTimelineRepository.appendObservationResult` and `appendNextDecision` are called with the original `repositoryId`, `loopId`, response, and runner-generated request input. The test also verifies no approved-held patch release or mutation-input release is opened.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest" test` ran 19 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 70 tests with 0 failures.
- Remaining risk: This is a backend service-level regression with mocked persistence and Local Agent transport, not a live Spring Boot/PostgreSQL/Local Agent smoke. It proves the orchestration contracts line up, but the stack still needs a live disposable smoke for runner endpoint -> durable queue row -> Local Agent claim -> completed response -> persisted timeline events. Model-driven next tool selection, side-effectful patch/test/rollback release, final report, RAG freshness update, and acknowledgement remain incomplete.
- Next recommended step: Add a quality smoke or integration-style test around the runner read-only path using durable rows, preferably in the existing quality harness: create/seed a timeline state that yields `QUEUE_READ_ONLY_OBSERVATION`, call `POST /api/code-agent/loop/runner/enqueue-read-only`, run or simulate one Local Agent `git.status` completion, and assert persisted timeline events before broadening into side-effectful patch/test/rollback orchestration.

### 2026-07-01 - Run 457

- Completed: Advanced the Milestone 6/7 runner from preview-only into the first safe queueing slice. Added `CodeAgentLoopRunnerService.enqueueReadOnlyNextStep` and `POST /api/code-agent/loop/runner/enqueue-read-only`. The runner still consumes `nextAction`, but now it can enqueue exactly one prepared read-only `git.status` candidate when `nextAction=QUEUE_READ_ONLY_OBSERVATION`, agent/workspace identity is present, the candidate is `USER_LOCAL_AGENT`, `NOT_REQUIRED`, non-side-effectful, and `mutationAllowed=false`. It reuses the existing `LocalAgentToolGatewayService.enqueueReadOnly` boundary, so connected-agent and approved-workspace checks remain centralized. Stop/wait/ask-user states return `NOT_ENQUEUED`; unsafe candidates return a refusal. This creates the first server-orchestrated observe -> decide -> enqueue-read-only-follow-up segment while keeping side-effectful tools behind approval/release.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest" test` ran 18 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 69 tests with 0 failures.
- Remaining risk: The runner now creates and pushes a read-only queue item, but this run did not execute a live Local Agent claim/complete cycle from the new endpoint. The existing gateway completion path should record the resulting observation and next-decision because the candidate carries `repositoryId` and `loopId`, but a dedicated runner end-to-end test still needs to prove endpoint enqueue -> Local Agent claim -> response completion -> timeline observation -> next decision. Model-driven tool selection, side-effectful patch/test/rollback release, final report, RAG freshness update, and acknowledgement remain incomplete.
- Next recommended step: Add a narrow end-to-end backend test or disposable quality smoke for the new runner read-only path: seed/prepare a loop with `QUEUE_READ_ONLY_OBSERVATION`, call `enqueue-read-only`, claim the queued `git.status` request as the Local Agent, complete it with a successful observation, and assert the loop timeline receives `LOCAL_AGENT_OBSERVATION_RESULT` and `LOOP_NEXT_DECISION_RECORDED`. Keep `patch.apply`, tests, rollback, and final publication gated.

### 2026-07-01 - Run 456

- Completed: Added the next Milestone 6/7 loop-runner slice without opening execution. New loop-specific code is placed under purpose-based packages (`com.learnbot.dto.loop` and `com.learnbot.service.agentloop`) instead of adding more files to the already crowded flat areas. `CodeAgentLoopRunnerService.previewNextStep` consumes `CodeAgentLoopPreviewService.nextAction`; when the action is `QUEUE_READ_ONLY_OBSERVATION` and the caller supplies Local Agent/workspace identity, it prepares a read-only `git.status` candidate with `USER_LOCAL_AGENT`, `NOT_REQUIRED`, `freshObservationOnly=true`, and `mutationAllowed=false`. For stop, ask-user, wait, or missing agent/workspace cases it returns a clear no-request/wait decision. Added `POST /api/code-agent/loop/runner/preview`, preserving repository-space authorization. Request creation, enqueue, push, claim, mutation, final result, publication, and acknowledgement remain disabled in the response and candidate.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest" test` ran 15 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopRunnerServiceTest,CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 66 tests with 0 failures.
- Remaining risk: This still does not execute the next request. It proves the server can turn the persisted next-action decision into a bounded read-only Local Agent tool candidate, but it does not yet enqueue that candidate, claim it through the Local Agent, observe the result, loop again, select a model-driven patch/test/rollback step, refresh RAG after mutation, or publish the final answer.
- Next recommended step: Continue Milestone 6/7 with the smallest safe execution slice: allow the runner to enqueue only the prepared read-only `git.status` candidate when `nextAction=QUEUE_READ_ONLY_OBSERVATION`, agent/workspace are present, and the candidate is non-side-effectful; then verify Local Agent claim/complete records a new observation and feeds the existing next-decision event path. Keep side-effectful `patch.apply`, tests, rollback, and final publication behind explicit approval/release.

### 2026-07-01 - Run 455

- Completed: Added the next narrow Milestone 6/7 loop surface needed for Codex-like behavior without enabling mutation. `CodeAgentLoopPreviewService.nextAction` now reads recent loop timelines and returns a structured read-only next action from the latest relevant audit events: `QUEUE_READ_ONLY_OBSERVATION` after an accepted `LOOP_NEXT_DECISION_RECORDED`, `STOP_WITH_REASON` after stop outcomes or failed-observation decisions, `WAIT_FOR_APPROVAL` when approval/observation state is incomplete, and `ASK_USER` when no timeline or only preview evidence exists. Added `CodeAgentLoopNextActionResponse` and `GET /api/code-agent/loop/next-action`, with repository-space authorization preserved. Every response keeps request creation, push, claim, mutation, final result, publication, and acknowledgement disabled.
- Verified: Restricted Maven still fails because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest" test` ran 11 tests with 0 failures. Approved broader loop verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopPreviewServiceTest,CodeAgentControllerTest,CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 62 tests with 0 failures.
- Remaining risk: This is still a read-only decision surface, not the full Codex-like loop runner. It does not yet call a model to select the next tool, create a follow-up Local Agent request, release/claim approved mutation work, execute project tests through the durable queue, refresh RAG after edits, or publish a final user acknowledgement.
- Next recommended step: Continue Milestone 6/7 by adding the smallest loop-runner slice that consumes `nextAction`, selects or prepares one safe follow-up tool request only when the action allows it, and keeps side-effectful tools behind explicit approval/release. Do not broaden Milestone 8 again until the runner can complete at least one observe -> decide -> next request/stop cycle.

### 2026-07-01 - Run 454

- Completed: Returned the immediate workstream from Milestone 8 expansion to the Milestone 6/7 agent loop gap. `CodeAgentLoopTimelineRepository` now appends an audit-only `LOOP_NEXT_DECISION_RECORDED` event after a Local Agent observation is persisted. The event records whether the observation was accepted or should stop the loop, preserves source/release/request context, marks follow-up tool selection as possible only after successful observations, and keeps request creation, push, claim, mutation, final result publication, and acknowledgement disabled. `LocalAgentToolGatewayService.complete` now writes the observation event, then the next-decision event, then any failure/timeout/cancellation/disconnection stop outcome. This makes the persisted timeline closer to the intended Codex-like loop shape: observe tool result -> decide next step or stop, without opening release/claim or mutation.
- Verified: `node scripts\quality\local-agent-flow\seed-approved-tool-executions.test.mjs` passed. Restricted Maven failed at dependency resolution because sandbox networking blocks Maven Central. Approved focused backend verification passed: `.\.tools\apache-maven-3.9.9\bin\mvn.cmd -f backend\pom.xml "-Dtest=CodeAgentLoopTimelineRepositoryTest,LocalAgentToolGatewayServiceTest" test` ran 51 tests with 0 failures. The new repository regression proves `LOOP_NEXT_DECISION_RECORDED` is `SERVER_LOCAL`, `COMPLETE_OR_PAUSE`, `may_mutate=false`, preserves source/release ids, and leaves request creation, push, claim, mutation, final result, publication, and acknowledgement disabled.
- Remaining risk: This still is not a complete Codex-like agent. The loop now records the next decision after observations, but it does not yet perform a real model-driven next-step selection, create the next safe tool request from that decision, release approved mutation requests, run project-specific tests through a durable loop runner, update RAG freshness after mutation, publish a final answer, or save user acknowledgement. It is a necessary loop-shape slice, not the full agent.
- Next recommended step: Continue Milestone 6/7 by adding a narrow read-only loop runner/decision surface that consumes the latest `LOCAL_AGENT_OBSERVATION_RESULT` and `LOOP_NEXT_DECISION_RECORDED` events and returns a structured next action (`ASK_USER`, `WAIT_FOR_APPROVAL`, `QUEUE_READ_ONLY_OBSERVATION`, or `STOP_WITH_REASON`) without enabling mutation. Only after that should the release/claim path for approved mutation be considered.

### 2026-07-01 - Run 453

- Completed: Added a dry-run-by-default quality seed contract for the next real Spring Boot/PostgreSQL Local Agent queue smoke, while correcting the roadmap priority back toward the Milestone 6 agent loop gap. `scripts/quality/local-agent-flow/seed-approved-tool-executions.mjs` emits reviewed SQL for four already-approved `local_agent_tool_executions` rows in the expected flow order: `patch.apply`, `command.runAllowed`, `git.status`, and `rollback.restore`. It requires explicit user/agent/workspace ids, keeps `executionTarget=USER_LOCAL_AGENT`, `approvalState=APPROVED`, `status=APPROVED`, includes cleanup SQL, and does not touch PostgreSQL unless `--execute` is passed. Added `seed-approved-tool-executions.test.mjs` and wired it into the regression harness as `local-agent-approved-flow-seed-contract`.
- Verified: `node scripts\quality\local-agent-flow\seed-approved-tool-executions.test.mjs`, `node scripts\quality\local-agent-flow\assert-approved-execution-flow-report.test.mjs`, and `dotnet build local-agent\LearnBot.LocalAgent.csproj` passed. A restricted harness run reached and passed the new `local-agent-approved-flow-seed-contract` step but still failed at backend Maven dependency resolution because sandbox networking blocks Maven Central. Approved full regression harness passed in `.tmp\quality\regression-harness-20260701-230525.json`.
- Remaining risk: The seed contract is preparatory evidence, not a completed agent loop. It does not yet run the Spring Boot/PostgreSQL durable queue end to end with a paired Local Agent, and it does not complete Milestone 6's core loop requirement: central planning/tool decision, Local Agent execution, observation return, and next-step decision until completion or approval. Continuing to expand Milestone 8 quality without closing that loop would create misleading confidence.
- Next recommended step: Keep the ultimate product goal unchanged, but change the immediate execution order: stop broadening Milestone 8 for now and return to Milestone 6/7 loop completion. The next narrow implementation should connect an actual loop timeline/runner slice to the already-proven Local Agent queue evidence: plan one step, enqueue or reference an approved Local Agent tool request, record the observation on the loop timeline, and produce the next non-mutating decision/stop outcome without opening broad mutation release.

### 2026-07-01 - Run 452

- Completed: Continued Milestone 8 with the next smallest server-orchestration proof without opening the production release/claim gate. Added `approved-server-queue-flow-contract`, a Local Agent self-test that starts a disposable local HTTP queue, has the real agent polling path call `/api/local-agents/tools/next`, executes approved `patch.apply`, `command.runAllowed`, `git.status`, and `rollback.restore` against a temporary approved workspace, posts each completed response back through `/api/local-agents/tools/{requestId}/response`, and writes the same approved-flow evidence report. The first `HttpListener` approach was replaced with a `TcpListener` mini HTTP server because the Windows listener failed with a handle error in this environment. The test keeps `serverLocalMutation=false`, uses a temporary workspace, requires approved tool requests, preserves session/user/agent/workspace identity, and leaves the public user-facing release gate closed.
- Verified: `dotnet build local-agent\LearnBot.LocalAgent.csproj` passed. `dotnet run --project local-agent -- self-test approved-server-queue-flow-contract --report .tmp\quality\local-agent-approved-flow\approved-server-queue-flow-report.json` passed with `patch.apply`, `command.runAllowed`, `git.status`, and `rollback.restore` all `SUCCEEDED`. `node scripts\quality\local-agent-flow\assert-approved-execution-flow-report.mjs --report .tmp\quality\local-agent-approved-flow\approved-server-queue-flow-report.json` accepted the report. The regression harness now includes `local-agent-approved-server-queue-flow-contract` and its report assertion; restricted execution still hit sandbox Maven Central denial, but approved full execution passed in `.tmp\quality\regression-harness-20260701-230103.json`.
- Remaining risk: This proves Local Agent HTTP polling/response queue behavior with a disposable in-process quality server, not yet the full Spring Boot/PostgreSQL production queue with durable seeded mutation rows and UI-visible final acknowledgement. It is stronger than the direct in-process Local Agent mutation proof, but the final product still needs a disposable live server queue smoke that pairs a real Local Agent, persists approved mutation/tool rows, verifies completed responses in PostgreSQL, and then handles RAG freshness or stale-index evidence before any normal release gate is opened.
- Next recommended step: Add the disposable live Spring Boot/PostgreSQL Local Agent mutation smoke or, if that is too large for one run, first add a dry-run-by-default quality seeding/assertion tool for approved `local_agent_tool_executions` rows that can be reviewed before execution.
