docker compose up -d --build  : using CPU
.\scripts\up.ps1 -Build  : auto using GPU
./scripts/up.ps1 -NoBuild
docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build  : using GPU


powershell.exe -ExecutionPolicy Bypass -File .\scripts\up.ps1 -Build

# LearnBot

LearnBot is a local RAG knowledge workspace for approved web pages, CSV/Excel files, and private Git repositories.
Uploaded source files are stored in MinIO. Git repositories are cloned into a Docker volume and indexed into PostgreSQL/pgvector.

## Stack

- Frontend: React + Vite
- Backend: Spring Boot
- Database: PostgreSQL + pgvector
- Local LLM runtime: Ollama
- Default chat model: `qwen3:8b-q4_K_M`
- Default embedding model: `bge-m3`

## Run

From the project root:

```bash
docker compose up -d --build
```

On Windows, use the helper script if you want GPU acceleration with CPU fallback:

```powershell
.\scripts\up.ps1 -Build
```

To build the deployment images with the GPU Compose overlay without starting services:

```bash
docker compose -f docker-compose.yml -f docker-compose.gpu.yml build
```

The script uses `docker-compose.gpu.yml` when `nvidia-smi` is available and Docker can attach the GPU to the Ollama container. If GPU startup fails, it starts the normal CPU-compatible Compose stack instead. To force CPU mode:

```powershell
.\scripts\up.ps1 -Cpu -Build
```

Open:

- Frontend Nginx: http://localhost:8083
- Backend: http://localhost:8080
- Ollama: http://localhost:11436
- MinIO API: http://localhost:19000
- MinIO Console: http://localhost:19001

Check whether Ollama and the optional reranker are using GPU memory while models are loaded:

```bash
docker compose exec ollama ollama ps
curl http://127.0.0.1:18081/ready
curl -X POST http://127.0.0.1:18081/unload
nvidia-smi
```

The GPU Compose overlay keeps the reranker disabled by default. Enable it only when reranking quality is worth the extra VRAM use:

```bash
LEARNBOT_RERANKER_ENABLED=true
LEARNBOT_RERANKER_WARMUP_ON_STARTUP=false
LEARNBOT_RERANKER_IDLE_UNLOAD_SECONDS=300
```

## Storage and Migration

Runtime data is not stored inside the Git-tracked project folder by default.

- PostgreSQL data: Docker named volume `learnbot_postgres-data`
- MinIO uploaded originals: Docker named volume `learnbot_minio-data`
- Git working copies: Docker named volume `learnbot_code-repos`

Use `pg_dump` and restore for PostgreSQL migration between PCs:

```bash
docker compose exec postgres pg_dump -U learnbot -d learnbot > learnbot-db.sql
```

Restore on another PC after starting PostgreSQL:

```bash
docker compose exec -T postgres psql -U learnbot -d learnbot < learnbot-db.sql
```

Git working copies can be recreated by reindexing registered repositories. MinIO data should be backed up separately if uploaded file originals must move with the database.

If encrypted Git tokens are stored, keep the same `LEARNBOT_CODE_CREDENTIAL_SECRET` value when migrating the database. Changing that secret makes previously stored tokens unreadable; re-enter the token from the UI if that happens.

## Disk retention and cleanup

LearnBot keeps search-critical data by default and only purges operational data automatically.

- Operation/crawl/diagnostic logs are retained for 14 days by default.
- Admin/security audit logs are retained for 180 days by default.
- RAG export ZIP files are retained for 14 days by default.
- Deleted-source originals and MinIO objects with no `source_objects` DB reference are deleted only after a 7-day grace period.
- Document/code chunks, embeddings, graph data, active source originals, saved answers, and Ollama models are not automatically deleted.

Admins can inspect and run cleanup from `GET /api/admin/storage/retention/preview` and `POST /api/admin/storage/retention/run`. The run endpoint defaults to dry-run unless `{"dryRun": false}` is passed. Docker container logs are size-rotated through compose logging options; Docker build cache remains manual via:

```powershell
.\scripts\cleanup.ps1 -DockerCache -DryRun
.\scripts\cleanup.ps1 -DockerCache -Until 168h
```

Named Docker volumes are intentionally never pruned by the helper script.

## Code RAG

Git repositories support public/no-auth and username/token authentication for HTTP(S), plus standard Git SSH URLs when the container has usable SSH credentials. Token storage is opt-in from the UI. Stored tokens are encrypted in PostgreSQL and reused for later manual indexing.

Indexing is asynchronous and can be cancelled. Reindexing creates a new index version and only re-embeds changed files; unchanged files reuse existing chunk embeddings. A failed reindex does not replace the active index.

The UI also provides:

- repository deletion
- failed/cancelled indexing history cleanup
- source file browsing with line highlights
- symbol reference lookup for method, class, control, and event names

### Conversational Code RAG

Code questions support conversation-aware follow-ups. A follow-up question keeps the user's original question for the final answer, but uses a separate effective search question for classification, retrieval, query expansion, and evidence ranking.

When a code conversation has previous turns, LearnBot extracts code anchors from prior evidence, including chunk id, file path, class, symbol, method, and line range. Those chunks are reloaded from PostgreSQL as pinned evidence before normal hybrid search runs. Pinned evidence receives a small ranking boost, but it is ignored when it is not relevant to the current question. If pinned evidence is missing, deleted, inaccessible, or unrelated, the request falls back to the normal code RAG search path.

The prompt includes recent Q/A summaries and previous code anchors in a separate conversation section. This section is used only to resolve references such as "that method" or "the previous file"; cited facts must still come from the current source-code context.

Diagnostics include conversation-specific notes such as whether conversation context was used, how many anchors were found, and how many pinned chunks were included in the final evidence.

### Code Graph RAG

Code indexing builds a versioned graph in PostgreSQL alongside chunks and embeddings. Neo4j is not required. Java source is analyzed with JavaParser Symbol Solver, while C# source is analyzed with a Roslyn semantic analyzer included in the backend image.

Resolved methods use qualified signatures containing their declaring type and parameter types. This separates overloads and same-named methods in different packages or namespaces. A relationship found only by text matching is stored conservatively as `REFERENCES`; it is not promoted to `CALLS` unless JavaParser, Roslyn, or the validated LLM fallback resolves it.

The graph can contain these relationships:

- structure: `DEFINES`, `CONTAINS`, `EXTENDS`, `IMPLEMENTS`, `OVERRIDES`
- execution and dependencies: `CALLS`, `INJECTS`, `RETURNS`, `ACCEPTS`, `THROWS`
- code semantics: `ANNOTATED_BY`, `READS_FIELD`, `WRITES_FIELD`
- framework semantics: `USES_ENTITY`, `MAPS_TO_TABLE`, `EXPOSES_ENDPOINT`
- UI semantics: `HANDLES_EVENT`, `BINDS_TO`
- conservative fallback: `REFERENCES`

Graph search uses bounded hop-by-hop traversal instead of an unbounded recursive query. It prevents cycles, limits seed nodes, per-node edges, per-hop candidates, and total traversal rows, then returns the best path for each related chunk. Search strategy depends on the question:

- call-flow questions traverse `CALLS` and related execution edges forward
- impact questions traverse callers and dependencies in reverse
- UI questions prioritize XAML event, binding, endpoint, and handler edges
- overview questions prioritize containment, inheritance, implementation, and injection edges

Graph-expanded evidence includes the path, edge sequence, depth, and path score. Code answers can therefore explain a connected flow such as `Controller -> Service -> Repository` instead of listing unrelated chunks.

Configure graph behavior with environment variables:

```bash
LEARNBOT_CODE_GRAPH_ENABLED=true
LEARNBOT_CODE_GRAPH_MAX_HOP=2
LEARNBOT_CODE_GRAPH_MAX_EXPANDED_RESULTS=12
LEARNBOT_CODE_GRAPH_MAX_SEED_NODES=24
LEARNBOT_CODE_GRAPH_MAX_EDGES_PER_NODE=12
LEARNBOT_CODE_GRAPH_MAX_CANDIDATES_PER_HOP=200
LEARNBOT_CODE_GRAPH_MAX_TRAVERSAL_ROWS=1000
LEARNBOT_CODE_GRAPH_LLM_RELATION_ENABLED=true
LEARNBOT_CODE_GRAPH_MAX_LLM_FILES=80
LEARNBOT_CODE_GRAPH_ROSLYN_ANALYZER_PATH=/app/roslyn/LearnBot.RoslynAnalyzer.dll
LEARNBOT_CODE_GRAPH_ROSLYN_MODE=AUTO
LEARNBOT_CODE_GRAPH_ROSLYN_TIMEOUT_SECONDS=120
LEARNBOT_CODE_GRAPH_EVIDENCE_RANKING_ENABLED=true
LEARNBOT_CODE_GRAPH_EVIDENCE_RANKING_DEBUG=false
LEARNBOT_CODE_GRAPH_DEPENDENCY_RESOLUTION_ENABLED=true
LEARNBOT_CODE_GRAPH_DEPENDENCY_ALLOWED_REPOSITORIES=https://repo.maven.apache.org/maven2
LEARNBOT_CODE_GRAPH_DEPENDENCY_MAX_ARTIFACTS=256
LEARNBOT_CODE_GRAPH_DEPENDENCY_MAX_BYTES=536870912
LEARNBOT_CODE_GRAPH_DEPENDENCY_TIMEOUT_SECONDS=120
```

`LEARNBOT_CODE_GRAPH_MAX_HOP` is constrained to 1-4 during traversal. When a traversal budget is reached, the best bounded results are returned with `graphTraversalTruncated` metadata instead of failing the search.

Code GraphRAG evidence ranking is deterministic and enabled by default. It combines hybrid search score, query term matches, graph path score, relationship type, graph depth, question intent, structured code evidence, and diversity penalties into `evidenceScore` metadata while preserving the original search `score`.

Roslyn `AUTO` mode selects `SAFE_SOLUTION`, `SAFE_PROJECT`, or `SIMPLE` from repository contents. `SAFE_*` modes parse project and solution descriptors statically; MSBuild targets, source generators, and repository code are never executed. Legacy `PROJECT` and `SOLUTION` config values are accepted as aliases for `SAFE_PROJECT` and `SAFE_SOLUTION`. A future `MSBUILD_WORKSPACE` mode must run only in an explicitly enabled isolated worker with network, time, and memory limits. Java dependency resolution also parses Maven/Gradle declarations without running the build. It uses the persistent `.dependency-cache` under the code workspace and only downloads release artifacts from configured HTTPS repository allow lists.

The optional LLM stage runs as a durable post-index enrichment job after the deterministic graph is active. Pending work survives restarts, retries up to three times, and is skipped when a newer index replaces it. It only accepts known graph node keys and approved relationship types, and records output with lower confidence. If JavaParser, dependency resolution, Roslyn, the LLM, or graph retrieval fails, indexing/search continues with the available deterministic graph or the existing keyword/vector search.

Each indexing job records `SUCCESS`, `PARTIAL`, `FAILED`, or `SKIPPED` diagnostics for the base graph, Java classpath, Java semantic analysis, Roslyn, and LLM enrichment. The Code workspace exposes these under **遺꾩꽍 吏꾨떒**. They are also available from:

```text
GET /api/code/repositories/{repositoryId}/jobs/{jobId}/diagnostics
```

Existing active indexes remain readable after an upgrade. Reindex each repository to create qualified signature nodes, expanded relationships, and multi-hop paths using the new analyzers. A failed reindex does not replace the previous active index.

The backend deployment image includes both Java 17 and the .NET 8 runtime required by Roslyn. Build deployment images with the GPU Compose overlay shown in the Run section; GPU access is assigned to Ollama, while source analysis remains CPU-based.

## Model Changes

Change models with environment variables:

```bash
LLM_MODEL=qwen3:8b-q4_K_M, qwen3:4b-instruct
EMBEDDING_MODEL=bge-m3
```

Changing the chat model is a config change. Vector search still works without the chat LLM as long as the embedding model is available. If the embedding model is unavailable, search falls back to keyword search.

Changing the embedding model can change vector dimensions, so existing documents must be reindexed and the pgvector column dimension must match the new model.

## Crawling Policy

Web ingestion is allow-list based. Configure allowed domains with:

```bash
LEARNBOT_CRAWLER_ALLOWED_DOMAINS=example.com,docs.spring.io,ollama.com
```

Web ingestion uses the allow list, robots.txt checks, basic rate limiting, and crawl audit logs. By default, the UI enables recursive crawling for the same host and descendant path of the submitted URL. The default recursive limits are depth 2 and 30 fetched pages.

```bash
LEARNBOT_CRAWLER_MAX_DEPTH=2
LEARNBOT_CRAWLER_MAX_PAGES_PER_REQUEST=30
LEARNBOT_CRAWLER_MIN_CONTENT_CHARS=200
```

Each crawled page is stored as a separate document under the same source so RAG citations keep the original page URL.

## Document RAG Indexing

Document ingestion is split into a fast searchable phase and slower quality enrichment phases.

Source status values:

- `INDEXING`: original extraction, chunking, and embedding are still running; the source is not searchable yet.
- `SEARCHABLE`: original chunks and deterministic context are stored; users can search and ask questions.
- `READY`: enabled post-processing completed or was skipped by configuration.
- `PARTIAL`: original search is available, but at least one post-processing stage failed or is waiting for retry.
- `FAILED`: extraction, embedding, or storage failed before the source became searchable.

The `INDEXED` value is still accepted for legacy data and import compatibility, but new document sources use `SEARCHABLE`, `READY`, or `PARTIAL` after successful base indexing.

Document graph rebuild runs as a durable background job instead of blocking ingestion. It stores graph nodes and edges in PostgreSQL using batched inserts. If graph rebuild fails, the source remains searchable and the UI marks it as `PARTIAL`.

LLM document context enrichment also runs as a background job. It replaces only generated `document_context` chunks, so original chunks remain available if the enrichment fails.

Document post-processing diagnostics are recorded for graph rebuild and LLM enrichment. The Documents UI exposes diagnostics next to the indexing job and shows retry buttons beside failed stages, so users can see why a retry is available before clicking it. Retry requeues only the failed post-processing stage; it does not re-run full document extraction or embedding.

Relevant APIs:

```text
GET  /api/document-indexing/jobs
GET  /api/document-indexing/jobs/{jobId}
GET  /api/document-indexing/jobs/{jobId}/diagnostics
POST /api/document-indexing/jobs/{jobId}/retry-enrichment
POST /api/document-indexing/jobs/{jobId}/retry-graph
```

## Conversational Document RAG

Document questions also support conversation-aware follow-ups. Conversational document RAG keeps three concepts separate:

- `originalQuestion`: the exact text typed by the user.
- `effectiveQuestion`: a short standalone search question generated from the follow-up and the previous document evidence.
- conversation focus: recent Q/A summaries and prior document evidence anchors included in the prompt as a separate section.

Previous document evidence is not appended to the search query as raw chat history. Instead, LearnBot extracts document anchors from prior answer evidence, including chunk id, document id, title, source URI, chunk index, page number, section title, heading path, and document type. The referenced chunks are reloaded from PostgreSQL and merged as pinned context before normal document retrieval. Pinned context is filtered for relevance and receives a small boost; if it cannot be loaded or is unrelated, the request falls back to normal document retrieval.

The final answer still cites only evidence chunks present in the current response context. Previous answers are used only to resolve follow-up references such as "that document", "that condition", or "the previous source".

Document conversation turns are stored in the RAG conversation tables. They keep the user question, generated effective question, answer, citations, evidence, diagnostics, and metadata. Conversation retention follows the existing RAG conversation retention policy.

### Document RAG streaming

The backend exposes SSE-compatible endpoints:

```text
POST /api/rag/ask/stream
POST /api/code/ask/stream
Accept: text/event-stream
```

Retrieval, context assembly, citation selection, and conversation preparation stay on the existing synchronous path. Only the Ollama chat call is streamed. The endpoint emits structured SSE events:

- `metadata`: request mode and whether the request is conversational.
- `evidence`: retrieved evidence available before the answer finishes.
- `delta`: buffered model text, emitted in small batches instead of per-token.
- `replace`: server-side fallback or answer repair replaced the visible text.
- `done`: final `AskResponse`.
- `error`: failure details.

Streaming cleanup is tied to Reactor `Flux`/`Mono.doFinally`. `CANCEL`, `ON_COMPLETE`, and `ON_ERROR` all release the stream permit; the implementation does not rely on `SseEmitter.onCompletion`, `onTimeout`, or `onError` for permit cleanup.

Streaming failure rules:

- If Ollama fails before the first `delta`, the server may fall back to the next candidate model or the frontend may fall back to the non-streaming `/ask` endpoint.
- If Ollama fails after the first `delta`, the stream emits an `error` event and no conversation turn is saved.
- Partial answers are visible while streaming but are not saveable and are not persisted as conversation turns.
- Client abort through `AbortController` must cancel the backend stream, dispose the Ollama subscription, release the permit, and avoid saving a conversation turn.

Regression tests for this area should include mid-stream failure and client abort:

- first-delta-before-failure: `error` event, partial answer not saved, permit returned.
- failure-before-first-delta: candidate model fallback or non-streaming JSON fallback, permit returned.
- client abort: browser `AbortController` cancellation, backend Flux cancel, Ollama stream subscription disposal, permit returned, no conversation turn saved.

## API

- `POST /api/sources/web` with `{ "url": "https://example.com/docs", "recursive": true, "maxDepth": 2, "maxPages": 30 }`
- `POST /api/sources/files` with multipart field `file`
- `GET /api/documents`
- `GET /api/document-indexing/jobs`
- `GET /api/document-indexing/jobs/{jobId}/diagnostics`
- `POST /api/document-indexing/jobs/{jobId}/retry-enrichment`
- `POST /api/document-indexing/jobs/{jobId}/retry-graph`
- `GET /api/documents/{documentId}`
- `POST /api/documents/{documentId}/reindex`
- `DELETE /api/documents/{documentId}`
- `POST /api/search` with `{ "query": "..." }`
- `POST /api/rag/ask` with `{ "question": "...", "mode": "qa" }`
- `POST /api/rag/ask/stream` with `{ "question": "...", "mode": "qa", "conversational": true }`, returns `text/event-stream`
- `GET /api/rag/conversations?domain=DOCUMENT`
- `GET /api/rag/conversations?domain=CODE`
- `GET /api/rag/conversations/{conversationId}`
- `DELETE /api/rag/conversations/{conversationId}`
- `POST /api/code/repositories` with `{ "gitUrl": "https://host/project.git", "branch": "main", "authType": "NONE" }`
- `POST /api/code/repositories/{repositoryId}/index`
- `DELETE /api/code/repositories/{repositoryId}`
- `DELETE /api/code/repositories/{repositoryId}/jobs`
- `POST /api/code/repositories/{repositoryId}/jobs/{jobId}/cancel`
- `GET /api/code/repositories/{repositoryId}/jobs`
- `GET /api/code/repositories/{repositoryId}/files`
- `GET /api/code/repositories/{repositoryId}/files/{fileId}`
- `POST /api/code/references` with `{ "repositoryId": "...", "symbol": "MainWindow" }`
- `POST /api/code/search` with `{ "repositoryId": "...", "query": "..." }`
- `POST /api/code/ask` with `{ "repositoryId": "...", "question": "...", "mode": "locate" }`
- `POST /api/code-agent/loop/preview` with `{ "repositoryId": "...", "instruction": "...", "maxSteps": 6 }`
- `GET /api/code-agent/loop/timelines?repositoryId=...&limit=5`

RAG answer responses include `confidence` and `diagnostics` fields so the UI can show when a response was generated by fallback logic or has weak evidence. The frontend renders natural-language RAG answers as Markdown, including headings, lists, inline code, and fenced code blocks.

Conversational RAG responses can also include `conversationId`, `turnId`, and `rewrittenQuestion`. For conversational requests, `rewrittenQuestion` is the effective standalone search question, not raw chat history.


## LearnBot Service Final Architecture Plan

LearnBot's long-term product direction is a web-first local RAG agent service with per-user Local Agents for user-owned workspace changes.

```text
User
 |
 |  Web conversation, review, approvals
 v
Web UI / optional learnbot CLI
 |
 v
Central LearnBot Server
 |
 +-- Document Agent  -> Document RAG
 |
 +-- Code Agent      -> Graph Code RAG
 |
 +-- Planning / tool orchestration
 |
 +-- Change proposal / diff validation
 |
 +-- Approval / session / audit / rollback metadata
 |
 |  Outbound tool request over WebSocket or equivalent
 v
User Local Agent / learnbot CLI
 |
 +-- Approved workspace roots only
 |
 +-- Read files / write approved patches
 |
 +-- Git status / diff
 |
 +-- Allowlisted test and build commands
 |
 +-- Rollback restoration
 |
 +-- Optional local diagnostics and logs
```

Responsibility split:

- The central server owns retrieval, ranking, planning, model calls, validation, approvals, conversation history, diagnostics, and orchestration.
- The Local Agent owns user-PC side effects: file writes, test/build execution, git operations, rollback restoration, and workspace-local diagnostics.
- Server-local apply/test/rollback may exist only as a prototype, shared sandbox, migration bridge, or admin/debug capability. It must not become the default product path for user-owned repository changes.
- Server-local Patch Agent mutation is disabled by default. Set `LEARNBOT_CODE_SERVER_LOCAL_MUTATION_ENABLED=true` only for prototype/admin-debug runs.
- The preferred network model is outbound connection from each Local Agent to the central server. The server should not depend on directly reaching user PCs by LAN IP or localhost.
- The CLI should eventually feel natural from PowerShell, similar to running `codex`: users can run `learnbot` to pair, start/stop/status the agent, register workspaces, open the web UI, run diagnostics, view logs, and optionally use lightweight CLI chat/fix/review commands.

### Local Agent Protocol Baseline

The shared backend contract for future Local Agent work starts with `AgentExecutionTarget`, `LocalAgentToolName`, `LocalAgentToolRequest`, and `LocalAgentToolResponse` DTOs. Initial tools are `agent.status`, `agent.doctor`, `workspace.list`, `workspace.add`, `file.read`, `patch.apply`, `git.status`, `git.diff`, `command.runAllowed`, and `rollback.restore`.

Side-effectful tools require approval metadata and must execute through `USER_LOCAL_AGENT` for user-owned workspaces. Failure states are explicit through `LocalAgentFailureCode`, including disconnected agents, unapproved workspaces, path escape, unsafe tools, approval denial, timeout, context mismatch, test failure, rollback refusal, and generic tool failure.

The central gateway skeleton exposes `GET /api/local-agents/status` for the current user. Users can issue a one-time-visible Local Agent credential with `POST /api/local-agents/pairing-token`; the server stores only a hash in `local_agent_tokens`. Authenticated web users can inspect non-secret token metadata with `GET /api/local-agents/tokens` and revoke their own token with `DELETE /api/local-agents/tokens/{tokenId}`. Token list responses include id, agent id, label, expiry, created time, last-seen time, revoked time, and active state, but never the raw credential. A Local Agent can then call `POST /api/local-agents/heartbeat` with `X-Local-Agent-Token`, agent id, version, capabilities, approved workspace summaries, configured transport, active transport, WebSocket failure count, and next WebSocket retry time.

Tool routing now has a durable polling baseline: the server records queued Local Agent work in `local_agent_tool_executions`, a token-authenticated Local Agent can claim the next request with `GET /api/local-agents/tools/next`, and it can persist a structured result with `POST /api/local-agents/tools/{requestId}/response`. For internal smoke and diagnostics, authenticated web users can enqueue only safe read-only requests through `POST /api/local-agents/tools/read-only` and inspect the persisted result through `GET /api/local-agents/tools/{requestId}`. That diagnostic enqueue path currently allows only `file.read`, `git.status`, and `git.diff`; mutation, command execution, patch, test, and rollback requests remain blocked. WebSocket transport work follows `docs/local-agent-websocket-transport.md`: the server skeleton is behind `LEARNBOT_LOCAL_AGENT_WEBSOCKET_ENABLED=false` by default, accepts only token-authenticated messages, registers connected sessions after `hello`/`heartbeat`, can push persisted read-only `tool.request` envelopes to connected agents, and can persist `tool.response` envelopes through the same completion service. The Local Agent CLI now accepts `polling`, `websocket`, and `auto` transport modes; `websocket` and `auto` try a live WebSocket hello/heartbeat, process pushed read-only `tool.request` messages during a bounded receive window, send `tool.response`, and keep durable polling active for fallback. The Code workspace shows disconnected/stale state, active transport state, WebSocket retry state, and Local Agent token metadata/revocation controls so side-effectful local work does not silently fall back to server-local execution and stale pairings can be retired.

Patch proposal and mutation execution are intentionally separated. `GET /api/code-agent/mutation-policy` exposes the current boundary for the web UI: patch proposals are available, the intended execution target for user-owned mutation is `USER_LOCAL_AGENT`, Local Agent patch/test/rollback execution is still disabled, and server-local mutation remains prototype/admin/debug-only behind `LEARNBOT_CODE_SERVER_LOCAL_MUTATION_ENABLED=false` by default.
Milestone 6 starts with a non-executing loop preview at `POST /api/code-agent/loop/preview`. It resolves repository access, persists an audit-only row in `code_agent_loop_timelines`, records preview events in `code_agent_loop_timeline_events`, and returns a bounded agent-loop outline with max steps, timeout, plan/tool/approval/observation/completion phases, stop conditions, and explicit `mutationEnabled=false`/cancellation-disabled flags. `GET /api/code-agent/loop/timelines` resolves the same repository access and returns recent persisted audit timelines with ordered events for the current user/repository, limited to a bounded read-only history window. The Code workspace can request and render the live preview as read-only loop telemetry, including mutation/timeline/cancellation state, planned phases, stop conditions, and warnings; it also fetches and renders recent persisted loop history with event `mayMutate=false` telemetry after preview refresh or a manual history refresh. The event rows classify the preview phases as model-decision, tool-selection, approval-checkpoint, observation-wait, and completion-decision previews, then append audit-only timeout, cancellation, final-result, and stop-outcome policy records so weak evidence, missing agent, tool failure, and approval denial have explicit non-executing outcomes before any real loop runner exists. When a Local Agent repository observation or patch dry-run response completes and the original tool request input identifies the code repository, the server appends a `LOCAL_AGENT_OBSERVATION_RESULT` event to the correlated loop timeline when `loopId` is present, otherwise to the latest audit timeline for that user/repository. That event records request/session/agent/workspace ids, status, source/release attempt linkage, fresh-observation, dry-run, mutation-applied, repository verification, snapshot-created, and warning details while keeping `may_mutate=false`. When a user approves or denies a prepared Local Agent tool request and the stored request input identifies the code repository, the server also appends a `LOCAL_AGENT_APPROVAL_DECISION` event with approval state and held/rejected status using the same `loopId` correlation fallback while keeping the request non-claimable unless a future explicit release path enables it. This preview/history path does not create, push, claim, release, or execute Local Agent mutation requests; it fixes the future loop contract before real apply/test/rollback/RAG-freshness work is enabled.
For the next migration step, `POST /api/code-agent/local-patch-request` can prepare a non-executing `patch.apply` Local Agent tool request. It validates the unified diff, records repository/workspace/agent ids, target files, expected indexed file hashes, snapshot requirement, snapshot policy, rollback policy, and stale-index policy, then persists the request as `APPROVAL_REQUIRED`. That request is not pushed to the Local Agent and cannot be claimed until a later approval/execution slice explicitly enables it.
The Code workspace can prepare and display this request for review, including request id, status, target files, expected hashes, request warnings, snapshot requirement/policy, rollback policy, stale-index policy, source repository identity, selected Local Agent workspace identity, and workspace/repository verification state. Users can explicitly approve or deny the prepared request through `POST /api/local-agents/tools/{requestId}/approval`. Approval moves it to `APPROVED_HELD`, which records intent but remains non-claimable and non-executing; denial moves it to `REJECTED`. `GET /api/local-agents/tools/{requestId}/readiness` exposes the non-executing preflight checklist for a held patch request: held approval state, user-local target, matching connected agent, approved workspace, `patch.apply` capability, `rollback.restore` capability, request schema, diff/target/expected hash data, snapshot requirement, target-file snapshot policy, rollback policy, stale-index policy, workspace/repository verification, latest linked dry-run snapshot manifest/rollback precondition observations, the latest server-recorded repository observation verification linked by `sourceRequestId` when available, a `snapshotReadiness` summary (`MISSING`, `INVALID`, `PREVIEW_ONLY`, or `CREATED`), a non-executing `rollbackReadiness` summary, a non-mutating `patchReleaseReadiness` summary, a non-public `patchExecutionGate` summary, a typed top-level `releaseAttemptModel` read model, and the explicit release gate. `snapshotReadiness` accepts snapshot evidence only from a non-mutating Local Agent dry-run with `dryRun=true` and `mutationApplied=false`; if mutation has already been applied, the snapshot evidence is marked `INVALID` and remains blocking. `rollbackReadiness` validates only the created manifest contract: target paths must be workspace-relative, snapshot source paths must stay under `files/`, restore preconditions must exist, and user approval must be required. `patchReleaseReadiness` combines the pre-apply prerequisites into one release-blocked view: explicit approval, repository verification, hash/context dry-run preflight, created snapshot, rollback manifest validation, `patch.apply` capability, and `rollback.restore` capability. `patchExecutionGate` mirrors whether those prerequisites are internally visible, but keeps `claimEnabled=false`, `writeHelperEnabled=false`, `mutationEnabled=false`, and `releaseGateEnabled=false`. It also reports `preReleaseRevalidation=REQUIRED_BEFORE_RELEASE`: any future release must run fresh Local Agent repository verification and dry-run/snapshot checks immediately before making the held request claimable instead of trusting stale readiness evidence. The typed `releaseAttemptModel` also remains mirrored inside `patchExecutionGate` for UI compatibility; it uses schema `learnbot.local-agent.patch-release-attempt.v1`, a 120-second stale window, and required evidence keys for release attempt id, source request id, fresh repository verification, fresh patch dry-run, created snapshot manifest, rollback manifest validation, and explicit user release approval. A disabled release-attempt persistence skeleton now exists in `local_agent_patch_release_attempts` with source request linkage, session/user/agent/workspace ids, status, claimable flag, stale window, evidence JSON, failure reasons, and timestamps. Readiness can surface the latest stored attempt as audit-only `latestAttempt` data including `ageSeconds`, `expiresAt`, `freshnessStatus` (`FRESH`, `STALE`, or `UNKNOWN`), `stale`, and `freshObservationRequirements` for repository verification after attempt creation, patch dry-run after attempt creation, snapshot creation after fresh dry-run, rollback validation after fresh snapshot, and user release approval after fresh evidence; these remain informational and never make the attempt claimable. If the internal release path is called while preconditions are visible, it records a non-claimable disabled attempt envelope and still refuses with the release gate disabled; no path creates a claimable attempt yet. The backend also has a disabled-by-default release skeleton that would later transition an `APPROVED_HELD` patch to claimable `APPROVED` only after gate evidence passes and an explicit release flag is enabled; with the current flag disabled, the transition refuses and no request becomes claimable. The release gate is currently disabled, so readiness reports why execution remains blocked instead of applying files. Workspace/repository verification is conservative: the prepared request records `UNVERIFIED` by default, and readiness may compute an effective `VERIFIED` result only from a trusted latest `MATCH` repository observation whose non-skipped checks all match. `MISMATCH` and `UNVERIFIED` observations remain blocking and visible. Even after effective verification passes, `snapshotReadiness=CREATED`, `rollbackReadiness=RESTORE_VALIDATED`, and `patchReleaseReadiness=PRECONDITIONS_READY_RELEASE_DISABLED`, the disabled release gate still prevents patch execution. The Code workspace can queue a read-only repository observation and compare observed local branch/HEAD/remote values with the indexed source metadata, showing `MATCH`, `MISMATCH`, or `UNVERIFIED`. When a `git.status` observation completes with source repository metadata, the backend records that comparison as `repositoryVerification` on the persisted observation output. Repository observation requests also carry `sourceRequestId`, so readiness can surface the latest recorded verification for the held patch request. This remains visibility-only and does not open mutation release. For local observations without release, `POST /api/local-agents/tools/{requestId}/dry-run` creates a separate `patch.apply` tool request with `dryRunOnly=true`, `mutationAllowed=false`, and `sourceRequestId` pointing at the held request. The original `APPROVED_HELD` request stays held, while the Local Agent can claim the dry-run clone and return hash/context observations plus a managed Local Agent snapshot manifest after dry-run preconditions pass. The Local Agent copies target files to `%USERPROFILE%\.learnbot\snapshots\<manifestId>\files\`, writes `manifest.json`, and still returns `mutationApplied=false`; patch execution, test, and rollback controls remain disabled until the later release gates are implemented.
The first real snapshot creation boundary is specified in `docs/local-agent-snapshot-implementation-plan.md`. Snapshot creation support is the recovery baseline before any patch application release is considered.

Disabled release attempts now also expose `releaseAttemptModel.latestAttempt.freshObservationRequestPlan`. This is an audit-only plan for future fresh `git.status` repository verification and non-mutating `patch.apply` dry-run requests linked to both `sourceRequestId` and `releaseAttemptId`. Each planned request keeps `enqueueEnabled=false`, `claimableAfterEnqueue=false`, and `mutationAllowed=false`, so it documents the future release workflow without making any Local Agent mutation claimable. The Code workspace display text is assembled by `freshObservationRequestPlan.js`, with a lightweight regression pinning the read-only `git.status` and dry-run `patch.apply` rows as non-enqueueing, non-claimable, and non-mutating.
The Code workspace renders this plan inside the internal patch execution gate as read-only audit data. The pre-release revalidation and release-attempt model summary text is assembled by `releaseAttemptModelSummary.js`, with a lightweight regression pinning the fresh dry-run/repository-check requirements, schema, stale window, and required evidence count. It does not add release, apply, test, rollback, claim, or fresh-observation enqueue controls.
The internal patch execution gate header, refusal message, and disabled claim/write-helper/release-gate/source-relationship text is assembled by `patchExecutionGateSummary.js`, with a lightweight regression pinning the blocked release-disabled rendering path and null fallback. It remains display-only and does not add claim, write-helper, release, execution, apply, test, rollback, or mutation controls.
The pre-apply patch release readiness checklist display text is assembled by `patchReleaseReadinessSummary.js`, with a lightweight regression pinning precondition, release-gate, mutation, prerequisite pass/blocking, and null fallback rendering. It remains display-only and does not add release, claim, request creation, push, write-helper, apply, test, rollback, or mutation controls.
The recorded repository verification readiness display text is assembled by `repositoryVerificationSummary.js`, with a lightweight regression pinning source-only fallback linkage, release-attempt/source id shortening, skipped-check filtering, 48-character indexed/local value truncation, unknown-value fallback, and null fallback rendering. It remains display-only and does not add repository observation creation, release, claim, request creation, push, write-helper, apply, test, rollback, or mutation controls.
The effective workspace verification readiness display text is assembled by `workspaceVerificationSummary.js`, with a lightweight regression pinning status, blocking release, reason, source, non-blocking, and null fallback rendering. It remains display-only and does not add workspace mutation, release, claim, request creation, push, write-helper, apply, test, rollback, or mutation controls.
The snapshot readiness display text is assembled by `snapshotReadinessSummary.js`, with a lightweight regression pinning invalid non-mutating-dry-run requirements, release-attempt linkage, snapshot state flags, manifest path/file count, readiness check rows, latest dry-run manifest text, observed/blocked fallback status, and the no-manifest guidance. It remains display-only and does not add snapshot creation, release, claim, request creation, push, write-helper, apply, test, rollback, or mutation controls.
The rollback manifest readiness display text is assembled by `rollbackReadinessSummary.js`, with a lightweight regression pinning status, message, release-attempt linkage, blocking release state, file count, user-approval requirement, rollback file-check rows, overflow hiding, and null fallback rendering. It remains display-only and does not add rollback restore, release, claim, request creation, push, write-helper, apply, test, or mutation controls.
The generic readiness check rows are filtered through `readinessChecksSummary.js`, with a lightweight regression pinning pass/blocking row output and excluding snapshot manifest and rollback restore precondition checks that are already rendered by dedicated snapshot/rollback summaries. It remains display-only and does not add release, claim, request creation, push, write-helper, apply, test, rollback, or mutation controls.
The Local Agent dry-run rollback observation display text is assembled by `dryRunRollbackObservationSummary.js`, with a lightweight regression pinning would-restore, restored, tool, restore-scope, minimal, empty-object, and null fallback rendering. It remains display-only and does not add rollback restore, release, claim, request creation, push, write-helper, apply, test, or mutation controls.
The Local Agent dry-run snapshot observation display text is assembled by `dryRunSnapshotObservationSummary.js`, with a lightweight regression pinning would-create, created, scope, location, manifest preview, file hash/context rows, minimal, empty-manifest, and null fallback rendering. It remains display-only and does not add snapshot creation, rollback restore, release, claim, request creation, push, write-helper, apply, test, or mutation controls.
The Local Agent dry-run result header and status rows are assembled by `dryRunResultSummary.js`, with a lightweight regression pinning expected refusal, failure, linked release-attempt evidence, preflight, mutation-applied, snapshot-created, minimal, and null fallback rendering. It remains display-only and does not add release, claim, request creation, push, write-helper, apply, test, rollback, snapshot creation, or mutation controls.
The Local Agent dry-run patch file rows are assembled by `dryRunPatchFilesSummary.js`, with a lightweight regression pinning context-ok/context-blocked formatting, missing-context fallback, and empty rendering. It remains display-only and does not add release, claim, request creation, push, write-helper, apply, test, rollback, snapshot creation, or mutation controls.
When the disabled internal release path creates a non-claimable release attempt envelope, its evidence now also includes `freshObservationRequestTemplates`. These templates describe the future read-only `git.status` request and non-mutating `patch.apply` dry-run request with `sourceRequestId`, `releaseAttemptId`, agent/workspace ids, tool input, approval state, and disabled enqueue/claim flags. The same envelope records a `freshObservationEnqueueBoundary` with `requestCreationEnabled=false`, `pushEnabled=false`, `claimableAfterEnqueue=false`, and `mutationAllowed=false`, documenting the future enqueue gate without creating any `local_agent_tool_executions` row or pushing any request to the Local Agent while the release gate remains disabled.
Readiness lookup now distinguishes future release-attempt-linked observations from older source-only observations. When a disabled release attempt exists, repository verification and patch dry-run lookup first search for observations whose input contains both `sourceRequestId` and `releaseAttemptId`; if none exist, readiness falls back to the existing source-only lookup and marks that data with `observationLinkage.status=SOURCE_ONLY_FALLBACK`. Linked observations are marked `RELEASE_ATTEMPT_LINKED`. This improves auditability without enabling release, claim, patch writes, test execution, rollback, or server-local mutation.
The backend gateway now has a guarded internal `enqueueReleaseAttemptFreshObservations` step for this model. It requires an `APPROVED_HELD` `patch.apply` source request, a latest disabled and non-claimable release attempt matching the same session/user/agent/workspace, a connected Local Agent, and an approved workspace. When those checks pass, it creates only two release-attempt-linked observation requests: read-only `git.status` and `patch.apply` with `dryRunOnly=true`, `mutationAllowed=false`, `sourceRequestId`, `releaseAttemptId`, and `freshObservationOnly=true`. It does not call the held-patch release transition, does not mark the source request claimable, and does not enable actual patch apply, tests, rollback restore, RAG freshness updates, final-answer generation, delivery controls, arbitrary shell execution, unapproved workspace writes, or server-local mutation.
`POST /api/local-agents/tools/{requestId}/fresh-observations` exposes that guarded observation-only step. If no disabled attempt exists yet, the backend first evaluates the refusal-only release boundary so a non-claimable disabled release-attempt envelope can be created when preconditions are already visible; it still refuses mutation release. The endpoint returns the two queued observation requests and never returns a claimable mutation request. The Code workspace shows this as a separate "Queue release fresh observations" action only after readiness indicates pre-apply prerequisites are visible. The existing "Release Local Agent patch disabled" action remains disabled and does not call the release endpoint.
When a release-attempt-linked repository observation or dry-run result is refreshed in the Code workspace, the frontend automatically refreshes the source held patch readiness once. This keeps `releaseAttemptModel.latestAttempt.freshObservationEvidenceStatus`, evidence completeness, snapshot readiness, and rollback readiness aligned with the latest linked observations without opening any release or mutation control. The request/result panels also show `freshObservationOnly` and the short release attempt id when those fields are present.
Backend regression coverage now pins the completed linked-evidence state: when both release-attempt-linked repository verification and dry-run output are present, readiness prefers those linked observations over source-only fallback, reports `freshObservationEvidenceCompleteness.status=ALL_LINKED_RELEASE_DISABLED`, reports `releaseAttemptFinalReadiness.status=READY_RELEASE_DISABLED`, and still keeps release gate, claim, write helper, mutation, request creation, push, apply, test, and rollback restore disabled.
Backend regression coverage also pins the invalid dry-run snapshot release-attempt path: if a dry-run output reports `snapshotCreated=true` but `mutationApplied=true`, readiness marks snapshot and rollback evidence `INVALID`, keeps `patchReleaseReadiness` blocked, and carries that blocked state through `releaseAttemptFinalReadiness`, `releaseEnablementChecklist`, and `releaseAttemptDisplaySummary` without enabling release, claim, write helper, request creation, push, apply, test, rollback restore, or mutation.
Backend regression coverage also pins the fresh-observation enqueue boundary itself: the two queued requests must receive new ids rather than reusing the held source patch request id, the source request remains `APPROVED_HELD` with approval `APPROVED`, and the created patch dry-run request still carries `dryRunOnly=true`, `mutationAllowed=false`, `sourceRequestId`, `releaseAttemptId`, and `freshObservationOnly=true`. MockMvc controller coverage now routes `POST /api/local-agents/tools/{requestId}/fresh-observations`, serializes the response, and verifies the API returns only the two observation request ids, never the held source patch id, with wire-name tool names and no mutation-allowed repository observation payload. Backend regression coverage also pins the completed dry-run observation path: completing a `patch.apply` dry-run result with `dryRun=true`, `mutationApplied=false`, and `sourceRequestId` records only that clone's result, does not release the held source patch, does not create mutation work, does not call `claimNext`, and does not push any follow-up request. MockMvc polling coverage now also verifies a token-authenticated Local Agent can claim that non-mutating dry-run request through `GET /api/local-agents/tools/next` and complete it through `POST /api/local-agents/tools/{requestId}/response` while the payload stays `mutationAllowed=false` and `mutationApplied=false`. The Local Agent executable also has `self-test tool-response-contract`, which feeds a `patch.apply` dry-run tool request through the same `HandleTool` path used by polling and WebSocket requests, verifies the response preserves session/request/user/agent/workspace/source request ids, keeps `mutationApplied=false`, creates rollback snapshot evidence, and leaves the workspace file unchanged. An opt-in live PostgreSQL repository smoke, `LocalAgentToolExecutionRepositoryLivePostgresTest` with `-Dlearnbot.live-postgres-tests=true`, now verifies a persisted dry-run clone moves from `APPROVED` to `RUNNING` to `SUCCEEDED` while the source patch stays `APPROVED_HELD`; the test is skipped by default and deletes its own rows.
The Code workspace displays this `observationLinkage` metadata for recorded repository verification, snapshot readiness, and rollback readiness so users can tell whether the visible evidence is release-attempt-linked, source-only fallback, or source-only. Its route-level smoke also covers the invalid dry-run snapshot case where `snapshotCreated=true` but `mutationApplied=true`: the panel must show `Snapshot readiness: INVALID`, the backend refusal message, the rollback invalid message, and `blocking release: true` instead of presenting the snapshot as release evidence. The same smoke now keeps the release-attempt summary aligned with that invalid evidence by rendering `release attempt final readiness: BLOCKED_RELEASE_DISABLED`, `release enablement checklist` prerequisites as false, `finalReadiness` and `rollbackReadiness` as blocking checklist items, and a display summary that says linked evidence is complete but release is blocked. `releaseAttemptDisplaySummary.test.mjs` also pins this helper-level fallback so linked evidence completeness alone cannot make a release appear ready when patch preconditions fail. This remains visibility-only and does not add release, apply, test, rollback, claim, or fresh-observation enqueue controls.
The same internal gate UI also shows the disabled `freshObservationEnqueueBoundary` from release attempt evidence, including disabled request creation, push, enqueue, claim-after-enqueue, and mutation flags. The display text is assembled by `freshObservationEnqueueBoundary.js`, with a lightweight regression pinning the audit-only boundary and planned request rows. This keeps the future release-attempt workflow inspectable without adding any execution control.
Readiness also summarizes fresh observation evidence status on `releaseAttemptModel.latestAttempt.freshObservationEvidenceStatus`. For the latest disabled release attempt, repository verification and patch dry-run evidence are marked `RELEASE_ATTEMPT_LINKED`, `SOURCE_ONLY_FALLBACK`, or `MISSING`, with request creation, push, claim, and mutation flags still disabled. This is audit-only and does not create or push Local Agent requests.
The Code workspace renders this evidence status inside the internal patch execution gate so users can see whether release-attempt-linked repository verification and dry-run evidence are present, missing, or using source-only fallback. The display text is assembled by `freshObservationEvidenceSummary.js`, with a lightweight regression covering linked, fallback-only, blocking, and disabled request creation/push/claim/mutation fields. It remains read-only status display.
Readiness also aggregates that list into `releaseAttemptModel.latestAttempt.freshObservationEvidenceCompleteness`. The summary counts linked, missing, fallback-only, and blocking evidence keys, reports whether all required fresh observations are release-attempt-linked, and explains that even complete evidence remains non-claimable while the release gate is disabled. It keeps request creation, push, claim, and mutation flags disabled.
The Code workspace also renders this completeness summary as read-only audit data inside the internal patch execution gate, including linked/missing/fallback/blocking counts and the non-claimable explanation. `freshObservationEvidenceSummary.test.mjs` pins the helper-level fallback text so source-only fallback evidence cannot appear claimable. It does not add any release, apply, test, rollback, claim, or fresh-observation enqueue control.
Readiness also exposes `releaseAttemptModel.latestAttempt.releaseAttemptFinalReadiness`, an audit-only final release state summary for the latest disabled attempt. It combines freshness, fresh evidence completeness, patch release preconditions, release gate state, disabled request creation/push/claim/write/mutation flags, and blocking reasons. This summary can report that an attempt is ready but still release-disabled, but it never makes a held patch claimable.
The Code workspace renders this final readiness summary as read-only audit data inside the internal patch execution gate, including final status, freshness, evidence completeness, patch precondition state, disabled execution flags, and blocking reasons. It does not add release, apply, test, rollback, claim, or fresh-observation enqueue controls.
When both linked observations are complete, readiness also exposes `releaseAttemptModel.latestAttempt.releaseAttemptDisplaySummary`, a compact read-only display model for the Code workspace. It reports linked evidence complete, release ready-but-disabled, evidence counts, blocking reasons, and disabled release/request-creation/push/claim/write-helper/apply/test/rollback/RAG freshness/final-answer/mutation flags. The Code workspace hoists this summary above the detailed gate list when present and falls back to the older readiness fields for older responses. This is display-only and does not call any release or execution endpoint.
The Code workspace display text for this summary is assembled by `releaseAttemptDisplaySummary.js`, with a lightweight Node regression covering both the primary backend summary path and the older-field fallback path. This keeps the UI wording and disabled-flag rendering testable without adding a frontend test framework.
Readiness also includes `releaseAttemptModel.latestAttempt.localAgentMutationExecutionSequencePlan`, an audit-only disabled sequence for the future mutation release path. The plan lists `patch.apply`, `command.runAllowed`, post-write `git.status`, and rollback fallback `rollback.restore` steps with approval state, execution target, ordering, and disabled request creation, push, claim, apply, test, rollback, and mutation flags. It is planning metadata only and does not create Local Agent requests.
The Code workspace renders this sequence plan as read-only audit data inside the internal patch execution gate, including step order, tool name, approval state, execution target, side-effect/rollback role, and disabled request creation, push, claim, apply, test, rollback, and mutation flags. It does not add any execution control.
Readiness also includes `releaseAttemptModel.latestAttempt.postMutationResultContract`, an audit-only disabled contract for the future Local Agent result envelope. It defines expected outcomes for patch apply, allowlisted verification, post-write repository observation, rollback fallback, and RAG freshness marking, while keeping request creation, push, claim, mutation, apply, test, rollback restore, and RAG freshness updates disabled.
The Code workspace renders this post-mutation result contract as read-only audit data inside the internal patch execution gate, including each expected disabled outcome and the disabled request creation, push, claim, apply, test, rollback restore, mutation, and RAG freshness flags. It does not add any release, apply, test, rollback, claim, request creation, push, or freshness update control.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationResultIntakeBoundary`, an audit-only disabled boundary for future completed Local Agent mutation result envelopes. It records the required source request link, release attempt link, patch apply proof, allowlisted verification disclosure, post-write observation, rollback fallback disclosure, RAG freshness disclosure, and accepted terminal statuses while keeping release gate, request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, RAG freshness updates, and final-answer generation disabled.
The Code workspace renders this mutation result intake boundary as read-only audit data inside the internal patch execution gate, including required outcome keys, accepted terminal statuses, disabled result aggregation, disabled final-answer generation, backend intake requirements, blocking keys, and prerequisite requirements. Its display text is assembled by `mutationResultIntakeBoundary.js`, with a lightweight Node regression covering the ready-but-disabled intake rendering path, source request linkage, release-attempt linkage, expected outcome coverage, mutation-applied proof, verification/rollback disclosure, RAG freshness disclosure, request-creation/push disabled requirement flags, blocked/null fallbacks, and backend blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, RAG freshness update, mutation, or final-answer generation controls.
The route-level Code workspace readiness panel also has a Vite SSR smoke test in `codeWorkspaceReadinessPanelSmoke.test.mjs`, which renders `CodeWorkspace.jsx` with a disabled release-attempt fixture and verifies that execution readiness, the internal patch execution gate, the release-attempt model, result-intake required outcomes, accepted terminal statuses, disabled request-creation/push requirement flags, mutation dispatch-envelope tool/approval/rollback/freshness text, mutation dispatch-preflight agent/workspace/capability text, mutation dispatch-decision refusal text, mutation request-blueprint tool/approval/input/output text, mutation request-creation policy/count text, mutation request-push policy/count text, mutation request-claim policy/count text, mutation execution-readiness source/check/blocking text, mutation tool-runner source/check/blocking text, result-completion source/check/blocking text, result-intake-persistence policy/blocking text, rollback-fallback policy/blocking text, RAG freshness policy/blocking text, result-aggregation policy/blocking text, publication policy/blocking text, final-answer generation policy/blocking text, final-answer completion policy/blocking text, final-answer persistence policy/blocking text, final-answer conversation-save policy/blocking text, final-answer user-visible completion policy/blocking text, final-response handoff policy/blocking text, final-answer delivery policy/blocking text, final-answer delivery-receipt policy/blocking text, mutation completion summary final-response/delivery-receipt items, mutation handoff summary final-response/delivery-receipt stages, acknowledgement-save disabled text, and refusal messages appear in the actual panel markup. This remains read-only and does not call browser APIs, release endpoints, dispatch decision, dispatch preflight, request blueprint, request creation, push, claim, running transition, execution, tool runner, write helper, apply, test, rollback, result intake, RAG freshness update, aggregation, publication, final-answer generation, final-answer completion, final-answer persistence, conversation save, user-visible completion, final-response handoff, delivery handoff, final-answer delivery, delivery receipt, acknowledgement save, completion handoff, or mutation paths.
The SSR smoke route props are assembled by `codeWorkspaceReadinessSmokeHarness.mjs` so the disabled release-attempt fixture can keep growing independently from the route render setup; the harness has a lightweight Node regression that checks the held request, disabled patch execution gate, release-attempt model shape, fresh-observation request/evidence/completeness/enqueue audit fields, generated props under the disabled flag guard, and the invalid dry-run snapshot fixture contract where `dryRunResult` links its request automatically while `snapshotReadiness` and `rollbackReadiness` stay `INVALID`.
The route-level Code workspace SSR smoke also verifies that disabled request blueprint/creation/push/claim, execution gate, write-helper safety, post-execution observation, observation-acceptance, fresh observation request/evidence/enqueue audit surfaces, the disabled release-fresh-observation queue control, final readiness, final mutation report, finalization, final-answer publication, and release enablement status/count/policy/blocking text appears in the actual readiness panel markup while all side-effect controls remain closed. The release-fresh-observation queue assertion is scoped to the same rendered button element so another disabled control cannot satisfy the check by accident.
The same smoke keeps a recursive safety guard over the disabled latest-attempt fixture and full route props so release, request creation, push, claim, running/execution, tool runner, write helper, apply, test, rollback restore, RAG freshness, result aggregation, publication, final-answer, delivery, receipt, and mutation control flags cannot accidentally become `true` without failing the route-level regression. That guard lives in `mutationDisabledFlagGuard.js` with a focused Node regression and a reusable assertion helper that reports the exact enabled flag paths; it combines an explicit denylist with Local Agent control prefix/suffix detection so newly added mutation-related `*Enabled`, `*Allowed`, `claimableAfter*`, or other claimable-style flags are harder to miss. The route prop harness regression also runs the assertion against generated props so fixture setup stays non-executing before the route render smoke runs.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationResultAggregationPlan`, an audit-only disabled plan for combining future accepted Local Agent mutation outcomes into the final mutation report. It maps patch apply, verification, post-write observation, rollback fallback, and RAG freshness outcomes into changed files, verification outcome, repository observation, rollback state, RAG freshness state, residual risks, and evidence/citations while keeping release gate, request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, RAG freshness updates, and final-answer generation disabled.
The Code workspace renders this mutation result aggregation plan as read-only audit data inside the internal patch execution gate, including source outcome keys, target report sections, ordered aggregation steps, disabled result aggregation, and disabled final-answer generation. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.finalAnswerPublicationBoundary`, an audit-only disabled boundary for the future point where an aggregated Local Agent mutation report could become the user-facing final answer. It requires fresh release-attempt readiness, a modeled aggregation plan, final report sections, and answer-quality guardrails while keeping release gate, request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, RAG freshness updates, publication, and final-answer generation disabled.
The Code workspace renders this final answer publication boundary as read-only audit data inside the internal patch execution gate, including publication prerequisites, required report sections, answer-quality guardrails, blocking keys, disabled publication, and disabled final-answer generation. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, publication, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationCompletionSummary`, an audit-only disabled consolidation of the latest Local Agent mutation readiness state. It combines release readiness, mutation execution sequencing, result intake, result aggregation, final mutation report contract, finalization, final answer publication, release enablement, rollback readiness, delivery receipt, and RAG freshness requirements into one latest-attempt status while keeping release gate, request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, publication, RAG freshness updates, and final-answer generation disabled.
The backend mutation completion summary now also preserves the source final-answer delivery-receipt gate schema, status, session, user, agent, and workspace ids, and its regression pins the aggregate disabled final-answer completion, persistence, conversation-save, user-visible completion, final-response handoff, delivery handoff, delivery receipt, and acknowledgement-save flags. The mutation handoff summary carries that source delivery-receipt context through the source completion-summary fields while still refusing release, request creation, push, claim, execution, result handling, final response, delivery, receipt, acknowledgement save, and mutation.
The Code workspace renders this mutation completion summary as read-only audit data inside the internal patch execution gate, including completion status, prerequisite items, blocking keys, disabled result aggregation, disabled publication, disabled final-answer generation, disabled final-answer delivery, disabled final-response handoff, disabled delivery handoff, disabled delivery receipt, and disabled acknowledgement save. Its display text is assembled by `mutationCompletionSummary.js`, with a lightweight Node regression covering the ready disabled completion-summary rendering path, final-answer delivery item rendering, delivery receipt item rendering, acknowledgement-save disabled rendering, and blocked delivery/receipt disabled fallback text. The route-level smoke fixture also renders the final-response handoff prerequisite item so the actual panel markup keeps final-response handoff and acknowledgement-save disabled text visible. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, publication, RAG freshness update, mutation, final-answer generation, final-response handoff, final-answer delivery, delivery handoff, delivery receipt, or acknowledgement controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationHandoffSummary`, an audit-only disabled aggregate for the future Local Agent mutation handoff. It consumes the mutation completion summary and groups the future handoff stages: dispatch decision, request creation, transport push, agent claim, tool execution, result intake, final response handoff, and delivery receipt. Even when completion prerequisites are ready, the summary reports `READY_HANDOFF_DISABLED` with release gate, request creation, push, claim, write helper, apply, test, rollback restore, RAG freshness update, result aggregation, publication, final-answer generation, final-answer completion, final-answer delivery, final-answer persistence, conversation save, user-visible completion, final-response handoff, delivery handoff, delivery receipt, claimable, and mutation controls disabled. It creates no request, pushes nothing, claims nothing, executes nothing, records no result, publishes no answer, and opens no server-local mutation path.
Backend readiness regression also pins the mutation completion and handoff summary context: the disabled audit models must keep source session, user, agent, workspace, source request, and release attempt ids, the handoff summary must retain the source completion summary context, and both summaries must keep explicit messages that execution, aggregation, publication, final response, delivery, receipt, acknowledgement save, and mutation remain disabled.
The Code workspace renders this mutation handoff summary as read-only audit data under the internal patch execution gate, including the source completion status, disabled handoff controls, stage list, blocking keys, and message. Its display text is assembled by `mutationHandoffSummary.js`, with a lightweight Node regression covering the disabled handoff rendering path, final-response stage rendering, delivery receipt stage rendering, acknowledgement-save disabled rendering, expanded disabled handoff controls, and blocked final-response/delivery-receipt fallback text. The route-level smoke fixture now includes the final-response stage as well as delivery-receipt so the actual panel markup pins both disabled handoff stages with acknowledgement-save still false. It does not add release, request creation, push, claim, execution, result intake, final response handoff, delivery handoff, delivery receipt, acknowledgement, or mutation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationDispatchEnvelopeContract`, an audit-only disabled contract for the future Local Agent mutation request envelope after a release gate is enabled. It records source request id, release attempt id, session/user/agent/workspace ids, execution target, ordered tool sequence, approval states, expected post-mutation result keys, rollback obligation, and RAG freshness obligation while keeping release gate, request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, publication, RAG freshness updates, and final-answer generation disabled.
The Code workspace renders this dispatch envelope contract as read-only audit data inside the internal patch execution gate, including source/release/session/agent/workspace ids, ordered tools, required approvals, expected outcome keys, rollback and RAG freshness obligations, blocking keys, and disabled dispatch/execution/publication flags. Its display text is assembled by `mutationDispatchEnvelopeContract.js`, with a lightweight Node regression covering the ready disabled dispatch-envelope rendering path. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, publication, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationDispatchPreflightBoundary`, an audit-only disabled boundary for validating future dispatch prerequisites before any Local Agent mutation request can be created. It checks the selected Local Agent connection, agent id match, approved workspace readiness, workspace identity verification, required capability coverage for `patch.apply`, `command.runAllowed`, `git.status`, and `rollback.restore`, and dispatch envelope readiness while keeping dispatch preflight, release gate, request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, publication, RAG freshness updates, and final-answer generation disabled.
The Code workspace renders this dispatch preflight boundary as read-only audit data inside the internal patch execution gate, including connection state, requested/connected agent ids, workspace readiness, workspace identity status, required and advertised capabilities, missing capability keys, dispatch envelope readiness, blocking keys, and disabled dispatch/execution/publication flags. Its display text is assembled by `mutationDispatchPreflightBoundary.js`, with a lightweight Node regression covering the ready disabled preflight rendering path. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, publication, RAG freshness update, mutation, dispatch preflight, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationDispatchDecisionModel`, an audit-only disabled decision surface for the future point where a preflight-ready Local Agent mutation could otherwise be dispatched. It consumes the dispatch envelope and preflight boundary, records a refusal decision, readiness inputs, blocking keys, and a user-visible refusal message while keeping dispatch decision, release gate, request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, publication, RAG freshness updates, and final-answer generation disabled.
The Code workspace renders this dispatch decision model as read-only audit data inside the internal patch execution gate, including decision status, refusal decision, envelope/preflight status, readiness inputs, blocking keys, user-visible refusal message, and disabled dispatch/execution/publication flags. Its display text is assembled by `mutationDispatchDecisionModel.js`, with a lightweight Node regression covering the refused dispatch-decision rendering path and the backend `envelopeStatus`/`preflightStatus` keys. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, publication, RAG freshness update, mutation, dispatch decision, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationRequestBlueprint`, an audit-only disabled blueprint for the future Local Agent mutation request that would be derived from the dispatch decision after release enablement. It records source/release/session/user/agent/workspace ids, ordered tool requests, expected input and output keys, approval states, and blocking keys while keeping request blueprint, request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, publication, RAG freshness updates, and final-answer generation disabled. It does not create a `local_agent_tool_executions` row, push a request to the Local Agent, make a held request claimable, or add a server-local mutation path.
The Code workspace renders this mutation request blueprint as read-only audit data inside the internal patch execution gate, including blueprint status, source decision/envelope status, source/release/session/agent/workspace ids, ordered tool requests, expected input/output keys, approval states, blocking keys, and disabled request blueprint/request creation/push/claim/mutation flags. Its display text is assembled by `mutationRequestBlueprint.js`, with a lightweight Node regression covering the refused request-blueprint rendering path. It does not add release, apply, test, rollback, claim, request creation, request blueprint, push, result aggregation, publication, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationRequestCreationGate`, an audit-only disabled gate that consumes the mutation request blueprint and explicitly refuses persistence, push, claim, and mutation while creation remains disabled. It records blueprint readiness, release gate state, request creation policy, expected request count, persisted/pushed/claimable request counts, policy checks, and blocking keys while keeping request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, publication, RAG freshness updates, and final-answer generation disabled. It does not create a `local_agent_tool_executions` row or open a server-local mutation path.
The Code workspace renders this mutation request creation gate as read-only audit data inside the internal patch execution gate, including creation gate status, blueprint readiness, release gate state, request creation policy, expected/persisted/pushed/claimable request counts, policy checks, blocking keys, and disabled request creation/push/claim/mutation flags. Its display text is assembled by `mutationRequestCreationGate.js`, with a lightweight Node regression covering the refused request-creation rendering path and the zero persisted/pushed/claimable counts. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, publication, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationRequestPushGate`, an audit-only disabled gate that consumes the request creation gate and explicitly refuses Local Agent transport push, pusher invocation, claim transition, and mutation while push remains disabled. It records creation-gate readiness, transport push policy, pusher invocation state, expected/persisted/pushed/claimable request counts, policy checks, and blocking keys while keeping request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, publication, RAG freshness updates, and final-answer generation disabled. It does not call `LocalAgentToolPusher`, push a request to the Local Agent, create a claimable request, or open a server-local mutation path.
The Code workspace renders this mutation request push gate as read-only audit data inside the internal patch execution gate, including push gate status, creation-gate readiness, transport/pusher policy, expected/persisted/pushed/claimable request counts, policy checks, blocking keys, and disabled push/request-creation/claim/mutation flags. Its display text is assembled by `mutationRequestPushGate.js`, with a lightweight Node regression covering the refused push rendering path, disabled pusher invocation, and zero pushed/claimable counts. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, publication, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationRequestClaimGate`, an audit-only disabled gate that consumes the request push gate and explicitly refuses Local Agent claim, `claimNext` invocation, running transition, and mutation while claim remains disabled. It records push-gate readiness, claim policy, expected/persisted/pushed/claimable/running request counts, policy checks, and blocking keys while keeping request creation, push, claim, write helper, apply, test, rollback restore, mutation, result aggregation, publication, RAG freshness updates, and final-answer generation disabled. It does not call `repository.claimNext`, make a request claimable or running, create a `local_agent_tool_executions` row, or open a server-local mutation path.
The Code workspace renders this mutation request claim gate as read-only audit data inside the internal patch execution gate, including claim gate status, source push-gate readiness, claim policy, `claimNext` invocation state, expected/persisted/pushed/claimable/running request counts, policy checks, blocking keys, and disabled request creation/push/claim/running/mutation flags. Its display text is assembled by `mutationRequestClaimGate.js`, with a lightweight Node regression covering the refused claim rendering path, disabled `claimNext` invocation, and zero claimable/running counts. It does not add release, apply, test, rollback, claim, request creation, push, result aggregation, publication, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationExecutionGate`, an audit-only disabled gate that consumes the request claim gate and explicitly refuses Local Agent tool execution, tool-runner invocation, write-helper invocation, completed transitions, apply, tests, rollback restore, RAG freshness updates, result aggregation, publication, final-answer generation, and mutation while execution remains disabled. It records claim-gate readiness, execution policy, tool-runner/write-helper invocation state, expected/persisted/pushed/claimable/running/completed request counts, policy checks, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, calling no write helper, mutating no files, and opening no server-local mutation path.
The Code workspace renders this mutation execution gate as read-only audit data inside the internal patch execution gate, including execution gate status, source claim-gate readiness, execution policy, tool-runner/write-helper invocation state, expected/persisted/pushed/claimable/running/completed request counts, policy checks, blocking keys, and disabled execution/apply/test/rollback/RAG freshness/result aggregation/publication/final-answer flags. Its display text is assembled by `mutationExecutionGate.js`, with a lightweight Node regression covering the refused execution rendering path, disabled tool-runner/write-helper invocation, zero running/completed counts, and disabled apply/test/rollback/freshness/publication/final-answer controls. It does not add release, apply, test, rollback, claim, request creation, push, execution, tool-runner, write-helper, result aggregation, publication, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationWriteHelperSafetyGate`, an audit-only disabled gate that consumes the mutation execution gate and explicitly refuses connecting the Local Agent write helper to public `patch.apply` mutation. It records execution-gate readiness, write-helper policy, expected request count, and required future safety checks for approved workspace containment, fresh snapshot manifest, hash recheck, guarded temp-file rewrite, and rollback contract while keeping release gate, request creation, push, claim, execution, write helper, apply, test, rollback restore, RAG freshness updates, result aggregation, publication, final-answer generation, and mutation disabled.
The backend write-helper safety gate assembly lives in `LocalAgentWriteHelperSafetyGateBuilder`, keeping the pre-mutation safety contract separate from the oversized Local Agent tool gateway without enabling execution.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationExecutionReadinessBoundary`, an audit-only disabled boundary for the future point where a modeled handoff could otherwise enter Local Agent mutation execution. It consumes the mutation handoff summary, mutation execution gate, and write-helper safety gate, records their source statuses, expected/completed request counts, readiness checks, and disabled runtime switches, and reports `REFUSED_EXECUTION_READINESS_DISABLED` only when those disabled inputs are modeled. It keeps release gate, request creation, push, claim, execution, tool runner, write helper, apply, test, rollback restore, result intake, RAG freshness updates, result aggregation, publication, final-answer generation, final-response handoff, delivery receipt, acknowledgement save, claimable, and mutation disabled. It creates no request, pushes nothing, claims nothing, runs nothing, calls no write helper, mutates no files, and opens no server-local mutation path.
Backend readiness regression also pins the mutation execution-readiness boundary context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source handoff-summary context, source delivery-receipt context, source execution-gate context, source write-helper safety context, disabled tool-runner/write-helper/result-intake flags, and the explicit refusal message that runtime execution, request creation, push, claim, write helper, apply, test, rollback restore, result intake, final-response handoff, delivery receipt, acknowledgement save, and mutation remain disabled. The downstream tool-runner boundary carries the execution-readiness delivery-receipt source context forward while runner invocation, running transition, result completion, acknowledgement save, and mutation remain refused.
The Code workspace renders this mutation execution-readiness boundary as read-only audit data under the internal patch execution gate, including source handoff/execution/write-helper statuses, expected/completed request counts, disabled runtime controls, readiness checks, blocking keys, and message. Its display text is assembled by `mutationExecutionReadinessBoundary.js`, with a lightweight Node regression covering the disabled execution-readiness rendering path, handoff/execution/write-helper readiness checks, runtime execution refusal, side-effect transport refusal, acknowledgement-save disabled rendering, and blocked/null fallback rendering. It does not add release, request creation, push, claim, execution, tool-runner, write-helper, apply, test, rollback restore, result intake, final-response handoff, delivery receipt, acknowledgement, or mutation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationToolRunnerBoundary`, an audit-only disabled boundary for the future point where Local Agent mutation requests could otherwise enter the runtime tool runner. It consumes the mutation execution-readiness boundary and mutation execution gate, records source statuses, expected/running/completed request counts, runner checks, and disabled running/result transitions, and reports `REFUSED_TOOL_RUNNER_DISABLED` only when those disabled execution inputs are modeled. It keeps request creation, push, claim, running transition, execution, tool runner, write helper, apply, test, rollback restore, result intake, RAG freshness updates, result aggregation, publication, final-answer generation, final-response handoff, delivery receipt, acknowledgement save, claimable, and mutation disabled. It creates no request, pushes nothing, claims nothing, transitions nothing to running or completed, invokes no runner, calls no write helper, mutates no files, and opens no server-local mutation path.
Backend readiness regression also pins the mutation tool-runner boundary context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source execution-readiness context, source execution-gate context, expected/running/completed request counts, disabled running/result-intake/tool-runner flags, and the explicit refusal message that runner invocation, running transition, result completion, write helper, apply, test, rollback restore, result intake, acknowledgement save, and mutation remain disabled.
The Code workspace renders this mutation tool-runner boundary as read-only audit data under the internal patch execution gate, including source execution-readiness/execution-gate statuses, tool-runner policy, expected/running/completed request counts, disabled runner controls, runner checks, blocking keys, and message. Its display text is assembled by `mutationToolRunnerBoundary.js`, with a lightweight Node regression covering the disabled tool-runner rendering path, execution-readiness check, execution-gate check, tool-runner policy refusal, running-transition refusal, result-completion-transition refusal, acknowledgement-save disabled rendering, backend blocking keys, and blocked/null fallback rendering. It does not add release, request creation, push, claim, running transition, execution, tool-runner, write-helper, apply, test, rollback restore, result intake, final-response handoff, delivery receipt, acknowledgement, or mutation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationResultCompletionBoundary`, an audit-only disabled boundary for the future point where Local Agent mutation runner output could otherwise become a completed result envelope. It consumes the mutation tool-runner boundary and post-execution observation gate, records source statuses, expected/completed/accepted/rejected result counts, result checks, and disabled completed-result transitions, and reports `REFUSED_RESULT_COMPLETION_DISABLED` only when those disabled inputs are modeled. It keeps tool runner, completed-result transition, completed-result persistence, post-execution observation, result intake, RAG freshness updates, result aggregation, publication, final-answer generation, final-response handoff, delivery receipt, acknowledgement save, claimable, and mutation disabled. It creates no request, pushes nothing, claims nothing, transitions no request to completed, persists no result envelope, captures no observation, saves no acknowledgement, and opens no server-local mutation path.
The Code workspace renders this mutation result-completion boundary as read-only audit data under the internal patch execution gate, including source tool-runner/post-execution-observation statuses, completion policy, expected/completed/accepted/rejected result counts, disabled result controls, result checks, blocking keys, and message. Its display text is assembled by `mutationResultCompletionBoundary.js`, with a lightweight Node regression covering the disabled result-completion rendering path, tool-runner readiness, post-execution observation readiness, completed-result transition refusal, result-envelope persistence refusal, observation-capture refusal, acknowledgement-save disabled rendering, backend blocking keys, and blocked/null fallback rendering. It does not add release, request creation, push, claim, running transition, tool-runner, write-helper, apply, test, rollback restore, completed-result transition, result persistence, observation capture, result intake, final-response handoff, delivery receipt, acknowledgement, or mutation controls.
Backend readiness regression also pins the result-completion boundary context: the audit model must keep the source session, user, agent, workspace, source request, and release attempt ids attached to the disabled result-completion read model, source tool-runner context, source delivery-receipt context, source post-execution observation context, acknowledgement-save disabled flags, and the explicit refusal message that completed-result transition, result persistence, observation capture, result intake, and mutation remain disabled.
The Code workspace renders this mutation write-helper safety gate as read-only audit data inside the internal patch execution gate, including safety gate status, schema, source execution-gate readiness/status, write-helper policy, source/release/session/agent/workspace ids, expected request count, policy checks, blocking keys, message, disabled write helper, disabled apply, disabled mutation, disabled rollback restore, and disabled request/claim/execution flags. Its display text is assembled by `mutationWriteHelperSafetyGate.js`, with a lightweight Node regression covering the refused write-helper rendering path, disabled write-helper/apply/mutation/rollback controls, and the required future workspace containment, fresh snapshot manifest, hash recheck, guarded rewrite, and rollback-contract checks. It does not add release, apply, test, rollback, claim, request creation, push, execution, write-helper, RAG freshness update, result aggregation, publication, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationPostExecutionObservationGate`, an audit-only disabled gate that consumes the mutation execution gate and explicitly refuses post-execution result capture, completed-result persistence, rollback fallback execution, RAG freshness updates, result aggregation, publication, final-answer generation, and mutation while post-execution observation remains disabled. It records execution-gate readiness, observation policy, expected/completed/accepted/rejected result counts, policy checks, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no completed result, calling no write helper, mutating no files, running no tests, restoring no rollback, updating no RAG freshness, aggregating no result, publishing no answer, and opening no server-local mutation path.
The backend post-execution observation gate assembly lives in `LocalAgentPostExecutionObservationGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the post-execution observation gate context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source execution-gate context, expected/completed/accepted/rejected result counts, disabled completed-result persistence, rollback fallback, RAG freshness, aggregation, publication, final-answer flags, and the explicit refusal message that completed-result capture, rollback fallback, RAG freshness update, aggregation, publication, final answer, and mutation remain disabled.
The Code workspace renders this mutation post-execution observation gate as read-only audit data inside the internal patch execution gate, including observation gate status, source execution-gate readiness, observation policy, expected/completed/accepted/rejected result counts, policy checks, blocking keys, and disabled post-execution/result-persistence/rollback-fallback/RAG freshness/result aggregation/publication/final-answer flags. Its display text is assembled by `mutationPostExecutionObservationGate.js`, with a lightweight Node regression covering the refused observation rendering path, zero completed/accepted/rejected counts, disabled completed-result persistence, disabled rollback fallback, and disabled RAG freshness/aggregation/publication/final-answer controls. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationObservationAcceptanceGate`, an audit-only disabled gate that consumes the post-execution observation gate and explicitly refuses accepting completed mutation observations, accepted-observation intake persistence, rollback fallback execution, RAG freshness updates, result aggregation, publication, final-answer generation, and mutation while acceptance remains disabled. It records post-execution observation readiness, acceptance policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted mutation result, calling no write helper, mutating no files, running no tests, restoring no rollback, updating no RAG freshness, aggregating no result, publishing no answer, and opening no server-local mutation path.
The backend observation acceptance gate assembly lives in `LocalAgentObservationAcceptanceGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the observation-acceptance gate context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source post-execution observation context, expected/completed/accepted/rejected/intake-persisted result counts, disabled acceptance, intake persistence, rollback fallback, RAG freshness, aggregation, publication, final-answer flags, and the explicit refusal message that accepted observation intake, rollback fallback, RAG freshness update, aggregation, publication, final answer, and mutation remain disabled.
The Code workspace renders this mutation observation acceptance gate as read-only audit data inside the internal patch execution gate, including acceptance gate status, source post-execution observation readiness, acceptance policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, and disabled acceptance/intake-persistence/rollback-fallback/RAG freshness/result aggregation/publication/final-answer flags. Its display text is assembled by `mutationObservationAcceptanceGate.js`, with a lightweight Node regression covering the refused acceptance rendering path, zero accepted/rejected/intake-persisted counts, disabled accepted-observation intake persistence, disabled rollback fallback, and disabled RAG freshness/aggregation/publication/final-answer controls. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, observation acceptance, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationResultIntakePersistenceGate`, an audit-only disabled gate that consumes the observation acceptance gate and explicitly refuses accepted mutation observation intake persistence, rollback fallback execution, RAG freshness updates, result aggregation, publication, final-answer generation, acknowledgement save, and mutation while intake persistence remains disabled. It records observation-acceptance readiness, intake persistence policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, restoring no rollback, updating no RAG freshness, aggregating no result, publishing no answer, saving no acknowledgement, and opening no server-local mutation path.
The backend result intake persistence gate assembly lives in `LocalAgentResultIntakePersistenceGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the result-intake-persistence gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source observation-acceptance context, expected/completed/accepted/rejected/intake-persisted result counts, disabled accepted-observation persistence, rollback fallback, RAG freshness, aggregation, publication, final-answer, final-response handoff, delivery, and delivery-receipt flags, and the explicit refusal message that accepted-observation persistence, rollback fallback, RAG freshness update, aggregation, publication, final-answer generation, and mutation remain disabled.
The Code workspace renders this mutation result intake persistence gate as read-only audit data inside the internal patch execution gate, including intake persistence gate status, source observation-acceptance readiness, intake persistence policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, and disabled intake-persistence/accepted-observation-persistence/rollback-fallback/RAG freshness/result aggregation/publication/final-answer/acknowledgement-save flags. Its display text is assembled by `mutationResultIntakePersistenceGate.js`, with a lightweight Node regression covering the refused intake-persistence rendering path, blocked/null fallbacks, accepted-observation persistence refusal, rollback fallback refusal, RAG freshness refusal, aggregation/publication/final-answer/acknowledgement-save refusal, and the backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, acknowledgement, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationRollbackFallbackGate`, an audit-only disabled gate that consumes the intake persistence gate and explicitly refuses rollback fallback execution, RAG freshness updates, result aggregation, publication, final-answer generation, acknowledgement save, and mutation while rollback fallback remains disabled. It records intake-persistence readiness, rollback fallback policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, executing no rollback fallback, updating no RAG freshness, aggregating no result, publishing no answer, saving no acknowledgement, and opening no server-local mutation path.
The backend rollback fallback gate assembly lives in `LocalAgentRollbackFallbackGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the rollback-fallback gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source result-intake-persistence context, expected/completed/accepted/rejected/intake-persisted result counts, disabled rollback fallback invocation, RAG freshness, aggregation, publication, final-answer, final-response handoff, delivery, and delivery-receipt flags, and the explicit refusal message that rollback fallback execution, RAG freshness update, aggregation, publication, final answer, and mutation remain disabled.
The Code workspace renders this mutation rollback fallback gate as read-only audit data inside the internal patch execution gate, including rollback fallback gate status, source intake-persistence readiness, rollback fallback policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, and disabled rollback-fallback/RAG freshness/result aggregation/publication/final-answer/acknowledgement-save flags. Its display text is assembled by `mutationRollbackFallbackGate.js`, with a lightweight Node regression covering the refused rollback-fallback rendering path, blocked/null fallbacks, rollback fallback execution refusal, RAG freshness refusal, aggregation/publication/final-answer/acknowledgement-save refusal, and the backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, acknowledgement, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationRagFreshnessGate`, an audit-only disabled gate that consumes the rollback fallback gate and explicitly refuses RAG freshness updates, result aggregation, publication, final-answer generation, acknowledgement save, and mutation while RAG freshness remains disabled. It records rollback-fallback readiness, RAG freshness policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, executing no rollback fallback, updating no RAG freshness, aggregating no result, publishing no answer, saving no acknowledgement, and opening no server-local mutation path.
The backend RAG freshness gate assembly lives in `LocalAgentRagFreshnessGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the RAG-freshness gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source rollback-fallback context, expected/completed/accepted/rejected/intake-persisted result counts, disabled freshness update invocation, aggregation, publication, final-answer, final-response handoff, delivery, and delivery-receipt flags, and the explicit refusal message that freshness update, aggregation, publication, final answer, and mutation remain disabled.
The Code workspace renders this mutation RAG freshness gate as read-only audit data inside the internal patch execution gate, including RAG freshness gate status, source rollback-fallback readiness, RAG freshness policy, source rollback-fallback gate status, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, and disabled RAG-freshness/result-aggregation/publication/final-answer/acknowledgement-save flags. Its display text is assembled by `mutationRagFreshnessGate.js`, with a lightweight Node regression covering the refused RAG-freshness rendering path, blocked/null fallbacks, RAG freshness update refusal, aggregation/publication/final-answer/acknowledgement-save refusal, and backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, acknowledgement, mutation, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationResultAggregationGate`, an audit-only disabled gate that consumes the RAG freshness gate and explicitly refuses mutation result aggregation, publication, final-answer generation, acknowledgement save, and mutation while result aggregation remains disabled. It records RAG-freshness readiness, result aggregation policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, source RAG freshness gate status, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, executing no rollback fallback, updating no RAG freshness, aggregating no result, publishing no answer, saving no acknowledgement, and opening no server-local mutation path.
The backend result aggregation gate assembly lives in `LocalAgentResultAggregationGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the result-aggregation gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source RAG-freshness context, expected/completed/accepted/rejected/intake-persisted result counts, disabled aggregation invocation, publication, final-answer, final-response handoff, delivery, delivery-receipt, and acknowledgement-save flags, and the explicit refusal message that aggregation, publication, final answer, and mutation remain disabled.
The Code workspace renders this mutation result aggregation gate as read-only audit data inside the internal patch execution gate, including result aggregation gate status, source RAG-freshness readiness, result aggregation policy, source RAG freshness gate status, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, and disabled result-aggregation/publication/final-answer/final-response/delivery/receipt/acknowledgement-save flags. Its display text is assembled by `mutationResultAggregationGate.js`, with a lightweight Node regression covering the refused result-aggregation rendering path, blocked/null fallbacks, result aggregation refusal, publication/final-answer/final-response/delivery/receipt/acknowledgement-save refusal, and backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-response handoff, final-answer delivery, delivery receipt, acknowledgement, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationPublicationGate`, an audit-only disabled gate that consumes the mutation result aggregation gate and explicitly refuses final-answer publication, final-answer generation, acknowledgement save, and mutation while publication remains disabled. It records result-aggregation readiness, publication policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, source result aggregation gate status, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, executing no rollback fallback, updating no RAG freshness, aggregating no result, publishing no answer, generating no final answer, saving no acknowledgement, and opening no server-local mutation path.
The backend publication gate assembly lives in `LocalAgentPublicationGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the publication gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source result-aggregation context, expected/completed/accepted/rejected/intake-persisted result counts, disabled publication invocation, final-answer, final-response handoff, delivery, delivery-receipt, and acknowledgement-save flags, and the explicit refusal message that publication, final answer, and mutation remain disabled.
The Code workspace renders this mutation publication gate as read-only audit data inside the internal patch execution gate, including publication gate status, schema, result-aggregation readiness, publication policy, source result aggregation gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled publication, final-answer generation, final-answer completion, final-answer delivery, final-response handoff, delivery receipt, and acknowledgement save. Its display text is assembled by `mutationPublicationGate.js`, with a lightweight Node regression covering the refused publication rendering path, blocked/null fallbacks, publication refusal, final-answer/final-response/delivery/receipt/acknowledgement-save refusal, and backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-response handoff, final-answer delivery, delivery receipt, acknowledgement, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationFinalAnswerGenerationGate`, an audit-only disabled gate that consumes the mutation publication gate and explicitly refuses final-answer generation, acknowledgement save, and mutation while final-answer generation remains disabled. It records publication readiness, final-answer generation policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, source publication gate status/schema, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, executing no rollback fallback, updating no RAG freshness, aggregating no result, publishing no answer, generating no final answer, saving no acknowledgement, and opening no server-local mutation path.
The backend final-answer generation gate assembly lives in `LocalAgentFinalAnswerGenerationGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the final-answer generation gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source publication context, expected/completed/accepted/rejected/intake-persisted result counts, disabled final-answer generation invocation, final-answer completion, final-answer delivery, final-response handoff, delivery-receipt, acknowledgement-save, and mutation flags, and the explicit refusal message that final-answer generation and mutation remain disabled.
The Code workspace renders this mutation final-answer generation gate as read-only audit data inside the internal patch execution gate, including final-answer generation gate status, schema, publication readiness, final-answer generation policy, source publication gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled final-answer generation, final-answer completion, final-answer delivery, final-response handoff, delivery receipt, acknowledgement save, and mutation. Its display text is assembled by `mutationFinalAnswerGenerationGate.js`, with a lightweight Node regression covering the refused final-answer-generation rendering path, blocked/null fallbacks, final-answer generation/completion/delivery/final-response/delivery-receipt/acknowledgement-save refusal, and backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-answer completion, final-answer delivery, final-response handoff, delivery receipt, acknowledgement, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationFinalAnswerCompletionGate`, an audit-only disabled gate that consumes the mutation final-answer generation gate and explicitly refuses final-answer completion, delivery, acknowledgement save, final-answer generation, publication, and mutation while completion and delivery remain disabled. It records final-answer generation readiness, final-answer completion policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, source final-answer generation gate status/schema, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, executing no rollback fallback, updating no RAG freshness, aggregating no result, publishing no answer, generating no final answer, completing no final answer, delivering no final answer, saving no acknowledgement, and opening no server-local mutation path.
The backend final-answer completion gate assembly lives in `LocalAgentFinalAnswerCompletionGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the final-answer completion gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source final-answer-generation context, expected/completed/accepted/rejected/intake-persisted result counts, disabled final-answer completion, final-answer delivery, final-response handoff, delivery-receipt, acknowledgement-save, and mutation flags, and the explicit refusal message that completion, delivery, and mutation remain disabled.
The Code workspace renders this mutation final-answer completion gate as read-only audit data inside the internal patch execution gate, including final-answer completion gate status, schema, final-answer generation readiness, final-answer completion policy, delivery state, source final-answer generation gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled final-answer completion, final-answer delivery, final-response handoff, delivery receipt, acknowledgement save, final-answer generation, and mutation. Its display text is assembled by `mutationFinalAnswerCompletionGate.js`, with a lightweight Node regression covering the refused final-answer-completion rendering path, blocked/null fallbacks, final-answer completion, delivery, final-response handoff, delivery-receipt/acknowledgement-save refusal, and backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-response handoff, delivery receipt, acknowledgement, final-answer generation, or final-answer completion controls.
Backend readiness regression also pins the final-answer generation and completion boundary context and refusal text: the disabled audit models must keep source session, user, agent, workspace, source request, and release attempt ids, upstream gate schema/status, and explicit messages that final-answer generation, completion, delivery, acknowledgement save, and mutation remain disabled.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationFinalAnswerPersistenceGate`, an audit-only disabled gate that consumes the mutation final-answer completion gate and explicitly refuses final-answer persistence, conversation-turn save, delivery, acknowledgement save, final-answer completion, final-answer generation, publication, and mutation while persistence remains disabled. It records final-answer completion readiness, final-answer persistence policy, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, source final-answer completion gate status/schema, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, executing no rollback fallback, updating no RAG freshness, aggregating no result, publishing no answer, generating no final answer, completing no final answer, delivering no final answer, saving no acknowledgement, persisting no final answer, saving no conversation turn, and opening no server-local mutation path.
The backend final-answer persistence gate assembly lives in `LocalAgentFinalAnswerPersistenceGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the final-answer persistence gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source final-answer-completion context, expected/completed/accepted/rejected/intake-persisted result counts, disabled final-answer persistence, conversation-turn save, user-visible completion, final-response handoff, delivery-receipt, acknowledgement-save, delivery, and mutation flags, and the explicit refusal message that persistence, conversation save, delivery, acknowledgement save, and mutation remain disabled.
The Code workspace renders this mutation final-answer persistence gate as read-only audit data inside the internal patch execution gate, including final-answer persistence gate status, schema, final-answer completion readiness, final-answer persistence policy, conversation-turn save state, source final-answer completion gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled final-answer persistence, conversation save, user-visible completion, final-response handoff, delivery receipt, acknowledgement save, delivery, final-answer completion, final-answer generation, and mutation. Its display text is assembled by `mutationFinalAnswerPersistenceGate.js`, with a lightweight Node regression covering the refused final-answer-persistence rendering path, blocked/null fallbacks, final-answer persistence, conversation-turn save, user-visible completion, final-response handoff, delivery receipt, acknowledgement-save, and final-answer delivery refusal, plus backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, user-visible completion, final-response handoff, delivery receipt, acknowledgement, final-answer generation, final-answer completion, or final-answer persistence controls.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationFinalAnswerConversationSaveGate`, an audit-only disabled gate that consumes the mutation final-answer persistence gate and explicitly refuses conversation-turn save, user-visible completion, delivery, acknowledgement save, final-answer persistence, final-answer completion, final-answer generation, publication, and mutation while conversation save remains disabled. It records final-answer persistence readiness, final-answer conversation-save policy, user-visible completion state, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, source final-answer persistence gate status/schema, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no accepted observation intake, calling no write helper, mutating no files, running no tests, executing no rollback fallback, updating no RAG freshness, aggregating no result, publishing no answer, generating no final answer, completing no final answer, persisting no final answer, saving no conversation turn, delivering no answer, saving no acknowledgement, marking no user-visible completion, and opening no server-local mutation path.
The backend final-answer conversation-save gate assembly lives in `LocalAgentFinalAnswerConversationSaveGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the final-answer conversation-save gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source final-answer-persistence context, expected/completed/accepted/rejected/intake-persisted result counts, disabled conversation-turn save, user-visible completion, final-response handoff, delivery-receipt, acknowledgement-save, delivery, and mutation flags, and the explicit refusal message that conversation save, user-visible completion, delivery, acknowledgement save, and mutation remain disabled.
The Code workspace renders this mutation final-answer conversation-save gate as read-only audit data inside the internal patch execution gate, including conversation-save gate status, schema, final-answer persistence readiness, final-answer conversation-save policy, conversation-turn save invocation state, user-visible completion state, source final-answer persistence gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled conversation save, user-visible completion, final-response handoff, delivery receipt, acknowledgement save, final-answer persistence, delivery, final-answer completion, final-answer generation, and mutation. Its display text is assembled by `mutationFinalAnswerConversationSaveGate.js`, with a lightweight Node regression covering the refused conversation-save rendering path, blocked/null fallbacks, conversation-turn save, user-visible completion, final-response handoff, delivery receipt, acknowledgement-save, and final-answer delivery refusal, plus backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-response handoff, delivery receipt, acknowledgement, final-answer generation, final-answer completion, final-answer persistence, conversation-save, or user-visible completion controls.
Backend readiness regression also pins the final-answer persistence and conversation-save boundary context and refusal text: the disabled audit models must keep source session, user, agent, workspace, source request, release attempt ids, upstream gate schema/status, acknowledgement-save disabled flags, and explicit messages that final-answer persistence, conversation-turn save, user-visible completion, delivery, acknowledgement save, and mutation remain disabled.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationFinalAnswerUserVisibleCompletionGate`, an audit-only disabled gate that consumes the mutation final-answer conversation-save gate and explicitly refuses user-visible completion, final-response handoff, acknowledgement save, conversation-turn save, final-answer persistence, delivery, final-answer completion, final-answer generation, publication, and mutation while user-visible completion remains disabled. It records conversation-save readiness, user-visible completion policy, final-response handoff state, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, source final-answer conversation-save gate status/schema, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no final answer, saving no conversation turn, marking no user-visible completion, handing off no final response, delivering no answer, saving no acknowledgement, publishing no answer, enabling no mutation, and opening no server-local mutation path.
The backend final-answer user-visible completion gate assembly lives in `LocalAgentFinalAnswerUserVisibleCompletionGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the final-answer user-visible completion gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source final-answer conversation-save context, expected/completed/accepted/rejected/intake-persisted result counts, disabled user-visible completion, final-response handoff, delivery-receipt, acknowledgement-save, and mutation flags, and the explicit refusal message that user-visible completion, final-response handoff, conversation save, acknowledgement save, and mutation remain disabled.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationFinalResponseHandoffGate`, an audit-only disabled gate that consumes the mutation final-answer user-visible completion gate and explicitly refuses final-response handoff, delivery handoff, final-answer delivery, acknowledgement save, user-visible completion, conversation-turn save, final-answer persistence, final-answer completion, final-answer generation, publication, and mutation while final-response handoff remains disabled. It records user-visible completion readiness, final-response handoff policy, source final-answer user-visible completion gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, disabled delivery handoff state, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no final answer, saving no conversation turn, marking no user-visible completion, handing off no final response, delivering no answer, saving no acknowledgement, publishing no answer, enabling no mutation, and opening no server-local mutation path.
The backend final-response handoff gate assembly lives in `LocalAgentFinalResponseHandoffGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the final-response handoff gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source final-answer user-visible completion context, expected/completed/accepted/rejected/intake-persisted result counts, disabled final-response handoff, delivery handoff, delivery-receipt, acknowledgement-save, final-answer delivery, user-visible completion, and mutation flags, and the explicit refusal message that final-response handoff, delivery handoff, final-answer delivery, acknowledgement save, and mutation remain disabled.
Backend readiness regression also pins the final-answer user-visible completion and final-response handoff boundary context and refusal text: the disabled audit models must keep source session, user, agent, workspace, source request, and release attempt ids, upstream gate schema/status, acknowledgement-save disabled flags, and explicit messages that user-visible completion, final-response handoff, delivery handoff, delivery receipt, acknowledgement save, final-answer delivery, and mutation remain disabled.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationFinalAnswerDeliveryGate`, an audit-only disabled gate that consumes the mutation final-response handoff gate and explicitly refuses final-answer delivery, delivery handoff, delivery receipt recording, acknowledgement save, final-response handoff, user-visible completion, conversation-turn save, final-answer persistence, final-answer completion, final-answer generation, publication, and mutation while final-answer delivery remains disabled. It records final-response handoff readiness, final-answer delivery policy, source final-response handoff gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, disabled delivery handoff, delivery receipt, and acknowledgement-save state, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no final answer, saving no conversation turn, marking no user-visible completion, handing off no final response, delivering no answer, recording no delivery receipt, saving no acknowledgement, publishing no answer, enabling no mutation, and opening no server-local mutation path.
The backend final-answer delivery gate assembly lives in `LocalAgentFinalAnswerDeliveryGateBuilder` so the oversized Local Agent tool gateway can continue being split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the final-answer delivery gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source final-response handoff context, expected/completed/accepted/rejected/intake-persisted result counts, disabled final-answer delivery, delivery handoff, delivery receipt, acknowledgement save, final-response handoff, user-visible completion, and mutation flags, and the explicit refusal message that final-answer delivery, delivery handoff, delivery receipt, acknowledgement save, final-response handoff, and mutation remain disabled.
Readiness also includes `releaseAttemptModel.latestAttempt.mutationFinalAnswerDeliveryReceiptGate`, an audit-only disabled gate that consumes the mutation final-answer delivery gate and explicitly refuses delivery receipt recording, acknowledgement save, final-answer delivery, delivery handoff, final-response handoff, user-visible completion, conversation-turn save, final-answer persistence, final-answer completion, final-answer generation, publication, and mutation while delivery receipt remains disabled. It records final-answer delivery readiness, delivery receipt policy, source final-answer delivery gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, disabled delivery receipt and acknowledgement-save state, and blocking keys while creating no request, pushing no request, claiming no request, running no request, completing no request, persisting no final answer, saving no conversation turn, marking no user-visible completion, handing off no final response, delivering no answer, recording no delivery receipt, saving no acknowledgement, publishing no answer, enabling no mutation, and opening no server-local mutation path.
The backend delivery receipt gate assembly lives in `LocalAgentFinalAnswerDeliveryReceiptGateBuilder` so the oversized Local Agent tool gateway can be split by feature without changing the disabled audit contract or enabling execution.
Backend readiness regression also pins the final-answer delivery receipt gate source context: the disabled audit model must keep source session, user, agent, workspace, source request, and release attempt ids, source final-answer delivery context, expected/completed/accepted/rejected/intake-persisted result counts, disabled delivery receipt, acknowledgement save, final-answer delivery, delivery handoff, final-response handoff, user-visible completion, and mutation flags, and the explicit refusal message that delivery receipt, acknowledgement save, final-answer delivery, and mutation remain disabled.
Backend readiness regression also pins the final-answer delivery and delivery-receipt boundary context and refusal text: the disabled audit models must keep source session, user, agent, workspace, source request, and release attempt ids, upstream gate schema/status, and explicit messages that final-answer delivery, delivery handoff, delivery receipt recording, acknowledgement save, and mutation remain disabled.
The Code workspace renders this mutation final-answer delivery receipt gate as read-only audit data inside the internal patch execution gate, including delivery receipt gate status, schema, final-answer delivery readiness, delivery receipt policy, source final-answer delivery gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled delivery receipt, disabled acknowledgement save, disabled final-answer delivery, disabled delivery handoff, disabled final-response handoff, disabled user-visible completion, disabled conversation save, disabled final-answer persistence, disabled final-answer completion, disabled final-answer generation, and disabled mutation. Its display text is assembled by `mutationFinalAnswerDeliveryReceiptGate.js`, with a lightweight Node regression covering the refused delivery-receipt rendering path, blocked/null fallbacks, delivery receipt refusal, acknowledgement-save refusal, final-answer delivery refusal, delivery handoff refusal, and backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-answer generation, final-answer completion, final-answer delivery, final-answer persistence, conversation-save, user-visible completion, final-response handoff, delivery handoff, delivery receipt, or acknowledgement controls.
The Code workspace renders this mutation final-answer delivery gate as read-only audit data inside the internal patch execution gate, including final-answer delivery gate status, schema, final-response handoff readiness, final-answer delivery policy, delivery handoff state, source final-response handoff gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled final-answer delivery, disabled delivery handoff, disabled delivery receipt, disabled acknowledgement save, disabled final-response handoff, disabled user-visible completion, disabled conversation save, disabled final-answer persistence, disabled final-answer completion, disabled final-answer generation, and disabled mutation. Its display text is assembled by `mutationFinalAnswerDeliveryGate.js`, with a lightweight Node regression covering the refused final-answer-delivery rendering path, blocked/null fallbacks, final-answer delivery refusal, delivery handoff refusal, delivery receipt refusal, acknowledgement-save refusal, final-response handoff refusal, and backend enabled-flag blocking keys. The mutation completion summary also displays `finalAnswerDeliveryEnabled` when present. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-answer generation, final-answer completion, final-answer persistence, conversation-save, user-visible completion, final-response handoff, delivery handoff, delivery receipt, acknowledgement, or final-answer delivery controls.
The Code workspace renders this mutation final-response handoff gate as read-only audit data inside the internal patch execution gate, including final-response handoff gate status, schema, user-visible completion readiness, final-response handoff policy, delivery handoff state, source final-answer user-visible completion gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled final-response handoff, disabled delivery handoff, disabled delivery receipt, disabled acknowledgement save, disabled final-answer delivery, disabled user-visible completion, disabled conversation save, disabled final-answer persistence, disabled final-answer completion, disabled final-answer generation, and disabled mutation. Its display text is assembled by `mutationFinalResponseHandoffGate.js`, with a lightweight Node regression covering the refused final-response-handoff rendering path, blocked/null fallbacks, final-response handoff refusal, delivery receipt refusal, acknowledgement-save refusal, final-answer delivery refusal, user-visible completion refusal, and backend enabled-flag blocking keys. The mutation completion summary also displays `deliveryHandoffEnabled` when present. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-answer generation, final-answer completion, final-answer persistence, conversation-save, user-visible completion, final-response handoff, delivery handoff, delivery receipt, acknowledgement, or final-answer delivery controls.
The Code workspace renders this mutation final-answer user-visible completion gate as read-only audit data inside the internal patch execution gate, including user-visible completion gate status, schema, final-answer conversation-save readiness, user-visible completion policy, final-response handoff state, source final-answer conversation-save gate status/schema, source/release/session/agent/workspace ids, expected/completed/accepted/rejected/intake-persisted result counts, policy checks, blocking keys, message, disabled user-visible completion, disabled final-response handoff, disabled delivery receipt, disabled acknowledgement save, disabled conversation save, disabled final-answer persistence, disabled delivery, disabled final-answer completion, disabled final-answer generation, and disabled mutation. Its display text is assembled by `mutationFinalAnswerUserVisibleCompletionGate.js`, with a lightweight Node regression covering the refused user-visible-completion rendering path, blocked/null fallbacks, user-visible completion refusal, final-response handoff refusal, delivery receipt refusal, acknowledgement-save refusal, conversation-turn save refusal, and backend enabled-flag blocking keys. It does not add release, apply, test, rollback, claim, request creation, push, execution, result aggregation, publication, RAG freshness update, mutation, final-answer generation, final-answer completion, final-answer persistence, conversation-save, user-visible completion, final-response handoff, delivery receipt, acknowledgement, or final-answer delivery controls.
The Code workspace code evidence, code search result, and reference result panels live in `CodeEvidencePanels.jsx`, while `CodeWorkspace.jsx` remains the route-level composition surface and preserves its existing exports.
The Code workspace file modal and code file viewer live in `CodeFilePanels.jsx`, preserving the existing portal close behavior, syntax-highlighted line rendering, highlighted-range scrolling, and route-level `CodeWorkspace.jsx` exports.
Readiness also includes `releaseAttemptModel.latestAttempt.releaseEnablementChecklist`, an audit-only disabled aggregate checklist for the future release transition. It combines final readiness, the Local Agent mutation execution sequence, the post-mutation result contract, rollback readiness, and the RAG freshness requirement into one latest-attempt decision surface while keeping release gate, request creation, push, claim, write helper, apply, test, rollback restore, mutation, and RAG freshness updates disabled.
The Code workspace renders this release enablement checklist as read-only audit data inside the internal patch execution gate, including checklist item pass/blocking state, blocking keys, and disabled release gate, claim, request creation, push, write helper, apply, test, rollback restore, mutation, and RAG freshness flags. It does not add any execution control.
`POST /api/local-agents/tools/{requestId}/release` is currently a refusal-only release boundary for the future transition from `APPROVED_HELD` to claimable Local Agent execution. It returns disabled-gate diagnostics, readiness gate data, the release attempt model, and release enablement checklist while keeping release gate, claim, request creation, push, write helper, apply, test, rollback restore, mutation, and RAG freshness updates disabled. If preconditions are visible it may create a non-claimable disabled release-attempt envelope for audit, but it does not make the held request claimable and does not push any Local Agent request.
The Code workspace now surfaces that release boundary as a disabled/audit-only release action for held requests. The UI does not call the release endpoint, create a new Local Agent request, push a request, make a request claimable, or enable apply, test, rollback restore, mutation, or RAG freshness updates.
Readiness also exposes `releaseAttemptModel.latestAttempt.finalMutationReportContract`, an audit-only disabled contract for the future final answer after Local Agent mutation. It requires changed files, verification/test outcome, post-write repository observation, rollback state, RAG freshness state, residual risks, and evidence/citations before the system can honestly report a completed change. It also records answer-quality guardrails: do not claim files changed without `mutationApplied=true`, do not hide failed or skipped verification, report RAG freshness after local file changes, and include rollback state plus residual risk on apply or verification failure. This contract does not enable release, request creation, push, claim, apply, test, rollback restore, mutation, RAG freshness updates, or final-answer generation.
The Code workspace renders this final mutation report contract inside the internal patch execution gate as read-only audit data. It shows the required final report sections, linked post-mutation outcome keys, disabled execution flags, and answer-quality guardrails without adding release, apply, test, rollback, claim, request creation, push, RAG freshness update, or final-answer generation controls.
Readiness also includes `releaseAttemptModel.latestAttempt.finalMutationReportFinalizationBoundary`, an audit-only disabled boundary for the future point where Local Agent mutation observations could be turned into the final answer. It checks whether release-attempt readiness, post-mutation outcome modeling, final report sections, and answer-quality guardrails are present, but keeps `finalAnswerGenerationEnabled=false` along with release gate, request creation, push, claim, apply, test, rollback restore, mutation, and RAG freshness updates disabled.
The Code workspace renders this finalization boundary inside the internal patch execution gate as read-only audit data. It shows finalization prerequisites, blocking keys, disabled execution flags, and the disabled final-answer generation state without adding release, apply, test, rollback, claim, request creation, push, RAG freshness update, mutation, or final-answer generation controls.

### Local Agent MVP Skeleton

The first local executable skeleton lives in `local-agent/` and builds as a .NET console app named `learnbot`. It is intended for the early internal pilot path, not as the final installer/service package yet.

Current commands:

- `learnbot pair --server http://localhost:8083 --agent-id <agent-id> --token <pairing-token> [--transport polling|websocket|auto]`
- `learnbot agent start [--once] [--interval-seconds 15] [--transport polling|websocket|auto]`
- `learnbot agent status`
- `learnbot agent token`
- `learnbot agent stop`
- `learnbot agent logs [--tail 80]`
- `learnbot workspace add <path>`
- `learnbot workspace list`
- `learnbot file read --workspace-id <workspace-id> --path <relative-path>`
- `learnbot git status --workspace-id <workspace-id>`
- `learnbot git diff --workspace-id <workspace-id> [--path <relative-path>] [--max-bytes <bytes>]`
- `learnbot doctor`
- `learnbot open`

The skeleton stores config in `%USERPROFILE%\.learnbot\agent.json`, or in `LEARNBOT_AGENT_CONFIG` when that environment variable is set for tests. It writes a minimal run-state file and `agent.log` next to that config, so internal users can inspect `learnbot agent status` and `learnbot agent logs --tail 80` before a Windows Service or installer exists. `learnbot agent token` reports paired state, agent id, a non-secret token fingerprint, and the web management URL without printing the raw credential. Long-running `agent start` logs transient loop failures and continues polling; `--once` returns a failure exit code for smoke tests. The stored transport mode defaults to `polling`; `websocket` and `auto` try a bounded WebSocket hello/heartbeat and fall back to REST heartbeat if the endpoint is disabled, unreachable, or rejected. When WebSocket is unavailable, the run-state records the configured transport, active fallback transport, consecutive WebSocket failures, and the next retry time while durable polling continues. During the bounded WebSocket receive window, pushed `tool.request` messages reuse the same safe local handler as polling and return `tool.response`; durable polling remains active as fallback. It sends heartbeat, reports approved local workspace summaries, polls the durable tool queue, and handles `agent.status`, `agent.doctor`, `workspace.list`, path-contained `file.read`, read-only `git.status`, and bounded read-only `git.diff`. File reads are limited to approved workspace roots, reject path traversal/workspace escape, reject binary files, and cap returned content. Git status uses a fixed `git status --porcelain=v1 -b --untracked-files=all` command with optional locks disabled, requires the approved workspace to be a Git worktree root, and also returns read-only repository identity observations when available: git root, current branch, HEAD commit, remote name, and remote URL. Git diff reads both staged and unstaged changes with `--no-ext-diff`, supports an optional workspace-contained relative `path`, and caps returned diff bytes. The Local Agent can also dry-run a `patch.apply` request if one is delivered by a future gated path: it requires `dryRunOnly=true`, rejects `mutationAllowed=true`, resolves the approved workspace, rejects path escape, verifies expected hashes or hunk context against local files, creates a managed snapshot of target files after preflight passes, and returns structured observations with `mutationApplied=false` and `snapshotCreated=true`. The `patch-dry-run-contract` self-test pins that even this successful dry-run snapshot path still returns `REJECTED`/`UNSAFE_TOOL`, leaves the workspace file unchanged, and requires separate rollback approval. It still returns a rejected tool result for patch mutation because patch file writes, command execution, tests, and rollback remain disabled until the release gate and rollback safety model are implemented.

A live polling smoke can be run against the local stack after `.\scripts\up.ps1 -Build`:

```powershell
.\scripts\local-agent-smoke.ps1 -Server http://localhost:8083 -WorkspacePath C:\Users\honeybadger\Desktop\LearnBot -ToolName file.read -Path README.md
.\scripts\local-agent-smoke.ps1 -Server http://localhost:8083 -WorkspacePath C:\Users\honeybadger\Desktop\LearnBot -ToolName git.status
.\scripts\local-agent-smoke.ps1 -Server http://localhost:8083 -WorkspacePath C:\Users\honeybadger\Desktop\LearnBot -ToolName git.diff -Path README.md
```

The script logs in, issues a pairing token, stores a temporary Local Agent config, registers the workspace, queues a read-only server request, runs the agent once, and verifies that the server persisted a `SUCCEEDED` tool response.

When the backend is started with `LEARNBOT_LOCAL_AGENT_WEBSOCKET_ENABLED=true`, the same helper can require the WebSocket path. With the normal Docker helper, set the environment variable before starting or recreating the stack so the backend enables `/api/local-agents/ws` and Nginx forwards the WebSocket upgrade:

```powershell
$env:LEARNBOT_LOCAL_AGENT_WEBSOCKET_ENABLED = "true"
.\scripts\up.ps1 -Build
.\scripts\local-agent-smoke.ps1 -Server http://localhost:8083 -WorkspacePath C:\Users\honeybadger\Desktop\LearnBot -ToolName file.read -Path README.md -Transport websocket
```

In WebSocket mode, the helper starts the Local Agent before enqueueing the request and fails unless the agent log shows that the request completed through the WebSocket tool path.
To verify fallback safety afterward, unset the flag or start the stack with the default `false` value and run the same smoke without `-Transport websocket`; the polling path should still persist a `SUCCEEDED` result.

For internal foreground pilot use, `scripts/local-agent.ps1` wraps the common setup and run commands without pretending to be the final installer:

```powershell
$env:LEARNBOT_AGENT_LOGIN_ID = "jinsu.kim"
$env:LEARNBOT_AGENT_PASSWORD = "admin1234"
.\scripts\local-agent.ps1 -Action setup -Server http://localhost:8083 -WorkspacePath C:\Users\honeybadger\Desktop\LearnBot -Transport polling
.\scripts\local-agent.ps1 -Action status
.\scripts\local-agent.ps1 -Action token
.\scripts\local-agent.ps1 -Action logs -Tail 80
.\scripts\local-agent.ps1 -Action start
.\scripts\local-agent.ps1 -Action background-start
.\scripts\local-agent.ps1 -Action background-stop
```

The helper performs web login, requests a pairing token, runs `learnbot pair`, registers the workspace, and starts the foreground polling loop. It accepts `-Transport polling|websocket|auto`; `websocket` and `auto` try WebSocket hello/heartbeat first, then keep polling available for durable tool queue fallback. It is not a Windows Service, MSI, or background process manager.
For the internal pilot, `background-start` launches the installed `learnbot.exe` in a hidden window, refuses duplicate starts when the recorded Local Agent process is running, and `background-stop` stops only the PID recorded by the Local Agent state file. This is still a helper, not a service supervisor.

To publish a lightweight local executable for the internal pilot:

```powershell
.\scripts\local-agent-install.ps1 -Action install
.\scripts\local-agent-install.ps1 -Action install -AddToUserPath
learnbot agent status
```

The install helper publishes `local-agent/` to `%USERPROFILE%\.learnbot\bin` by default. It can optionally add that directory to the user's PATH. This remains a framework-dependent pilot install and does not create a Windows Service, MSI, updater, or background process manager.
After publishing, `scripts/local-agent.ps1` uses the installed `%USERPROFILE%\.learnbot\bin\learnbot.exe` automatically when it exists; otherwise it falls back to `learnbot` on PATH or `dotnet run --project local-agent`.
